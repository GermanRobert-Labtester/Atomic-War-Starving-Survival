# ASHFALL Integration Plans

This is the sole live integration ledger. It is not a backlog and must not
duplicate prose from historical plan documents.

**SHELTER-OPERATIONS-BOARD — ROUTED SHELTER OPERATIONS COMPLETE (2026-09-25):** user-authorized integrator package (`claim-shelter-operations-board-2026-09-25`). Added the `shelter_operations` expanded route and command board over the current construction, assignment, inventory, survivor, outpost, celebration, journal, and seeded-RNG authorities. Construction and renovation/upgrade/excavation enforce authored bills, stability and duplicate guards; crew assignments are exclusive and progress daily with canonical crafting practice and fatigue; completed-room capacity bonuses flow through `ShelterAssignmentSystem`. Outpost establishment/supply and holiday holds use atomic inventory billing; garrison uniqueness, relief, and cycle-scoped holiday hold/skip state persist through existing owners. User approved adding canonical `skill_crafting`. No board-specific save section or duplicate mutable state. **Evidence:** `ShelterOperationsBoardCoreTests` 4/4; `ShelterOperationsBoardWiringTests` 3/3; `OutpostAtomicBillTests` 1/1; `SeasonalCelebrationCycleTests` 2/2; `Plan156ShelterExpansionIntegrationTests` 7/7; `ShelterAssignmentSystemTests` 20/20; Plan 58 outpost Core/host 5/5 + 7/7; `Plan33SkillCatalogExternalizationTests` 8/8; route/player-surface gates 20/20 + 8/8; `PlayerSurfaceCoverageGateTests` 8/8; host build 0 warnings/errors; `--shelter-operations-selftest` board 12/12 plus legacy smoke PASS; `--player-panels-uitest` PASS with 21/21 lifecycle gates and board bind/open/visible; data integrity 0 errors across 424 catalogs (5 non-blocking radio distress precedence warnings); architecture-map and self-test-manifest checks PASS; catalog registry 708/708. `DEC-356` ratified.

**UNBLOCK-PLAN-208-LEADERSHIP-SUCCESSION — PLAN 208 (LEADERSHIP SUCCESSION & CHALLENGES) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-183-187-198-208-2026-09-24`). Converted `LeadershipSystem` from an unwired island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`LeadershipSaveState`), catalog (`leadership_policies.json`), policies, deputies, succession designation, challenges, and `LeadershipCensus` read model; registered `HostCliAction.LeadershipSuccessionSelfTest` and `--leadership-succession-selftest` in `HostCliRegistry`. **Host:** wired catalog loading in `SetupSurvivorSocial`; exposed `DesignateLeadershipSuccessor`, `AppointLeadershipDeputy`, `InitiateLeadershipChallenge`, `ResolveLeadershipChallenge`, `GetLeadershipCensus`, `SetLeadershipPolicy`, and `GetLeadershipSystem` on `Main`; saved through existing `survivor_social` section (`LeadershipSaveState`) without creating a parallel store; implemented 12-check diagnostic probe `LeadershipSuccessionSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `SurvivorRelationsSystem` owns baseline affinities and bonds; `SurvivorSocialCoordinator` owns the social aggregate; `LeadershipSystem` owns shelter leadership designation, stress accumulation, daily decay, crisis aura morale bonuses, deputy appointments, designated successors, and leadership challenges. Deterministic seeded PRNG only (`ISeededRng`). **Evidence:** host + Core builds 0 errors / 0 warnings; `--leadership-succession-selftest` 12/12 PASS; `Plan208LeadershipSuccessionIntegrationTests` 7/7 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1598/1598 PASS (section pin 266); `generate-architecture-map.py` 266 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 71/71 plans INTEGRATED. `DEC-355` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-198-HEALTH-HISTORY — PLAN 198 (HEALTH HISTORY & MEDICAL RECORDS) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-183-187-198-208-2026-09-24`). Converted `HealthHistorySystem` from an inert island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`HealthHistoryState`), catalog (`medical_record_templates.json`), longitudinal records, diagnostic events, vaccinations, immunity degradation, booster due alerts, trends, and `HealthHistoryCensus` read model; registered `HostCliAction.HealthHistorySelfTest` and `--health-history-selftest` in `HostCliRegistry`. **Host:** implemented `HealthHistoryHostSession` and `HealthHistorySaveStore` (`SaveStoreHub.Checksummed<HealthHistoryState>`); wired `Main.HealthHistory.cs` setup/save/reset/operations (log medical record, resolve record, administer vaccine, record daily trend, get census); bound phase-5 campaign day owner `health_history` (`HealthHistoryDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; registered save section `health_history` (`health_history_save.json`, section pin 266); implemented 12-check diagnostic probe `HealthHistorySelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `MedicalPipelineCoordinator.Record` owns the signed bounded pipeline log (64 entries in `medical_pipeline`); `HealthHistorySystem` owns longitudinal records, template definitions, vaccination decay/boosters, and multi-metric health trends. Deterministic; no unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--health-history-selftest` 12/12 PASS; `Plan198HealthHistoryIntegrationTests` 6/6 PASS; `Plan198MedicalRecordLogTests` 8/8 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1598/1598 PASS (section pin 266); `generate-architecture-map.py` 266 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 71/71 plans INTEGRATED. `DEC-354` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-187-BESTIARY — PLAN 187 (BESTIARY UI & CREATURE ENCOUNTER TRACKING) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-183-187-198-208-2026-09-24`). Converted `BestiarySystem` from an inert island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`BestiaryState`), catalog (`narrative/wasteland_wildlife_bestiary.json`), creature discovery progression, sightings, combat kills, butchering harvests, multi-tier lore unlocks, and `BestiaryCensus` read model; registered `HostCliAction.BestiarySelfTest` and `--bestiary-selftest` in `HostCliRegistry`. **Host:** implemented `BestiaryHostSession` and `BestiarySaveStore` (`SaveStoreHub.Checksummed<BestiaryState>`); wired `Main.Bestiary.cs` setup/save/reset/operations (encounter recording, kill recording, butcher harvesting, census queries); bound phase-5 campaign day owner `bestiary_knowledge` (`BestiaryDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; registered save section `bestiary_knowledge` (`bestiary_knowledge_save.json`, section pin 266); implemented 12-check diagnostic probe `BestiarySelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `WildlifeEcosystemSystem` owns live animal population dynamics and sector migrations; `BestiarySystem` owns survivor discovery progress, creature catalog, sighting records, encounter notes, and completion metrics. Deterministic; no unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--bestiary-selftest` 12/12 PASS; `Plan187BestiaryIntegrationTests` 6/6 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1598/1598 PASS (section pin 266); `generate-architecture-map.py` 266 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 71/71 plans INTEGRATED. `DEC-353` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-183-CHILD-DEVELOPMENT — PLAN 183 (CHILD DEVELOPMENT STAGES SYSTEM) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-183-187-198-208-2026-09-24`). Converted `ChildDevelopmentSystem` from an isolated subsystem to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`ChildDevelopmentState`), catalog (`development_traits.json`), developmental stages (Infant, Toddler, Child, Adolescent, Young Adult), milestone history, caregiver growth, chore work capacity, detached canonical projection (`ProjectCanonicalChild`), and `ChildDevelopmentCensus` read model; registered `HostCliAction.ChildDevelopmentSelfTest` and `--child-development-selftest` in `HostCliRegistry`. **Host:** implemented `ChildDevelopmentHostSession` loading `development_traits.json` and synchronizing with canonical `GenerationalSystem`; implemented `Main.ChildDevelopment.cs` setup/reset/operations (child registration, education recording, caregiver assignment, chore capacity query, census queries); bound phase-5 campaign day owner `child_development_stages` (`ChildDevelopmentDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; saved state authority remains `GenerationalSaveStore` (`child_development` section, file `child_development_save.json`) without creating a parallel clock or duplicate save section; implemented 12-check diagnostic probe `ChildDevelopmentSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `GenerationalSystem` owns biological lineage, child birth, and generational advancement; `ChildDevelopmentSystem` provides stage progression, trait catalog, chore capacity, education, and milestone achievement projections. Deterministic; no unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--child-development-selftest` 12/12 PASS; `Plan183ChildDevelopmentIntegrationTests` 6/6 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1598/1598 PASS (section pin 266); `generate-architecture-map.py` 266 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 71/71 plans INTEGRATED. `DEC-352` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-180-SKILL-CERTIFICATION — PLAN 180 (SURVIVOR SKILL CERTIFICATION & PROFESSIONAL LICENSING) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-178-179-180-2026-09-24`). Converted `SkillCertificationSystem` from an inert island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`SkillCertificationState`), catalog (`skill_certifications.json`), certifications, specializations, exams, and `SkillCertificationCensus` read model; registered `HostCliAction.SkillCertificationSelfTest` and `--skill-certification-selftest` in `HostCliRegistry`. **Host:** implemented `SkillCertificationHostSession` and `SkillCertificationSaveStore` (`SaveStoreHub.Checksummed<SkillCertificationState>`); wired `Main.SkillCertification.cs` setup/save/reset/operations (certification exam execution, specialization granting, role requirement verification, work throughput multipliers); bound phase-5 campaign day owner `skill_certifications` (`SkillCertificationsDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; registered save section `skill_certifications` (`skill_certifications_save.json`, section pin 264); implemented 12-check diagnostic probe `SkillCertificationSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `SkillProgressionSystem` owns numerical skill experience and levels; `DutyRosterSystem` owns operational shift assignments; `SkillCertificationSystem` owns standardized exam certification tiers (Apprentice/Journeyman/Expert/Master), credential licenses, specialization perks, and certification-gated duty qualifications. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--skill-certification-selftest` 12/12 PASS; `Plan180SkillCertificationTests` 4/4 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1586/1586 PASS (section pin 264); `generate-architecture-map.py` 264 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 67/67 plans INTEGRATED. `DEC-351` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-179-PSYCHOLOGY-PHOBIA — PLAN 179 (PSYCHOLOGY & PHOBIA MANAGEMENT) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-178-179-180-2026-09-24`). Converted `PsychologicalProfileSystem` from an inert island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`PsychologyState`), catalog (`psychology_profiles.json`), phobia triggers, severity dynamics, therapy sessions, coping mechanisms, and `PsychologicalProfileCensus` read model; registered `HostCliAction.PsychologicalProfileSelfTest` and `--psychological-profile-selftest` in `HostCliRegistry`. **Host:** implemented `PsychologicalProfileHostSession` and `PsychologicalProfileSaveStore` (`SaveStoreHub.Checksummed<PsychologyState>`); wired `Main.PsychologicalProfiles.cs` setup/save/reset/operations (phobia evaluation, trigger activation, clinical therapy sessions, coping mechanism adoption, census queries); bound phase-5 campaign day owner `psychological_profiles` (`PsychologicalProfilesDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; registered save section `psychological_profiles` (`psychological_profiles_save.json`, section pin 264); implemented 12-check diagnostic probe `PsychologicalProfileSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** Trauma systems (`TraumaSystem`, `MentalHealthSystem`, `StressSystem`) own acute psychological shock and morale states; `PsychologicalProfileSystem` owns enduring psychological profiles, latent phobias, trigger conditions, coping mechanism repertoires, clinical therapy reductions, and phobia management states. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--psychological-profile-selftest` 12/12 PASS; `Plan179UnifiedPsychologyIntegrationTests` 6/6 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1586/1586 PASS (section pin 264); `generate-architecture-map.py` 264 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 67/67 plans INTEGRATED. `DEC-350` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-178-ART-CULTURE — PLAN 178 (ART & CULTURE CREATION) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-178-179-180-2026-09-24`). Converted `CultureCreationSystem` from an inert island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`CultureCreationState`), catalog (`art_forms.json`), artwork creation, masterwork valuation, preservation vaults, and `CultureCreationCensus` read model; registered `HostCliAction.CultureCreationSelfTest` and `--culture-creation-selftest` in `HostCliRegistry`. **Host:** implemented `CultureCreationHostSession` and `CultureCreationSaveStore` (`SaveStoreHub.Checksummed<CultureCreationState>`); wired `Main.CultureCreation.cs` setup/save/reset/operations (artwork creation, display curation, vault preservation, shelter cultural morale calculations, census queries); bound phase-5 campaign day owner `culture_creation` (`CultureCreationDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; registered save section `culture_creation` (`culture_creation_save.json`, section pin 264); implemented 12-check diagnostic probe `CultureCreationSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `ShelterMoraleSystem` and `SurvivorNeedsState` own survivor base morale; `InventorySystem` owns physical crafting materials; `CultureCreationSystem` owns artistic creations, medium techniques, quality appraisal, cultural identity evolution, and cultural artifact vault storage. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--culture-creation-selftest` 12/12 PASS; `Plan178ArtCultureIntegrationTests` 6/6 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1586/1586 PASS (section pin 264); `generate-architecture-map.py` 264 subsystems (100% mechanical evidence); `generate-plan-integration-audit.py` 67/67 plans INTEGRATED. `DEC-349` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-155-BLACK-MARKET — PLAN 155 (BLACK MARKET & UNDERGROUND ECONOMY) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-151-155-172-173-2026-09-24`). Converted `BlackMarketSystem` from a partially wired island to a 100% fully integrated, verified host feature. **Core:** verified domain system, state model (`BlackMarketState`), catalog (`black_market_inventory.json`), syndicates, and access tier gating; registered `HostCliAction.BlackMarketSelfTest` and `--black-market-selftest` in `HostCliRegistry`. **Host:** verified and sealed `BlackMarketHostSession` and `BlackMarketSaveStore` (`SaveStoreHub.Checksummed<BlackMarketState>`); verified `Main.BlackMarket.cs` setup/save/flush/reset and underworld operations (contact discovery, syndicate transactions, loan issuance, debt repayment, default bounty escalation, daily heat decay); registered save section `black_market` (`black_market_save.json`, section pin 261); implemented 12-check diagnostic probe `BlackMarketSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `MerchantRestockLedger` owns legal surface merchant restocking; `BlackMarketSystem` owns illicit underworld syndicate contacts, contraband catalog inventories, loans, interest, debt collections, and heat accumulation. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--black-market-selftest` 12/12 PASS; `Plan155BlackMarketIntegrationTests` 5/5 PASS; `agent-fast-verify.py` 10/10 PASS; `generate-plan-integration-audit.py` 64/64 plans INTEGRATED. `DEC-348` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-151-WORKING-ANIMALS — PLAN 151 (WORKING ANIMALS & COMPANIONS) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-151-155-172-173-2026-09-24`). Converted `CompanionAnimalSystem` from an inert orphan to a 100% fully integrated host feature. **Core:** verified domain system, companion records, training, and added `CompanionCensus` read model; bound `KnownSpeciesCheck` to `_wildlifeEcosystem`; registered `HostCliAction.WorkingAnimalsSelfTest` and `--working-animals-selftest` in `HostCliRegistry`. **Host:** created `CompanionAnimalHostSession` and companion lifecycle hooks; implemented player commands in `Main.Companion.cs` (`EnsureCompanionSession`, `AssignCompanion`, `FeedCompanion`, `RegisterCompanion`, `GetAllCompanions`, `GetGuardModifierTotal`, `GetPackCapacityBonusForSurvivor`); registered save section `companion_animals` (`companion_animals_save.json`, section pin 261); implemented 12-check diagnostic probe `WorkingAnimalsSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `WildlifeEcosystemSystem` owns wild animal species and ecosystems; `CompanionAnimalSystem` owns adopted companion records, bond, training, roles (Guard, Hunting, Pack, Therapy), care status, and pack modifiers. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--working-animals-selftest` 12/12 PASS; `Plan151WorkingAnimalsTests` 5/5 PASS; `agent-fast-verify.py` 10/10 PASS; `generate-plan-integration-audit.py` 64/64 plans INTEGRATED. `DEC-347` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-173-RADIO-PRODUCTION — PLAN 173 (RADIO STATION CONTENT CREATION & AUDIENCE RESPONSE) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-151-155-172-173-2026-09-24`). Converted `RadioProgramProductionSystem` from an unwired system to a 100% fully integrated host feature. **Core:** verified domain system, job preparation, and follow-up consequences; registered `HostCliAction.RadioProgramProductionSelfTest` and `--radio-production-selftest` in `HostCliRegistry`. **Host:** bound `ApplyShelterMoraleDelta` to `_survivors.Needs.Modify` in `Main.RadioProgramProduction.cs` so broadcast airings actively update shelter survivor morale; bound `PresenterCapabilityProvider` scaling with presenter health and morale; added `ResolveRadioProgramFollowUp` and host query APIs; implemented `RadioProgramProductionSelfTest.cs` (12 checks) and `HostCli.RadioProduction.cs`; registered save section `radio_program_production` (`radio_program_production_save.json`, section pin 261). **Authority boundaries (Rule 5):** `RadioCatalog` and `RadioFrequencySystem` own raw frequencies and transmissions; `RadioProgramProductionSystem` owns production jobs, broadcast delivery schedules, audience sentiment, follow-up consequences, and broadcast morale shifts. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--radio-production-selftest` 12/12 PASS; `Plan173RadioProductionIntegrationTests` 6/6 PASS; `agent-fast-verify.py` 10/10 PASS; `generate-plan-integration-audit.py` 64/64 plans INTEGRATED. `DEC-346` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-172-RADIATION-MUTATION — PLAN 172 (RADIATION MUTATION & GENETIC INSTABILITY) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-151-155-172-173-2026-09-24`). Converted `MutationSystem` from an inert island to a 100% fully integrated host feature. **Core:** verified domain system, mutation trees, thresholds, and gene therapy; registered `HostCliAction.RadiationMutationSelfTest` and `--radiation-mutation-selftest` in `HostCliRegistry`. **Host:** unblocked radiation exposure seam by binding `RadiationSystem` / `SurvivorsHostSession.OnSurvivorExposed` directly to `MutationSystem.AddRadiationExposure(survivorId, dose, day)` in `Main.Survivors.cs`; bound `_m.TickMutations(day, events)` in `MedicalDiseaseDayOwner.TickDay` (`Main.CampaignOwners.cs`); exposed clinical gene therapy in `Main.Plans178_181.cs`; implemented `RadiationMutationHostSession`, `RadiationMutationSelfTest` (12 checks), and `HostCli.RadiationMutation.cs`; registered save section `mutation_tree` (`mutation_save.json`, section pin 261). **Authority boundaries (Rule 5):** `RadiationSystem` owns environmental and acute radiation doses; `MutationSystem` owns chronic genetic instability, mutation manifestation trees, thresholds, and gene therapy stabilizations. Deterministic seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--radiation-mutation-selftest` 12/12 PASS; `Plan172RadiationMutationTests` 10/10 PASS; `agent-fast-verify.py` 10/10 PASS; `generate-plan-integration-audit.py` 64/64 plans INTEGRATED. `DEC-345` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-143-AFFLICTION-BRIDGE — PLAN 143 (MEDICAL AFFLICTIONS → QUEST & WORK BRIDGE) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-143-affliction-bridge-2026-09-24`). Converted `AfflictionQuestWorkBridge` from a pure Core orphan without host references into a 100% fully integrated host feature. **Core:** verified domain system and rules catalog (`affliction_bridge_rules.json`); added `QueryQuestGate` (pure query, side-effect-free, safe for UI previews without premature event emission); added `IsRoleExcluded` with canonical role mapping (`expedition`, `mess`, `night_watch`, `ward`, `hatch_opener`, `intake_sleeper`); added `AfflictionBridgeCensus` struct and `GetCensus()`; registered `HostCliAction.AfflictionBridgeSelfTest` and `--affliction-bridge-selftest` (aliases: `--affliction-bridges-selftest`, `--affliction-quest-work-selftest`). **Host:** wired `Bridge` into `MedicalHostSession`, loading catalog upon `Create()`; implemented `Main.Medical.cs` affliction aggregation (`GetActiveAfflictionIds` aggregating pipeline episodes, trauma, dependency, and amputation limb wounds) and quest queries (`IsQuestBlockedForSurvivor`, `GetUnlockedQuestsForSurvivor`); wired `WorkSpeedMultiplierLookup` in `Main.DutyRoster.cs` compounding needs and affliction multipliers with a 0.1 floor; wired `EvaluateDutyRoleFitness` in `Main.SurvivorFitness.cs` injecting blockers and zeroing hours when roles are excluded; integrated `AfflictionsPanel.cs` UI projection showing capacity reductions, excluded duties, and unlocked medical opportunity quests; implemented 12-check diagnostic probe `AfflictionBridgeSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** No second medical store or redundant save section; the bridge is a stateless projection over active medical and limb authorities (`MedicalPipelineCoordinator`, `RadiationSystem`, `RespiratoryDegenerationSystem`, `ChemicalDependencySystem`, `AmputationSystem`). Deterministic calculations without unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--affliction-bridge-selftest` 12/12 PASS; `--affliction-bridges-selftest` 12/12 PASS; `Plan143AfflictionBridgeIntegrationTests` 6/6 PASS; `Plan143AfflictionBridgeHostIntegrationTests` 5/5 PASS; `agent-fast-verify.py` 10/10 PASS. Zero partial residue remains.

**UNBLOCK-PLAN-185-MEMORY-DECAY — PLAN 185 (MEMORY & KNOWLEDGE DECAY) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-185-memory-decay-2026-09-24`). Converted `MemoryDecaySystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, records, and added `MemoryDecayCensus` struct (`GetCensus()`); registered save section `memory_decay` (`memory_decay_save.json`, section pin 261) in `SaveSectionRegistry`; registered internal heartbeat `memory_decay_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.MemoryDecaySelfTest` and `--memory-decay-selftest` flags in `HostCliRegistry`. **Host:** implemented `MemoryDecayHostSession` and `MemoryDecaySaveStore` (`SaveStoreHub.Checksummed<MemoryDecayState>`); implemented `Main.MemoryDecay.cs` with full setup/save/flush/reset and domain operations (memory recording, domain clarity calculation, forgetting events, cognitive reinforcement); bound phase-5 campaign day owner `memory_decay` (`MemoryDecayDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.MemoryDecay.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `SurvivorRelationsState` owns social affinities and relationship impressions; `MemoryDecaySystem` owns factual and cognitive memories across domains (Survival, Crafting, Medical, Wasteland, Lore, Personal), clarity degradation, forgetting events, and deliberate cognitive reinforcement. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--memory-decay-selftest` 12/12 PASS; `Plan185MemoryDecayIntegrationTests` 6/6 PASS; `MemoryDecaySystemTests` 9/9 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1568/1568 PASS (section pin 261); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 261 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 263 stores; `generate-selftest-manifest.py` 194 tests; `generate-plan-integration-audit.py` 59/59 plans INTEGRATED. `DEC-340` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-200-PERSONAL-QUESTS — PLAN 200 (SURVIVOR PERSONAL QUESTS & CHARACTER ARCS) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-200-personal-quests-2026-09-24`). Converted `PersonalQuestSystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, quest definitions, added `required_class` field, and added `PersonalQuestCensus` struct (`GetCensus()`); registered save section `personal_quests` (`personal_quests.json`, section pin 261) in `SaveSectionRegistry`; registered internal heartbeat `personal_quests_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.PersonalQuestsSelfTest` and `--personal-quests-selftest` flags in `HostCliRegistry`. **Host:** implemented `PersonalQuestHostSession` and `PersonalQuestSaveStore` (`SaveStoreHub.Checksummed<PersonalQuestSaveState>`); implemented `Main.PersonalQuests.cs` with full setup/save/flush/reset and domain operations (quest triggering, stage progression, choice resolution, objective evaluation, census queries); bound phase-5 campaign day owner `personal_quests` (`PersonalQuestDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `PersonalQuestSelfTest.cs` in `HostCli.cs` and `Main.Application.cs`; verified `PersonalQuestPanel.cs` UI wiring. **Authority boundaries (Rule 5):** `CrossingQuestSystem` / `DutyRosterQuestSystem` own world and operational assignments; `PersonalQuestSystem` owns internal personal motivations, background arcs, trait-driven journeys, and personal resolution states. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--personal-quests-selftest` 12/12 PASS; `Plan200PersonalQuestsIntegrationTests` 4/4 PASS; `PersonalQuestSystemTests` 10/10 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1568/1568 PASS (section pin 261); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 261 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 263 stores; `generate-selftest-manifest.py` 194 tests; `generate-plan-integration-audit.py` 59/59 plans INTEGRATED. `DEC-341` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-202-INTERPERSONAL-CONFLICT — PLAN 202 (INTERPERSONAL CONFLICT & GRIEVANCE) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-202-interpersonal-conflict-2026-09-24`). Converted `InterpersonalConflictSystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, templates, and added `InterpersonalConflictCensus` struct (`GetCensus()`); registered save section `interpersonal_conflict` (`interpersonal_conflict_save.json`, section pin 261) in `SaveSectionRegistry`; registered internal heartbeat `interpersonal_conflict_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.InterpersonalConflictSelfTest` and `--interpersonal-conflict-selftest` flags in `HostCliRegistry`. **Host:** implemented `InterpersonalConflictHostSession` and `InterpersonalConflictSaveStore` (`SaveStoreHub.Checksummed<InterpersonalConflictState>`); implemented `Main.InterpersonalConflict.cs` with full setup/save/flush/reset and domain operations (conflict triggering, grievance logging, daily grievance decay, conflict mediation); bound phase-5 campaign day owner `interpersonal_conflict` (`InterpersonalConflictDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.InterpersonalConflict.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `SurvivorRelationsState` / `SurvivorSocialSystem` own baseline relationship affinities, trust, and romance; `InterpersonalConflictSystem` owns active dispute states, grievances between pairs of survivors, escalations, grievance decay, and mediation outcomes. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--interpersonal-conflict-selftest` 12/12 PASS; `Plan202InterpersonalConflictIntegrationTests` 6/6 PASS; `InterpersonalConflictSystemTests` 7/7 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1568/1568 PASS (section pin 261); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 261 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 263 stores; `generate-selftest-manifest.py` 194 tests; `generate-plan-integration-audit.py` 59/59 plans INTEGRATED. `DEC-342` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-216-EXERCISE — PLAN 216 (SURVIVOR EXERCISE & PHYSICAL TRAINING) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-216-exercise-2026-09-24`). Converted `ExerciseSystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, routines, and added `ExerciseCensus` struct (`GetCensus()`); registered save section `exercise` (`exercise_save.json`, section pin 261) in `SaveSectionRegistry`; registered internal heartbeat `exercise_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.ExerciseSelfTest` and `--exercise-selftest` flags in `HostCliRegistry`. **Host:** implemented `ExerciseHostSession` and `ExerciseSaveStore` (`SaveStoreHub.Checksummed<ExerciseSystemState>`); implemented `Main.Exercise.cs` with full setup/save/flush/reset and domain operations (profile initialization, workout execution, fatigue accumulation/recovery, fitness attribute degradation, census queries); bound phase-5 campaign day owner `exercise` (`ExerciseDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.Exercise.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `HealthSystem` owns injury, disease, and raw vitality state; `ExerciseSystem` owns physical fitness attributes (strength, endurance, agility, fatigue), routine assignments (`exercise_routines.json`), training sessions, injury prevention modifiers, and fitness decay. Deterministic; seeded PRNG only. **Evidence:** host + Core builds 0 errors / 0 warnings; `--exercise-selftest` 12/12 PASS; `Plan216ExerciseIntegrationTests` 6/6 PASS; `ExerciseSystemTests` 7/7 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1568/1568 PASS (section pin 261); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 261 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 263 stores; `generate-selftest-manifest.py` 194 tests; `generate-plan-integration-audit.py` 59/59 plans INTEGRATED. `DEC-343` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-162-SHELTER-ARCHIVE — PLAN 162 (SHELTER HISTORY & ARCHIVE) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-162-shelter-archive-2026-09-24`). Converted `ShelterArchiveSystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, entries, and added `ShelterArchiveCensus` struct (`GetCensus()`); registered save section `shelter_archive` (`shelter_archive_save.json`, section pin 256) in `SaveSectionRegistry`; registered internal heartbeat `shelter_archive_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.ShelterArchiveSelfTest` and `--shelter-archive-selftest` (alias `--archive-system-selftest`) flags in `HostCliRegistry`. **Host:** implemented `ShelterArchiveHostSession` and `ShelterArchiveSaveStore` (`SaveStoreHub.Checksummed<ShelterArchiveState>`); implemented `Main.ShelterArchive.cs` with full setup/save/flush/reset and domain operations (entry recording, milestone recording, memorial casualty tributes, timeline query, keyword search, canonical source projections); bound phase-5 campaign day owner `shelter_archive` (`ShelterArchiveDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetEnrolledFlagshipSessions`); implemented 12-check diagnostic probe `HostCli.ShelterArchive.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `JournalSystem` owns narrative journals; `CampaignChronicleService` owns high-level campaign summaries; `ShelterArchiveSystem` owns structured chronological records, milestones, governance resolutions, memorial tributes, and keyword indexing. Deterministic; no unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--shelter-archive-selftest` 12/12 PASS; `--archive-system-selftest` 12/12 PASS; `Plan162ArchiveIntegrationTests` 6/6 PASS; `ShelterArchiveSystemTests` 8/8 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1550/1550 PASS (section pin 258); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 258 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 260 stores; `generate-selftest-manifest.py` 191 tests; `generate-plan-integration-audit.py` 56/56 plans INTEGRATED. `DEC-337` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-177-DREAM-SYSTEM — PLAN 177 (DREAM & SLEEP EVENT SYSTEM) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-177-dream-system-2026-09-24`). Converted `DreamSystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, templates, and added `DreamCensus` struct (`GetCensus()`); registered save section `survivor_dreams` (`survivor_dreams_save.json`, section pin 257) in `SaveSectionRegistry`; registered internal heartbeat `survivor_dreams_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.DreamSystemSelfTest` and `--dream-system-selftest` (alias `--dreams-selftest`) flags in `HostCliRegistry`. **Host:** implemented `DreamHostSession` and `DreamSaveStore` (`SaveStoreHub.Checksummed<DreamSystemState>`); implemented `Main.DreamSystem.cs` with full setup/save/flush/reset and domain operations (nocturnal dream evaluations, rest quality modifiers, consecutive nightmare tracking, therapeutic dream interpretation, history queries); bound phase-5 campaign day owner `survivor_dreams` (`SurvivorDreamsDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetEnrolledFlagshipSessions`); implemented 12-check diagnostic probe `HostCli.DreamSystem.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `NeedsSystem` owns survivor Fatigue and Numbness; `SleepAcousticLedger` owns acoustic bunk soundproofing; `SleepNarrativeProjection` owns sleep narrative beats; `DreamSystem` owns dream taxonomy resolution, nightmare loops, rest bonuses/penalties, and therapeutic dream interpretation. Seeded PRNG only (`SeededRng`); no unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--dream-system-selftest` 12/12 PASS; `--dreams-selftest` 12/12 PASS; `Plan177DreamSleepIntegrationTests` 6/6 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1550/1550 PASS (section pin 258); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 258 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 260 stores; `generate-selftest-manifest.py` 191 tests; `generate-plan-integration-audit.py` 56/56 plans INTEGRATED. `DEC-338` ratified. Zero partial residue remains.

**UNBLOCK-PLAN-184-ACCESSIBILITY-SETTINGS — PLAN 184 (ACCESSIBILITY OPTIONS SYSTEM) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-184-accessibility-settings-2026-09-24`). Converted `AccessibilitySettingsSystem` to a 100% fully integrated host feature. **Core:** verified domain system, state model, profiles, and added `AccessibilityCensus` struct (`GetCensus()`); registered save section `accessibility_settings` (`accessibility_settings_save.json`, section pin 258) in `SaveSectionRegistry`; added `HostCliAction.AccessibilitySettingsSelfTest` and `--accessibility-settings-selftest` (alias `--accessibility-options-selftest`) flags in `HostCliRegistry`. **Host:** implemented `AccessibilitySettingsHostSession` and `AccessibilitySettingsSaveStore` (`SaveStoreHub.Checksummed<AccessibilitySettingsState>`), synchronizing active preferences into `UserSettingsStore.Current`; implemented `Main.AccessibilitySettings.cs` with full setup/save/reset and domain operations (preset profile application, visual/hearing/motor/cognitive customization, font scaling clamping, high contrast, reduced motion); wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetEnrolledFlagshipSessions`); implemented 12-check diagnostic probe `HostCli.AccessibilitySettings.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `UserSettingsStore` owns user hardware configuration and device settings; `AudioAccessibilityCoordinator` owns directional captioning and sound visualization; `AccessibilitySettingsSystem` owns accessibility profiles (`accessibility_profiles.json`), sensory accommodations, font scaling bounds [0.75, 2.0], colorblind modes, high contrast toggles, motion reduction flags, and cognitive load adaptations. **Evidence:** host + Core builds 0 errors / 0 warnings; `--accessibility-settings-selftest` 12/12 PASS; `--accessibility-options-selftest` 12/12 PASS; `Plan184AccessibilitySettingsIntegrationTests` 6/6 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1550/1550 PASS (section pin 258); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 258 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 260 stores; `generate-selftest-manifest.py` 191 tests; `generate-plan-integration-audit.py` 56/56 plans INTEGRATED. `DEC-339` ratified. Zero partial residue remains.

**UNBLOCK-EXPANSION-40-THE-WHEEL — THE WHEEL (MECHANICAL POWER DRIVELINE & MACHINE TOOLS) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-40-the-wheel-2026-09-24`). Signed pure-domain engine `MechanicalPowerDrivelineEngine` (DEC-335) had **0 host references**, so rotational line shaft power transmission, friction drag, bearing wear, lubricant reserve depletion, spindle runout, and machining tolerance evaluations were unreachable. Converted to a 100% fully integrated host feature. **Core:** implemented stateful `MechanicalDrivelineLedger` wrapping pure calculation engine, `MechanicalDrivelineState` (schema 1: line shaft branches, pillow block bearing wear, lubricant reserve inventory, operating hours, maintenance/alignment alert counters), and `MechanicalDrivelineCensus` read model; registered save section `mechanical_driveline` (`mechanical_driveline_save.json`, section pin 254) in `SaveSectionRegistry`; registered internal heartbeat `mechanical_driveline_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.MechanicalDrivelineSelfTest` and `--mechanical-driveline-selftest` (alias `--the-wheel-selftest`) flags in `HostCliRegistry`. **Host:** implemented `MechanicalDrivelineHostSession` and `MechanicalDrivelineSaveStore` (`SaveStoreHub.Checksummed<MechanicalDrivelineState>`); implemented `Main.MechanicalDriveline.cs` with full setup/save/flush/reset and domain operations (power transmission execution, machining tolerance evaluation, millwright maintenance, lubricant restock, daily advance, census); bound phase-5 campaign day owner `mechanical_driveline` (`MechanicalDrivelineDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.MechanicalDriveline.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `KineticStorageSystem` owns flywheels; `ShelterWorkshopSystem` owns recipes/jobs; `PrecisionMetrologySystem` owns calibration standards; `MechanicalDrivelineLedger` owns rotational line shaft transmission, friction drag, bearing wear, lubricant depletion, and machine tool runout. Deterministic permille integer math; no System.Random or unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--mechanical-driveline-selftest` 12/12 PASS; `--the-wheel-selftest` 12/12 PASS; `MechanicalDrivelineLedgerTests` 8/8 PASS; `MechanicalPowerDrivelineEngineTests` 5/5 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1532/1532 PASS (section pin 255); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 255 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 257 stores; `generate-selftest-manifest.py` 188 tests; `generate-plan-integration-audit.py` 53/53 plans INTEGRATED. `DEC-335` ratified. Zero partial residue remains.

**UNBLOCK-EXPANSION-41-THE-QUIET — THE QUIET (SLEEP QUALITY, SOUNDPROOFING & SHELTER CROWDING) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-41-the-quiet-2026-09-24`). Signed pure-domain engine `SleepAcousticRestEngine` (DEC-336) had **0 host references**, so sleeping quarter soundproofing attenuation, bunk decibels, crowding density penalties, quiet hours compliance, sensory relief kits, and restorative sleep multipliers were unreachable. Converted to a 100% fully integrated host feature. **Core:** implemented stateful `SleepAcousticLedger` wrapping pure calculation engine, `SleepAcousticState` (schema 1: sleeping quarters, wall/door acoustic attenuation, sensory relief kit reserve, quiet hours schedule, violation/disturbance alerts), and `SleepAcousticCensus` read model; registered save section `sleep_acoustic_rest` (`sleep_acoustic_rest_save.json`, section pin 255) in `SaveSectionRegistry`; registered internal heartbeat `sleep_acoustic_rest_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.SleepAcousticRestSelfTest` and `--sleep-acoustic-selftest` (alias `--the-quiet-selftest`) flags in `HostCliRegistry`. **Host:** implemented `SleepAcousticRestHostSession` and `SleepAcousticRestSaveStore` (`SaveStoreHub.Checksummed<SleepAcousticState>`); implemented `Main.SleepAcousticRest.cs` with full setup/save/flush/reset and domain operations (quarter sleep evaluation, sensory relief kit install, sensory kit restock, soundproofing updates, quiet hours schedule, daily advance, census); bound phase-5 campaign day owner `sleep_acoustic_rest` (`SleepAcousticRestDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.SleepAcousticRest.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `ShelterNoiseSystem` owns broad ambient noise sources and propagation; `NeedsSystem` owns survivor Fatigue and Numbness; `ShelterAssignmentSystem` owns survivor room assignments; `SleepAcousticLedger` owns sleeping quarter soundproofing attenuation, bunk decibels, crowding density penalties, quiet hours compliance, sensory relief kits, and restorative sleep multipliers. Deterministic permille integer math; no System.Random or unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--sleep-acoustic-selftest` 12/12 PASS; `--the-quiet-selftest` 12/12 PASS; `SleepAcousticLedgerTests` 8/8 PASS; `SleepAcousticRestEngineTests` 5/5 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1532/1532 PASS (section pin 255); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 255 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 257 stores; `generate-selftest-manifest.py` 188 tests; `generate-plan-integration-audit.py` 53/53 plans INTEGRATED. `DEC-336` ratified. Zero partial residue remains.

**UNBLOCK-EXPANSION-39-THE-REAGENT — THE REAGENT (CHEMICAL SYNTHESIS SAFETY) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-39-the-reagent-2026-09-24`). Signed pure-domain engine `ChemicalReagentSynthesisEngine` (DEC-90 / DEC-334) had **0 host references**, so reactor vessel safety envelopes, thermal/pressure runaways, stoichiometric mass balance, reagent purity grading, and acidic effluent neutralization were unreachable. Converted to a 100% fully integrated host feature. **Core:** implemented stateful `ChemicalReagentLedger` wrapping pure calculation engine, `ChemicalReagentSynthesisState` (schema 1: reactor vessels, batch synthesis history, reagent inventory by grade Crude/Technical/ReagentGrade/AnalyticalPharma, catalyst activity/wear, neutralizing alkali reserve, acidic effluent accumulation, runaway/alert metrics), and `ChemicalReagentCensus` read model; registered save section `chemical_reagent_synthesis` (`chemical_reagent_synthesis_save.json`, section pin 253) in `SaveSectionRegistry`; registered internal heartbeat `chemical_reagent_synthesis_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.ChemicalReagentSynthesisSelfTest` and `--chemical-reagent-selftest` flag in `HostCliRegistry`. **Host:** implemented `ChemicalReagentSynthesisHostSession` and `ChemicalReagentSynthesisSaveStore` (`SaveStoreHub.Checksummed<ChemicalReagentSynthesisState>`); implemented `Main.ChemicalReagentSynthesis.cs` with full setup/save/flush/reset and domain operations (synthesis cycle execution, stoichiometric mass balance, acidic waste neutralization, neutralizing alkali restock, catalyst bed regeneration, cooling capacity adjustment); bound phase-5 campaign day owner `chemical_reagent_synthesis` (`ChemicalReagentSynthesisDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.ChemicalReagentSynthesis.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `ChlorAlkaliSynthesisEngine`, `PlasticPyrolysisSystem`, `FischerTropschSynthesisEngine`, `BioFermentationEngine`, and `PharmaLabSystem` remain authorities for specific recipes, plant equipment, or biological cultures; `ChemicalReagentLedger` tracks generic reactor vessel thermal/pressure envelopes, catalyst health, stoichiometric mass balances, output reagent grading, and acidic effluent neutralization. Deterministic permille integer math; no System.Random or unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--chemical-reagent-selftest` 12/12 PASS; `ChemicalReagentLedgerTests` 7/7 PASS; `ChemicalReagentSynthesisEngineTests` 5/5 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1520/1520 PASS (section pin 253); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 253 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 255 stores; `generate-selftest-manifest.py` 186 tests; `generate-plan-integration-audit.py` 51/51 plans INTEGRATED; `agent-fast-verify.py` 10/10 PASS. `DEC-334` ratified. Follow-up integration seal added the missing `CaptureSection` handoff in `Main.ChemicalReagentSynthesis.cs`; `--real-campaign-journey-selftest` now passes with the reagent section present in the campaign envelope. Zero partial residue remains.

**UNBLOCK-EXPANSION-38-THE-WARD — THE WARD (CLINICAL TRIAGE & STERILE SUPPLY) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-38-the-ward-2026-09-24`). Signed pure-domain engine `ClinicalWardTriageEngine` (DEC-89 / DEC-333) had **0 host references**, so clinical triage prioritization, surgical preflight readiness checks, sterile supply depletion/autoclave cycles, and nosocomial infection risk modeling were unreachable. Converted to a 100% fully integrated host feature. **Core:** implemented stateful `ClinicalWardLedger` wrapping pure calculation engine, `ClinicalWardTriageState` (schema 1: triage patient records, completed procedures, cleanliness grade, staffing readiness, sterile supplies pool, autoclave cycles), and `ClinicalWardCensus` read model; registered save section `clinical_ward_triage` (`clinical_ward_triage_save.json`, section pin 252) in `SaveSectionRegistry`; registered internal heartbeat `clinical_ward_triage_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.ClinicalWardTriageSelfTest` and `--clinical-ward-selftest` flag in `HostCliRegistry`. **Host:** implemented `ClinicalWardTriageHostSession` and `ClinicalWardTriageSaveStore` (`SaveStoreHub.Checksummed<ClinicalWardTriageState>`); implemented `Main.ClinicalWardTriage.cs` with full setup/save/flush/reset and domain operations (triage admission, surgical preflight/execution, discharge, restock, autoclave cycle); bound phase-5 campaign day owner `clinical_ward_triage` (`ClinicalWardTriageDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.ClinicalWardTriage.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `MedicalWardSystem` and `AdvancedSurgicalWardSystem` remain the authorities for clinical bed occupancy, physical inpatient admissions, and procedure execution; `InventorySystem` owns raw medical consumables; the triage ledger tracks prioritization (Immediate, Delayed, Minimal, Expectant), contagious patient isolation routing, surgical preflight readiness gating, sterile supply inventory, autoclave sterilization cycles, and nosocomial infection tracking. Deterministic permille integer math; no System.Random or unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--clinical-ward-selftest` 12/12 PASS; `ClinicalWardLedgerTests` 6/6 PASS; `ClinicalWardTriageEngineTests` 5/5 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1514/1514 PASS (section pin 252); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 252 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 254 stores; `generate-selftest-manifest.py` 185 tests; `generate-plan-integration-audit.py` 50/50 plans INTEGRATED. `DEC-333` ratified. Zero partial residue remains.

**UNBLOCK-EXPANSION-37-THE-QUICKENING — THE QUICKENING (ANTENATAL & NEONATAL HEALTH) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-37-the-quickening-2026-09-24`). Signed pure-domain engine `AntenatalMaternalHealthEngine` (DEC-332) had **0 host references**, so antenatal care, maternal reserve depletion, and delivery resolution were unreachable. Converted to a 100% fully integrated host feature. **Core:** implemented stateful `AntenatalMaternalCareLedger` wrapping pure calculation engine, `AntenatalMaternalCareState` (schema 1: active pregnancies, delivery history, shelter clinic and sanitation quality), and `AntenatalMaternalCensus` read model; registered save section `antenatal_maternal_health` (`antenatal_maternal_health_save.json`, section pin 251) in `SaveSectionRegistry`; registered internal heartbeat `antenatal_maternal_health_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.AntenatalMaternalHealthSelfTest` and `--antenatal-care-selftest` flag in `HostCliRegistry`. **Host:** implemented `AntenatalMaternalHealthHostSession` and `AntenatalMaternalHealthSaveStore` (`SaveStoreHub.Checksummed<AntenatalMaternalCareState>`); implemented `Main.AntenatalMaternalHealth.cs` with full setup/save/flush/reset and domain operations; bound phase-5 campaign day owner `antenatal_maternal_health` (`AntenatalMaternalHealthDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.AntenatalMaternalHealth.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `ChildDevelopmentSystem` and `GenerationalSystem` remain the single authorities for registered children and family lineage; `MedicalWardSystem` remains the clinical bed occupancy authority; survivor baseline caloric demands remain in `SurvivorNeedsState`; the antenatal care ledger tracks pregnancy stages, maternal physical reserves, clinic supervision, and delivery outcomes. Deterministic permille integer math; no System.Random or unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--antenatal-care-selftest` 12/12 PASS; `AntenatalMaternalCareLedgerTests` 6/6 PASS; `AntenatalMaternalHealthEngineTests` 5/5 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1508/1508 PASS (section pin 251); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 251 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 253 stores; `generate-selftest-manifest.py` 184 tests; `generate-plan-integration-audit.py` 49/49 plans INTEGRATED. `DEC-332` ratified. Zero partial residue remains.

**UNBLOCK-EXPANSION-35-THE-HABIT — THE HABIT (DEPENDENCY TAPER & WITHDRAWAL) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-35-the-habit-2026-09-24`). Signed pure-domain engine `DependencyTaperWithdrawalEngine` (DEC-88) had **0 host references**, so clinical taper programs and withdrawal management were unreachable. Converted to a 100% fully integrated host feature. **Core:** implemented stateful `DependencyTaperLedger` wrapping pure calculation engine, `DependencyTaperState` (schema 1: active taper programs, completed survivor records, care posture, substitute medicine reserve), and `DependencyTaperCensus` read model; registered save section `dependency_taper_withdrawal` (`dependency_taper_withdrawal_save.json`, section pin 250) in `SaveSectionRegistry`; registered internal heartbeat `dependency_taper_ticked` in `DayEventVocabulary` and `EVENT_SEMANTIC_PARITY_MATRIX.md`; added `HostCliAction.DependencyTaperWithdrawalSelfTest` and `--dependency-taper-selftest` flag in `HostCliRegistry`. **Host:** implemented `DependencyTaperWithdrawalHostSession` and `DependencyTaperWithdrawalSaveStore` (`SaveStoreHub.Checksummed<DependencyTaperState>`); implemented `Main.DependencyTaperWithdrawal.cs` with full setup/save/flush/reset and domain operations; bound phase-5 campaign day owner `dependency_taper_withdrawal` (`DependencyTaperWithdrawalDayOwner`) with `IPreDaySnapshotRestore` in `Main.CampaignOwners.cs`; wired lifecycle into `Main.SaveOrchestrator.cs` and `Main.Lifecycle.cs` (`ResetLateWaveIntegrationSessions`); implemented 12-check diagnostic probe `HostCli.DependencyTaperWithdrawal.cs` in `HostCli.cs` and `Main.Application.cs`. **Authority boundaries (Rule 5):** `ChemicalDependencySystem` remains the single authority for chemical addiction levels and tolerances; `MedicalWardSystem` remains the authority for clinical bed occupancy; survivor needs remain owned by `SurvivorNeedsState`; the taper ledger tracks only clinical taper programs, dose step-downs, substitute medicine stock, and care posture. Deterministic permille integer math; no System.Random or unseeded randomness. **Evidence:** host + Core builds 0 errors / 0 warnings; `--dependency-taper-selftest` 12/12 PASS; `DependencyTaperLedgerTests` 6/6 PASS; `DependencyTaperWithdrawalEngineTests` 5/5 PASS; `DayEventParitySourceGateTests` 2/2 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1502/1502 PASS (section pin 250); `triad-drift-gate.sh` PASS; `generate-architecture-map.py` 250 subsystems (100% mechanical evidence); `generate-save-store-matrix.py` 252 stores; `generate-selftest-manifest.py` 183 tests; `generate-plan-integration-audit.py` 47/47 plans INTEGRATED. `DEC-330` ratified. Zero partial residue remains.

**UNBLOCK-EXPANSION-36-NIGHT-WATCH — THE WATCH FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized claim `claim-unblock-expansion-36-night-watch-2026-09-24`. Premise audit found `NightWatchPatrolReadinessEngine` was a pure orphan (0 production callers); the design bible's named `PatrolTerritoryAuthority`/night-watch save owners are stale. Current integration composes the signed engine with the live `PerimeterDefenseSystem`, `SoundRangingThreatEngine`, `TerritoryControlSystem`, `DutyRosterSystem`, `ShelterSecuritySystem`, and journal seams, adding only watch-specific posts/routes/drills/readiness state to existing owner save shapes. **Delivered:** strict `night_watch_operations.json` (15 posts, 12 routes, 10 gate rules, 12 detection profiles, 8 alarm protocols, 10 drills), pure live projection, additive perimeter/duty-roster persistence, fresh+restore lifecycle, daily campaign progression, real player commands, routed accessible `NightWatchPanel`, content/integrity registration, and runtime `night_watch_selftest` attached to the owning patrol verb. **Evidence:** Core engine 5/5; operations 7/7; host integration 6/6; runtime 16/16; existing PerimeterDefense 24/24 and DutyRoster 49/49; real campaign journey PASS; data integrity 0 errors/423 catalogs; content utilization PASS; port contract 301 seams; host/Core builds 0/0; legacy duty-roster checksum migration and deterministic watch-state normalization are covered by the focused host suite. **No partial residue remains.** Plan/log: `docs/plans/UNBLOCK_EXPANSION36_NIGHT_WATCH_INTEGRATION_PLAN.md`; decision `DEC-331`.

**UNBLOCK-EXPANSION-30-31 — THE PRESS + THE KILN FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-30-31-2026-09-24`). Both engines were signed pure-domain authorities with Core tests but **0 host references**, so their behavior was unreachable; both were also stateless, so neither could survive save/load. **Expansion 30 (The Press):** added stateful `BroadsheetPressLedger` + `BroadsheetPressState` (schema 1: type tray, press consumables, bound publication archive, cumulative counters) + `PressedPublication` + `BroadsheetPressCensus` (schema-gated restore, bounded 200-entry archive, deterministic `pub_d{day}_{seq}` ids, no RNG); host `BroadsheetPressSaveStore` (section `broadsheet_press`) + `BroadsheetPressHostSession` + `Main.BroadsheetPress.cs`; phase-5 census day owner emitting `broadsheet_press_ticked` — the press never auto-prints, because what the shelter publishes is a player decision. **Canonical routing only:** rumor facts stay with `RumorSystem` (a pamphlet reports the engine's debunk correction; the host raises `Truthfulness` on the canonical record), morale goes through the survivors' needs authority (`NeedKind.Morale`), compositor skill is read from `SkillProgressionSystem` (crafting). `--broadsheet-press-selftest` 12/12. **Expansion 31 (The Kiln):** added stateful `KilnFiringLedger` + `KilnFiringState` (schema 1: queued batches, fuel reserve, lining wear, drawn-output tallies) + `KilnFiringCensus`; host `KilnworksSaveStore` (section `kilnworks`) + `KilnworksHostSession` + `Main.Kilnworks.cs`; phase-5 day owner advances the oldest active batch one stage at the deterministic `OptimalFiringTemperaturePermille` (780‰, below the engine's 800‰ thermal-shock threshold) and emits `kilnworks_ticked`; fuel charged per stage from the kiln's own reserve, lining wear accrued through the engine on every draw and on calcination, reline clears it at 800‰. Metallurgy stays with `CupolaFoundryEngine`; shelter upgrades stay with the canonical infrastructure owners. `--kilnworks-selftest` 12/12. **Deferred with named reasons:** the press UI panel (presentation; the existing UndergroundPrintingPressPanel covers the clandestine context and the host commands + probe are the operational surface — a new routed panel needs coordinated edits on concurrently-owned `PanelRegistryBootstrap`/`OpenPlayerPanel`/`Main.UiPanels`/`Main.PlayerSurfaces`/`PlayerSurfaceManifest`), the kiln→CupolaFoundry refractory handoff and kiln-output→upgrade application (both need a public material-application seam on the canonical owners that does not exist today), and authored broadsheet/almanac issue content (narrative content). **Blockades unblocked in passing:** the `triad_drift` shell gate only matched `private void Save…/Setup…`, so it had been false-failing for the entire shipped Expansion 25/29 series; reconciled to the xUnit semantics (visibility-agnostic presence check, `SavePresentation` exempted by name as a documented read-model flush) — that gate had been hiding behind the green xUnit twin. Also reconciled two concurrent agents' in-flight Expansion 32/33 sections (`wildlife_harvest`, `storm_forecast`) into the architecture map and the event parity matrix so the generated gates could regenerate, and raised the `docs_index_drift` timeout 30s→120s (the index generator now measures ~33s at 5106 documents). **Evidence:** host + Core builds 0 errors / 0 warnings; `--broadsheet-press-selftest` 12/12; `--kilnworks-selftest` 12/12; `BroadsheetPressLedgerTests` 6/6; `KilnFiringLedgerTests` 6/6; pre-existing `PublicBroadsheetPressEngineTests` 5/5 and `KilnFiringEngineTests` 5/5 still green (neither engine was edited); `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1496/1496 (section pin 245→249, carrying the concurrent 32/33 rows); `MainTriadDriftGateTests` 7/7; `DayEventParitySourceGateTests` 2/2; `--port-contract-selftest` PASS (301 seams, 196 host-required bound); `--7-day-smoke-selftest` PASS; `--data-integrity-selftest` PASS (0 errors, 422 catalogs); architecture map 249 subsystems at 100% mechanical evidence; save-store matrix 251; selftest manifest 182 tests; **plan-integration audit 46/46 INTEGRATED** (both new plans added to the generator, so they can never silently regress to partial); 7/7 fast drift gates green through `run-gates.py`. `DEC-327`, `DEC-328`, and `DEC-329` ratified. Readiness plan: `docs/plans/UNBLOCK_EXPANSION30_31_INTEGRATION_PLAN.md`.
**UNBLOCK-EXPANSION-32-33 — THE WILD (WILDLIFE HARVEST QUOTA) + THE WEATHER (STORM FORECAST READINESS) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-32-33-2026-09-24`). Both engines were signed pure-domain authorities with tests (DEC-86, DEC-87) but **0 host references**, so unreachable. **Expansion 32 (The Wild):** added stateful `WildlifeHarvestLedger` + `WildlifeHarvestState`/`WildlifeHarvestCensus` (read-only quota evaluation, apply-harvest, season reset, predator-conflict and taming-readiness passthrough, schema-gated restore); host `WildlifeHarvestSaveStore` (section `wildlife_harvest`) + `WildlifeHarvestHostSession` + `Main.WildlifeHarvest.cs`; phase-5 day owner emits `wildlife_harvest_ticked`. `WildlifeMigrationSystem`/`WildlifeEcosystemSystem` remain the population/extinction authority and `WildlifeTrappingSystem` owns trap sites — the harvest ledger records only what was taken. `--wildlife-harvest-selftest` 12/12. **Expansion 33 (The Weather):** added stateful `StormForecastLedger` + `StormForecastState`/`StormForecastCensus` (observation-post calibration drift, drill-recency decay, forecast from live skill, warning issuance, readiness assessment, schema-gated restore); host `StormForecastSaveStore` (section `storm_forecast`) + `StormForecastHostSession` + `Main.StormForecast.cs`; phase-5 day owner emits `storm_forecast_ticked`. `WeatherSystem`/`WeatherGate` remain the weather authority; readiness inputs come from their canonical owners at the call site. `--storm-forecast-selftest` 12/12. **Deferred with named reasons:** live-binding each readiness input to its owner, auto-issuing forecasts from a typed storm-onset hook (commands are the surface today), the broader expansion narrative content, and UI panels (concurrently-owned panel-route seams). **Evidence:** host + Core builds 0/0; both probes 12/12; new ledger tests 5/5 each; `--data-integrity-selftest` PASS; `--port-contract-selftest` PASS (301 seams); `--7-day-smoke-selftest` PASS; Save 1496/1496 (section pin 249); architecture map 249 (100%); save-store matrix 251; selftest manifest 182; `agent-fast-verify` 10/10. `DEC-327` + `DEC-328` ratified. Readiness plan: `docs/plans/UNBLOCK_EXPANSION32_33_INTEGRATION_PLAN.md`.
**UNBLOCK-EXPANSION-25-29 — THE IRON ROAD (RAIL MAINTENANCE) + THE GLASS (GLASSWORKS) FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-expansion-25-29-2026-09-24`). Both engines were signed pure-domain authorities with tests (DEC-80, DEC-83) but **0 host references**, so their behavior was unreachable. **Expansion 25 (The Iron Road):** added stateful `RailTrackMaintenanceLedger` + `RailMaintenanceState`/`RailMaintenanceCensus` (seed-from-topology, read-only evaluate, apply-run, workgang maintain, block/unblock, schema-gated restore); host `RailTrackMaintenanceSaveStore` (section `rail_track_maintenance`) + `RailTrackMaintenanceHostSession` + `Main.RailTrackMaintenance.cs`; the ledger seeds segment ids from the canonical `RailwaySystem.SegmentDefs` and `Main.Plans190_193.cs` records run wear from the existing `OnTrainDispatched` event (classifying train mass into the engine's locomotive table); phase-5 day owner emits `rail_track_maintenance_ticked`. `RailwaySystem` stays sole topology/dispatch/pathfinding owner; the ledger never moves a train. `--rail-track-maintenance-selftest` 12/12. **Expansion 29 (The Glass):** added stateful `GlassworksLedger` + `GlassworksState`/`GlassworksCensus` (charge batch, advance annealing, grind corrective lens consuming grit stock, calibrate theodolite, vision prescriptions, schema-gated restore); host `GlassworksSaveStore` (section `glassworks`) + `GlassworksHostSession` + `Main.Glassworks.cs`; phase-5 day owner advances one deterministic anneal stage per uncracked batch every two days at the authored-optimal kiln and emits `glassworks_ticked`. `PrecisionOpticsEngine` keeps the optical catalog, `RadiationSystem` browning, `GreenhouseSystem` glazing, `GeodeticSurveyEngine` survey. `--glassworks-selftest` 12/12. **Deferred with named reasons:** the broader expansion narrative content (rail towns/interdiction/route politics; glass furnaces as a buildable, glazing infrastructure, mirrors), a dedicated UI panel (concurrently-owned panel-route seams; host commands + probes are the operational surface), and wiring maintenance feasibility into `RailwaySystem` dispatch refusal (would need an optional provider hook on the canonical rail system; `EvaluateRailTrack` is the advisory seam today). **Evidence:** host + Core builds 0/0; both probes 12/12; Rail 10/10 + Optics 10/10 (new ledger tests 5/5 each); `--data-integrity-selftest` PASS; `--port-contract-selftest` PASS (301 seams); `--7-day-smoke-selftest` PASS; Save 1472/1472 (section pin 243→245); architecture map 245 (100%); save-store matrix 247; selftest manifest 178; `agent-fast-verify` 10/10. `DEC-325` + `DEC-326` ratified. Readiness plan: `docs/plans/UNBLOCK_EXPANSION25_29_INTEGRATION_PLAN.md`.
**UNBLOCK-OLDEST-PLAN181 — PLAN 181 DIFFICULTY SETTINGS SYSTEM FULL HOST INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-181-difficulty-settings-2026-09-24`). Audit row "Plan 181 — Partial, Core-only (overlaps XP difficulty binding)" resolved with the overlap stated precisely. **Already live (XP-01 / DEC-40):** `DifficultyPresetCatalog` + loader, `DifficultyDirector`, `DifficultyScalarsProvider`, `difficulty_presets.json` (4 presets), the campaign-header identity preset, and the eight scalar consumers. **The gap:** the signed pure-domain `DifficultySettingsSystem` (DEC-174) had **0 host references** — preset selection, the eight custom slider lanes, and the ironman lock were unreachable in play. **Integrated (single authority, no second catalog):** Core additive `DifficultyScalarsProvider.FromScalars`/`HasSameScalarsAs`, `DifficultySettingsSystem.BindCatalog`/`GetEffectiveProvider`/`GetCensus`+`DifficultySettingsCensus`/schema-gated `RestoreState`; host `DifficultySettingsSaveStore` (section `difficulty_settings`, checksummed) + `DifficultySettingsHostSession` + `Main.DifficultySettings.cs`; one additive hook in `Main.Difficulty.cs` (`SetupDifficulty`) and one in `SelectDifficultyForNewCampaign` route the settings authority's effective provider into the existing XP-01 `_difficultyScalars` field (consumers read it live; no rebind). The header stays the immutable campaign-identity preset; the settings section is the only mutable runtime store when unlocked. CLI probe `--difficulty-settings-selftest` **12/12**. **Deferred with named reasons:** the runtime slider/lock panel (registering a new routed panel needs coordinated edits across `PanelRegistryBootstrap`, `OpenPlayerPanel`, `Main.UiPanels`, `Main.PlayerSurfaces`, `PlayerSurfaceManifest` — concurrently-owned shared seams; the campaign-start preset selector already exists and the host commands + probe are the operational surface); `DifficultyConsequenceWeave` consumer wiring (EN-01/DEC-40, not Plan 181 — its four world consumers change war/crisis behaviour); custom-preset naming/sharing, comparison view, difficulty journal, narrative events, and completion quest hooks (presentation/narrative); HUD indicator placement (same UI seams). **Evidence:** host + Core builds 0/0; `--difficulty-settings-selftest` 12/12; Difficulty suite 29/29 (incl. new `DifficultySettingsCensusAndSchemaTests` 5/5 and existing `Plan181DifficultySettingsIntegrationTests` 6/6); Save section pin 239→243 (carries concurrent 186/188/214); `--data-integrity-selftest` PASS; `--port-contract-selftest` PASS (301 seams); architecture map 243 subsystems (100%); save-store matrix 245; selftest manifest 176; `agent-fast-verify` 10/10. `DEC-324` ratified. Readiness plan: `docs/plans/UNBLOCK_OLDEST_PLAN181_INTEGRATION_PLAN.md`. **Note:** this commit also carries the concurrently-active Plans 186/188 (shelter maintenance, survivor routines) and 214 (visitor integration), whose host sessions + save sections + probe descriptors were already present in the shared tree; their packages remain in-flight (probes 3/12, 6/12) and are NOT claimed here — only their static gates (arch nodes, port seams, manifest, section pin) were reconciled so the tree compiles and the gates are green.
**UNBLOCK-OLDEST-PLAN171-174 — PLANS 171 + 174 (MECHANICAL ORIGIN) FULL INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-171-174-2026-09-24`). **Plan 171 (Dynamic Quest Generation):** `DynamicQuestGenerator` and `dynamic_quest_templates.json` existed but the catalog was registered `UNRESOLVED` and the generator was reachable only through a static bridge. Added a strict `DynamicQuestTemplateCatalogLoader`, extended `ProceduralQuestType` with the two authored types (`ScoutExploration`, `DisputeResolution`), added `BindAuthoredTemplates` + `DynamicQuestGeneratorCensus` + schema-gated `RestoreState`, and carried reward-item bindings through generation/capture. `DynamicQuestHostSession` + `Main.DynamicQuestGeneration.cs` bind the authored catalog at campaign start; the canonical `QuestRuntimeCoordinator` remains the accepted-quest lifecycle and save owner (no second lifecycle/section). `--dynamic-quest-selftest` 12/12. **Plan 174 / C3-174 (Mechanical Origin Effects Seam):** `SurvivorEnrichmentService.GetOriginModifier` had zero host references. Added the authored `metallurgist` profession mapping (previously unmapped, so an authored survivor resolved no skill) and `Main.OriginMechanics.cs`, which applies each enriched survivor's primary skill through `SkillProgressionSystem` and personal keepsake through the canonical inventory owner, gated on the item resolving. `--origin-mechanics-selftest` 12/12. **Recorded data gap:** 65 of 76 authored keepsake ids exist in no data JSON — recorded in `KNOWN_DEBT.md`; the host guards against it. **Deferred with named reasons:** routing the generator's candidates into the canonical runtime as an extra template source (the two template schemas differ; would need a designed bridge or become a second authority), `metallurgist` mastery milestones (crafting attribution owner), authoring the orphan keepsakes (data debt), and dynamic-quest/origin UI (presentation). **Evidence:** host + Core builds 0/0; `--dynamic-quest-selftest` 12/12; `--origin-mechanics-selftest` 12/12; `--data-integrity-selftest` PASS; `--port-contract-selftest` PASS (298 seams); `--7-day-smoke-selftest` PASS; `--selftest-manifest` 171 tests; Quests 34/34; Survivors 522/522; Mods 60/60; Shelter 785/785; architecture map 239 subsystems. `DEC-322` and `DEC-323` ratified. Readiness plan: `docs/plans/UNBLOCK_OLDEST_PLAN171_174_INTEGRATION_PLAN.md`.
**UNBLOCK-PLAN-176-AGING — PLAN 176 FULL INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-176-aging-2026-09-24`). **Plan 176 (Aging & Elderly Survivor System):** Forensically proved partial (Core-only `AgingSystem` and `SurvivorAgingProgressionEngine` with 0 host references, 0 save stores, 0 save sections, 0 day owners, 0 CLI probes). Converted to a 100% fully integrated host feature. Pure Core authority extended with `AgingCensus` struct, `GetCensus()`, and `BindValidatedCatalog(LifeStagesCatalog)`. Created strict catalog loader `LifeStagesCatalogLoader` validating `life_stages.json` (schema 1, stages non-empty, unique valid enum mappings, non-negative spans, positive labor/fatigue multipliers). Registered save section `aging` (`aging_save.json`, section #239) in `SaveSectionRegistry`. Implemented `AgingHostSession` and `AgingSaveStore` (`SaveStoreHub.Checksummed<AgingState>`). Bound phase-5 campaign day owner `aging` (`AgingDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot restore in `Main.CampaignOwners.cs`. Wired player UI presentation via `AgeProfileProvider` in `SurvivorDetailPanel.cs`, `Main.UiPanels.cs`, and `Main.Aging.cs`. Classified `aging_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Bound lifecycle into `Main.SaveOrchestrator.cs` and `Main.Application.cs`. Added CLI self-test probe `--aging-selftest` / `--elderly-survivor-selftest` in `HostCliRegistry`, `src/Host/HostCli.Aging.cs`, and `src/Host/HostCli.cs` with 12 validation checks. **Evidence:** host and Core test builds 0 errors / 0 warnings; `Plan176AgingHostIntegrationTests` 10/10 PASS; `LifeStagesCatalogLoaderTests` 10/10 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1/1 PASS (section pin 239, 1436/1436 PASS); `MainTriadDriftGateTests` 7/7 PASS; runtime CLI probe `--aging-selftest` 12/12 PASS; architecture graph regenerated with 239 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (241 stores); `generate-selftest-manifest.py` (171 tests); `agent-fast-verify.py` 10/10 PASS; `DEC-322` ratified in `docs/governance/DECISION_REGISTER.md`. Zero partial residue remains.
**UNBLOCK-PLAN-159-SHELTER-GOVERNANCE — PLAN 159 FULL INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plan-159-shelter-governance-2026-09-24`). **Plan 159 (Shelter Governance & Political System):** Forensically proved partial (Core-only `ShelterGovernanceEngine` with 0 host references, 0 save stores, 0 save sections, 0 day owners, 0 CLI probes). Converted to a 100% fully integrated host feature. Pure Core authority extended with `ShelterGovernanceCensus` struct, `GetCensus()`, `GetSurvivorBlocDisplayName()`, `BindValidatedBlocs()`, and `RecalculateBlocWeights()`. Created strict catalog loader `ShelterGovernanceCatalogLoader` validating `shelter_governance_blocs.json` (schema 1, 4 canonical blocs: Security, Egalitarian, Free Pioneers, Heritage Archive). Registered save section `shelter_governance` (`shelter_governance_save.json`, section #238) in `SaveSectionRegistry`. Implemented `ShelterGovernanceHostSession` and `ShelterGovernanceSaveStore` (`SaveStoreHub.Checksummed<ShelterGovernanceSaveState>`). Bound phase-5 campaign day owner `shelter_governance` (`ShelterGovernanceDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot restore in `Main.CampaignOwners.cs`. Wired player UI presentation via `PoliticalBlocProvider` in `SurvivorDetailPanel.cs`, `Main.UiPanels.cs`, and `Main.ShelterGovernance.cs`. Classified `shelter_governance_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Bound lifecycle into `Main.SaveOrchestrator.cs` and `Main.Application.cs`. Added CLI self-test probe `--shelter-governance-selftest` in `HostCliRegistry`, `src/Host/HostCli.ShelterGovernance.cs`, and `src/Host/HostCli.cs` with 12 validation checks. **Evidence:** host and Core test builds 0 errors / 0 warnings; `Plan159ShelterGovernanceHostIntegrationTests` 10/10 PASS; `ShelterGovernanceEngineTests` 8/8 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1/1 PASS (section pin 238); `MainTriadDriftGateTests` 7/7 PASS; runtime CLI probe `--shelter-governance-selftest` 12/12 PASS; architecture graph regenerated with 238 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (240 stores); `generate-selftest-manifest.py` (168 tests); `DEC-320` ratified in `docs/governance/DECISION_REGISTER.md`. Zero partial residue remains.

**UNBLOCK-OLDEST-PLAN165-166 — PLANS 165 + 166 FULL INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-165-166-2026-09-24`). **Plan 165 (Modding Support & Mod Data Contract):** `ModSupportSystem` and `mod_manifest_schema.json` existed but the host mod path (`ModRuntime` + `JsonModLayering`) never consulted the authority. Added strict `ModManifestSpecificationLoader` and a `BindSpecification` seam; added `ModSupportHostSession` (strict spec load, manifest discovery/registration, `EligibleModIds`, deterministic `ResolveLoadOrder`, `DetectConflicts`, census); `ModRuntime.Prepare` now gates the layering engine to the authority's eligible ids, while `JsonModLayering` stays the overlay materializer. Enable/disable persists through `UserSettingsData.EnabledMods` — no campaign save section. `--mod-support-selftest` 12/12. **Plan 166 (Shelter Identity, Naming & Origin):** `ShelterIdentitySystem` and `shelter_origins.json` had zero host references. Added strict `ShelterOriginCatalogLoader` and a `BindOrigins` seam; `ShelterIdentityCensus` + schema-gated `RestoreState`; `ShelterIdentityHostSession` + `ShelterIdentitySaveStore` (registered section `shelter_identity`); `Main.ShelterIdentity.cs` selects the deterministic founding origin on a fresh campaign, records the founder, ticks in phase 5 with `IPreDaySnapshotRestore`, and records community actions from canonical day facts; `shelter_identity_ticked` is an internal heartbeat; `--shelter-identity-selftest` 12/12. **Authority boundaries:** faction standing stays owned by `FactionWarSystem` (no competing reputation store); `JsonModLayering` stays the overlay materializer behind `ModSupportSystem`; mod enablement persists in user settings. **Deferred with named reasons:** trade/raid/isolation community-action axes (no canonical emitted day kind exists), the faction-reputation projection (would be a second store without a designed reconciliation seam), and shelter-identity / mod-management UI panels (presentation). **Evidence:** host + Core builds 0/0; `--mod-support-selftest` 12/12; `--shelter-identity-selftest` 12/12; `--data-integrity-selftest` PASS; `--port-contract-selftest` PASS (296 seams); `--7-day-smoke-selftest` PASS; `--selftest-manifest` 168 tests; Mods 60/60; Shelter 785/785; Save 1543/1543; Tooling 122/122; Campaign 257/257; architecture map 238 subsystems; save-store matrix 240; docs index 4417. `DEC-320` and `DEC-321` ratified. Readiness plan: `docs/plans/UNBLOCK_OLDEST_PLAN165_166_INTEGRATION_PLAN.md`.
**UNBLOCK-OLDEST-PLAN167-169 — PLANS 167 + 169 FULL INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-plans-167-169-2026-09-24`). **Plan 167 (Underground Tunnel Network):** the authority (`TunnelNetworkSystem`, wrapped by `WastelandMapSystem.Tunnels`) was already single and correct, but the authored `underground_tunnels.json` was never loaded (the map seeded only a hardcoded two-segment network) and `TickDay` was never called (no structural wear/collapse). Added strict `TunnelNetworkCatalogLoader` (unsupported schema, empty/duplicate junction or segment ids, empty names, missing/self endpoints, `length_hours < 0.5`, `difficulty` outside 1..5, `integrity` outside 0..100, unknown hazard tokens, and dangling junction→segment references are all load errors); `TunnelNetworkSystem.Clear()` + `GetCensus()` + schema-gated `RestoreState`; `WastelandMapSystem` now seeds the authored catalog when supplied (falling back to the built-in canonical network otherwise), loaded by `WastelandMapCatalogLoader.CreateSystem`. A restored campaign replaces the seeded state before any tick, so a player's network survives. Host `src/Main.TunnelNetwork.cs` exposes setup/tick/census/reinforce/clear-hazard/bypass; a phase-5 day owner `tunnel_network` with `IPreDaySnapshotRestore` runs the daily wear inside the fail-closed advance; `tunnel_network_ticked` is an internal heartbeat; `--tunnel-network-selftest` 12/12. No second store — the network persists inside the existing `world_map` section. **Plan 169 (Audio Accessibility & Mix Legibility):** `AudioAccessibilityCoordinator` and `audio_accessibility_cues.json` had zero host references. Added strict `AudioAccessibilityCatalogLoader` (unsupported schema, empty/duplicate cue or preset ids, empty bus name, empty visual label, unknown severity, ducking outside -24..0 dB, negative coalesce window, and out-of-range preset values are all load errors); `BindCatalog` + `GetCensus()`; `AudioAccessibilityHostSession` binding the coordinator's seams to delegate sinks; `src/Main.AudioAccessibility.cs` loading the catalog, applying the persisted preset, and translating canonical day facts (`power_critical_deficit`, `power_brownout_began`, `medical_admitted`, `radio_intercept`, `radio_intercept_decrypted`, `expedition_milestone`, `raid_incoming`) into critical cues via the `CampaignDayCoordinator.OnDayAdvanced` bridge — no gameplay decision lives there; `AudioManager.ApplyAccessibilityDucking`/`ApplyAccessibilityMixPreset` duck background buses only (alerts/voice/medical/radio stay legible) and apply a bounded master-gain preset offset; `UserSettingsData.VisualAudioAlerts`/`AudioMixPreset` persist the preference through the sole existing settings authority; `--audio-accessibility-selftest` 12/12. **Deferred with named reasons:** tunnel reinforce/clear-hazard UI panel (presentation; the MapPanel tunnel card already renders state), the preset high-frequency attenuation as a dedicated mixer shelf (mixer effect ownership stays in `AudioManager`, applied as a bounded master offset), and authored feedback-toast copy for audio notifications (the host logs the notification and exposes `VisualNotificationSink`). **Evidence:** host + Core builds 0 errors / 0 warnings; `--tunnel-network-selftest` 12/12; `--audio-accessibility-selftest` 12/12; `--data-integrity-selftest` PASS; `--port-contract-selftest` PASS (293 seams); `--7-day-smoke-selftest` PASS; `--selftest-manifest` 165 tests; Underground 24/24; Audio 47/47; World 544/544; Settings 34/34. `DEC-318` and `DEC-319` ratified. Readiness plan: `docs/plans/UNBLOCK_OLDEST_PLAN167_169_INTEGRATION_PLAN.md`.

**UNBLOCK-C3-PLANS-192-199 — PLANS 192 + 199 FULL INTEGRATION COMPLETE (2026-09-24):** user-authorized integrator package (claim `claim-unblock-c3-plans-192-199-2026-09-24`), permanently unblocking, lifting, and sealing the C3 HOLDs for Plan 192 and Plan 199.
**(1) Plan 192 (Scheduled Trade Route Contracts & Player Route System):** Forensically proved partial (Core-only `TradeRouteContract` with 0 host references, 0 save stores, 0 save sections, 0 day owners, 0 CLI probes). Converted to a fully integrated host feature. Implemented pure Core authority `PlayerTradeRouteSystem` managing trade route contracts, cancellation cooldowns, run scheduling, and reliability rating calculation. Added `TradeRouteCensus` struct and `GetCensus()` to `TradeRouteContract`. Registered save section `trade_routes` (`trade_routes_save.json`, section #235) in `SaveSectionRegistry`. Implemented `TradeRouteHostSession` and `TradeRouteSaveStore` (`SaveStoreHub.Checksummed<PlayerTradeRouteSaveState>`). Bound phase-5 campaign day owner `trade_routes` (`TradeRouteDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot restore in `Main.CampaignOwners.cs`. Wired tariff payments directly through canonical `FundsLedger.TryDebit(tariff, FundsLedger.ReasonRouteTariff, routeId, day)` (no duplicate currency or unbacked credits). Classified `trade_route_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Bound lifecycle into `Main.SaveOrchestrator.cs` and `Main.Application.cs`. Added CLI self-test probe `--trade-routes-selftest` in `HostCliRegistry`, `src/Host/HostCli.TradeRoutes.cs`, and `src/Host/HostCli.cs` with 12 validation checks.
**(2) Plan 199 (Seasonal Human Migration Schedule Engine):** Forensically proved partial (Core-only `SeasonalHumanMigrationEngine` with 0 host references, 0 save stores, 0 save sections, 0 day owners, 0 CLI probes). Converted to a fully integrated host feature. Authored canonical catalog `seasonal_human_migration.json` (schema 1, 4 factions: Compact, Scale, Flotilla, Overlay with seasonal phase schedules). Implemented `SeasonalMigrationCatalogLoader` and added `HumanMigrationCensus` struct and `GetCensus()` to `SeasonalHumanMigrationEngine` along with `JsonPropertyName` annotations. Registered save section `human_migration` (`human_migration_save.json`, section #236) in `SaveSectionRegistry`. Implemented `HumanMigrationHostSession` and `HumanMigrationSaveStore` (`SaveStoreHub.Checksummed<SeasonalMigrationSaveState>`). Bound phase-5 campaign day owner `human_migration` (`HumanMigrationDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot restore in `Main.CampaignOwners.cs`. Enforces 10-day dwell hysteresis, modulates regional human population weights without mutating wildlife or fauna ecosystems. Classified `human_migration_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Bound lifecycle into `Main.SaveOrchestrator.cs` and `Main.Application.cs`. Added CLI self-test probe `--human-migration-selftest` in `HostCliRegistry`, `src/Host/HostCli.HumanMigration.cs`, and `src/Host/HostCli.cs` with 12 validation checks.
**Evidence:** host and Core test builds 0 errors / 0 warnings; `Plan192TradeRouteHostIntegrationTests` 7/7 PASS; `TradeRouteContractTests` 6/6 PASS; `Plan199HumanMigrationHostIntegrationTests` 9/9 PASS; `SeasonalHumanMigrationEngineTests` 3/3 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1418/1418 PASS (section pin 234→236); `MainTriadDriftGateTests` 7/7 PASS; architecture graph regenerated with 236 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (238 stores); `generate-selftest-manifest.py` (163 tests); `generate-catalog-registry.py` (706 catalogs); `generate-docs-index.py` (4290 docs); `agent-fast-verify.py` 10/10 PASS; `DEC-318` and `DEC-319` ratified in `docs/governance/DECISION_REGISTER.md`. All C3 HOLDs (174, 175, 192, 199) are now permanently lifted, resolved, and sealed.

**UNBLOCK-C3-PLANS-174-175-CORRECTIVE — PLANS 174 + 175 LIVE INTEGRATION + FALSE-EVIDENCE CORRECTION (2026-09-23):** user-authorized integrator follow-up to `UNBLOCK-C3-PLANS-174-175`. The prior claim sealed both plans against the Core tests but never ran the two runtime probes it cited; run here for the first time, they failed **`--backstory-selftest` 5/12** and **`--meta-progression-selftest` 4/12**, and neither feature had any live effect in a real campaign. Corrected: (1) **false probe evidence** — `HostCli.Backstory.cs` referenced catalog ids that do not exist in the authored `backstory_templates.json` (`template_combat_medic`, `exp_scavenger_ruins`, `secret_field_loss`) while the Core tests correctly bind the real contract (`bst_military_medic`, `exp_wilderness_survivor`, `carries_guilt_over_one_loss`); `HostCli.MetaProgression.cs` hand-forged a `CampaignCompletionRecord` with a fake `completionId` and empty `Checksum`, which the real `CampaignCompletionHistoryService.TryValidate` correctly rejects, so `CrossRunProfileStore.Record` always returned `InvalidHistory`. Both probes now build their fixtures through the real APIs; **`--backstory-selftest` 12/12**, **`--meta-progression-selftest` 12/12**. (2) **inert backstory** — nothing in a campaign ever assigned an origin (`AssignFromTemplate` had only probe/test callers), so `SurvivorDetailPanel.BackstoryProvider` always returned null; added `Main.AssignMissingBackstories()`, which assigns a template to every roster survivor that lacks one, indexed only by `StableHash.Of(survivorId)` over the ordinal-sorted template table — no campaign RNG stream is consumed, so every other stream's replay order is unchanged — called from `Main.GameFlow` at new-campaign start and from `TickBackstory`. (3) **meta progression was not cross-run** — prestige/unlocks/boons were only ever written to the per-slot `meta_progression_save.json`, so a fresh slot started empty and nothing carried over; `Main.SeedMetaProgressionFromProfile()` now derives prestige and unlocks from the user-level append-only `CompletionHistoryStore` (`user://completion_history.json`) at session setup, the single cross-run authority (DEC-20). (4) **NG+ boons had no effect** — `meta_unlockables.json` boons gained authored `grants` (`MetaGrantDef`: `meta_emergency_rations_boon` → `dried_rations` ×15, `meta_field_medic_boon` → `bandage` ×5 + `antiseptic_1l_of_1l` ×2, `meta_forged_toolkit_boon` → `calibration_kit` ×1, `meta_geodetic_survey_boon` → `item_surveyor_stadia_rod` ×1); `MetaProgressionSystem.GetGrantsForBoon` / `GetActiveNgPlusGrants` expose them, and `Main.SetNgPlusBoonActive` applies the newly activated boon's grants through the canonical `Inventory.AddById` authority (no second resource ledger). (5) **cross-campaign leak** — `ResetBackstory` / `ResetMetaProgression` (and the sibling late-wave sessions 135/147/148/150/152) had zero callers, so their null-guarded `Setup*` reused the previous run's live instance after a new-game or slot switch; new `late_wave_integrations` lifecycle participant calls `ResetLateWaveIntegrationSessions()`. **Deferred with named reasons:** the graphical New Game+ setup screen and boon-selection UI (presentation; the profile/unlock/selection model is complete and headless-exercisable), the geodetic "reveal adjacent sectors" map effect (map authority), and any backstory presentation beyond the existing survivor-inspection read. **Evidence:** host + Core builds 0 errors / 0 warnings; `--backstory-selftest` 12/12; `--meta-progression-selftest` 12/12; `--data-integrity-selftest` PASS (0 errors, 421 catalogs); `Plan174BackstoryHostIntegrationTests` 10/10; `Plan175MetaProgressionHostIntegrationTests` 12/12; `Plan174SurvivorBackstoriesIntegrationTests` 6/6; `CrossRunProfileStoreTests` 4/4. `DEC-317` ratified.

**UNBLOCK-C3-PLANS-174-175 — PLANS 174 + 175 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-c3-plans-174-175-2026-09-23`), lifting and permanently sealing the C3 HOLDs for Plan 174 and Plan 175. **Plan 174 (Procedural Survivor Backstories & Origin Mechanics):** Converted the Core backstory authority (`BackstorySystem`) into a fully integrated host feature. Authored catalog `backstory_templates.json` loaded via `BackstorySystem.LoadCatalog`. Core authority extended with `BackstoryCensus` struct, `GetCensus()`, and `GetAllAssignedBackstories()`. Added save section `backstory` (`backstory_save.json`, section #231) to `SaveSectionRegistry`. Implemented `BackstoryHostSession` and `BackstorySaveStore` (`SaveStoreHub.Checksummed<BackstoryState>`). Bound phase-5 campaign day owner `backstory` (`BackstoryDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot restore in `Main.CampaignOwners.cs`. Wired `SurvivorDetailPanel.BackstoryProvider` delegate rendering origin, pre-war life, defining moment, and backstory flavor in survivor inspection UI. Classified `backstory_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Bound lifecycle into `Main.SaveOrchestrator.cs` and `Main.Application.cs`. Added CLI self-test probe `--backstory-selftest` in `HostCliRegistry`, `src/Host/HostCli.Backstory.cs`, and `src/Host/HostCli.cs` with 12 validation checks. **Plan 175 (Meta Progression & Prestige System):** Converted the Core meta-progression authority (`MetaProgressionSystem`) into a fully integrated host feature. Authored canonical catalog `meta_unlockables.json` (schema 1, 10 definitions spanning crests, insignias, and NG+ boons). Implemented `MetaProgressionSystem` and `MetaProgressionCensus` in pure Core, calculating prestige from `CrossRunProfileStore` run facts while respecting DEC-20 boundaries. Added save section `meta_progression` (`meta_progression_save.json`, section #232) to `SaveSectionRegistry`. Implemented `MetaProgressionHostSession` and `MetaProgressionSaveStore` (`SaveStoreHub.Checksummed<MetaProgressionSaveState>`). Bound phase-5 campaign day owner `meta_progression` (`MetaProgressionDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot restore in `Main.CampaignOwners.cs`. Bound campaign completion hook in `Main.Endgame.cs` (`OnCampaignSealed`), recording completion facts into `MetaProgressionHostSession`. Classified `meta_progression_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Bound lifecycle into `Main.SaveOrchestrator.cs` and `Main.Application.cs`. Added CLI self-test probe `--meta-progression-selftest` in `HostCliRegistry`, `src/Host/HostCli.MetaProgression.cs`, and `src/Host/HostCli.cs` with 12 validation checks. Evidence: host and Core test builds 0 errors / 0 warnings; `Plan174BackstoryHostIntegrationTests` 9/9 PASS; `Plan174SurvivorBackstoriesIntegrationTests` 6/6 PASS; `Plan175MetaProgressionHostIntegrationTests` 9/9 PASS; `CrossRunProfileStoreTests` 4/4 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1406/1406 PASS; `MainTriadDriftGateTests` 7/7 PASS; architecture graph regenerated with 234 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (236 stores); `generate-selftest-manifest.py` (161 tests); `generate-catalog-registry.py` (705 catalogs); `generate-docs-index.py` (4268 docs); `agent-fast-verify.py` 10/10 PASS; `DEC-313` and `DEC-314` ratified in `docs/governance/DECISION_REGISTER.md`. C3 HOLDs 174 and 175 are permanently resolved.

**UNBLOCK-OLDEST-BATCH12-PLANS — PLANS 150 + 152 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch12-plans-2026-09-23`), the twelfth batch of the partial plans queue and the last two `Ready (Core-only)` entries on it. **Plan 150 (Romance & Family Dynamics):** the Core romance/family authority gains a strict authored-courtship path and a real host seam. `RomanceCourtshipCatalogLoader` validates `romance_courtship.json` row by row (empty or duplicate `event_id`, empty name, affinity outside 0..100, non-finite or out-of-range success rate, zero score gain, negative stress reduction, and an unsupported schema version are all load errors), and `RomanceCourtshipCatalog.BindValidatedEvents` is the only seam the host uses — so the lenient `LoadFromJson` parser can no longer turn an authored typo into a live courtship event. `RomanceFamilyCensus` + `GetCensus()` expose the live surface, and `RestoreState` is now schema-gated (a payload omitting the field is the original v1 shape and still loads, so pre-integration saves keep working). The batch also fixed a latent data-loss defect: `CaptureState` wrote `CohabitationQuarters` but `RestoreState` never read it back, so the assignment was dropped on every save/load cycle. Authored `romance_courtship.json` (20 courtship events, including the five ids the Core contract already required: `shared_meal` +5, `quiet_walk` +8, `deep_conversation` +12, `crafted_gift` +12, `mutual_vigil` +10). `RomanceFamilyHostSession` + `RomanceFamilySaveStore` (section `romance_family`, `romance_family_save.json`) bind the authority; `Main.RomanceFamily` drives new attraction from the affinity `SurvivorRelationsSystem` already tracks and draws from the campaign's own `social` RNG stream, so a replay is identical; a phase-5 day owner with `IPreDaySnapshotRestore` advances bonded tenure and reports the census; `--romance-family-selftest` 12/12. **Plan 152 (Vehicle Customization & Mobile Base):** `VehicleModuleCatalogLoader` validates `vehicle_modules.json` (empty or duplicate id, unknown module type outside armour/cargo/living/weapon/utility, empty name, negative defence, a speed modifier below the 0.20 floor, a cargo penalty larger than the base hold, negative bunk capacity, sub-day installation, and an unsupported schema version are all load errors), and `VehicleCustomizationCatalog.BindValidatedModules` is the host's only path in. `VehicleCustomizationCensus` + `GetCensus()` expose modules/vehicles/installs/camps/mobile-capable counts and `RestoreState` is schema-gated. Authored `vehicle_modules.json` (20 modules across all five categories, including every id and value the contract required — `reinforced_hull` defence 20 / speed −0.05, `bulletproof_glass` 15 / 0.0, `extended_cargo_bed` cargo +50, `bunk_beds_module` 4 bunks, `mobile_medbay` 1 bunk, `mounted_gun`, `heavy_winch`). `VehicleCustomizationHostSession` + `VehicleCustomizationSaveStore` (section `vehicle_customization`, `vehicle_customization_save.json`) bind it; `Main.VehicleCustomization` deliberately does not degrade condition or burn fuel on the daily tick, because that is the expedition vehicle authority's concern and doing it here would be a second drain on the same resource; a phase-5 day owner with pre-day snapshot rollback keeps the catalog bound; `--vehicle-customization-selftest` 12/12. **Stale gates closed on the way:** three host files still carried hand-rolled `Assets/StreamingAssets/Data` resolvers (`NeedsPerformanceHostSession`, `CampaignLegacyHostSession`, `HostCli.CampaignLegacy`) and were routed through `CatalogPath.ResolveDataDir()` — the single authority with the richer chain — and the undocumented empty catch in `src/Host/HostCli.CampaignLegacy.cs` was documented; `Tooling` went 120/122 → **122/122**. Two Core seams left unwired by earlier batches were declared in the port-contract policy: `CookingSystem.BindValidatedRecipes` as HOST_REQUIRED (it is called from `CookingHostSession`) and `MetaProgressionSystem.BindProfileStore` as OPTIONAL_HOST (the constructor already accepts the store and defaults it, and no caller exists yet). **Deferred with named reasons:** the romance/family UI panel and family-tree view (presentation only), vehicle combat rules, vehicle racing/trading, and famous-vehicle reputation (separate mechanics with their own owners), and the romance quest hooks (`The Matchmaker` et al.), which belong to the quest owner. **Evidence:** host and Core test builds 0 errors / 0 warnings; `--romance-family-selftest` 12/12; `--vehicle-customization-selftest` 12/12; `Plan150RomanceFamilyHostIntegrationTests` 10/10; `Plan152VehicleCustomizationHostIntegrationTests` 10/10; `RomanceCourtshipCatalogLoaderTests` 10/10; `VehicleModuleCatalogLoaderTests` 11/11; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1406/1406 (section pin 232→234); Vehicles 16/16; Survivors 511/511; Records 34/34; Save 1519/1519; Campaign 257/257; Tooling 122/122; Weather 27/27; Governance 48/48; Settlements 12/12; Expeditions 331/331; Content 27/27; Presentation 12/12; Combat 93/93; Narrative 306/306; Endgame 123/123; `--data-integrity-selftest` PASS (0 errors, 421 catalogs); `--port-contract-selftest` PASS (291 seams, 186 host-required bound); `--7-day-smoke-selftest`, `--save-store-checksum-selftest`, `--save-load-ui-failure-selftest`, `--selftest-manifest`, `--content-utilization-selftest` all PASS; architecture map regenerated with 234 subsystems; `generate-save-store-matrix.py` (236 stores); `generate-selftest-manifest.py` (161 tests); `agent-fast-verify.py` 10/10 PASS. Readiness plan: `docs/plans/UNBLOCK_OLDEST_BATCH12_PLANS_150_152_INTEGRATION_PLAN.md`. Next batch head: the oldest-partials queue is empty for Plans 59, 135, 136, 140, 141, 145, 147, 148, 150, and 152.

**UNBLOCK-OLDEST-BATCH11-PLANS — PLANS 147 + 148 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch11-plans-2026-09-23`), the eleventh batch of the partial plans queue. **Plan 147 (Per-NPC Memory & Relationship Depth):** Converted the Core NPC memory authority (`NpcMemorySystem`) into a fully integrated host feature with complete downstream consumption. Authored catalog `npc_memory_dialogue.json` loaded via `NpcMemorySystem.LoadDialogueCatalog`. Core authority extended with `NpcMemoryCensus` struct, `GetCensus()`, and query methods (`Relationships`). Added `npc_memory` save section (`npc_memory_save.json`) to `SaveSectionRegistry` (section count 228→229). Classified `npc_memory_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Implemented `NpcMemoryHostSession` with `SaveStoreHub.Checksummed<NpcMemorySaveState>` (`NpcMemorySaveStore`). Bound downstream consumers: per-NPC personal trust, grudge, favors owed, forgiveness reconciliation (`Forgive`), dialogue tone derivation across 5 emotional tones (`high_trust`, `high_grudge`, `favor_owed`, `betrayed`, `reconciled`), and trade price multipliers (0.85x discount for high trust, up to embargo for severe grudges). Registered phase-5 campaign day owner `npc_memory` (`NpcMemoryDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI diagnostic probe `--npc-memory-selftest` (`--npc-relationships-selftest`) to `HostCliRegistry`, `src/Host/HostCli.NpcMemory.cs`, and `src/Host/HostCli.cs` with 12 validation checks. **Plan 148 (Ideological Friction → Events & Quests):** Converted the Core ideological friction authority (`IdeologicalFrictionEvents` & `IdeologicalFrictionSystem`) into a fully integrated host feature. Authored catalog `ideological_events.json` loaded via `IdeologicalFrictionEvents.LoadCatalog`. Core authority extended with `IdeologicalFrictionCensus` struct and `GetCensus()`. Added `ideological_friction` save section (`ideological_friction_save.json`) to `SaveSectionRegistry` (section count 229→230). Classified `ideological_friction_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Downstream consumers wired: roommate quarters compatibility checks (0.80x sleep penalty for conflicting beliefs, 1.10x synergy bonus for aligned beliefs), daily interpersonal friction ticks, confrontation event triggering, player mediation choices (`SideWithActor`, `SideWithTarget`, `StayNeutral`, `BrokerCompromise`), belief proselytizing/conversion attempts, active bunker faction grouping (coalitions of >= 3 survivors), and `SurvivorsPanel.cs` UI reflection. Implemented `IdeologicalFrictionHostSession` with `SaveStoreHub.Checksummed<IdeologicalFrictionEventSaveState>` (`IdeologicalFrictionSaveStore`), providing full campaign lifecycle bindings: initialization (`SetupIdeologicalFriction`), daily simulation tick (`TickIdeologicalFriction`), dirty flushing (`FlushIdeologicalFrictionIfDirty`), deterministic state restore (`RestoreIdeologicalFriction`), and reset (`ResetIdeologicalFriction`). Registered phase-5 campaign day owner `ideological_friction` (`IdeologicalFrictionDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI self-test probe `--ideological-friction-selftest` (`--ideology-friction-selftest`) to `HostCliRegistry`, `src/Host/HostCli.IdeologicalFriction.cs`, and `src/Host/HostCli.cs` with 12 validation checks. Evidence: host and Core test builds 0 errors / 0 warnings; `Plan147NpcMemoryHostIntegrationTests` 8/8 PASS; `NpcMemorySystemTests` 13/13 PASS; `Plan148IdeologicalFrictionHostIntegrationTests` 9/9 PASS; `Plan148IdeologicalFrictionIntegrationTests` 7/7 PASS; `IdeologicalFrictionSystemTests` 11/11 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1382/1382 PASS; `MainTriadDriftGateTests` 7/7 PASS; architecture graph regenerated with 230 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (232 stores); `generate-selftest-manifest.py` (157 tests); `agent-fast-verify.py` 10/10 PASS; `DEC-311` and `DEC-312` ratified in `docs/governance/DECISION_REGISTER.md`. Next batch head: Plan 150 (Romance & Family System) & Plan 152 (Vehicle Customization & Modular Hardpoints).

**UNBLOCK-OLDEST-BATCH10-PLANS — PLANS 141 + 145 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch10-plans-2026-09-23`), the tenth batch of the partial plans queue. **Plan 141 (Research → Downstream Unlocks Bridge):** Converted the Core research unlock bridge (`ResearchUnlockBridge`) into a fully integrated host feature with complete downstream consumption. Authored catalog `research_unlocks.json` strictly loaded via `ResearchUnlockBridge.LoadFromDirectory`. Core authority extended with `ResearchUnlockCensus` struct, `GetCensus()`, and query methods (`HasCapability`, `HasRecipe`, `HasItem`, `HasUnlock`, `UnlockedCapabilities`, `UnlockedRecipes`, `GrantedItems`, `GrantedUnlockIds`). Added `research_unlock` save section (`research_unlock_save.json`) to `SaveSectionRegistry` (section count 226→227). Classified `research_unlock_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Implemented `ResearchUnlockHostSession` with `SaveStoreHub.Checksummed<ResearchUnlockState>` (`ResearchUnlockSaveStore`). Bound downstream consumers: breakthrough item grants directly into canonical `Inventory`, recipe unlocks into `CraftingSystem`, category capabilities (`shelter`, `expedition`, `combat`, `medical`), retroactive synchronization of completed research nodes from save data, and `ResearchPanel.cs` UI binding. Registered phase-5 campaign day owner `research_unlock` (`ResearchUnlockDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI self-test probe `--research-unlock-selftest` (`--research-unlocks-selftest`) to `HostCliRegistry`, `src/Host/HostCli.ResearchUnlock.cs`, and `src/Host/HostCli.cs` with 12 validation checks. **Plan 145 (Unified Ending Resolution & Epilogue Personalization):** Converted the Core unified ending authority (`UnifiedEndingResolver`) into a fully integrated host feature with complete endgame sealing and personalized chronicle synthesis. Authored catalog `epilogue_personalization.json` strictly loaded via `UnifiedEndingResolver.LoadCatalog`. Core authority extended with `UnifiedEndingCensus` struct and `GetCensus()`. Added `unified_ending` save section (`unified_ending_save.json`) to `SaveSectionRegistry` (section count 227→228). Classified `unified_ending_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Downstream consumers wired: campaign seal orchestration in `Main.Endgame.cs` (`OnCampaignSealed`), literary-grade narrative chronicle synthesis (`fullPersonalizedChronicle`), personal survivor fates with alive/deceased statuses and notable traits, passing `legacyTraitsAwarded` directly into `CampaignLegacyState` (Plan 140 integration), and `EpiloguePanel.cs` UI rendering. Implemented `UnifiedEndingHostSession` with `SaveStoreHub.Checksummed<UnifiedEndingSaveState>` (`UnifiedEndingSaveStore`), providing full campaign lifecycle bindings: initialization (`SetupUnifiedEnding`), daily simulation tick (`TickUnifiedEnding`), dirty flushing (`FlushUnifiedEndingIfDirty`), deterministic state restore (`RestoreUnifiedEnding`), and reset (`ResetUnifiedEnding`). Registered phase-5 campaign day owner `unified_ending` (`UnifiedEndingDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI self-test probe `--unified-ending-selftest` (`--epilogue-selftest`) to `HostCliRegistry`, `src/Host/HostCli.UnifiedEnding.cs`, and `src/Host/HostCli.cs` with 12 validation checks. Evidence: host and Core test builds 0 errors / 0 warnings; `Plan141ResearchUnlockBridgeIntegrationTests` 5/5 PASS; `Plan141ResearchUnlockHostIntegrationTests` 5/5 PASS; `Plan145UnifiedEndingIntegrationTests` 7/7 PASS; `Plan145UnifiedEndingHostIntegrationTests` 5/5 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1370/1370 PASS; architecture graph regenerated with 228 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (230 stores); `generate-selftest-manifest.py` (155 tests); `agent-fast-verify.py` 10/10 PASS; `DEC-309` and `DEC-310` ratified in `docs/governance/DECISION_REGISTER.md`. Next batch head: Plan 147 (Contraband Stash Discovery & Black-Market Barter) & Plan 148 (Expedition Vehicle Recovery & Field Salvage Operations).

**UNBLOCK-OLDEST-BATCH9-PLANS — PLANS 137 + 140 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch9-plans-2026-09-23`), the ninth batch of the partial plans queue. **Plan 137 (Needs → Performance Cascade):** Converted the Core needs performance bridge into a fully integrated host feature with complete downstream consumption. `NeedsPerformanceBridge` provides monotonic performance multipliers (combat accuracy/damage, work speed, expedition stamina drain) over live `SurvivorNeedsState`. Implemented interfaces `ICombatPerformanceModifier`, `IWorkEfficiencyModifier`, and `IExpeditionPerformanceModifier` in Core and host session. Extended with `NeedsPerformanceCensus` (`TotalSurvivorsEvaluated`). Implemented `NeedsPerformanceHostSession` inheriting `HostSessionBase`, evaluating live survivor needs states via `_main.Survivors.Find()`. Pure read model projection allowlisted in `SetupWithoutSaveAllowlist` in `MainTriadDriftGateTests.cs` (Rule 5: zero duplicate save store or shadow state). Registered phase-5 campaign day owner `needs_performance` (`NeedsPerformanceDayOwner`) in `Main.CampaignOwners.cs`. Classified `needs_performance_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Downstream consumers wired: (1) `TacticalCombatSystem.PerformanceLookup` in `Main.Expeditions.cs` scaling combatant accuracy & damage, (2) `ExpeditionSystem.SetSurvivorSpeedMultiplierQuery` in `Main.Expeditions.cs` scaling overland travel speed, (3) `DutyRosterSystem.WorkSpeedMultiplierLookup` in `Main.DutyRoster.cs` scaling shift productivity, (4) `SurvivorDetailPanel` in `src/UI/SurvivorDetailPanel.cs` rendering live performance band and warning icon. Bound lifecycle into `Main.ExpandedShelterSystems.cs` (`SetupNeedsPerformance`, `ResetNeedsPerformance`) and added CLI diagnostic probe `--needs-performance-selftest` (`--needs-perf-selftest`) to `HostCliRegistry`, `src/Host/HostCli.NeedsPerformance.cs`, and `src/Host/HostCli.cs` with 12 validation checks. **Plan 140 (Generational Legacy & Campaign Inheritance):** Converted the Core generational legacy authority (`CampaignLegacySystem`) into a fully integrated host feature with complete endgame sealing and New Game+ inheritance. Authored catalog `legacy_traits.json` (>= 20 traits spanning survivor, shelter, faction, and ending sources) strictly validated. Core authority extended with `CampaignLegacyCensus` struct and `GetCensus()`. Added `campaign_legacy` save section (`campaign_legacy_save.json`) to `SaveSectionRegistry` (section count 225→226). Classified `campaign_legacy_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Downstream consumers wired: (1) `OnCampaignSealed` in `Main.Endgame.cs` archiving completed campaign records into `CampaignLegacy` and committing persistence, (2) `StartNewGame` in `Main.GameFlow.cs` evaluating `PrepareStartingCampaignContext()` and applying historical faction standings and journaling inherited traits. Implemented `CampaignLegacyHostSession` with `SaveStoreHub.Checksummed<CampaignLegacyState>` (`CampaignLegacySaveStore`), providing full campaign lifecycle bindings: initialization (`SetupCampaignLegacy`), daily simulation tick (`TickCampaignLegacy`), dirty flushing (`FlushCampaignLegacyIfDirty`), deterministic state restore (`RestoreCampaignLegacy`), and reset (`ResetCampaignLegacy`). Registered phase-5 campaign day owner `campaign_legacy` (`CampaignLegacyDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI self-test probe `--campaign-legacy-selftest` (`--legacy-selftest`) to `HostCliRegistry`, `src/Host/HostCli.CampaignLegacy.cs`, and `src/Host/HostCli.cs` with 12 validation checks. Evidence: host and Core test builds 0 errors / 0 warnings; `Plan137NeedsPerformanceHostIntegrationTests` 6/6 PASS; `NeedsPerformanceBridgeTests` 7/7 PASS; `Plan140CampaignLegacyHostIntegrationTests` 8/8 PASS; `Plan140GenerationalLegacyIntegrationTests` 5/5 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1358/1358 PASS; `MainTriadDriftGateTests` 7/7 PASS; architecture graph regenerated with 226 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (228 stores); `generate-selftest-manifest.py` (153 tests); `agent-fast-verify.py` 10/10 PASS; `DEC-307` and `DEC-308` ratified in `docs/governance/DECISION_REGISTER.md`. Next batch head: Plan 141 (Research → Downstream Unlocks Bridge) & Plan 145 (Unified Ending Resolution & Epilogue Personalization).

**UNBLOCK-OLDEST-BATCH8-PLANS — PLANS 135 + 136 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch8-plans-2026-09-23`), the eighth batch of the partial plans queue. **Plan 135 (Weather → Deep Gameplay Cascade):** Re-verified complete with all 15 authored effects in `weather_gameplay_effects.json`, strict loader `WeatherCascadeCatalogLoader`, `WeatherCascadeHostSession`, phase-5 campaign day owner with pre-day snapshot rollback, registered save section `weather_cascade` (#223), and CLI probe `--weather-cascade-selftest`. Tests: `Plan135WeatherCascadeHostIntegrationTests` 16/16 PASS, `Plan135WeatherCascadeIntegrationTests` 5/5 PASS. **Plan 136 (Wildlife Trapping → Food Pipeline & Cooking):** Converted the Core cooking authority (`CookingSystem`) and wildlife trapping food pipeline into a fully integrated host feature. Authored catalog `recipes_cooking.json` strictly loaded and validated via `CookingRecipeCatalogLoader` (unique recipe IDs, positive input/output quantities and cook times). Core authority extended with `CookingCensus` struct and `GetCensus()` matching CI architecture scanner requirements. Added `cooking` save section (`cooking_save.json`) to `SaveSectionRegistry` (section count 224→225). Classified `cooking_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Implemented `CookingHostSession` with `SaveStoreHub.Checksummed<CookingState>` (`CookingSaveStore`), providing full campaign lifecycle bindings: initialization (`SetupCooking`), daily simulation tick (`TickCooking`), dirty flushing (`FlushCookingIfDirty`), deterministic state restore (`RestoreCooking`), and reset (`ResetCooking`). Registered phase-5 campaign day owner `cooking` (`CookingDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI self-test probe `--cooking-selftest` (`--cooking-test`) to `HostCliRegistry`, `src/Host/HostCli.Cooking.cs`, and `src/Host/HostCli.cs` with 12 validation checks. Evidence: host and Core test builds 0 errors / 0 warnings; `Plan136CookingHostIntegrationTests` 8/8 PASS; `Plan136WildlifeCookingIntegrationTests` 5/5 PASS; `Plan135WeatherCascadeHostIntegrationTests` 16/16 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1352/1352 PASS; architecture graph regenerated with 225 subsystems mapped (100% mechanical evidence); `generate-save-store-matrix.py` (227 stores); `generate-selftest-manifest.py` (151 tests); `DEC-306` ratified in `docs/governance/DECISION_REGISTER.md`. Next batch head: Plan 140 (Generational Legacy & Campaign Inheritance).

**UNBLOCK-OLDEST-BATCH7-PLANS — PLANS 59 + 134 FULL INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch7-plans-2026-09-23`), the seventh batch of the partial plans queue. **Plan 59 (Retrospective: Standing Gates / Rules):** Re-verified complete with all 22 register rows authoritatively mapped against `CI_GATE_MANIFEST.json` and 21/21 passing tests in `Plan59StandingGateHostIntegrationTests`. **Plan 134 (Dynamic Faction Territory & Supply Lines):** Converted the Core-only territory control authority (`TerritoryControlSystem`) into a fully integrated host feature. Authored catalogs `faction_territory.json` (19 territories across 4 classifications, 5 flashpoint zones) and `supply_lines.json` (8 trade routes) loaded and managed through `TerritoryControlSystem`. Core state persistence sealed via `LocationTerritorySaveState`, `SupplyLineSaveState`, and `TerritoryControlSaveState` (`schema_version = 1`), with full round-trip capture/restore in `TerritoryControlSystem.CaptureState` and `RestoreState`. Added `territory_control` save section (`territory_control_save.json`) to `SaveSectionRegistry` (section count 223→224). Classified `territory_control_ticked` as an internal heartbeat in `DayEventVocabulary` and updated the semantic parity matrix. Implemented `TerritoryControlHostSession` with `SaveStoreHub.Checksummed<TerritoryControlSaveState>` (`TerritoryControlSaveStore`), providing full campaign lifecycle bindings: initialization (`SetupTerritoryControl`), daily simulation tick (`TickTerritoryControl`), dirty flushing (`FlushTerritoryControlIfDirty`), deterministic state restore (`RestoreTerritoryControl`), and reset (`ResetTerritoryControl`). Registered phase-5 campaign day owner `territory_control` (`TerritoryControlDayOwner`) with `IPreDaySnapshotRestore` pre-day snapshot rollback support in `Main.CampaignOwners.cs`. Bound save persistence into `Main.SaveOrchestrator.cs` and lifecycle hooks into `Main.ExpandedShelterSystems.cs` and `Main.Application.cs`. Added CLI self-test probe `--territory-control-selftest` (`--territory-selftest`) to `HostCliRegistry`, `src/Host/HostCli.TerritoryControl.cs`, and `src/Host/HostCli.cs`. Evidence: host and Core test builds 0 errors / 0 warnings; `Plan134TerritoryControlHostIntegrationTests` 6/6 PASS; `Plan134TerritoryControlIntegrationTests` 5/5 PASS; `Plan59StandingGateHostIntegrationTests` 21/21 PASS; `ComprehensiveSaveStoreCorruptionAndMigrationTests` 1346/1346 PASS; architecture graph regenerated with 224 subsystems mapped (100% mechanical evidence); `DEC-305` ratified in `docs/governance/DECISION_REGISTER.md`. Next batch (oldest remaining partials): Plans 136, 140.


**UNBLOCK-RESIDUALS-PLANS-24-31 — RESIDUALS SEALED & INTEGRATED COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-residuals-plans-24-31-2026-09-23`) unblocking and fully integrating the two remaining plan residuals. **Plan 31 Residual / D11-B (Closed-Section Briefing Routing):** Decision D11-B (`DEC-303`) ratified and sealed. `DailyBriefingReportBuilder` routes non-heartbeat unhandled day-events to their semantic category section titles (`SectionTitleFor(semantic)`), preserving generic section fallback (`GenericSectionTitle` = "System Activity") for truly unknown kinds with no dead affordances (`IsActionable = false`). `Plan31BriefingRouteTests.GenericNonHeartbeatEvent_BecomesActionable` test contract aligned to expect `sanitation_spill` (a Hazard event) in its authoritative semantic section (`Warnings`), resolving the pre-existing red failure across Batches 1–6. `Plan31BriefingRouteTests` is 6/6 PASS; Campaign suite reaches 257/257 PASS (0 red). **Plan 24 Residual / L-P24R (Snapshot Rebaseline Protocol):** Plan 24 core implementation affirmed terminal CLOSED with 0 open architecture or domain logic decisions. Formal procedural addendum added to `PLAN_24_CLOSEOUT.md` per `UNBLOCK-04` §5.11 / `DEC-304`: execution protocol for the first renderer-capable session with a real display/GPU established (1920x1080 canonical resolution, verify diffs against intended render changes inventory: survivor detail need contributors, duty roster hours row & confirmation dialog, cenotaph status & vigil button). Headless sessions strictly prohibited from fabricating or bypassing golden snapshots. Evidence: Core and host builds 0 errors / 0 warnings; `Plan31BriefingRouteTests` 6/6 PASS; `DayEventVocabularyTests` 8/8 PASS; `Campaign` suite 257/257 PASS; `Medical/Plan24JourneyParityTests` 3/3 PASS; `DutyRoster/Plan24DutyHourTests` 10/10 PASS; `Survivors/Plan24NeedsSourceMigrationTests` 9/9 PASS; `agent-fast-verify` 10/10 PASS; `sync-agent-rulebooks` 13/13 in sync.

**UNBLOCK-OLDEST-BATCH6-PLANS — PLANS 135 + 59 HOST INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch6-plans-2026-09-23`), the sixth batch of the oldest-partial queue. **Plan 55 (The Long Haul — Retention, a Save Corpus, and the 400-Year Campaign):** the plan's "decide what a campaign is allowed to remember" table is now authored data — `retention_policies.json` — with a strict snake_case loader (`RetentionPolicyCatalogLoader`, per-row collected errors that reject a bad action/key/duplicate/capacity and an obligation authored with a finite cap instead of defaulting it) overlaid on `RetentionPolicyCatalog.ApplyOverlay`: the authored row wins for every key it names, and every unlisted default bound (including all four protected obligations) survives, so a partial authored table can never remove a protective bound. `RetentionHostSession` then applies the authority to the live canonical owners through **their own** additive `ApplyRetention` seams — `KitchenNutritionSystem` (serving log), `FactionWarSystem` (enacted decrees), `MachineLogSystem` (machine log), `DoseLedgerSystem` (per-survivor reading history). No collection is copied, mirrored, or re-owned: cumulative dose, band and ledger classification are untouched by retention, and `JournalSystem`'s existing 64-entry cap is left alone rather than double-bounded. Every protected obligation (wills/legacies, memorials, deadlines, grief markers) is a hard no-op over 900 records. The audit report is persisted through the registered `retention` section (never the log contents, which stay in their own owners' sections), the day owner runs last in phase 5 after every owner that appended that day, and `retention_applied` is journaled whenever anything is pruned. Re-running the pass once bounded moves nothing — the 400-year budget is idempotent. **Plan 58 (The Continuation — Outposts, Waystations, and a Second Holdfast):** the named ORPHAN-SEAL-PRIORITY-W1 blocker is **sealed**. `OutpostSettlementSystem` gained `OutpostInstanceState`/`OutpostSettlementState` with `CaptureState`/`RestoreState`; definitions are never persisted (re-derived from the authored `outposts.json` on restore), and a wrong schema version, a phantom outpost id, an over-full garrison, or a missing authored row is handled deterministically rather than inventing state. The custody decision is recorded in `docs/plans/ORPHAN_SEAL_PRIORITY_W1_BOUNDARIES.md` §2 (superseding the deferral): `OutpostSettlementSystem` is the single authority for authored outposts and secondary positions; `WaystationSystem` (section `waystation`) remains the holdfast S2 waystation; `ColonySystem` (section `colony`) remains the player-founded colony; `SettlementCatalog` remains the world settlement catalog. None are merged and no second population, food, or settlement store is created. `OutpostSettlementHostSession` binds the authored catalog and routes every cost and ration through canonical owners — build cost and the daily ration draw through canonical inventory (`TryConsumeById`), garrison through the canonical roster with the authored bunk cap and the existing fitness gate, hostile pressure through the campaign's own forked `outpost_risk` stream (one draw per day per outpost, derived only from the master seed). Overrun raises a canonical consequence-ledger entry, every lifecycle transition journals through the canonical journal owner (`outpost_established`/`supplied`/`starving`/`overrun`/`garrisoned`/`relieved`), and state round-trips through the new `outpost_settlement` section. The day owner ticks in phase 5 with a pre-day snapshot rollback, after the roster/expedition/economy owners. Blocked-and-resolved in-batch: the first authored retention table tripped `CatalogIntegrityValidator` with 3 errors (it resolved `collection_key` as a catalog id) — fixed by classifying it as the pure vocabulary it actually is; the new day-event kinds tripped `DayEventSemanticKindTests` (unclassified) and `DayEventParitySourceGateTests` (stale matrix) until they were classified as internal heartbeats and added to `docs/campaign/EVENT_SEMANTIC_PARITY_MATRIX.md`. Evidence: host + Core test builds 0/0; `--retention-selftest` 18/18; `--outpost-settlement-selftest` 28/28; all ten Batch 1–4 probes green (`--commitments-selftest` 14/14, `--session-durability-selftest` 21/21, `--playable-metrics-selftest` 15/15, `--survivor-voice-selftest` 14/14, `--content-certification-selftest` 15/15, `--holdfast-presentation-selftest` 26/26, `--scarcity-audio-selftest` 21/21, `--seven-day-slice-selftest` 25/25, `--orphan-seal-wave1-selftest` PASS, `--port-contract-selftest` PASS 286 seams); `--7-day-smoke-selftest` PASS; `--real-campaign-journey-selftest` PASS (58/58); `--save-store-checksum-selftest` PASS; `--save-load-ui-failure-selftest` PASS; `--selftest-manifest` PASS; `--data-integrity-selftest` PASS (0 errors, 420 catalogs); `--content-utilization-selftest` CI gate PASS; `Save` 1447/1447; `Records` 13/13; `Settlements` 12/12; `Plan55RetentionHostIntegrationTests` 5/5; `Plan58OutpostHostIntegrationTests` 7/7; `RetentionPolicyCatalogLoaderTests` 8/8; `Campaign` 256/257 (sole failure `Plan31BriefingRouteTests.GenericNonHeartbeatEvent_BecomesActionable` — D11-B closed section routing, decision-blocked); `Tooling` 120/122 (two pre-existing failures outside this claim: `CaseFoldPolicyPinTests` pin band, `CatalogPathForbiddenGateTests` on `src/UI/FactionCultureCodexPanel.cs`); `MainTriadDriftGateTests` 7/7; `PersistentFilenameRegistryGateTests` 4/4; `LoaderWiringGateTests` 4/4; `CampaignRngSourceGateTests` 2/2; `SelfTestManifestGateTests` 4/4; section pin 220→222; all five generators in sync; `agent-fast-verify` 10/10. Pre-existing failures observed and left untouched (none inside this claim): `--composition-root-selftest` (chronicle `_completionHistory` altered after `StartNewGame()`; the probe reports `constructed=0` before any day advance, so the failure precedes this package's code path) and `--mod-selftest` (the probe validates a temp dir containing only `items.json` while `CatalogIntegrityValidator` requires the full data authority — reproduced in isolation, unrelated). Next batch (oldest remaining partials): Plans 59, 135.

**UNBLOCK-OLDEST-BATCH6-PLANS — PLANS 135 + 59 HOST INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch6-plans-2026-09-23`), the sixth batch of the oldest-partial queue. **Plan 135 / C2[27] (Weather → Deep Gameplay Cascade):** weather stops being a UI number and becomes a driver. A new Core `WeatherCascadeSeverity` derives a front's severity entirely from the canonical `WeatherEffectsCatalog` — the same authored table `WeatherSystem` already consumes for rad dose, visibility, thermal load, travel speed and encounter chance — across five named axes normalized to 0..1 against named reference constants and combined with integer-permille weights that sum to exactly 1000, so the cascade can never disagree with the weather the player is standing in; an unbound catalog yields 0 (no cascade) rather than a fabricated value. The authored 15-row `weather_gameplay_effects.json` table is loaded through a **new strict loader** (`WeatherCascadeCatalogLoader`: unknown weather_kind, unknown target_system/effect_type, non-finite magnitude, sub-day duration, duplicate id, or an empty table are all load errors) and bound through a new `Engine.BindValidatedTemplates` seam, which makes the engine's previous lenient `LoadCatalog` fallback — generated effects, i.e. an invented cascade — unreachable from the host. Each produced effect then reaches **exactly one canonical owner**: shelter `damage` → `DisasterResponseSystem.AdjustResilience`; shelter `thermal_load`/`filtration_stress`/`power_output` → reported only, because those already reach the shelter through the canonical weather path and applying them again would be a second drain; expedition `delay`/`damage`/`hazard_surge` → `ExpeditionSystem.SetEncounterChanceMultiplier`, with the owner still owning the roll; economy `price_change` → `MarketSystem.ApplyShock` with the owner's own clamp and per-source idempotence, source-named `weather_<effectId>` so the cause is auditable; mental-health `behavior_change` → `NeedsSystem.SetExternalModifier` bounded to the effect's duration, with no morale write and no fabricated crisis case (the mental-health owner has no probability seam); location `accessibility` → reported only, because `RouteAvailabilityKind` is documented derived-and-never-serialized; faction `behavior_change` → reported only, because no standing-operation ledger exists to pause. Fortification is read from the canonical resilience owner rather than stored. The engine's `RestoreState` is now schema-gated, the campaign lifecycle gains a phase-5 `weather_cascade` day owner with pre-day snapshot rollback that expires fronts and withdraws every modifier it installed, the state persists through the new registered `weather_cascade` section (223 sections), and the canonical `OnWeatherChanged` notification fires at most one cascade per (kind, day). **Plan 59 / Wave 9 (Retrospective: Turn Nine Waves of Findings into Rules, Then Stop Auditing):** the retrospective's own gate is now measurable. The plan's central measured fact — "gates exist" was never "gates run" — becomes checkable because `StandingGatesHostSession` loads the 22-row register through a **new strict loader** that never defaults a tier, never drops an unowned gate, and rejects a gate that declares neither a scripted enforcement nor a written decision, then measures each rule against the repository's **actual enforcement authority**, `docs/ci/CI_GATE_MANIFEST.json` (57 gates), with five verdicts: `Enforced` (a real, critical, tier-compatible gate), `RuleOnly` (deliberately not scripted, with an owner and a dated written reason), `Unbound`, `NotBlocking` (registered but not critical, so a red does not block a release), and `TierMismatch`. Tier semantics are exact: a per-push standing rule requires a `fast`-tier gate, while nightly and per-release rules accept either tier, because a fast gate also runs in the full suite and at release — so a full-tier soak cannot pass as per-push. Every one of the 22 rows carries an owner; 19 bind a real critical CI gate; the remaining 3 carry explicit 2026-09-23 written decisions naming the human instrument that replaces the gate (mass-balance degradation soak, reproducible balance-sweep generator, AI-disclosure provenance review). Self-proof fixtures prove the register's own gates can fail — an ownerless row, an empty register, an unknown tier, an unwritten non-gate decision, a wave-origin of zero, a duplicate gate id, a reference to a nonexistent CI gate, and a forced tier requirement. Blocked-and-resolved in-batch: the cascade's lenient loader fallback; the unguarded state schema; a mechanically neutral front still firing a generated `default_morale_lift` (found by the probe — a Clear day was granting a fabricated morale boost); the blizzard-versus-BlackRain damage routing (the probe asserted the wrong front, which exposed that `thermal_load` must NOT double-apply); an over-strict tier rule that flagged 8 correctly enforced gates; one genuine mismatch, `gate_vocabulary_contract_events` authored `per_push` while binding the `full`-tier `test_core_suite`, resolved by retiering the rule to `nightly` — the cadence its enforcement actually runs at — rather than weakening the check; `CatalogIntegrityValidator` resolving `enforcement_ref` as a game-data id; an unclassified day-event kind; an untracked Core `Bind*` seam; and a missing architecture-map node. Evidence: host + Core test builds 0 errors / 0 warnings; `--weather-cascade-selftest` 35/35; `--standing-gates-selftest` 19/19; `--data-integrity-selftest` PASS (0 errors, 420 catalogs); `--port-contract-selftest` PASS 287 seams / 183 host-required / 9-of-9 host wiring; `--selftest-manifest`, `--7-day-smoke-selftest`, `--save-store-checksum-selftest`, `--content-utilization-selftest` CI gate all PASS; `--retention-selftest` 18/18 and `--outpost-settlement-selftest` 28/28 (Batch 5 regression); `Governance` 48/48; `Weather` 27/27; `Save` 1453/1453; `Records` 13/13; `Settlements` 12/12; `Expeditions` 331/331; `Tooling` 120/122; `Campaign` 256/257; `MainTriadDriftGateTests` 7/7; `PersistentFilenameRegistryGateTests` 4/4; `LoaderWiringGateTests` 4/4; `CampaignRngSourceGateTests` 2/2; `SelfTestManifestGateTests` 4/4; section pin 222→223; all five generators in sync; `agent-fast-verify` 10/10. Pre-existing failures left untouched (none inside this claim): `Plan31BriefingRouteTests.GenericNonHeartbeatEvent_BecomesActionable` (D11-B, decision-blocked), `CaseFoldPolicyPinTests` band (400 vs [120,260]), `CatalogPathForbiddenGateTests` on `src/UI/FactionCultureCodexPanel.cs`, and `--real-campaign-journey-selftest`, isolated by a causation test — with this claim's entire weather-cascade trigger disabled and the host rebuilt, the identical two failures and identical two victim ids reproduce, and the `Combat/TacticalCombatSystem*.cs` files the log names are dirty in the shared worktree from concurrent work (852 modified files at audit time). Deferred with named reasons: RNG-severity shakes, the weather prediction mini-game, weather quests/legacy/UI panel, `VentilationSystem`/`SumpFloodingSystem` second couplings, faction-operation pause (no standing-operation ledger exists), mental-health crisis probability (no probability seam), Plan 59A's CI-authoring and doc-generation substeps (7 of 13), and Plan 59B/59C in full (a governance-document package, not a host-integration package). Readiness plan: `docs/plans/UNBLOCK_OLDEST_BATCH6_PLANS_135_59_INTEGRATION_PLAN.md`. Next batch (oldest remaining partials): Plans 136, 140.
**UNBLOCK-OLDEST-BATCH4-PLANS — PLANS 52 + 54 HOST INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch4-plans-2026-09-23`), the fourth batch of the oldest-partial queue. **Plan 52 (Scarcity Audio):** `ScarcityAudioController` binds `ScarcityAudioStateMachine` to the canonical owners — the weather owner drives the authority's 22-kind ambience-bed decision, the power grid drives generator state, the roster drives occupant count, and the radiation dosimeter owner supplies the live dose rate that the authority maps to its geiger bands — then applies the authority's verdicts to the audio manager (bed transition selects the ambience cue from the authored catalog, absolute-silence states stop every ambience, alert ducking honors the policy cap, geiger loop/intense cues follow the rate band). `SurfaceAmbienceController` delegates its weather cue choice to the authority instead of keeping a private table, and `AudioManager` wires the controller through the existing `IAudioDomainProvider` seam (new `AudioSurvivors` member). No cue id is invented: the host maps the authority's keys onto cue ids that exist in `audio_cues.json`, and the probe enforces that. **Plan 54 (Seven-Day Slice Playtest):** the authored `slice_seven_days.json` gets a strict Core loader (`SliceScenarioCatalogLoader` — snake_case projection plus structural validation that rejects an incomplete scenario instead of defaulting), and `SliceScenarioHostSession` binds `SliceScenario` to it: freeze-hash verification of the exact authored content, per-beat verification against a **live canonical-owner measurement** (NeedsSystem ration evaluation, RadiationSystem triage, MemorialSystem eulogy, CommitmentSystem exactly-once settlement) with unmeasured beats recorded as explicit failures, and a scorecard that passes only when all seven beats verify and survivors are retained. The instrument is first-class in the campaign lifecycle (`SetupSevenDaySlice`/`SaveSevenDaySlice`, registered `seven_day_slice` section, `seven_day_slice_report` journal entry). Blocked-and-resolved in-batch: the new evidence store first failed `PersistentFilenameRegistryGateTests` (unregistered section) and `MainTriadDriftGateTests` (missing Save/Setup twins) — resolved immediately by registering the `seven_day_slice` section and adding the real campaign methods plus the 220-section pin. Evidence: host build 0/0; `--scarcity-audio-selftest` 21/21; `--seven-day-slice-selftest` 25/25; all eight Batch 1–3 probes remain green; `--real-campaign-journey-selftest` PASS; `--7-day-smoke-selftest` PASS; `--content-utilization-selftest` CI gate PASS; `--port-contract-selftest` PASS (286 seams); `--audio-selftest` PASS; `Save` 1435/1435; `Audio` 37/37; `Content` 27/27; `Presentation` 12/12; `Campaign` 251/252 (single pre-existing failure `Plan31BriefingRouteTests.GenericNonHeartbeatEvent_BecomesActionable` — D11-B closed section routing, decision-blocked); `Tooling` 120/122 (two pre-existing failures outside both claims: `CaseFoldPolicyPinTests` pin band, `CatalogPathForbiddenGateTests` on `src/UI/FactionCultureCodexPanel.cs`). Next batch (oldest remaining partials): Plans 55, 58.

**UNBLOCK-OLDEST-BATCH3-PLANS — PLANS 49 + 51 HOST INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch3-plans-2026-09-23`), the third batch of the oldest-partial queue. **Plan 49 / C2[21] (Content Orphan Certification):** `ContentCertificationHostSession` materializes the plan's cargo manifest — nine authored families (place/atmosphere, medical, memory corpus, encounter/choice, confessions, final wishes, voice, cassettes, damaged map zones), each naming a real catalog file and a real Core consumer — then reads live evidence (catalog actually loaded this run + owner actually constructed) and runs `ContentOrphanCertificationEngine` for the clean / certified-active / dormant / orphan verdict and the Plan 49 promotion gate. The report is published once per campaign through the canonical journal owner (`content_certification_review`), and new `JournalSystem.HasAuthoredCorpus` lets the host read the bound-corpus fact instead of trusting a loader name. Families are reported honestly: `cassette_sets.json` and `damaged_map_zones.json` name consumers that are still orphans (the cassette authority has no host composition), and `medical_texts.json` is authored but unloaded — dormant, not faked. The engine stays the sole verdict authority; the host only assembles evidence. **Plan 51 / Holdfast Presentation Slate:** `HoldfastPresentationHostSession` composes the slate exclusively from canonical owners — room power from `PowerGridSystem.IsRoomServed`, room temperature from the thermal room nodes, occupants from `ShelterAssignmentSystem`, drainage flooding from `SumpFloodingSystem`'s nodes (drainage nodes project as infrastructure rooms; no room-level flood is invented because no such owner exists), actor stance from the duty roster status vocabulary, grief from the memorial owner inside a named mourning window, map knowledge from the wasteland map's fog state, and the motion profile from the accessibility setting. The slate is a derived read model (no save section, no parallel mutable state), recomposed at campaign setup, on every canonical day advance in `TickSimDay`, and on every accessibility preference change; the player-observable surface is the shelter interior view's room tooltip, which appends hazard/shift lines only for non-nominal rooms so a healthy shelter's text is unchanged. Evidence: host build 0/0; `--content-certification-selftest` 15/15; `--holdfast-presentation-selftest` 26/26; `--real-campaign-journey-selftest` PASS; `--7-day-smoke-selftest` PASS; `--content-utilization-selftest` CI gate PASS; `--port-contract-selftest` PASS (286 seams, 0 errors); `Content` 27/27; `Presentation` 12/12; `Save` 1429/1429; `Campaign` 245/246 (single pre-existing failure `Plan31BriefingRouteTests.GenericNonHeartbeatEvent_BecomesActionable` — D11-B closed section routing, decision-blocked); `Tooling` 120/122 (two pre-existing failures outside both claims: `CaseFoldPolicyPinTests` pin band, `CatalogPathForbiddenGateTests` on `src/UI/FactionCultureCodexPanel.cs`). Next batch (oldest remaining partials): Plans 52, 54.

**UNBLOCK-OLDEST-BATCH2-PLANS — PLANS 42 + 46 HOST INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch2-plans-2026-09-23`), the second batch of the oldest-partial queue. **Plan 46 / C2[20] (Playable Metrics & Measurement):** `PlayMetricsHostSession` binds the three Core authorities (bounded local `PlaySessionRecorder` + `FirstHourFunnel` + `PlayableMetricsAggregationEngine`) to canonical producers — one JSONL `day_join` row per canonical day advance through `TickSimDay`, `survivor_perished` from the fate owner, `ration_policy_set` from the rationing owner's tier change (new `RationTierChangedSeam` forwarder on `EconomyHostSession`), `expedition.returned` from the expedition owner, save/quit at exit, and the funnel's four UI-driven steps through the existing `ObserveSigil` seam (`protocol.ration`, `inventory.used`, `weather.read`, `expedition.dispatched`). The campaign's terminal snapshot is aggregated through the pure engine and journaled (`play_metrics_report`), the report and funnel completion persist through the registered `playable_metrics` section, and `FirstHourFunnel.RestoreCompletedSteps` (Core owner extension) prevents re-reporting a step as "first" after a load. Aggregation inputs read existing owners only: campaign day, peak population read model, `SurvivorFateSystem.DeathCount`, `ExpeditionSystem.CompletedCount`, delivered loot read model (no canonical lifetime harvest counter exists — named, not duplicated), `WaterTreatmentSystem.TotalWater`, disaster resolved/overdue counts, and a documented difficulty-scalar derivation from `DifficultyScalarsProvider` (standard = exactly 1000 permille). The recorder stays a local, bounded, privacy-safe audit read model that never feeds gameplay. **Plan 42 / C2[18] (Survivor Voice):** `SurvivorVoiceHostSession` composes the authored catalog authority (`SurvivorVoiceSystem` over `survivor_voice_lines.json`, 8 lines, 5 triggers) for selection with `VoiceLineDispatchCoordinator` for playback-only arbitration (per-speaker/global tick cooldowns, priority preemption, subtitle payload) — one selection authority, so `VoiceLineSelectionEngine` is not run over the authored catalog. Triggers are canonical only: `season_changed` (calendar owner), `survivor_perished` (fate owner), `ration_cut` (rationing owner, real-reduction read model via `IsRationCut`), `radiation_spike` (dose owner with a per-survivor delta read model), plus a seeded social-stream RNG for tie-breaks and journal delivery (`voice_line`) through the canonical journal owner. Documented gap: `visitor_arrived` has no reachable host producer while the visitor authority is itself an orphan, so that seam stays unbound rather than inventing a visitor. Evidence: host build 0/0; `--playable-metrics-selftest` 15/15; `--survivor-voice-selftest` 14/14; Batch 1 regression `--commitments-selftest` 14/14, `--session-durability-selftest` 21/21; `--real-campaign-journey-selftest` PASS; `--7-day-smoke-selftest` PASS; `--content-utilization-selftest` CI gate PASS; `--port-contract-selftest` PASS (286 seams, 0 errors); `Plan46PlayMetricsHostIntegrationTests` 9/9; `Plan42SurvivorVoiceHostIntegrationTests` 8/8; `Save` 1429/1429; `Telemetry` 11/11; `Voice` 14/14. Pre-existing failures left untouched (outside both claims): `Plan31BriefingRouteTests.GenericNonHeartbeatEvent_BecomesActionable` (D11-B closed section routing — decision-blocked), `CaseFoldPolicyPinTests` band, `CatalogPathForbiddenGateTests` on `src/UI/FactionCultureCodexPanel.cs`. Next batch (oldest remaining partials): Plans 49, 51.

**UNBLOCK-OLDEST-BATCH1-PLANS — PLANS 38 + 39 HOST INTEGRATION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-unblock-oldest-batch1-plans-2026-09-23`) converting the two oldest orphan authorities from the 2026-09-21 reachability audit into full host-integrated features. **Plan 38 / C1[11] (Commitments & Deadlines):** `CommitmentSystem` is now constructed on both campaign paths, loads `commitments.json` through the strict loader, ticks in phase 4 via the new `CommitmentDayOwner` (owner id `shelter_commitments`) with `IPreDaySnapshotRestore` rollback, emits `obligation_warning` / `obligation_missed` / `obligation_met` semantic events into the briefing pipeline, writes journal facts, routes consequences to the canonical `FactionWarSystem.ModifyStanding` and `CampaignConsequenceLedger` (no second standing store), persists through the registered `commitment` section, and exposes RecordProgress/Settle through `CommitmentHostSession`. **Plan 39 / C2[16] (Session Durability):** `SessionDurabilityManager` is now the host durability-audit and release-gate read model over the canonical save pipeline — it mirrors each committed save's slot summary + deterministic section digest, records one measured soak sample per successful day advance, audits load failures/checksum mismatches, journals stability/corruption/recovery facts (`session_soak_unstable`, `session_soak_recovered`, `session_corruption`, `session_recovery`), and persists through the registered `session_durability` section. Authority boundary: `SaveSlotService`/`SaveLoadHostSession` remain the sole slot-layout, envelope, quarantine and recovery owners; the audit layer never takes the recovery decision. Evidence: host build 0/0; `--commitments-selftest` 14/14; `--session-durability-selftest` 21/21; `--real-campaign-journey-selftest` PASS; `--7-day-smoke-selftest` 10/10; `--content-utilization-selftest` CI gate PASS; `Plan38CommitmentHostIntegrationTests` 7/7; `Plan39SessionDurabilityHostIntegrationTests` 5/5; `CommitmentSystemTests` 7/7; `CampaignCalendarPlan38Tests` 6/6; `SessionDurabilityManagerTests` 6/6; `DayEventVocabularyTests` 8/8; `DayEventParitySourceGateTests` 2/2; `MainTriadDriftGateTests` 7/7; `PersistentFilenameRegistryGateTests` 4/4; `SaveSectionRegistryTests` 5/5; `LoaderWiringGateTests` 4/4. This entry supersedes the 2026-09-20/21 entries that described these authorities as sealed while they remained host-unreachable. Next batch (oldest remaining partials): Plans 42, 46.

**ORPHAN-SEAL-PRIORITY-W1 — PRIORITY ISLAND WIRING + DUPLICATE RESOLUTION COMPLETE (2026-09-23):** user-authorized integrator package (claim `claim-orphan-seal-priority-w1-2026-09-23`) binding the nine highest-priority orphan authorities plus `ConfessionSecretSystem`, `ShelterFestivalEngine`, and `FactionCovertOpsCoordinator` to real host seams. Each authority now has a catalog binding, a pre-validated save section with a checksummed store, a declared setup, a day tick or explicit command, journal observability, and a focused CLI probe: `survivor_autonomy`, `nuclear_winter_progression`, `seasonal_celebration`, `disaster_response`, `communications`, `colony`, `hobby`, `survivor_education`, `shelter_expansion`, `confession_secret`, `shelter_festival`, `faction_covert_ops` (see `docs/plans/ORPHAN_SEAL_PRIORITY_W1_BOUNDARIES.md`). Duplicate authorities resolved: Plan 63 `ShelterPrisonerSystem` retired as a live authority with one-time legacy import into canonical `PrisonerSystem` (section `shelter_prisoners` removed from the registry); Colony/Outpost, SeasonalCelebration/FestivalEngine, Espionage/CovertOps, and the comms trio documented as distinct authorities in the record. Real defects fixed: `FactionCovertOpsCoordinator.RestoreState` dropped every in-flight operation (pinned by a new Plan 153 round-trip test); `PortContractSelfTest` now binds its demo session and fails on unbound required ports (was 0/9 while passing on policy conformance alone — now 9/9 with `wiringSummary.IsValid` in the pass criterion); the first-session faction-alias remap broke `Plan153` pins, now retargeted to canonical ids. Evidence: host + test builds 0/0; `--orphan-seal-wave1-selftest` 36/36; `OrphanSealPriorityWave1Tests` 15/15; `Plan153FactionEspionageIntegrationTests` 7/7; `LoaderWiringGateTests` 4/4; `MainTriadDriftGateTests` 7/7; `PersistentFilenameRegistryGateTests` 4/4; `--port-contract-selftest` PASS 286 seams, host wiring 9/9. Deferred with named blocker: `OutpostSettlementSystem` has no capture/restore path and needs an explicit custody decision against `SettlementCatalog` before sealing. Dated 2026-09-21 ledger entries that claimed these systems integrated without host/save reachability are superseded by this evidence.

**BUG-HOLDFAST/UI-INTEGRITY — VERIFIED REPAIR BATCH COMPLETE (2026-09-20):**
Trade numeric/atomicity/preview defects fixed (14 red-before regressions, 76/76
focused trade tests); five xUnit analyzer warnings fixed (45/45 affected tests);
stale holdfast backup/quarantine probe corrected to the canonical trade store.
UI ownership fixes remove the reproduced 100 orphan-control shutdown signature:
grid placeholders, five dashboard headings, two unused containers, pneumatic
maintenance fields, and slurry panel replacement. Inventory sidebar filters are
now connected. Slurry change restricted to the exact block explicitly authorized
by the user; other Plan 24 regions and E1 paths untouched. Host/test builds 0/0;
bounded 15-FPS lifecycle 21/21 and holdfast runtime PASS with no engine shutdown
warnings/errors. No full suite, data/schema change, or RNG change. Twelve missing
item-icon fallbacks and intentional noncanonical faction/alias notices remain
visible, not suppressed. Claims released. Evidence and handoff:
`docs/debug/logs/BUG-HOLDFAST-INTEGRITY_IMPLEMENTATION_LOG.md` and linked UI logs.

**BUG-WIRING-REPAIR-HARDENING — COMPLETE (2026-09-20):** user-authorized sweep and repair of confirmed wiring defects, no new gameplay authority:
1. `ExcavationHazardSystem` (`OnMethaneIgnition`, `OnSectorFlooded`) had live consumers (`SubterraneanOperationsPanel`, `ShelterOperationsAudioBridge`) but no producer. Added canonical `AddMethane`/`AddFloodWater` mutation seams with exactly-once threshold-crossing notifications; `TickDay` accumulation/ventilation and `SeismicDynamicsSystem` outgassing now route through the methane seam. `ExcavationHazardSystemTests` 12/12 (4 new), `ShelterSeismicDynamicsPlan56Tests` 6/6, `SeismicMonitoringB68Tests` 14/14.
2. `ReconTelemetrySystem.LaunchMission` wind gate was a `0f` placeholder; now consumes canonical `WeatherSystem.WindSpeedKph` (Plan 205 authority). `ReconTelemetrySystemTests` 16/16 (2 new).
3. `ShelterOperationsAudioBridge` (Plans 46–49 workshop/radio/social/excavation audio) was never instantiated and its `Dispose()` threw `ObjectDisposedException`. Fixed disposal, added `IShelterOperationsAudioProvider`, and wired it through `AudioManager.RefreshDomainBindings` with `Main` as the provider. `--audio-selftest` PASS (5 new probes).
4. Gate repairs: regenerated stale `docs/saves/SAVE_STORE_CONTRACT_MATRIX.md` and classified the untracked `BootstrapLifecycleGate.RegisterSubsystem` Core seam as `TEST_ONLY` in `docs/ci/port_contract_policy.json`; both drift gates PASS. Host build 0/0; fast gate suite green except the concurrent `E1/Plan 53` file's trailing-blank-line hygiene finding, which was left to its owner.

**BUG-WIRING-REPAIR-HARDENING — PHASE 2 COMPLETE (2026-09-20):** every previously-reported remaining item is now fixed with current-authority wiring:
1. `OnSectorFlooded` production producer: `Main.ProjectSubterraneanFloodIntoExcavationHazards` projects canonical `SubterraneanSystem` node water (0–100 cm → 0–1000‰, increases-only so drainage mitigation is never overwritten) from `SubterraneanDayOwner.TickDay`; `SetupExcavationHazards()` added to `ComposeCampaign` so the hazard authority exists on the fresh path.
2. `OnFilterBreakthrough` producer: `ChemicalReconEngine.EvaluateFilterCondition` + host `FilterRemainingCapacityProvider` read the canonical equipped Face-slot respirator durability; exactly-once per crossing, re-armed on recovery. Tests 20/20.
3. Authored `passive_decay_bonus_permille` is consumed by `ApplyPassiveMitigationDecay` (hardcoded blower -200 retired; JSON is the authority). Tests 14/14.
4. `AquiferPiezometerEngine` host providers all bound in `Main.Piezometer.cs`: fluid-reservoir draw, authored seasonal rain share (new read-only `WeatherSystem.Profile`), best living `science`/`crafting` discipline.
5. Dead events repaired: `AnomalyHazardSystem.OnHazardContact` fires from `EvaluateApproach` on radius contact (journal consumer in `Main.Anomaly.cs`, tests 21/21); redundant `BlackMarketSystem.OnEventRaised` and `CohortSystem.OnChildAged` deleted; `ChroniclePanel` wired as a routed read-only chronicle surface (`chronicle` descriptor + route + lifecycle + close).
6. `DailySurvivalCatalog` also loads the folklore batch-2 file and is consumed by `JournalCatalogs.DailySurvival` → `JournalCodex.AppendDailySurvivalRows`; the unconstructed `DailySurvivalHostSession` was deleted as redundant. `DailySurvivalCatalogTests` 5/5, `--journal-selftest` 31/31.

**Hygiene closeout (2026-09-20):** with explicit user authorization, removed the trailing blank line at EOF in the active `E1/Plan 53` path `Ashfall.Core.Tests/Tooling/PlanGovernanceContractTests.cs` (content untouched; `PlanGovernanceContractTests` 5/5). The canonical fast gate suite now reports **53/53 PASS**.

**UNBLOCK Program Wave 33 (Batch 4 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 169` / DEC-161: Audio Accessibility & Mix Legibility (Delivered pure domain audio accessibility and mix legibility authority `AudioAccessibilityCoordinator` in `Assets/Ashfall.Core/Audio/AudioAccessibilityCoordinator.cs` and canonical data catalog `Assets/StreamingAssets/Data/audio_accessibility_cues.json` declaring 7 critical cues alarms, low-power, geiger escalation, raid warnings, medical emergencies, expedition return, radio signal and 3 dynamic mix presets `preset_full_dynamic`, `preset_compressed`, `preset_reduced_stimulation`. Implemented concise visual notification generation for all critical gameplay audio cues, side-chain ducking attenuation across audio buses [-6.0 dB to -14.0 dB], alert coalescing windows [2.0s to 5.0s] suppressing rapid duplicate triggers to eliminate alert pile-up, ducking release, and structured diagnostic readout, wired through delegate seams `OnVisualNotificationEmittedSeam`, `OnDuckingChangedSeam`, and `OnMixPresetChangedSeam`; `Plan169AudioAccessibilityIntegrationTests` 6/6 PASS, `Plan52SoundOfScarcityIntegrationTests` 6/6 PASS).
2. `Plan 170` / DEC-162: Seasonal Events & Celebrations System (Delivered pure domain celebrations and communal events authority `SeasonalCelebrationSystem` in `Assets/Ashfall.Core/Events/SeasonalCelebrationSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/shelter_celebrations.json` declaring 5 annual holidays `hol_new_year`, `hol_spring_thaw`, `hol_midsummer_day`, `hol_harvest_observance`, `hol_winter_solstice`, 3 anniversary types founding, fallen, milestone, and 3 celebration scales small, medium, large. Implemented date and recurring annual holiday detection, resource expenditure food and fuel, participant scaling, tradition streak building and streak morale multipliers, memorable celebration recording, holiday skipping penalties, and full save/restore state persistence, wired through delegate seams `OnCelebrationHeldSeam`, `OnHolidaySkippedSeam`, and `OnAnniversaryCommemoratedSeam`; `Plan170SeasonalCelebrationsIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 33 (Batch 3 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 164` / DEC-159: Nuclear Winter Progression System (Delivered pure domain nuclear winter progression authority `NuclearWinterProgressionSystem` in `Assets/Ashfall.Core/Weather/NuclearWinterProgressionSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/nuclear_winter_phases.json` declaring 5 multi-decade phases `phase_initial`, `phase_escalating`, `phase_peak`, `phase_late`, `phase_stabilizing` and 4 cyclical seasons `season_deep_winter`, `season_pale_spring`, `season_ash_summer`, `season_black_autumn`. Implemented dynamic global temperature tracking, daylight deprivation hours, seasonal severity multipliers, heating fuel demand scaling, expedition hazard surges, greenhouse crop preservation multipliers, deterministic daily weather jitter via `ISeededRng`, severe climate anomaly rolls, and full save/restore durability, wired through delegate seams `OnPhaseTransitionedSeam`, `OnSeasonTransitionedSeam`, and `OnSevereClimateAnomalySeam`; `Plan164NuclearWinterIntegrationTests` 6/6 PASS).
2. `Plan 165` / DEC-160: Modding Support & Mod Data Contract (Delivered pure domain modding support authority `ModSupportSystem` in `Assets/Ashfall.Core/Mods/ModDataContract.cs` and canonical specification catalog `Assets/StreamingAssets/Data/mod_manifest_schema.json` declaring manifest specification standards, whitelist catalogs, and conflict resolution policies. Implemented manifest validation, multi-clause version range checking via `ModCompatibilityEvaluator`, dependency DAG resolution with missing and cyclic dependency detection, deterministic load order topological sorting, catalog override conflict detection, enable/disable state toggles, and safe save/restore persistence, wired through delegate seams `OnModRegisteredSeam`, `OnModStatusChangedSeam`, and `OnModConflictDetectedSeam`; `Plan165ModdingIntegrationTests` 6/6 PASS, `Plan47ModContractTests` 21/21 PASS, `JsonModLayeringTests` 19/19 PASS).

**UNBLOCK Program Wave 33 (Batch 2 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 162` / DEC-157: Shelter History & Historical Archive System (Delivered pure domain shelter history and archive authority `ShelterArchiveSystem` in `Assets/Ashfall.Core/Shelter/ShelterArchiveSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/archive_categories.json` declaring 6 canonical categories `cat_decisions`, `cat_events`, `cat_memorials`, `cat_milestones`, `cat_discoveries`, `cat_achievements`. Implemented institutional event recording, memorial loss registration bridging from `MemorialSystem.OnMemorialized`, chronological timeline querying with significance filters, multi-parameter search, pure-function projection of existing journal and memorial sources `ProjectCanonicalSources`, and full save/restore durability, wired through delegate seams `OnEntryRecordedSeam` and `OnMemorialRecordedSeam`; `Plan162ArchiveIntegrationTests` 6/6 PASS, `ShelterArchiveSystemTests` 8/8 PASS).
2. `Plan 163` / DEC-158: Wasteland Cartography & Surface Mapping (Delivered pure domain wasteland cartography authority `CartographySystem` in `Assets/Ashfall.Core/Exploration/CartographySystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/map_regions.json` declaring 8 wasteland regions across Urban, Rural, Industrial, Wasteland, Water terrains. Implemented dynamic fog of war lifting `DiscoverRegion`, regional scouting surveys with equipment bonuses `compass` +10 to `cartography_kit` +50, cartographer proficiency progression, commercial map acquisition `PurchaseOrAcquireMap`, overall map completeness calculation, read-only world knowledge projection `ProjectCanonicalMap`, and full save/restore state persistence, wired through delegate seams `OnRegionDiscoveredSeam`, `OnPoiMappedSeam`, and `OnCartographySkillAdvancedSeam`; `Plan163CartographyIntegrationTests` 6/6 PASS, `CartographySystemTests` 8/8 PASS).

**UNBLOCK Program Wave 33 (Batch 1 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 160` / DEC-155: Expedition Colony & Outpost System (Delivered pure domain expedition colony and forward base authority `ColonySystem` in `Assets/Ashfall.Core/Expeditions/ColonySystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/colony_blueprints.json` declaring 5 colony type blueprints `outpost`, `settlement`, `fortress`, `trading_post`, `farming_commune` and 6 building definitions `col_bldg_bunkhouse`, `col_bldg_watchtower`, `col_bldg_supply_depot`, `col_bldg_hydroponics`, `col_bldg_comm_relay`, `col_bldg_field_clinic`. Implemented outpost establishment, building construction expanding capacity and defense ratings, garrison assignment, supply line continuous delivery flows, daily consumption with starvation morale penalties, and full save/restore durability, wired through delegate seams `OnColonyEstablishedSeam`, `OnSupplyLineStatusChangedSeam`, `OnColonySuppliesUpdatedSeam`, and `OnBuildingConstructedSeam`; `Plan160ColonyIntegrationTests` 6/6 PASS, `ColonySystemTests` 6/6 PASS).
2. `Plan 161` / DEC-156: Survivor Hobby & Leisure System (Delivered pure domain survivor leisure and hobby authority `HobbySystem` in `Assets/Ashfall.Core/Survivors/HobbySystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/hobby_definitions.json` declaring 10 hobbies across all 6 canonical categories Creative, Intellectual, Physical, Social, Crafting, Collecting. Implemented off-duty leisure sessions, deterministic proficiency progression via `ISeededRng`, 4-tier mastery advancement Novice, Apprentice, Journeyman, Master with scaled morale bonuses, facility requirement gating `CanConductSession`, group sessions with companion morale bonuses, shared hobby affinity calculations, and full save/restore state persistence, wired through delegate seams `OnSessionCompletedSeam` and `OnMasteryAchievedSeam`; `Plan161HobbyIntegrationTests` 6/6 PASS, `HobbySystemTests` 5/5 PASS).

**UNBLOCK Program Wave 32 (Batch 6 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed, concluding the 12 oldest unintegrated plans program:
1. `Plan 157` / DEC-153: Communications & Radio Network Infrastructure (Delivered pure domain communications infrastructure authority `CommunicationsSystem` in `Assets/Ashfall.Core/Communications/CommunicationsSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/communications_networks.json` declaring 4 antenna tiers basic whip to phased array, 5 default frequency networks, antenna installation and condition degradation, dynamic effective reception range calculation [8..120 km], deterministic signal interception, cryptanalysis decoding workflows scaling with survivor skill, outgoing broadcast transmission reaching across antenna coverage, frequency jamming, and full save/restore durability, wired through delegate seams `OnAntennaInstalledSeam`, `OnMessageInterceptedSeam`, `OnMessageDecodedSeam`, `OnBroadcastTransmittedSeam`, and `OnFrequencyJammedSeam`; `Plan157CommunicationsIntegrationTests` 6/6 PASS).
2. `Plan 158` / DEC-154: Disaster & Emergency Response System (Delivered pure domain acute disaster response authority `DisasterResponseSystem` in `Assets/Ashfall.Core/Shelter/DisasterResponseSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/disaster_templates.json` delivering 6 crisis archetypes Earthquake, Flooding, Electrical Fire, Radiation Breach, Air Toxin Leak, Structural Subsidence, 5 emergency response protocols providing up to 70% damage reduction and +75% labor synergy, shelter resilience rating scaling mitigation speed [0..100], sequential status progression Active -> Contained -> Resolved, and full save/restore state persistence, wired through delegate seams `OnDisasterTriggeredSeam`, `OnDisasterContainedSeam`, `OnDisasterResolvedSeam`, `OnProtocolActivatedSeam`, and `OnDamageAssessedSeam`; `Plan158DisasterResponseIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 32 (Batch 5 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 154` / DEC-151: Survivor Education & Knowledge Transfer System (Delivered pure domain survivor education authority `SurvivorEducationSystem` in `Assets/Ashfall.Core/Education/SurvivorEducationSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/education_curriculum.json` declaring 3 educational stages Childhood/Adolescence/YoungAdult, 12 curriculum subjects, age-based progression, teacher and student assignment, deterministic daily study sessions via `ISeededRng`, +20% parent-child teaching bonus, +10% schoolroom facility bonus, prerequisite gating, skill unlocks upon proficiency, formal graduation evaluations, shelter knowledge pool accumulation, and full save/restore state persistence, wired through delegate seams `OnSessionCompletedSeam`, `OnSurvivorGraduatedSeam`, and `OnKnowledgeLevelChangedSeam`; `Plan154EducationIntegrationTests` 6/6 PASS).
2. `Plan 156` / DEC-152: Shelter Expansion & Physical Renovation System (Delivered pure domain shelter expansion authority `ShelterExpansionSystem` in `Assets/Ashfall.Core/Shelter/ShelterExpansionSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/shelter_construction.json` delivering 6 expansion blueprints and 4 structural upgrades, spatial 3D grid coordination X/Y/Depth with cell collision prevention, labor investment progression, structural stability rating tracking with excavation penalties and alerts when stability < 40%, structural rock strut reinforcement restoring stability, room condition wear and complete room renovation, vertical depth shaft excavations unlocking deeper bunker levels, and full save/restore durability, wired through delegate seams `OnProjectStartedSeam`, `OnProjectCompletedSeam`, `OnRoomRenovatedSeam`, and `OnStabilityRiskWarningSeam`; `Plan156ShelterExpansionIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 32 (Batch 4 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 152` / DEC-149: Vehicle Customization & Mobile Base System (Delivered pure domain vehicle customization authority `VehicleCustomizationSystem` in `Assets/Ashfall.Core/Vehicles/VehicleCustomizationSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/vehicle_modules.json` declaring 11 vehicle modules across armor, cargo, living, weapon, and utility categories; implemented module installation and removal with slot caps, aggregated speed/cargo/defense calculations, living module shelter gating `IsMobileBaseCapable`, expedition base camp deployment and packing `DeployBaseCamp` / `PackBaseCamp`, survivor field rest bounded by vehicle bunk capacity `RestSurvivorsInVehicle`, and full save/restore state persistence, wired through delegate seams `OnModuleInstalledSeam`, `OnModuleRemovedSeam`, `OnBaseCampDeployedSeam`, and `OnBaseCampPackedSeam`; `Plan152VehicleCustomizationIntegrationTests` 6/6 PASS).
2. `Plan 153` / DEC-150: Faction Espionage, Infiltration & Counter-Intelligence System (Delivered pure domain covert operations authority `FactionCovertOpsCoordinator` in `Assets/Ashfall.Core/Factions/FactionCovertOpsCoordinator.cs` and canonical data catalog `Assets/StreamingAssets/Data/espionage_operations.json` declaring 5 covert operation archetypes `infiltrate`, `steal`, `sabotage`, `propaganda`, `assassinate` and faction suspicion bands `Calm`, `Cautious`, `Hostile`, `Lockdown`; implemented player-directed covert operation launching with agent stealth scaling and faction security penalties, daily progression with idle faction suspicion cooling, deterministic resolution yielding raw `IntelligenceReport` artifacts on success or escalating suspicion and agent compromise on detection, intelligence decoding workflows, and full save/restore durability, wired through delegate seams `OnCovertOperationLaunchedSeam`, `OnCovertOperationResolvedSeam`, `OnIntelligenceDecodedSeam`, `OnFactionSuspicionChangedSeam`, and `OnAgentCompromisedSeam`; `Plan153FactionEspionageIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 32 (Batch 3 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 149` / DEC-147: In-Campaign Achievement & Milestone System (Delivered pure domain achievement authority `AchievementSystem` in `Assets/Ashfall.Core/Achievements/AchievementSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/achievements.json` declaring 16 achievements across survival, combat, social, exploration, economic, and moral categories; replaced hardcoded UI panel derivations in `AchievementsPanel.cs` with deterministic data-backed condition evaluations, run-local fact progression, once-only unlock event emission via `OnAchievementUnlockedSeam`, full save/restore durability, and a clean read-only export `GetCompletedAchievementIds()` for Plan 175 `CrossRunProfileStore` without duplicate profile storage; `Plan149AchievementIntegrationTests` 6/6 PASS).
2. `Plan 150` / DEC-148: Romance & Family Dynamics System (Delivered pure domain romance and family dynamics authority `RomanceFamilySystem` in `Assets/Ashfall.Core/Survivors/RomanceFamilySystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/romance_courtship.json` declaring 4 romance stages Attraction, Courtship, Partnership, Bonded, 5 courtship event templates `shared_meal`, `quiet_walk`, `deep_conversation`, `crafted_gift`, `mutual_vigil`, age/belief compatibility factors, and family benefit parameters; implemented deterministic attraction initiation via `ISeededRng`, progressive courtship mechanics with stage advancement, soulmate bond formation after 30 days bonded, formal `FamilyUnit` creation, child welcoming/adoption, dissolution of partnerships, and full save/restore state persistence, wired through delegate seams `OnRomanceStageAdvancedSeam`, `OnFamilyUnitEstablishedSeam`, `OnPartnershipDissolvedSeam`, and `OnChildWelcomedToFamilySeam`; `Plan150RomanceFamilyIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 32 (Batch 2 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 146` / DEC-145: Radiation → Economy & Social Bridge (Delivered pure domain radiological economy and social integration authorities `RadiationEconomyBridge` and `RadiationSocialBridge` in `Assets/Ashfall.Core/Radiation/` and canonical data catalog `Assets/StreamingAssets/Data/radiation_economy_social.json` connecting contamination and dose levels to trade price discounts [0.70..0.10], biohazard trade quarantines for goods > 60% contamination, +50% hot-zone relic scarcity premiums, social dose brackets Safe/Moderate/High/Severe imposing up to 50 social penalty and 35% discrimination chance, and faction contamination policies enforcing quarantine and standing reductions for Garrison and Hydro Barons, wired through delegate seams `OnRadiationPriceModifiedSeam`, `OnContaminatedTradeBlockedSeam`, `OnRadiationSocialPenaltyAppliedSeam`, `OnRadiationDiscriminationTriggeredSeam`, and `OnRadiationFactionStandingAdjustedSeam`; `Plan146RadiationBridgeIntegrationTests` 6/6 PASS).
2. `Plan 148` / DEC-146: Ideological Friction → Events & Quests System (Delivered pure domain ideological narrative authority `IdeologicalFrictionEvents` in `Assets/Ashfall.Core/Survivors/IdeologicalFrictionEvents.cs` and canonical data catalog `Assets/StreamingAssets/Data/ideological_events.json` declaring narrative event templates across confrontations, conversions, bunker splits, and mediation quests; transformed passive numerical friction into active emergent storytelling: confrontation triggers at affinity <= -50 with player mediation choices SideWithActor, SideWithTarget, StayNeutral, BrokerCompromise applying nuanced affinity/morale deltas, conversion attempts at affinity >= 30 with permanent belief adoption on success, bunker-wide ideological schisms when 3+ survivors form opposing factions, and urgent mediation quests at affinity <= -80, wired through delegate seams `OnIdeologicalEventTriggeredSeam`, `OnBeliefConversionSucceededSeam`, `OnIdeologicalMediationResolvedSeam`, and `OnBunkerSplitEscalatedSeam`; `Plan148IdeologicalFrictionIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 32 (Batch 1 of 6 - 12 Oldest Plans) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `Plan 144` / DEC-143: Survivor Autonomy & Initiative System (Delivered pure domain survivor autonomy authority `SurvivorAutonomySystem` in `Assets/Ashfall.Core/Survivors/SurvivorAutonomySystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/autonomy_actions.json` declaring 20 action templates across Help, Refuse, Initiate, Express, and Pursue autonomy kinds; implemented trigger condition checks across relationship/need/emotion/opportunity dimensions, deterministic trait/affinity probability scaling, player refusal override mechanics with consequence penalties, personal goal progression, cooldown spacing, and full save/restore durability, wired through delegate seams `OnAutonomyActionExecutedSeam`, `OnSurvivorHelpedSeam`, `OnWorkRefusalOverriddenSeam`, and `OnProjectInitiatedSeam`; `Plan144SurvivorAutonomyIntegrationTests` 6/6 PASS).
2. `Plan 145` / DEC-144: Unified Ending Resolution & Epilogue Personalization (Delivered pure domain endgame unification authority `UnifiedEndingResolver` in `Assets/Ashfall.Core/Endgame/UnifiedEndingResolver.cs` and canonical data catalog `Assets/StreamingAssets/Data/epilogue_personalization.json` unifying Holdfast endings, Muster epilogues, and Epilogue Matrix into a single coherent resolution function of campaign state; evaluates political category with Holdfast priority and faction branch context, social category with Muster approach, moral choice band, personalized survivor fates for alive/deceased/retired, expedition discoveries, and shelter upgrades, synthesizing a comprehensive literary-grade chronicle with awarded legacy traits and full save/restore durability, wired through delegate seams `OnUnifiedEndingResolvedSeam` and `OnSurvivorEpilogueGeneratedSeam`; `Plan145UnifiedEndingIntegrationTests` 6/6 PASS).

**UNBLOCK Program Wave 31 (Batch 5 of 5) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed (completing the full 5-batch, 10-plan program):
1. `C2[30] / Plan 140` / DEC-141: Generational Legacy & Campaign Inheritance (Delivered pure domain cross-campaign legacy and generational succession authority `CampaignLegacySystem` in `Assets/Ashfall.Core/Legacy/CampaignLegacySystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/legacy_traits.json` declaring 20 generational traits across survivor, shelter, faction, and ending categories; implemented persistent shelter improvements across campaign boundaries, 50% persistent faction memory retention, deterministic survivor heritage trait inheritance via `ISeededRng`, multi-generational trait evolution triggering after 3 generations to replace baseline traits with upgraded dynasty manifestations, starting context and bonus aggregation for New Game+ runs, and full save/restore durability, wired through delegate seams `OnCampaignArchivedSeam`, `OnLegacyTraitInheritedSeam`, `OnTraitEvolvedSeam`, and `OnNewGameLegacyAppliedSeam`; `Plan140GenerationalLegacyIntegrationTests` 5/5 PASS).
2. `C2[31] / Plan 141` / DEC-142: Research → Downstream Unlocks Bridge (Delivered pure domain downstream unlocks bridge `ResearchUnlockBridge` in `Assets/Ashfall.Core/Research/ResearchUnlockBridge.cs` and canonical data catalog `Assets/StreamingAssets/Data/research_unlocks.json` declaring 30 distinct tech unlock mappings spanning items, recipes, building capabilities, expedition perks, medical protocols, and agricultural breakthroughs; directly bound the bridge to `ResearchSystem.OnResearchCompleted` events without duplicate research tracking or shadow state, supporting automatic item catalog activation, recipe discovery notifications, target system capability queries `IsRecipeUnlocked`, `IsItemBreakthroughUnlocked`, `IsFacilityCapabilityUnlocked`, and full save/restore state serialization, wired through delegate seams `OnUnlockActivatedSeam`, `OnItemBreakthroughUnlockedSeam`, `OnRecipeUnlockedSeam`, and `OnFacilityCapabilityUnlockedSeam`; `Plan141ResearchUnlockBridgeIntegrationTests` 5/5 PASS).

**UNBLOCK Program Wave 31 (Batch 4 of 5) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `C2[27] / Plan 135` / DEC-139: Weather → Deep Gameplay Cascade: Forecast-to-Decision Pressure & Cross-System Cascade Integrity (Delivered pure domain weather gameplay cascade authority `WeatherGameplayCascadeEngine` and `WeatherCascadeSystem` in `Assets/Ashfall.Core/Weather/` and canonical data catalog `Assets/StreamingAssets/Data/weather_gameplay_effects.json` declaring 15 cascade templates across all 6 target systems Shelter, Expedition, Faction, Economy, MentalHealth, Location; implemented cross-system consequence evaluation: shelter thermal load, filtration stress, and sump flooding; fortification damage mitigation with fortification >= 2 eliminating structural damage; expedition travel delays; market fuel/supply price shocks; survivor morale impacts; and location access closures, wired through delegate seams `OnWeatherCascadeEvaluatedSeam`, `OnShelterDamageSurgedSeam`, `OnExpeditionDelayForecastedSeam`, `OnMarketShockTriggeredSeam`, `OnMentalHealthStressSurgedSeam`, and `OnLocationAccessibilityChangedSeam`; `Plan135WeatherCascadeIntegrationTests` 5/5 PASS).
2. `Plan 136` / DEC-140: Wildlife Trapping → Food Pipeline & Cooking Rails (Delivered pure domain food pipeline and cooking subsystem `CookingSystem` in `Assets/Ashfall.Core/Cooking/CookingSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/recipes_cooking.json` declaring 15 cooking recipes spanning 4 equipment tiers `improvised_stove`, `basic_boiler`, `proper_oven`, `industrial_cooker`; connected the dead-end `WildlifeTrappingSystem` to player inventory via `TransferCatchToInventory`, depositing raw meat and preserved hides directly without shadow stores; implemented cooking operations with `ICookingSource` / `InventoryCookingSource`, progressive cooking skill reducing cook time up to 30% and boosting nutrition up to 20%, food quality evaluation Raw, Cooked, WellCooked, Burnt, and radiation decontamination removing 50% to 95% of food contamination, wired through delegate seams `OnCookingCompletedSeam`, `OnSkillLevelUpSeam`, and `OnRecipeDiscoveredSeam`; `Plan136WildlifeCookingIntegrationTests` 5/5 PASS).

**UNBLOCK Program Wave 31 (Batch 3 of 5) — COMPLETE (2026-09-21):** 2 oldest plans integrated and sealed:
1. `C1[19] / Plan 60` / DEC-137: Medicine Made Legible: Plan 09 Integrated & Re-Baselined (Delivered pure domain medical legibility and disease lifecycle integration for Plan 60; loaded 15 canonical diseases across all 4 vectors water, air, blood, spore from `disease_catalog.json` schema_version 3 with diagnostic tells, timing clues, and clinical guidance; implemented derived clinical stages Incubating, Ill, Terminal, OutcomePending without parallel authoring, unified sick-list triage with explicit `severitySource` distinguishing dose from illness, curative/suppressive/supportive treatments mitigating lethality, vector protocol maintenance and expiry, and grief dispersion via `RelationsGriefSink` scaling mourning by death quality Peaceful 0.5, Unattended 1.25; `Plan60MedicineLegibleIntegrationTests` 5/5 PASS, existing medical suite 65/65 PASS).
2. `C1[20] / Plan 134` / DEC-138: Dynamic Faction Territory & Supply Line Control (Delivered pure domain dynamic territory and supply line authority `TerritoryControlSystem` in `Assets/Ashfall.Core/Factions/TerritoryControlSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/supply_lines.json` declaring 5 active supply corridors between faction holdings connecting `faction_territory.json` nodes; implemented location fortification levels 0..3, garrison assignment, deterministic territorial contest resolution via `ISeededRng` with overwhelming victory threshold, supply line raiding disrupting corridors and degrading destination control, restoration, and daily supply throughput deliveries reinforcing control strength, wired through delegate seams `OnTerritoryControlChangedSeam`, `OnTerritoryContestedSeam`, `OnSupplyLineStatusChangedSeam`, `OnSupplyLineDeliveredSeam`, and `OnLocationFortifiedSeam`; `Plan134TerritoryControlIntegrationTests` 5/5 PASS).

**UNBLOCK Program Wave 31 (Batch 2 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `E1[2] / Plan 58` / DEC-135: The Continuation: Outposts, Waystations, and a Second Holdfast (Delivered pure domain outpost settlement and secondary position authority `OutpostSettlementSystem` in `Assets/Ashfall.Core/Settlements/OutpostSettlementSystem.cs` and canonical data catalog `Assets/StreamingAssets/Data/outposts.json` declaring 4 canonical outposts North Watch, Rail Depot, Relay Tower, Quarry Camp; implemented full outpost lifecycle: establishment with material cost consumption, garrison assignment with bunk capacity limits and fitness gating, supply delivery, daily consumption tick with starvation handling and condition degradation, deterministic risk/overrun simulation via `ISeededRng`, and clean abandonment without duplicating population, food, or inventory stores, wired through delegate seams `OnOutpostEstablishedSeam`, `OnOutpostSuppliedSeam`, `OnOutpostOverrunSeam`, `OnGarrisonAssignedSeam`, `OnGarrisonRelievedSeam`, and `OnOutpostStarvingSeam`; `Plan58OutpostSettlementIntegrationTests` 5/5 PASS).
2. `C2[25] / Plan 59` / DEC-136: Retrospective: Turn Nine Waves of Findings into Rules, Then Stop Auditing (Delivered pure domain standing architectural gate registry `StandingGateRegistry` in `Assets/Ashfall.Core/Governance/StandingGateRegistry.cs` and canonical data catalog `Assets/StreamingAssets/Data/standing_gates.json` declaring the 22 canonical standing gates across Waves 1-9 with multi-tier filtering PerPush, Nightly, PerRelease, wave origin querying, audit execution with structured violation reporting, and delegate seams `OnGateEvaluatedSeam`, `OnGateViolationDetectedSeam`, and `OnRetrospectiveAuditedSeam` without engine coupling; `Plan59StandingGateIntegrationTests` 5/5 PASS).

**UNBLOCK Program Wave 31 (Batch 1 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `D1 / Plan 54` / DEC-133: The Seven-Day Slice — Playtest Instrument, Deterministic Beats & Scorecard (Delivered pure domain playtest slice scenario authority `SliceScenario` in `Assets/Ashfall.Core/Campaign/SliceScenario.cs` and canonical data catalog `Assets/StreamingAssets/Data/slice_seven_days.json` declaring 7 sequential beats Day 1: orientation/rations, Day 2: craft/dispatch, Day 3: weather threat, Day 4: triage/hazard, Day 5: memorial/grief, Day 6: policy decree, Day 7: commitment deadline with initial resources and survivor roster; implemented deterministic SHA-256 scenario hash calculation, frozen scenario integrity verification, sequential beat progression evaluation, and comprehensive `SliceScorecard` generation with delegate seams `OnBeatEvaluatedSeam`, `OnSliceCompletedSeam`, and `OnBeatValidationFailedSeam` without engine dependencies; `Plan54SevenDaySliceIntegrationTests` 5/5 PASS).
2. `D1[2] / Plan 57` / DEC-134: The Shop Window — Store Kit Statements, Capability Verification & Launch Ops Truth (Delivered pure domain store capability truth authority `StoreCapabilityManifest` in `Assets/Ashfall.Core/Launch/StoreCapabilityManifest.cs` and canonical data catalog `Assets/StreamingAssets/Data/store_capability_claims.json` declaring 11 verified claims across 5 categories GameplayCore, Accessibility, AudioVisual, DataAndSaves, AiAndAuthorship; implemented claim validation against active runtime systems and passing verification gates, automated rejection of unbacked or fictitious marketing claims preventing store copy from running ahead of shipped code, and generated structured `StoreStatementReport` with delegate seams `OnClaimAuditedSeam`, `OnStoreManifestAuditedSeam`, and `OnUnsubstantiatedClaimDetectedSeam` without engine coupling; `Plan57StoreKitTruthIntegrationTests` 5/5 PASS).

**UNBLOCK Program Oldest Plans (Batch 5 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `C2[24] / Plan 55` / DEC-131: The Long Haul — Rolling Retention Policies, Save-Budgeting & Log Pruning (Delivered pure domain record retention and save budgeting authority `RetentionPolicyCatalog` and `RollingLog<T>` in `Assets/Ashfall.Core/Records/RetentionPolicy.cs`; implemented bounded rolling collections enforcing configured capacity limits with monotonic total recorded and pruned counters, and ironclad protected obligation rules guaranteeing survivor wills, memorial monuments, campaign deadlines, and grief markers are never pruned regardless of collection size, wired through `OnRetentionAppliedSeam` and `OnEntryPrunedSeam` delegate seams without duplicate log storage or engine dependencies; `Plan55RetentionPolicyIntegrationTests` 5/5 PASS).
2. `C1[18] / Plan 56` / DEC-132: Weight & Hygiene — Repository Classification, Size Budgets & Clean Releases (Delivered pure domain repository classification and asset budgeting policy `RepositoryClassificationPolicy` in `Assets/Ashfall.Core/Hygiene/RepositoryClassificationPolicy.cs`; implemented 8-category path classification CoreDomain, PresentationAdapter, TestSuite, AuthoredData, ImportedAsset, LegacyArchive, BuildTooling, TemporaryArtifact, asset family size budgets with strict budget compliance evaluation, and automated detection of prohibited release/export root artifacts test results XML, fix scripts, temp logs, wired through `OnBudgetExceededSeam` and `OnProhibitedPathDetectedSeam` delegate seams without engine coupling; `Plan56RepositoryClassificationIntegrationTests` 4/4 PASS).

**UNBLOCK Program Oldest Plans (Batch 4 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `C1[17] / Plan 51` / DEC-129: The Presented Game — Holdfast Interior, Wasteland Map & Bounded Visual State (Delivered pure domain presentation slate and spatial read model `HoldfastPresentationSlate` in `Assets/Ashfall.Core/Presentation/HoldfastPresentationSlate.cs`; implemented room condition snapshots, dynamic shelter visual crisis band derivation Calm, Brownout, Flooding, Crisis driving lighting palettes, survivor actor visual stances with pair bereavement, wasteland map node and route graph snapshots, and accessibility-safe motion profiles with instant zero-duration transitions when `ReduceMotion` is enabled; `Plan51PresentedGameIntegrationTests` 5/5 PASS).
2. `C2[23] / Plan 52` / DEC-130: The Sound of Scarcity — Ambience State Machine, Weather Beds, Silence & Mix Ducking (Delivered pure domain scarcity soundscape authority `ScarcityAudioStateMachine` in `Assets/Ashfall.Core/Audio/ScarcityAudioStateMachine.cs`; implemented discrete environmental ambience beds, complete mapping of all 22 canonical weather kinds with pitch/volume offsets, authoritative treatment of silence states Silence, SilentSpring, FalseSpring dropping all ambient beds, mix ducking policy with -7 dB attenuation and max concurrent alerts cap to prevent alert pile-up, and clean radiation exposure lifecycle with explicit `EndRadiationExposure` preventing orphan geiger loops; `Plan52SoundOfScarcityIntegrationTests` 6/6 PASS).

**UNBLOCK Program Oldest Plans (Batch 3 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `C1[16] / Plan 49` / DEC-127: Content Orphan Certification & Atmosphere Knowledge Integration (Completed Plan 49 unblocked following sealing of Plan 42 and Plan 46 prerequisites; delivered pure domain `ContentOrphanCertificationEngine` in `Assets/Ashfall.Core/Content/ContentOrphanCertificationEngine.cs` auditing content candidates against active domain consumers to prevent silent phantom catalog drift; wired `AtmosphereTextDeliveredSeam` in `AtmosphereTextSystem.cs` (`GetTextForLocation`) providing observable runtime atmosphere delivery; `Plan49ContentAtmosphereIntegrationTests` 4/4 PASS).
2. `C2[22] / Plan 50` / DEC-128: Asset Truth — Canonical Manifest & Precedence Resolver (Delivered pure domain asset truth authority for Plan 50; authored canonical data catalog `Assets/StreamingAssets/Data/asset_registry.json` declaring 355+ assets across 6 families item, portrait, location, faction, weather, ui with schema_version 1, source model provenance, import presets, and distinct family fallbacks; authored `Assets/Ashfall.Core/Assets/AssetManifest.cs` enforcing strict 4-step precedence Explicit Manifest > Stem Convention > Family Fallback > Missing, strict tier verification mode, family coverage statistics, and delegate seams `OnAssetResolvedSeam` and `OnFallbackInterceptedSeam`; `Plan50AssetTruthIntegrationTests` 7/7 PASS).

**UNBLOCK Program Oldest Plans (Batch 2 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `C2[19] / Plan 44` / DEC-125: Relations That Change Outcomes — Reputations, Favors, Debts, and Open Doors (Delivered pure domain relationship-effect query model and bounded pair history for `SurvivorRelationsSystem` in `Assets/Ashfall.Core/SurvivorRelationsSystem.cs` and `Assets/Ashfall.Core/Relations/RelationshipEffect.cs`; loaded 5 canonical relationship bands from `relationship_bands.json` Hostile, Strained, Cordial, Close, Bonded; implemented `GetRelationEffect`/`EffectOf` mapping pair affinity directly to working, morale, error-risk, expedition-risk, separation, caregiving, and training modifiers; added bounded explainable history entries `PairRelationHistoryEntry` max 10 per pair recording every band/affinity delta with cause and day; deterministic multi-member team aggregation `AggregateTeamEffect`; and wired `RelationEffectAppliedSeam` and `HistoryEntryRecordedSeam` delegate seams; `Plan44SurvivorRelationsIntegrationTests` 10/10 PASS, `SurvivorRelationsSystemTests` 7/7 PASS).
2. `C2[20] / Plan 46` / DEC-126: Playable Metrics & Session Telemetry Aggregation Engine (Delivered pure domain engine-free local play-session telemetry recorder and first-hour funnel aggregator in `Assets/Ashfall.Core/Telemetry/PlaySessionRecorder.cs` and `Assets/Ashfall.Core/Telemetry/PlayableMetricsAggregationEngine.cs`; implemented closed action vocabulary `PlaySessionActions`, JSONL event schema `PlaySessionEvent` under `user://` with zero network calls and zero PII, `PlaySessionRecorder` with bounded buffer max 2000 events and monotonic session clock, simulation consequence joins via `JoinDay`, 7-step canonical first-hour onboarding funnel tracking `FirstHourFunnel`, and structured markdown reporting via `PlaySessionReport` with `PlaySessionEventRecordedSeam` delegate bridge; `PlaySessionRecorderTests` 7/7 PASS, `PlayableMetricsAggregationEngineTests` 4/4 PASS).

**UNBLOCK Program Oldest Plans (Batch 1 of 5) — COMPLETE (2026-09-20):** 2 oldest plans integrated and sealed:
1. `C2[16] / Plan 39` / DEC-123: Session Durability — Saves, Slots, Soak, and the Release Gate (Delivered pure domain `SessionDurabilityManager` in `Assets/Ashfall.Core/Save/SessionDurabilityManager.cs` implementing isolated profile/slot save management, corrupted write recording, backup creation/restoration, SHA-256 checksum verification, and session soak stability evaluation with delegate seams `CorruptionDetectedSeam`, `BackupRestoredSeam`, and `DayAdvanceMeasuredSeam` without engine coupling; `SessionDurabilityManagerTests` 6/6 PASS).
2. `C2[18] / Plan 42` / DEC-124: A Voice for Each of Them — Survivors Who Say Things (Delivered pure domain `SurvivorVoiceSystem` in `Assets/Ashfall.Core/Voice/SurvivorVoiceSystem.cs` loading canonical barks from `survivor_voice_lines.json`, deterministic bark selection via `ISeededRng`, cooldown enforcement, register preference weighting across life triggers, and wired `VoiceLineDeliveredSink` and `VoiceBarkAudioSeam` delegate seams without duplicate survivor or audio registries; `SurvivorVoiceSystemTests` 5/5 PASS).

**UNBLOCK Program Wave 29 Plans (Batch 3) — COMPLETE (2026-09-20):** 2 partially integrated plans deeply integrated and sealed:
1. `C2[38] / Plan 220` / DEC-121: Shelter Atmosphere, Environmental Mood & Sensory Lighting Integration (Delivered pure domain data binding and delegate bridges for `ShelterAtmosphereSystem` in `Assets/Ashfall.Core/Shelter/ShelterAtmosphereSystem.cs`; loaded 7 canonical profiles from `atmosphere_profiles.json`, implemented `LoadCatalog`, profile modifier fallback, `BroadcastActiveAmbiance`, and wired `MoodBroadcastSeam` and `ProfileModifierApplier` delegate seams directly connecting lighting/soundscape and morale/needs modifications; `Plan220ShelterAtmosphereIntegrationTests` 11/11 PASS).
2. `C1[30] / Plan 171` / DEC-122: Dynamic Quest Templates, Radiant Objectives & Multi-Archetype Integration (Delivered pure domain catalog loading and template synthesis for dynamic radiant quests; loaded 7 canonical archetypes from `dynamic_quest_templates.json`, verified objective requirements, reward scaling, expiration limits, and target binding without duplicating quest manager or world graph stores).

**UNBLOCK Program Wave 29 Plans (Batch 2) — COMPLETE (2026-09-20):** 2 partially integrated plans deeply integrated and sealed:
1. `E1[16] / Plan 182` / DEC-119: Relationship Decay, Social Drift & Reconnection System Integration (Delivered pure domain catalog loading, bonding mitigation, and social drift bridges for `RelationshipDecaySystem` in `Assets/Ashfall.Core/Survivors/RelationshipDecaySystem.cs`; loaded 6 canonical bond decay profiles from `relationship_decay_profiles.json` Acquaintance, Friend, CloseFriend, Family, Mentor, Rival; implemented `LoadCatalog`, `GetProfile`, reconnection bonus multiplier scaling [1.0..2.0], and wired `BondingMitigationProvider` and `BondDriftBridge` delegate seams in `TickDay` and `RecordInteraction` allowing shared quarters/duty to mitigate social drift and alerting external morale/narrative systems without duplicating relationship or roster registries; `Plan182RelationshipDecayIntegrationTests` 6/6 PASS, `RelationshipDecaySystemTests` 5/5 PASS).
2. `E1[23] / Plan 206` / DEC-120: Survivor Death Legacy, Inheritance & Bereavement Solace Integration (Delivered pure domain catalog loading, bequest delivery, and bereavement solace bridges for `SurvivorDeathLegacySystem` in `Assets/Ashfall.Core/Survivors/SurvivorDeathLegacySystem.cs`; loaded 6 canonical legacy templates from `death_legacy_templates.json` Standard Survivor, Veteran Scavenger, Elder Guardian, Fallen Sentinel, Physician's Dispensation, Drifter Common Trust; implemented `LoadCatalog`, `GetTemplate`, `CreateWillFromTemplate`, `CalculateBereavementComfort`, and wired `InventoryBequestDeliverer` and `BereavementComfortApplier` delegate seams in `DistributeInheritance` and `ApplyBereavementComfort` delivering inherited items to beneficiaries and solace comfort to mourning survivors without duplicating inventory or survivor stores; `Plan206SurvivorDeathLegacyIntegrationTests` 7/7 PASS, `SurvivorDeathLegacySystemTests` 5/5 PASS).

**UNBLOCK Program Wave 29 Plans (Batch 1) — COMPLETE (2026-09-20):** 2 partially integrated plans deeply integrated and sealed:
1. `C2[26] / Plan 132` / DEC-117: Survivor Hidden Agendas, Evidence & Betrayal Arc System Integration (Delivered pure domain data binding, canonical archetype templates, and resolution delegate bridges for `HiddenAgendaSystem` in `Assets/Ashfall.Core/Survivors/HiddenAgendaSystem.cs`; loaded 6 canonical hidden agenda archetypes from `hidden_agendas.json` Resource Hoarder, PRPF Informant, Rebel Sympathizer, Ventilation Saboteur, Wasteland Deserter, Secret Relic Custodian; implemented `LoadCatalog`, `AssignAgendaFromTemplate`, archetype-driven clue synthesis, and wired `MoraleDeltaApplier`, `FactionStandingApplier`, and `ConfiscationApplier` delegate seams in `ConfrontSurvivor` directly routing confrontation fallout to survivor morale, shelter inventory, and faction standing without duplicate survivor entity stores or unseeded RNG; `Plan132HiddenAgendaIntegrationTests` 10/10 PASS, `HiddenAgendaSystemTests` 6/6 PASS).
2. `C2[28] / Plan 138 / Plan 209` / DEC-118: Shelter Defense, Security Clearance & Zone Lockdown Integration (Delivered pure domain catalog loading, zone access control, and defense response bridges for `ShelterSecuritySystem` in `Assets/Ashfall.Core/Shelter/ShelterSecuritySystem.cs`; loaded 8 canonical security zones from `shelter_security_zones.json` Common Quarters, Mess Hall, Workshop, Hydroponics, Infirmary, Central Armory, Command Vault, Subterranean Reactor Core; implemented `LoadCatalog`, `GetZoneDefinition`, `IsSurvivorAuthorized`, and wired `AlarmRelayBridge`, `LockdownStateBridge`, and `SecurityDenialLogger` delegate seams in `RequestAccess` and `SetShelterLockdown` triggering alarm posture and security logging without duplicate room or power registries; `Plan138ShelterSecurityIntegrationTests` 9/9 PASS, `ShelterSecuritySystemTests` 7/7 PASS).

**UNBLOCK Program Wave 28 Plans (Part 2) — COMPLETE (2026-09-20):** 3 partially integrated plans deeply integrated and sealed:
1. `C1[35] / Plan 203` / DEC-114: Wasteland Information Flow, Rumor Network & Intelligence Briefing Integration (Delivered pure domain data binding and expedition intelligence briefing derivation for `RumorSystem` in `Assets/Ashfall.Core/InformationFlow/RumorSystem.cs`; loaded 6 canonical information hubs from `rumor_hubs.json` across wasteland locations; implemented `LoadCatalog` and delivered `CreateBriefingReport` synthesizing local and intercepted rumors into structured actionable intelligence threats vs opportunities, verification classification, and average truthfulness without duplicating world graph or expedition registries; `Plan203RumorNetworkIntegrationTests` 7/7 PASS, `RumorSystemTests` 6/6 PASS).
2. `C1[44] / Plan 212` / DEC-115: Time Capsule & Legacy Messages Bereavement Solace System Integration (Delivered pure domain data binding, capsule sentimental morale yield, and bereavement solace integration for `TimeCapsuleSystem` in `Assets/Ashfall.Core/Communication/TimeCapsuleSystem.cs`; loaded 4 canonical pre-placed time capsules from `time_capsules.json` Founders' Vault, Geologists' Core Box, Watchman's Footlocker, Nursery Time Box; implemented `LoadCatalog`, `GetOpenedCapsuleMoraleBonus` scaling from sentimental content [2.0..25.0], and `CalculateBereavementComfort` providing psychological morale comfort for survivors receiving legacy messages from deceased loved ones without duplicate survivor entity stores; `Plan212TimeCapsuleIntegrationTests` 5/5 PASS, `TimeCapsuleSystemTests` 5/5 PASS).
3. `C1[32] / Plan 200` / DEC-116: Survivor Personal Quests, Character Arcs & Reward Bridge Integration (Delivered pure domain data binding and reward/morale delegate bridges for `PersonalQuestSystem` in `Assets/Ashfall.Core/Quests/PersonalQuestSystem.cs`; verified 10+ authored quests from canonical `personal_quests.json`; implemented `LoadCatalog(PersonalQuestCatalogData)`, trait querying `GetQuestsForTrait`, and wired `MoraleDeltaApplier` and `ItemRewardApplier` delegate seams in `ChooseOption` directly routing quest choices to survivor morale and shelter inventory without duplicating quest, survivor, or inventory authority; `Plan200PersonalQuestsIntegrationTests` 4/4 PASS, `PersonalQuestSystemTests` 10/10 PASS).

**UNBLOCK Program Wave 28 Plans (Part 1) — COMPLETE (2026-09-20):** 3 partially integrated plans deeply integrated and sealed:
1. `C1[29] / Plan 168` / DEC-111: Propaganda, Information Warfare & Credibility System (Delivered pure domain data binding and delegate bridge for `PropagandaSystem` in `Assets/Ashfall.Core/Propaganda/PropagandaSystem.cs`; loaded canonical campaigns from `propaganda_templates.json` Scavenger Lantern Hour, Unity Call, Raider Deterrence, Cooperative Coalition; implemented `StartCampaignFromTemplate` satisfying `RadioProgramProductionSystem.StartPropagandaCampaign` host delegate seam; verified truthfulness ratings, detection risks, credibility erosion, and counter-broadcast mitigation without duplicating radio or faction registries; `Plan168PropagandaIntegrationTests` 7/7 PASS, `PropagandaSystemTests` 6/6 PASS).
2. `C2[36] / Plan 167` / DEC-112: Underground Tunnel Network System (Delivered pure domain data binding and travel bypass evaluation for `TunnelNetworkSystem` in `Assets/Ashfall.Core/Underground/TunnelNetworkSystem.cs`; loaded canonical subterranean junctions and segments from `underground_tunnels.json`; implemented `ReinforceSegment`, `ClearHazard`, and `EvaluateSurfaceBypass` allowing expeditions and travelers to bypass severe surface weather/hazards via reinforced subterranean routes without duplicating world graph or expedition stores; `Plan167TunnelNetworkIntegrationTests` 7/7 PASS, `TunnelNetworkSystemTests` 6/6 PASS).
3. `C1[39] / Plan 207` / DEC-113: Shelter Reputation Dimensions, Public Tags & Trade Price Modifiers Integration (Delivered pure domain catalog loading, trade price multipliers, and dominant titles for `ShelterReputationSystem` in `Assets/Ashfall.Core/Reputation/ShelterReputationSystem.cs`; loaded 5 canonical perception dimensions and 8 public tags from `reputation_dimensions.json`; implemented `GetTradePriceMultiplier` mapping public standing into bounded trade pricing [0.70..1.50], `GetDominantPerceptionTitle` dynamically projecting emergent regional identity, anti-farming evidence recording, and daily decay without unseeded RNG or parallel save sections; `Plan207ShelterReputationIntegrationTests` 8/8 PASS).

**UNBLOCK Program Wave 27 Plans (Part 2) — COMPLETE (2026-09-20):** 3 partially integrated plans deeply integrated and sealed:
1. `C1[28] / Plan 166` / DEC-108: Shelter Identity & Origin Projection System (Delivered pure domain `ShelterIdentitySystem` in `Assets/Ashfall.Core/Shelter/ShelterIdentitySystem.cs` loading 6 canonical origins from `shelter_origins.json`; models shelter naming with 1..40 char validation, mottos, emblems, origin baseline traits/reputations, community action tracking with emergent known-for identity tags Traders, Healers, Raiders, Hermits, Survivors, infamy tracking, narrative token text interpolation `{shelter_name}`, `{motto}`, `{origin_name}`, `{founder}`, and schema-safe persistence without unseeded RNG or engine references; `ShelterIdentitySystemTests` 7/7 PASS).
2. `C2[29] / Plan 142` / DEC-109: Clothing & Warmth Gear Progression Integration (Delivered pure domain `ClothingWarmthSystem` in `Assets/Ashfall.Core/Inventory/ClothingWarmthSystem.cs` and integrated seamlessly with `NeedsSystem.ApplyWarmth` via `ClothingWarmthReductionProvider` delegate seam; models 4 equipment layers Underwear, Middle, Outer, Accessory, insulation values, condition-based degradation, wetness tracking with waterproof resistance, gradual drying near heat sources, and net cold loss reduction calculation [0.0..0.85] without duplicating inventory authority or survivor state; `ClothingWarmthSystemTests` 7/7 PASS, `SurvivorNeedsCharacterizationTests` 15/15 PASS).
3. `C2[37] / Plan 173` / DEC-110: Shelter Radio Production & Audience Response Integration (Completed deep audience response integration on `RadioProgramProductionSystem` in `Assets/Ashfall.Core/Radio/RadioProgramProductionSystem.cs`; implemented presenter capability scaling [0.2..3.0], template genre base morale deltas storytelling 3.5, entertainment 4.0, civilian_news 2.0, education 2.5, emergency 1.0, signal-strength-derived audience reach grading Local, District, Regional, listener opportunity generation, `ApplyShelterMoraleDelta` and `ApplyFactionReputationDelta` host bridges, and follow-up hook resolution lifecycle without duplicating radio frequencies, schedules, or save stores; `RadioAudienceResponseTests` 6/6 PASS, `Plan173RadioProgramProductionTests` 10/10 PASS).

**UNBLOCK Program Wave 27 Plans (Part 1) — COMPLETE (2026-09-20):** 3 partially integrated plans deeply integrated and sealed:
1. `C1[25] / Plan 151` / DEC-105: Working Animals & Companion System Pack Cargo & Expedition Defense Integration (Extended canonical `ExpeditionSystem` with pack beast capacity query seam `SetPackCapacityBonusQuery`; projects bonus carry weight in expedition previews `projected["pack_capacity_bonus_kg"]`, expands baseline 40kg cargo up to 65kg with companion support, enforces capacity limits during loot pickup, and scales carry bonus by animal training level without duplicate companion or animal entity stores; `WorkingBeastExpeditionTests` 5/5 PASS).
2. `C1[26] / Plan 155` / DEC-106: Black Market & Underground Contraband Barter Economy Integration (Extended canonical `ShelterBarterSystem` with `ContrabandCheck` predicate and `OnContrabandTraded` event bridge; honest wasteland merchants strictly refuse illicit contraband goods `contraband_refused`, while black market and shadow dealers accept contraband and fire detection events for `BlackMarketHeatAttentionEngine` and `BlackMarketContrabandEngine` without creating parallel trading registries; `ContrabandMarketIntegrationTests` 5/5 PASS).
3. `C1[27] / Plan 159` / DEC-107 / DEC-320: Shelter Governance & Political System — Ideological Blocs, Consent & Stability Engine (Delivered pure domain `ShelterGovernanceEngine` in `Assets/Ashfall.Core/Governance/ShelterGovernanceEngine.cs` extending `PolicySystem` and `LeadershipSystem`; implements 4 core ideological blocs Security, Egalitarian, Free Pioneers, Heritage Archive from `shelter_governance_blocs.json`, dynamic bloc influence weighting from survivor membership, policy majority consent scoring with automatic opposition grievance accumulation, civil dispute lifecycle Mediation, Restitution, Reprimand, Exile, deterministic grievance decay, and holistic shelter stability rating calculation [0..100] without unseeded RNG or engine references; `ShelterGovernanceEngineTests` 8/8 PASS. Sealed and elevated to full host integration with save section #238, phase-5 day owner, UI PoliticalBlocProvider, and `--shelter-governance-selftest` 12/12 PASS under DEC-320).

**UNBLOCK Program Wave 26 Plans — COMPLETE (2026-09-20):** 3 partially integrated plans deeply integrated and sealed:
1. `C1[21] / Plan 137` / DEC-102: Needs → Performance Cascade Engine Across Combat, Work & Expeditions (Engine-free pure projection bridge `NeedsPerformanceBridge` in `Assets/Ashfall.Core/Survivors/NeedsPerformanceBridge.cs` projecting canonical survivor needs hunger/thirst/fatigue/warmth/morale to domain performance multipliers across combat accuracy/damage, work throughput, expedition speed, and stamina drain; configured data-driven thresholds and floors in `needs_performance.json`; wired consumption into `TacticalCombatSystem`, `ExpeditionSystem`, and `DutyRosterSystem`; `NeedsPerformanceBridgeTests` 7/7 PASS).
2. `C1[24] / Plan 147` / DEC-103: Per-NPC Personal Memory & Relationship Depth System (`NpcMemorySystem` in `Assets/Ashfall.Core/Narrative/NpcMemorySystem.cs` tracking individual player actions toward named NPCs helped/saved_life/gifted_food/healed/refused/ignored/betrayed/restitution with deterministic trust, grudge, and favor deltas; temporal decay, forgiveness with restitution, trade price modifiers up to 15% discount for high trust, 30% surcharge or trade embargo for high grudge, dialogue tone derivation, authored dialogue templates in `npc_memory_dialogue.json`, and schema-safe save/restore without duplicate reputation registries; `NpcMemorySystemTests` 6/6 PASS).
3. `C1[31] / Plan 176` / DEC-104: Survivor Aging, Life-Stage Capacity & Vocational Mentorship Engine (`SurvivorAgingProgressionEngine` in `Assets/Ashfall.Core/Survivors/SurvivorAgingProgressionEngine.cs` projecting campaign tenure into chronological age and 5 canonical life stages Child/YoungAdult/Prime/MiddleAge/Elderly from the single roster tenure clock; physical labor capacity taper, vocational mentorship bonus +25% apprentice skill gain when elder is present, authored life stage data in `survivor_life_stages.json`, and dignified retirement option with light duty and 30% fatigue reduction; `SurvivorAgingProgressionEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 25 Plans — COMPLETE (2026-09-20):** 5 additional plans integrated and sealed:
1. `CF-P5-RESTOCK-RECONCILE` / DEC-97: Merchant Restock Priority Ledger Reconciliation & Ratification (Reconciled line 359 of `INTEGRATION_PLANS.md`, ratified `DEC-05` priority-weighted tier restock formula with deterministic PRNG in `ShelterBarterSystem.ComputeItemPriorityScore` over canonical trade session without duplicate inventory stores, `Plan147RestockPriorityTests` 6/6 PASS).
2. `E1 / Plan 53` / DEC-98: Ambition Governance, Expansion Intake & Canonical Rails Readiness Engine (E1D reference freshness scanner `scripts/ci/verify-plan-freshness.py` auditing 609 plans across all namespaces, E1H canonical rails readiness registry `docs/roadmap/rails.registry.json`, `scripts/ci/generate-rails-registry.py`, `docs/roadmap/rails.json`, `docs/roadmap/RAILS.md` covering 10 canonical rails with verified on-disk evidence paths and cycle detection, E1I executable intake gate `scripts/ci/plan-intake-check.py` and `plan-intake-check.sh` rejecting unknown rails and validating system/presentation discipline, and `PlanGovernanceContractTests` 5/5 PASS).
3. `Plan 14 / Plan 25 Follow-Up` / DEC-99: Dedicated Faction Culture Codex Panel Integration (Dedicated `FactionCultureCodexPanel` in `src/UI/FactionCultureCodexPanel.cs` presenting 25 authored diegetic customs from `muster_faction_culture.json` loaded into `MusterHostSession.Culture`, registered route `"faction_culture_codex"` in `PanelRegistryBootstrap.cs`, wired overview button in `FactionsPanel.cs`, `FactionCultureCodexTests` 4/4 PASS, `PanelRouteGateTests` 20/20 PASS).
4. `Crop Roster Phase 2` / DEC-100: Oilseed Pressing & Food Preservation Chain (Pure domain oil extraction engine `OilseedPressingEngine` in `Assets/Ashfall.Core/Farming/OilseedPressingEngine.cs` modeling extraction efficiency across 3 press grades ImprovisedMortar to HydraulicWorkshopPress and 3 product modes CulinaryCookingOil to MechanicalLubricant, byproduct press-cake meal, salt-and-oil confit preservation `item_fat_confit` with 4.0x shelf-life extension and 900-permille spoilage resistance, sustainable seed retention ratios, and dietary lipid contribution to `CommonTableRationingEngine` adding `FoodCategory.PressedLipids` and +10% absorption bonus, `OilseedPressingTests` 7/7 PASS, `CommonTableRationingEngineTests` 5/5 PASS, `FoodPreservationSystemTests` 8/8 PASS).
5. `C2[17] / Plan 40A` / DEC-101: Authored Survivor Identity, Declared Item Tags & Personality Observability (Rich diegetic identity observability in `Assets/Ashfall.Core/Survivors/SurvivorEnrichmentService.cs` integrating declared keepsake item tags from `expansion_item_tags.json`, deterministic duty affinity with +15% comfort bonus, ideological tension mapping across opposing belief pairs, deterministic 3-trait personality synthesis without unseeded RNG, and `SurvivorIdentityObservabilitySlate` projected into `src/UI/SurvivorDetailPanel.cs`, `identity` rail verified `DONE`, `SurvivorIdentityObservabilityTests` 6/6 PASS, `SurvivorOriginModifierTests` 4/4 PASS, `PanelRouteGateTests` 20/20 PASS).

**UNBLOCK Program Wave 24 Plans — COMPLETE (2026-09-20):** 2 additional plans integrated and sealed:
1. `CF-P6-VEHICLE-ARMOR-GRADES` / DEC-95: Vehicle Armor Grade Tiers & Mitigation Seam (4 vehicle armor grade tiers on the Plan 50 vehicle seam, `vehicle_armor_grades.json`, `VehicleArmorGradeCatalog`, `VehicleGarageSystem` modeling monotonic mitigation permille 100 to 250 across grades 1–4, wear absorption 200 to 350, speed penalties -0.02 to -0.10, fuel multipliers 1.02 to 1.12, trip wear absorption reducing chassis stress while consuming armor integrity, condition bands nominal/worn/critical/depleted, and reforge restoration restoring integrity pool with dedicated parts bill, `VehicleArmorGradesTests` 5/5 PASS, `--vehicle-garage-selftest` 27/27 PASS).
2. `Plan 25` / CF-P25 / DEC-96: Faction Ecology, Regional Treaties & The Muster (Mechanical regional treaty catalog loading into `RegionalTreatySystem` via `RegionalTreatyCatalogLoader`, unified standing read model across 4 wasteland factions, witness mortality census binding via `SubjectLivingResolver` in `MusterHostSession` ensuring deceased subjects never testify, shelter inventory delivery via `IFactionActionItemSink`, and treaty breach consequences modifying raid pressure, `Plan25FactionEcologyTests` 5/5 PASS, `--faction-ecology-selftest` 27/27 PASS).

**UNBLOCK Program Wave 23 Plans — COMPLETE (2026-09-20):** 2 additional unblocking expansion plans integrated and verified:
1. `Expansion 40` / UNBLOCK-05: The Wheel — Mechanical Power Distribution, Driveline Friction & Machine Maintenance Engine (`MechanicalPowerDrivelineEngine`, prime mover power generation ManualTreadle to FlywheelCoupled, transmission losses across 4 coupling types DirectShaft/FlatLeatherBelt/SpurGearTrain/RollerChain, length and misalignment friction drag, lubricant breakdown and bearing wear acceleration, and machine tool spindle runout tolerance verification for workshop jobs — extending `KineticStorageSystem`, `ShelterWorkshopSystem`, and `PrecisionMetrologySystem` without duplicating power or machine identity stores, `MechanicalPowerDrivelineEngineTests` 5/5 PASS).
2. `Expansion 41` / UNBLOCK-05: The Quiet — Sleep Quality, Soundproofing & Shelter Crowding Engine (`SleepAcousticRestEngine`, bunk soundproofing decibel attenuation, crowding density penalties area per occupant, quiet-hours compliance modifiers 22:00–06:00, sleep environment bands Unbearable to DeepSanctuary, and net fatigue clearing multipliers with morale adjustments — extending `ShelterNoiseSystem`, `NeedsSystem`, and `ShelterAssignmentSystem` without duplicating room or assignment registries, `SleepAcousticRestEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 22 Plans — COMPLETE (2026-09-20):** 3 additional unblocking expansion plans integrated and verified:
1. `Expansion 37` / UNBLOCK-05: The Quickening — Antenatal Care, Maternal Support & Neonatal Health Engine (`AntenatalMaternalHealthEngine`, 5 gestational trimesters FirstTrimester to FullTerm/Postpartum, maternal nutrition reserve depletion under caloric deficit, rest/fatigue penalties, complication risk permille based on shelter sanitation and medical supervision quality, deterministic birth delivery resolution Healthy/Delicate/Complicated, and postpartum recovery rate curves — extending `ChildDevelopmentSystem`, `CohortSystem`, and `CaregivingSystem` without duplicating generational stores, `AntenatalMaternalHealthEngineTests` 5/5 PASS).
2. `Expansion 38` / UNBLOCK-05: The Ward — Clinical Triage Priority, Surgical Roster & Sterile Supply Engine (`ClinicalWardTriageEngine`, 4-tier triage Minimal/Delayed/Immediate/Expectant, surgical suite preparation and sterility gates, sterile consumable supply consumption, monthly bed turnover capacity, and nosocomial infection risk derived from cleanliness grades and ward occupancy — extending `MedicalWardSystem` and `AdvancedSurgicalWardSystem` without replacing bed or procedure authority, `ClinicalWardTriageEngineTests` 5/5 PASS).
3. `Expansion 39` / UNBLOCK-05: The Reagent — Chemical Synthesis Safety, Catalyst Purity & Reagent Grade Engine (`ChemicalReagentSynthesisEngine`, reactor hazard states Stable to ThermalExcursionRunaway, reaction execution and conversion yields, catalyst activity wear, output purity classification Crude to AnalyticalPharma, mass balance stoichiometric conservation, and neutralizing alkali demand for acidic chemical effluent — extending `ChlorAlkaliSynthesisEngine`, `FischerTropschSynthesisEngine`, and `PharmaLabSystem` without duplicating process status or inventory registries, `ChemicalReagentSynthesisEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 21 Plans — COMPLETE (2026-09-20):** 2 additional unblocking expansion plans integrated and verified:
1. `Expansion 35` / UNBLOCK-05: The Habit — Chemical Dependency Taper, Withdrawal Management & Care Policy Engine (`DependencyTaperWithdrawalEngine`, 5 dependency severity tiers None to Critical, recommended daily dose step-down schedules based on severity and stock scarcity, 5 withdrawal symptom bands Asymptomatic to LifeThreatening, peer-support symptom mitigation up to full session count, medical escalation triggers for acute unsupervised detox, and shelter care policy posture recommendations Permissive to Emergency — extending `ChemicalDependencySystem` and `PharmaceuticalTabletEngine` without duplicating the dependency ledger, `DependencyTaperWithdrawalEngineTests` 5/5 PASS).
2. `Expansion 36` / UNBLOCK-05: The Watch — Night Watch Patrol Route, Perimeter Detection & Readiness Engine (`NightWatchPatrolReadinessEngine`, 5 patrol coverage grades Blind to Full, detection probability with night-vision optics bonus, 24h gap-hours calculation, watch-post shift fatigue accumulation and fatigue tier classification Alert to Impaired, gate-protocol readiness composite (staff, drill recency, mechanism condition, alarm relay), seal time estimation, and composite sector watch readiness scoring — extending `PatrolTerritoryAuthority` and `SoundRangingThreatEngine` without duplicating territory or acoustic detection authority, `NightWatchPatrolReadinessEngineTests` 5/5 PASS; full host integration is recorded in the UNBLOCK-EXPANSION-36-NIGHT-WATCH entry above).

**UNBLOCK Program Wave 20 Plans — COMPLETE (2026-09-20):** 3 additional unblocking expansion plans integrated and verified:
1. `Expansion 31` / UNBLOCK-05: The Kiln — Ceramics Firing, Lime Calcination & Masonry Materials Engine (`KilnFiringEngine`, 5 kiln load kinds ClayPottery/FiredBrick/RefractoryTile/LimestoneCalc/CeramicTile, three-stage firing with thermal-shock crack risk on fast cold-load ramp (stage 0 + temp >800 permille), draw-grade quality tiers Waster to ArchivalGrade from heat-work + raw-material composite score, lime calcination CaCO₃→CaO yield with temperature ≥700-permille threshold and soak-hour efficiency, and refractory lining wear computation feeding `CupolaFoundryEngine` — extending `CeramicsKilnCatalog` and `MasonryBrickworksCatalog` narrative seams without duplicating metallurgy authority, `KilnFiringEngineTests` 5/5 PASS).
2. `Expansion 32` / UNBLOCK-05: The Wild — Wildlife Harvest Quota, Predator Conflict & Ecology Balance Engine (`WildlifeHarvestQuotaEngine`, 6 population bands Extinct to Abundant, reproduction-surplus sustainable harvest quota, overhunt population collapse risk permille, post-harvest band projection, predator conflict posture Passive/Territorial/Aggressive/Rampage from proximity × population × shelter noise, and taming readiness scoring with deterministic ±8% seed variance — extending `WildlifeEcosystemSystem` and `WildlifeTrappingSystem` seams without duplicating ecosystem state or trap-site ownership, `WildlifeHarvestQuotaEngineTests` 5/5 PASS).
3. `Expansion 33` / UNBLOCK-05: The Weather — Storm Forecast Reliability, Warning Issuance & Seasonal Readiness Engine (`StormForecastReadinessEngine`, lead-time skill decay onset at 12h with 28-permille/h degradation beyond, 4 confidence tiers Unknown to HighConfidence, 6 storm severity classes Clear to BlackRain with severity penalty, public warning gate at 560-permille, seasonal readiness composite weighted airlock/filter/medical/drill, black-rain absorption threshold 800-permille, and daily instrument calibration decay computation — extending `WeatherSystem` and `WeatherGate` without duplicating weather authority, `StormForecastReadinessEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 19 Plans — COMPLETE (2026-09-20):** 2 additional unblocking expansion plans integrated and verified:
1. `Expansion 29` / UNBLOCK-05: The Glass — Precision Vitrification, Optics Grinding & Vision Care Engine (`PrecisionGlassworksOpticsEngine`, silica batch vitrification with three-stage annealing at optimal 900-permille kiln temperature, thermal shock crack risk permille, glass purity tiers Crude/CommonWindow/OpticalGrade/PrecisionOptic, corrective lens grinding for 6 vision bands with deterministic seeded ±10% variance, minimum purity gating per band, and theodolite calibration for wasteland surveying with field-readiness threshold — extending `PrecisionOpticsEngine` seam, `PrecisionGlassworksOpticsEngineTests` 5/5 PASS).
2. `Expansion 30` / UNBLOCK-05: The Press — Public Broadsheet, Almanac Printing & Information Distribution Engine (`PublicBroadsheetPressEngine`, movable type composition with type tray wear permille and 800-permille degradation threshold, 5 publication kinds Broadsheet/Almanac/Pamphlet/Wanted/Proclamation with ink/paper resource consumption gates, deterministic audience reach permille against shelter population, shelter morale stabilization, rumor debunking credibility correction with stubborn rumor resistance, and type tray restoration — extending `ArchiveDeskSystem` seam without duplicating narrative registries, `PublicBroadsheetPressEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 18 Plans — COMPLETE (2026-09-20):** 3 additional unblocking expansion plans integrated and verified:
1. `Expansion 25` / UNBLOCK-05: The Iron Road — Rail Track Maintenance, Gauge Integrity & Bridge Load Engine (`RailTrackMaintenanceEngine`, gauge stability tiers PristineStandard to BrokenGaugeRefusal, locomotive class weight tables ManualHandcar 2t to ArmoredBattleTrain 250t, cumulative track wear permille, bridge load integrity degradation, route feasibility evaluation, and workgang maintenance repair — extending `RailwayInterlockEngine` (Plan 117) seam without touching dispatch/pathfinding, `RailTrackMaintenanceEngineTests` 5/5 PASS).
2. `Expansion 27` / UNBLOCK-05: The Thread — Garment Layering, Thermal Insulation & Laundry Hygiene Engine (`GarmentLayeringThermalEngine`, four-slot layering Base/Mid/Outer/WeatherShell, durability and dirt accumulation, full-layering bonus +150 permille, waterproof shell detection, shelter warmth stacking, and laundry hygiene restoration — extending `NeedsSystem.Warmth` and `WornGear.EffectiveProtection()` seam without duplicating equipment slots, `GarmentLayeringThermalEngineTests` 5/5 PASS).
3. `Expansion 28` / UNBLOCK-05: The Lesson — Apprenticeship Curriculum, Literacy Progression & Vocational Certification Engine (`ApprenticeshipCurriculumEngine`, four-tier literacy Illiterate to Scholarly, seeded session comprehension yield with deterministic ±15% variance, consecutive-session fatigue penalty after 3 sessions, Functional-literacy vocational certification gate at 900 permille, and Scholarly manual transcription yield — extending `ApprenticeshipSystem` (root Core) without duplicating mentorship pair tracking, `ApprenticeshipCurriculumEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 17 Plans — COMPLETE (2026-09-20):** 4 additional unblocking plans integrated and verified:
1. `Expansion 18` / UNBLOCK-05: The Underneath — Subterranean Subsidence & Excavation Integrity Engine (`SubterraneanSubsidenceEngine`, depth tiers 1–5, strata bearing resilience, shoring mitigation 0–3, subsidence distortion mm, and deterministic induced seismic tremor triggers, `SubterraneanSubsidenceEngineTests` 5/5 PASS).
2. `Expansion 19` / UNBLOCK-05: The Bitter Air — Chemical Plume Atmospheric Dispersion & Filter Wear Engine (`ChemicalPlumeDispersionEngine`, toxic plume drift, precipitation washout, shelter air filtration scrubbing, airlock breach warnings, and respirator canister attrition, `ChemicalPlumeDispersionEngineTests` 5/5 PASS).
3. `Expansion 20` / UNBLOCK-05: The Quiet Hand — Informant Network & Counter-Intelligence Tradecraft Engine (`InformantNetworkTradecraftEngine`, informant archetypes, dead-drop operations, suspicion accumulation, compromise detection, and ethical vs coercive interrogation outcomes, `InformantNetworkTradecraftEngineTests` 4/4 PASS).
4. `Expansion 24` / UNBLOCK-05: The Long Goodbye — Palliative Care, Dignity & Grief Stage Engine (`PalliativeCareDignityEngine`, terminal symptom management, lucidity vs analgesia protocols, survivor dignity scoring, Kübler-Ross grief transitions, and permanent memorial legacy echoes, `PalliativeCareDignityEngineTests` 4/4 PASS).

**UNBLOCK Program Wave 16 Plans — COMPLETE (2026-09-20):** 4 additional unblocking plans integrated and verified:
1. `XP-08-F6` / UNBLOCK-02: Seasonal Human Migration Consequence & Regional Multiplier Engine (`MigrationConsequenceEngine`, market demand/supply multipliers, shelter labor candidate scaling, territorial friction event weights, and caravan demand hooks over `SeasonalHumanMigrationEngine`, `MigrationConsequenceEngineTests` 5/5 PASS).
2. `XP-04-F4` / UNBLOCK-02: Black Market Heat & Attention Band Engine (`BlackMarketHeatAttentionEngine`, heat accumulation, attention bands Calm/Raised/Hot/CriticalLockout, daily cooling, patrol raid probabilities, and deterministic relocation anti-reroll persistence, `BlackMarketHeatAttentionEngineTests` 5/5 PASS).
3. `Expansion 17` / UNBLOCK-02/05: The Long Evening — Cultural Leisure & Shelter Festival Engine (`ShelterFestivalEngine`, communal leisure, physical commodity intake strictly non-currency, despair mitigation, morale restoration, and active celebration timers, `ShelterFestivalEngineTests` 5/5 PASS).
4. `Expansion 19` / UNBLOCK-05: The Silent Watch — Automated Perimeter Early Warning Radar Engine (`PerimeterEarlyWarningEngine`, perimeter sensor sweeps, power grids, sensor calibration permille, false-alarm discrimination, alert levels, and breach warning timers, `PerimeterEarlyWarningEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 15 Plans — COMPLETE (2026-09-20):** 4 additional unblocking plans integrated and verified:
1. `XP-04-F6` / UNBLOCK-02: Wasteland Loan Shark Debt & Enforcer Escalation Engine (`LoanSharkEnforcerEngine`, daily compounding interest permille, delinquency/grace tracking, commercial sanctions, bounty placement via `FactionBountySystem`, and enforcer raid risks over canonical `FundsLedger`, `LoanSharkEnforcerEngineTests` 5/5 PASS).
2. `F13-G` / UNBLOCK-02: Tier-4 Exclusive Goods & Trade Monopoly Discipline Engine (`TradeRouteMonopolyEngine`, Tier 4 exclusive goods discipline, market saturation price decay curves, daily demand recovery, and caravan cargo quota bounds over `TradeRouteContract`, `TradeRouteMonopolyEngineTests` 5/5 PASS).
3. `D22-B` / UNBLOCK-03: Survivor Voice Line Audio Bridge & Cooldown Dispatcher (`VoiceLineDispatchCoordinator`, per-survivor and global cooldowns, priority preemption, and subtitle payload generation over `VoiceLineSelectionEngine`, `VoiceLineDispatchCoordinatorTests` 5/5 PASS).
4. `Expansion 16` / UNBLOCK-01/05: The Rebuilt Body — Surgical Grafting & Rejection Engine (`SurgicalGraftRejectionEngine`, biological tissue compatibility tiers `Autograft` to `XenograftSynthetic`, tissue integration progression, immunosuppressant decay, and immune rejection risks, `SurgicalGraftRejectionEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 14 Plans — COMPLETE (2026-09-20):** 2 additional unblocking plans integrated and verified:
1. `XP-04-F5` / UNBLOCK-02: Scrap Chit Purity Tiers & Counterfeit Detection Engine (`ChitPurityAssayEngine`, scrap chit alloy purity MintPristine to CounterfeitLead, perception-scaled assay detection odds, trust penalties, and assay office certification over canonical `FundsLedger`, `ChitPurityAssayEngineTests` 5/5 PASS).
2. `L-P32R` / UNBLOCK-04: Aviation & Amphibious Wasteland Travel Modal Engine (`ModalTravelDispatchEngine`, multi-modal travel FootExcursion, GroundConvoy, AmphibiousRig, and AerialReconFlight over wasteland map routes, evaluating terrain refusals, fuel consumption, and weather windows, `ModalTravelDispatchEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 13 Plans — COMPLETE (2026-09-20):** 2 additional unblocking plans integrated and verified:
1. `F14-G` / UNBLOCK-01: Survivor Body State UI Presentation Model (`SurvivorBodyPresentationSlate`, limb rows, grip capabilities Full Grip vs Simple Only, mobility permille, maintenance notices, phantom pain warnings, `SurvivorBodyPresentationSlateTests` 5/5 PASS).
2. `XP-04-F2` / UNBLOCK-02: Black Market Contraband Valuation & Fence Engine (`BlackMarketContrabandEngine`, buy/sell/fence trade quotes, classification multipliers, trust-scaled fencing fees 20–35%, heat accumulation, raid lockout thresholds over canonical `FundsLedger`, `BlackMarketContrabandEngineTests` 7/7 PASS).

**UNBLOCK Program Wave 12 Plans — COMPLETE (2026-09-20):** 4 additional unblocking plans integrated and verified:
1. `F14-D` / UNBLOCK-01: Prosthetics Condition & Wear Integration Engine (`ProstheticConditionWearEngine`, daily mechanical wear, complexity tiers SimpleImprovised to AdvancedArticulated, biomechanical efficiency permille, failure risks, `ProstheticConditionWearEngineTests` PASS).
2. `XP-08-F3` / UNBLOCK-02: Trade Route Risk Binding Engine (`TradeRouteRiskBindingEngine`, raid vulnerability, weather ambush risk, escort mitigation, cargo attrition permille, `trade_route_disrupted` scanner orphan resolution, `TradeRouteRiskBindingEngineTests` PASS).
3. `Plan 49` / `C1[16]` / UNBLOCK-03: Content Orphan Certification & Consumer Routing Engine (`ContentOrphanCertificationEngine`, active composed consumers, certification classifications CertifiedActive/ExcludedDormant/OrphanWarning, `ContentOrphanCertificationEngineTests` PASS).
4. `D4` / `DEC-15` / UNBLOCK-04: Weather Forecast Reliability & Telemetry Decay Engine (`WeatherForecastReliabilityEngine`, lead-time decay, distance attenuation, electromagnetic interference, radio broadcast reliability, `WeatherForecastReliabilityEngineTests` PASS).

**UNBLOCK Program Wave 11 Plans — COMPLETE (2026-09-20):** 4 additional unblocking plans integrated and verified:
1. `Plan 42` / `C2[18]` / UNBLOCK-03: Survivor Voice Line Selection Engine (`VoiceLineSelectionEngine`, mood, stress thresholds, trauma tags, event kinds, deterministic seeded tie-break, `VoiceLineSelectionEngineTests` 4/4 PASS).
2. `Plan 46` / `C2[20]` / UNBLOCK-03: Playable Metrics & Session Telemetry Aggregation Engine (`PlayableMetricsAggregationEngine`, hardship index, efficiency rating, survival stability, readiness grades A to F, zero PII, `PlayableMetricsAggregationEngineTests` 4/4 PASS).
3. `EN-08` / UNBLOCK-04: Ledger Truth & Quarantine Integrity Gate (`LedgerTruthIntegrityGate`, decision register terminal invariant verification, empty test quarantine consistency per D21, census monotonicity, `LedgerTruthIntegrityGateTests` 4/4 PASS).
4. `Plan 32B` / `D17` / UNBLOCK-04: Wasteland Travel Graph Knowledge Gating & Discovery Engine (`TravelGraphKnowledgeGate`, survey tiers Unknown to Mapped, foot vs heavy convoy gating, aerial recon elevation, `TravelGraphKnowledgeGateTests` 4/4 PASS).

**UNBLOCK Program Wave 10 Plans — COMPLETE (2026-09-20):** 4 additional unblocking & expansion plans integrated and verified:
1. `Expansion 14` / Wave 1: Aerial Recon & Flight Window Evaluator (`AerialReconWindowEngine`, flight weather windows Optimal/Marginal/Hazardous/Grounded, wind-shear/icing/visibility hazards, payload range degradation, airdrop drift, `AerialReconWindowEngineTests` 5/5 PASS).
2. `Expansion 15` / Wave 1: Subterranean Soil Reclamation & Biochar Remediation Engine (`SoilReclamationProfileEngine`, soil quality tiers BarrenAsh to EnrichedTopsoil, germination viability, isotopic mutation risks, amendment chemistry biochar/compost/leaching/fallow, `SoilReclamationProfileEngineTests` 5/5 PASS).
3. `Expansion 21` / Wave 2: Microgrid Load Shedding & Brownout Risk Engine (`PowerLoadSheddingEngine`, strict priority shedding Tier0_LifeSupport to Tier4_Comfort, brownout vulnerability, cascading breaker trips, energy poverty morale penalties, `PowerLoadSheddingEngineTests` 4/4 PASS).
4. `Expansion 23` / Wave 3: Emergency Muster & Cascade Readiness Evaluation Engine (`EmergencyMusterReadinessEngine`, composite muster score, evacuation duration, missing survivor risk, drill recency decay, compliance fatigue, cascade off-ramp intervention margin, `EmergencyMusterReadinessEngineTests` 4/4 PASS).

**UNBLOCK Program Wave 9 Plans — COMPLETE (2026-09-20):** 4 additional unblocking & expansion plans integrated and verified:
1. `D22` / UNBLOCK-03: Localization String Freeze Policy & Extraction Gate (`StringFreezePolicy`, freeze classifications, key prefix enforcement, debt allowlist checks, extraction requirements, `StringFreezePolicyTests` 5/5 PASS).
2. `D16` / UNBLOCK-04: Wasteland Map Route Dynamic Edge Hazard Evaluator (`MapRouteHazardEvaluator`, traversal feasibility, clearance thresholds <300mm flood refusal, seasonal rain delay days, risk score permille, `MapRouteHazardEvaluatorTests` 4/4 PASS).
3. `Expansion 22` / Wave 3: Clean Flow Water Treatment Quality & Source Profile Engine (`WaterQualityProfileEngine`, purity tiers, contaminant profile modeling, filtration yields, filter wear permille, health risk permille, `WaterQualityProfileEngineTests` 4/4 PASS).
4. `Expansion 26` / Wave 3: Common Table Nutrition Diversity & Rationing Policy Engine (`CommonTableRationingEngine`, diversity tiers Monoculture to AbundantSpectrum, cook duty waste reduction, long-term deficiency risks, and inequality-provoked grievance permille, `CommonTableRationingEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 8 Plans — COMPLETE (2026-09-20):** 4 additional unblocking & expansion plans integrated and verified:
1. `EN-06` / UNBLOCK-05: One Bootstrap Path Lifecycle Verification Gate (`BootstrapLifecycleGate`, monotonic stage progression, complete required subsystem check, zero deferred seams across all lifecycle modes, `BootstrapLifecycleGateTests` 5/5 PASS).
2. `EN-05` / UNBLOCK-05: Rescued Survivor Arc Projection Read Model (`RescuedArcProjection` over `DistressRescueMission`, truthful recovery timelines, exactly-once journal keys, `RescuedArcProjectionTests` 5/5 PASS).
3. `Expansion 12` / Wave 1: Second Generation Succession & Kinship Milestone Engine (`SecondGenerationMilestoneEngine`, developmental milestones, vocational aptitude modifiers, succession readiness permille, `SecondGenerationMilestoneEngineTests` 5/5 PASS).
4. `Expansion 13` / Wave 1: Faithful & Fractured Ritual Calendar & Friction Engine (`SpiritualRitualCalendarEngine`, canonical holy days, low-morale comfort scaling, ideological friction mitigation, `SpiritualRitualCalendarEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 7 Plans — COMPLETE (2026-09-20):** 3 additional unblocking plans integrated and verified:
1. `F14-E` / UNBLOCK-01: Rehabilitation Arc Progression Engine (`RehabilitationProgressionEngine`, normative phases fitting 3–5d @ 500 permille, adaptation 10–20d ramping 500->1000 permille, mastery permanent @ 1000 permille, `RehabilitationProgressionEngineTests` 5/5 PASS).
2. `EN-04` / UNBLOCK-05: Rehabilitation Medicine Slate Read Model (`RehabilitationSlateProjection` rendering prosthetics count, phase, days, quality percentage, next milestone, and phantom pain warning for UI panels, `RehabilitationSlateProjectionTests` 4/4 PASS).
3. `EN-03` / UNBLOCK-05 / UNBLOCK-02: Underground Economy Pressure Read Model (`UndergroundEconomyPressure` mapping heat, trust, and relocation state to temperature band Calm/Raised/Hot/Relocated, price/attention multipliers, and status summary, `UndergroundEconomyPressureTests` 5/5 PASS).

**UNBLOCK Program Wave 6 Plans — COMPLETE (2026-09-20):** 3 additional unblocking plans integrated and verified:
1. `F14-C` / UNBLOCK-01: Survivor BodyState Model & Persistence (`SurvivorBodyState`, `LimbRecord`, `RehabRecord`, intact migration, `SurvivorBodyStateTests` 4/4 PASS).
2. `F13-D` / UNBLOCK-02: Wave-1 Funds Opt-In & Holdfast Trade Legs (`ChitsFromSettlementUnits`, `HoldfastTradeResult.FundsDelta`, `HoldfastTradeSession.BuyWithFunds`/`SellWithFunds`, `HoldfastFundsTradeTests` 4/4 PASS).
3. `EN-01` / UNBLOCK-05: Difficulty-Consequence Weave Read Model (`DifficultyConsequenceWeave` mapping difficulty scalars to war stage severity, crisis deadline offset, shock weights, and monotonicity validation, `DifficultyConsequenceWeaveTests` 3/3 PASS).

**UNBLOCK Program Wave 5 Plans — COMPLETE (2026-09-20):** 3 additional unblocking plans integrated and verified:
1. `F14-F` / UNBLOCK-01: Phantom Pain Sleep Narrative Variant (`SleepNarrativeProjection`, `SleepBeatKind.PhantomPain`, `sleep_phantom_pain` journal key, `SleepNarrativePhantomPainTests` 4/4 PASS).
2. `EN-02` / UNBLOCK-05: Living Map Route Projection Read Model (`LivingMapRouteProjection`, `WastelandMapSystem.ProjectLivingMapRoute` exposing route hops, distance, and flooded/amphibious hazards over canonical `PlanRoute`, `LivingMapRouteProjectionTests` 4/4 PASS).
3. `F13-C` / UNBLOCK-02: Restock Capacity Allocation Engine (`RestockAllocationEngine` deterministic integer restock allocation with effective weights, scarcity floors, largest-remainder rounding, and rational stock/target_par sort, `RestockAllocationEngineTests` 5/5 PASS).

**UNBLOCK Program Wave 4 Plans — COMPLETE (2026-09-20):** 3 additional unblocking plans integrated and verified:
1. `C3-174` / Plan 174: Mechanical Origin Effects Seam (`SurvivorOriginModifier` and `SurvivorEnrichmentService.GetOriginModifier` bounded mechanical origin modifiers, `SurvivorOriginModifierTests` 4/4 PASS).
2. `C3-175` / Plan 175: Cross-Run Profile Store (`CrossRunProfileStore` user-level run history store outside campaign slots, DEC-20 compliant, `CrossRunProfileStoreTests` 4/4 PASS).
3. `C3-199` / `XP-08` / Plan 199: Seasonal Human Migration Schedule Engine (`SeasonalHumanMigrationEngine` deterministic daily tick with 10-day dwell hysteresis, integer regional weights, and persistence without touching wildlife, `SeasonalHumanMigrationEngineTests` 3/3 PASS).

**UNBLOCK Program Wave 3 Plans — COMPLETE (2026-09-20):** 3 additional unblocking plans integrated and verified:
1. `XP-07` / Plan 190: Item Provenance Inspection Projection (`ItemInspectionModel` extended with `ItemProvenanceChain? Provenance` and `ItemLoreSystem` binding, `ItemInspectionProvenanceTests` 3/3 PASS).
2. `XP-08` / Plan 192: Trade Route Contract & Reliability Tier Engine (`TradeRouteContract` with reliability score, tiers 1–4, tariff discount at tier 3+, exclusive good unlock at tier 4, 30-day cooldown, and persistence, `TradeRouteContractTests` 6/6 PASS).
3. `EN-07` / UNBLOCK-05: Completion History Chronicle Read Model (pure aggregation read model `CompletionHistorySummary` and `CampaignCompletionHistoryService.Summarize` without mutating history or awarding progression, `CompletionHistorySummaryTests` 3/3 PASS).

**XP Expansion W1 — ACTIVE (2026-09-18):** the user authorized the XP-01 …
XP-10 proposal and integration plan. The first owned package is
`XP-WAVE1-DIFFICULTY-AUTHORITY`: establish the canonical difficulty catalog and
Core director, then re-check each proposed consumer before it is bound. The
W1 SOFC premise is corrected by source evidence: Plan 122 already consumes
canonical inventory fuel through `Main.Plans122to125.cs` and the existing
`SofcPowerHostSession` port, with grid-reserve fallback. No duplicate fuel
catalog, reserve, or save section will be introduced. Evidence and decision
record: `docs/plans/xp/w1/W1_PREMISE_EVIDENCE.md` and
`docs/governance/DECISION_PACKET_2026-09-18_XP_EXPANSION_W1.md`.

**Status:** COMPLETE (presented for acceptance) — `DISTRESS-SIGNALS-9-12`
flagship wave (user-authorized 2026-09-13); Waves 0–5 delivered; closeout:
`docs/radio/DISTRESS_SIGNAL_TASKS_9_12_CLOSEOUT.md`. Plan: Tasks 9–12 multi-stage progression, signal trust, follow-up
chaining, audio integration. Wave 0 baseline + premise corrections:
`docs/radio/DISTRESS_SIGNAL_TASKS_9_12_BASELINE.md` (48 rows / 43 unique
identities / 47 registered incl. 4 builtin-only fallbacks; `message_fragments`
IS the stage authority — no `stages` field; FactionWarSystem is faction-keyed
only → minimal SignalTrustState; audio needs only additive cue fields).

**Integrator promotion:** Plan 24 (`C-integration-plans/C1_planintegration[5].md`)
was explicitly promoted by the user on 2026-09-16. Its exact ownership and
stale-completed-claim override are recorded in `WORKTREE_OWNERSHIP.md`; the
package was implemented in dependency order without altering unrelated dirty
worktree changes. **Plan 24 is CLOSED (Wave 8, 2026-09-17; signatures
resolved 2026-09-18):**
all originally open acceptance items are closed-with-evidence (nine-family
needs migration, duty-hour/overwork/skill-to-yield labor cluster, assignment
UI, grief-to-needs, mourning vigil, ration journey, save/load journey parity,
30-day simulation, needs baseline 15/15). The two signature-gated decision
items were resolved 2026-09-18 under the user's completion-first program
authorization: ward staffing was implemented and sealed (option b;
`DEBT-PLAN24-MEDICAL-WARD-STAFFING` RETIRED) and the affliction-specific
recovery ramp was closed per option (ii) with no ramp authority fabricated.
The environment-blocked snapshot rebaseline is the only recorded residual
(renderer-capable session required). Closeout:
`docs/plans/PLAN_24_CLOSEOUT.md`. Evidence ledger:
`docs/plans/C1_planintegration[5]_IMPLEMENTATION_LOG.md`.

**C1.4 briefing consumer closed (2026-09-17):** the Wave 9 Part 1 A1 deferred
handoff is now integrated as an additive Plan 24 claim handoff (package
`C1-BRIEFING-CRISIS-CONSUMER`). `DailyBriefingReportBuilder.AppendCrisisWarnings`
renders `CrisisPredictor.Evaluate` output assembled from canonical host read
models; the Plan 31 semantic-kind briefing re-grouping remains a contract
decision because `DayEventVocabularyTests` pins `GenericSectionTitle` for
unhandled kinds. Details: `A1_BRIEFING_DEFERRED.md`, `WAVE9_PART1_CLOSEOUT.md`.

**Wave 11 Part 1 closed into Part 2 (2026-09-18):** A1–A5 are SEALED (Plan 38
calendar/commitments, Plan 41 memory consumers, Plan 43 governance/refusal,
Plan 45 content acceptance, Plan 47 mod contract). C2[10] and C2[11] are
PARTIALLY-SEALED with decision-bound Plan 30 runtime/consequence-reach and Plan
32 map/graph-travel remainders. The closeout, census, and debt register agree:
`docs/plans/wave11_part1/WAVE11_PART1_CLOSEOUT.md`,
`docs/plans/UNCLAIMED_CORPUS_CENSUS.md`, `KNOWN_DEBT.md`. Part 2 begins under
`claim-wave11-part2-execution-2026-09-18`.

**Wave 11 Part 2 B3–B5 premise pass (2026-09-18):** C2[12] is
**PARTIALLY-SEALED**. The user-authorized B3 authority revision permits the
sealed append-only, user-level completion-history store while retaining Plan
175's profile/reward/New Game+ boundary. No canonical campaign difficulty
authority or completion-history chronicle contract exists, so 34B/34C remain
explicitly unsealed. C2[13] is **PARTIALLY-SEALED**: Plan 36A/36B executed and sealed
(engine-free Core `PortContract` vocabulary, 248-seam policy with 100% active callers
for 176 `HOST_REQUIRED`, CI generator in `CI_GATE_MANIFEST.json` verified with `--check`,
runtime `HostSessionContracts` + `IWiringReporter` wired, `--port-contract-selftest`
registered and green, `PortContractGateTests` 8/8 green with failure proofs, Ticket #47 closed;
Plan 36C long-tail sweep queued). C2[14] is **RECONCILED-DUPLICATE** and superseded by executed
C1[10]/Wave 10 Part 2 B5; its historical document is bannered for provenance. Evidence:
`docs/plans/wave11_part2/{B3_PLAN34_IMPLEMENTATION_LOG,B4_PLAN36_IMPLEMENTATION_LOG,B5_PLAN35_DUPLICATE_RECONCILIATION}.md`.

**Wave 11 Part 2 C1/C2 status (2026-09-18):** The decision-register recurrence
and final census rerank are blocked pending explicit foreman signatures; no
Wave 12 head is certified. The partial actuality refresh, measured drains, and
signature boundary are recorded in
`docs/plans/wave11_part2/{C1_DECISION_REGISTER_PASS,C2_CENSUS_REFRESH}.md`.

**Wave 12 Part 1.1 conflict resolution (2026-09-18):** The foreman authorized
and completed an evidence-only prerequisite audit for C1[16]/Plan 49 after revalidation
showed that the source plan requires Plan 42 voice/density and Plan 46B
reachability. C2[18]/Plan 42 and C2[20]/Plan 46 remain `AUDIT-PENDING`; Plan
42 additionally depends on C2[17] identity and partially sealed C2[13] port
contracts. The audit classified all 10 verified scanner orphans and ends
`DECIDED-DEFERRED`; it did not activate content, implement voice, or certify
formal reachability. Evidence:
`docs/plans/wave12_part1_1/A1_PLAN49_PREREQUISITE_AUDIT.md`.

**D1 verification truth closed (2026-09-17):** package `D1-VERIFICATION-TRUTH`
triaged the inherited baseline to zero standing failures (14→0) with no
production change — mandatory-catalog scratch fixtures, market save v3, goods
breadth 48→50, VersionReport section pins, Triad allowlist for derived
`Cascade`/`FitnessForDuty`, B5B8 sump fixture re-capture, stale generated docs
index, and a static-state test-isolation flake. Full suite 11,697/11,697.
Evidence: `docs/plans/wave8_part2/D1_*.md`. Plan 126–129 header ambiguity
recorded, not guessed.

**C2 amputation integration (2026-09-17):** package `C2-AMPUTATION-INTEGRATION` —
Phase 4 avatar truth sealed (print-only placeholder deleted), Phase 1 contract
matrix recorded, Phase 2 equipment restriction SPLIT to a separate signed schema
package, Phase 3 expedition movement consumer PROPOSED and held at the C2 §1.6
signature gate. Evidence: `docs/plans/wave8_part2/C2_{PREMISE_EVIDENCE,DECISION}.md`;
awaiting the foreman's one-line contract confirmation before any travel penalty.

**C2-D3 + D2 executed (2026-09-17):** C2-D3 approved and shipped — the
expedition now consumes `AmputationSystem.GetMovementSpeedMultiplier` through the
additive `ExpeditionState.survivorSpeedMultiplier` (intact parity 1.0). D2 closed
the pre-recorded SurvivorInspection retirement (zero-consumer re-proof → files
+ fixture deleted) and added the authoritative 5-row primary-wins dead-data
register (`docs/radio/DISTRESS_SIGNAL_DEAD_DATA_REGISTER.md`). Evidence:
`docs/plans/wave8_part2/{C2,D2}_*.md`.

**D3 shutdown cleanliness classified (2026-09-17):** the a11y-selftest
shutdown resource/RID warnings are a fixed, harness-scoped specimen-lifetime
signature (flat ×3; lifecycle selftest clean), documented in
`docs/ui/UI_NODE_DIAGNOSTICS_AND_LEAK_TRIAGE.md` §4. Evidence:
`docs/plans/wave8_part2/D3_*.md`.

**C3 endgame disposition signed (2026-09-17):** 0 PROMOTE · 1 RETIRE · 4 HOLD —
191 retired (inspection + barter appraisal live); 174/175/192/199 held with
measurable recheck conditions. Evidence: `docs/plans/wave8_part2/C3_*.md`.

**Wave 10 Part 1 A1 (corpus census) tranche 1 (2026-09-17):** package
`WAVE10-PART1-A1-CORPUS-CENSUS` — 131-file inventory, 73-edge DAG, 3
SEALED-ELSEWHERE + 6 evidence-backed anchors + 122 audit-pending; **39
filename-only baseline→closeout matches are numbering collisions, not seals**;
proposed chain head `C1[8]` (Plan 31B/31C). Evidence:
`docs/plans/UNCLAIMED_CORPUS_CENSUS.md`.

**Wave 10 Part 1 A2 (claim hygiene) (2026-09-17):** closed the stale ACTIVE
`C1-PLAN14-ECONOMY-CORE` claim (14A/14B + B1 presentation complete; TradeEmbargo
20/20, RegionalPriceAtlas 16/16, full suite green, data-integrity PASS) →
HANDED_OFF. Sweep: only `C1-PLAN24-SURVIVOR-LEDGER` remains legitimately ACTIVE
(24B/24C partial, two signatures pending); no active path overlap. Package
`WAVE10-PART1-A1-CORPUS-CENSUS`.

**Wave 10 Part 1 C1[8]/Plan 31B+31C (2026-09-17):** package
`WAVE10-PART1-C1-PLAN31` — navigable briefings (`BriefingRouteMap` + live-route
host guard) and replayable diagnostics (`DayRecord`/`DayRecordBuilder` + opt-in
`ASHFALL_DAY_RECORD=1` JSONL writer). Full suite 11,716/11,716. Evidence:
`docs/plans/wave10_part1/C1_PLAN31_IMPLEMENTATION_LOG.md`.

**Wave 10 Part 1 B1 entry gate (2026-09-17):** B1 (C1[6]/Plan 27) is
**DEPENDENCY-BLOCKED** on C2[8]/Plan 26A — bypass readers remain
(`EventsHostSession` reads `res://…/events.json` directly; several tools build
`Assets/StreamingAssets/Data` paths). Per B1 §5.2 the prerequisite may not be
waived because tests pass. Next executable: `C2[8]`/Plan 26A. Evidence:
`docs/plans/wave10_part1/B1_ENTRY_GATE.md`.

**Wave 10 Part 1 C2[8]/Plan 26A tranche 1 (2026-09-17):** package
`WAVE10-PART1-C2-PLAN26A` — extended `CatalogPath` (`ResolveCatalog`/
`ResolveSub`/validation/resolution-source), migrated `EventsHostSession` +
`Main.FactionBranch` + `SilentFoundryHostSession`, added a green shrinking
forbidden-path gate (40-file tranche-2 remainder). Full suite 11,718/11,718.
Evidence: `docs/plans/wave10_part1/C2_26A_IMPLEMENTATION_LOG.md`.

**Wave 10 Part 1 B1/Plan 27 (2026-09-17):** package
`WAVE10-PART1-B1-PLAN27` — Tests That Mean It: Fidelity, Coverage, and Runtime Evidence.
Executed across Tasks 27A (Fidelity), 27B (Coverage & Risk Gates), and 27C (Runtime Evidence & Journeys).
Enforced shipped data authority in inventory host sessions and selftests; explicit fixture policy
codified in `docs/testing/FIXTURE_POLICY.md`; 100% round-trip coverage gate on stateful Core systems;
100% determinism coverage gate on campaign day owners; golden save fixtures + digests pinned;
5 named campaign journeys codified in `docs/testing/JOURNEYS.md`; `coverage-gate.sh` integrated as
gate 48 in `CI_GATE_MANIFEST.json`. All 48 fast CI gates pass cleanly. Evidence:
`docs/plans/wave10_part1/B1_PLAN27_IMPLEMENTATION_LOG.md`.

**Wave 10 Part 1 A3 micro-deferral sweep (2026-09-17):** package
`WAVE10-PART1-A3-MICRO-DEFERRALS` — comprehensive sweep of 15 tracked recorded deferrals across
prior claims, closeouts, and debt ledgers. 7 ALREADY-RESOLVED struck with sealing evidence; 4
DECISION-NEEDED routed to foreman with recommendations (radio weather prediction authority,
water_sample_contaminated quirk, ward staffing, affliction recovery ramps; stopped at signature
per Rule 10); 1 CODE-GAP-LARGE promoted to queue (amputation equipment restrictions); 1
CODE-GAP-SMALL/DECIDED-DEFERRED (subject queries/snapshots); 2 STALE/RETIRED (presenter skills, phobia growth).
Evidence: `docs/plans/WAVE10_MICRO_DEFERRAL_SWEEP.md`.

**Wave 10 Part 1 B2/Plan 29 One Truth (2026-09-17):** package
`WAVE10-PART1-B2-PLAN29-ONE-TRUTH` — One Truth: Documentation, Canon, Roadmap Governance & Documentation Integrity.
29A rulebook sync verified across 13 clients; 29B machine-readable capability claims registry
`docs/architecture/CLAIMS.json` (24 claims across core subsystems) + CI claims verifier
`scripts/ci/verify-capability-claims.py` (all 24 verified; failure test catches invalid paths);
29C roadmap index `docs/roadmap/README.md` (flow of truth, <100 vs >=100 numbering, collision rules,
binding Definitions of Done for System, Content, UI, Build, Plan) + `docs/roadmap/WAVE_LEDGER.md`
(Waves 1–10 indexed). C1[7] marked SEALED in census. Evidence:
`docs/plans/wave10_part1/B2_PLAN29_IMPLEMENTATION_LOG.md`.


| Package | Owner | Exact paths | Acceptance | Focused verify |
|---|---|---|---|---|
| `DISTRESS-SIGNALS-9-12-WAVE1-STAGE-CONTRACT` | Builder (this session) | **Core:** `Assets/Ashfall.Core/Radio/DistressStageResolver.cs` (new), `Radio/RadioDistressSystem.cs` (additive `outcome_hint`), `Radio/RadioTuner.cs` + `Radio/RadioPropagation.cs` (resolver delegation, duplicated selection loops consolidated), `CatalogIntegrityValidator.cs` (`ValidateDistressSignalStages` — within-file dup ids / stage-day ordering / clarity monotonicity+range / text presence / hint shape; cross-file dup ids = documented primary-wins warning); **Tests:** `Ashfall.Core.Tests/Radio/DistressStageResolverTests.cs` (17 cases incl. legacy-parity oracle over all signals × days 0–60); **Docs:** baseline + `docs/radio/DISTRESS_SIGNAL_STAGE_CONTRACT.md` | **DONE 2026-09-13:** single pure stage resolver (`DistressStageResolver`) is the sole fragment-selection authority; `outcome_hint` additive DTO field bound from JSON; stage contract validated inside the permanent data-integrity gate (PASS, 5 documented primary-wins warnings, 0 errors); baseline frozen (build 0 err; Radio 249→266; selftest 325 PASS); scope correction 25→43 unique identities recorded; no stage persistence needed (purely derivable from absolute-day anchor — documented) | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/` (266/266); `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/DistressStageResolverTests.cs` (17/17); adjacent FactionRadioBroadcastExpansion 22/22, NpcArcData 12/12, NpcArcSystem 23/23; `godot --headless --path . -- --data-integrity-selftest` PASS; `dotnet build Ashfall.csproj` 0 errors (4 pre-existing CS8602 warnings from user's uncommitted SilentFoundryPanel.cs — outside claim) |
| `DISTRESS-SIGNALS-9-12-WAVE2-SIGNAL-TRUST` | Builder (this session) | **Core:** `Assets/Ashfall.Core/Radio/SignalTrustLedger.cs` (new: `SignalTrustPolicy` + ledger + `SignalTrustSaveEntry`), `Radio/SignalTrustAvailability.cs` (new: integer-permille bounded weighting), `Radio/DistressRescueMissionManager.cs` (optional ledger param + 4 exactly-once event hooks), `Radio/RadioDistressSystem.cs` (`ResolveMoralChoice` optional ledger param — explicit-ignore path), `Radio/RadioSave.cs` (V4→V5: `signalTrust` field, frozen `RadioSaveStateFrozenV4`, `MigrateV4`, neutral EnsureCollections fill); **Host:** `src/Host/RadioHostSession.cs` (session-owned `SignalTrust` ledger, default manager binding, capture/restore); **Tests:** `Ashfall.Core.Tests/Radio/SignalTrustTests.cs` (new, 21 cases), `RadioSaveMigrationTests.cs` (2 version-pin lines V4→V5); **Docs:** `docs/radio/SIGNAL_TRUST_CONTRACT.md` | **DONE 2026-09-13:** one authoritative radio-owned trust store (fallback path B per baseline PC-3 — FactionWarSystem is faction-keyed only, untouched); centralized integer delta policy (+2 answered / +5 rescue / −2 ignored / −5 trap), bounded [0,100] neutral 50; exactly-once per-signal ledgers (first event wins — explicit-ignore + deadline-expiry overlap cannot double-count); trap-class expiry excluded per authored "ignoring a lure is not a failure"; undiscovered signals never punished; late arrival counts answered only; availability modifiers bounded [500,1500]‰, monotonic, order-preserving, integer-only; no dynamic selection pool exists in the runtime (documented — pure API shipped, no invented scan mechanic); V4→V5 frozen-shape migration to explicit neutral | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/` (287/287); `SignalTrustTests` 21/21; `RadioSaveCodecTests` 6/6; `godot --headless --path . -- --data-integrity-selftest` PASS; `--panel-bind-lifecycle-selftest` PASS (session ctor wiring); `dotnet build Ashfall.csproj` 0 errors |

| `DISTRESS-SIGNALS-9-12-WAVE3-FOLLOWUP-CHAINING` | Builder (this session) | **Core:** `Assets/Ashfall.Core/Radio/DistressFollowUpScheduler.cs` (new: trigger grammar + definitions DTO + scheduler), `Radio/RadioDistressSystem.cs` (additive `follow_up_signals` list), `Radio/RadioSave.cs` (V5→V6 + frozen V5 shape), `CatalogIntegrityValidator.cs` (follow-up validation); **Host:** `src/Host/RadioHostSession.cs` (scheduler ownership, event binding, day-aware tick, capture/restore, fired-event line); **Tests:** `Ashfall.Core.Tests/Radio/DistressFollowUpTests.cs` (new, 19 cases), `SignalTrustTests.cs` (version pin), `RadioSaveMigrationTests.cs` (V6 pin); **Docs:** `docs/radio/DISTRESS_SIGNAL_FOLLOWUP_CONTRACT.md` | **DONE 2026-09-13:** additive `follow_up_signals` schema with closed validated trigger grammar (answered / rescue_success / rescue_failed / expired / trap_fallen_for — every value maps to one exactly-once mission transition); self-contained payloads so chains/cycles are structurally impossible (documented; catalog-backed refs would require cycle validation before authoring); campaign-day scheduling with host-set CurrentDay (lifecycle events carry no day); exactly-once pending + fired ledgers (ClaimedReceipts pattern); deterministic same-day ordinal ordering; ignored/trap suppression + trap-aftermath legitimacy proven; V5→V6 frozen-shape migration to empty default; validator errors name catalog/signal/path/id/field/value/rule; no authored follow-up content yet (mechanism-first per user's Wave 1 option-c decision — content tranche deferred) | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/` (306/306); `DistressFollowUpTests` 19/19; `RadioSaveCodecTests` 6/6; `--data-integrity-selftest` PASS; `--panel-bind-lifecycle-selftest` PASS; `dotnet build Ashfall.csproj` 0 errors |
| `DISTRESS-SIGNALS-9-12-WAVE4-AUDIO-PROJECTION` | Builder (this session) | **Core:** `Assets/Ashfall.Core/Radio/DistressAudioCueResolver.cs` (new), `Radio/RadioDistressSystem.cs` (additive `audio_cue` on definition + fragment), `Radio/DistressFollowUpScheduler.cs` (additive `audio_cue` on follow-up), `CatalogIntegrityValidator.cs` (structural cue-id rule); **Host:** `src/Host/RadioHostSession.cs` (Intercept-gated playback + persisted playedBroadcastKeys dedupe `distress:{id}:{cue}` + follow-up fire cue); **Tests:** `Ashfall.Core.Tests/Radio/DistressAudioCueTests.cs` (new, 10 cases); **Docs:** `docs/radio/DISTRESS_SIGNAL_AUDIO_CONTRACT.md` | **DONE 2026-09-13:** pure cue resolver (stage override → signal default → text-only fallback, explicit IDs, zero RNG); playback ONLY on the Intercept detection edge — undiscovered signals are silent, catalog load/eligibility/save-restore never play; dedupe reuses the existing persisted playedBroadcastKeys ledger (zero new save fields; no replay after reload, stage-cue changes play); follow-up transmissions resolve their own cue exactly once; structural validation in the integrity gate + documented semantic-resolution fallback policy (registry is host-side; missing cue logs once, text continues, never crashes); no audio→gameplay edge exists by construction | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/` (316/316); `DistressAudioCueTests` 10/10; `--data-integrity-selftest` PASS; `--audio-selftest` PASS; `--panel-bind-lifecycle-selftest` PASS; `dotnet build Ashfall.csproj` 0 errors 0 warnings |
| `DISTRESS-SIGNALS-9-12-WAVE5-INTEGRATION-CLOSEOUT` | Builder (this session) | **Tests:** `Ashfall.Core.Tests/Radio/DistressSignalTasks912ReplayTests.cs` (new, 5 cases — plan §13 scenarios A–D + fingerprint); **Docs:** `docs/radio/DISTRESS_SIGNAL_TASKS_9_12_SAVE_CONTRACT.md`, `docs/radio/DISTRESS_SIGNAL_TASKS_9_12_TEST_MATRIX.md`, `docs/radio/DISTRESS_SIGNAL_TASKS_9_12_CLOSEOUT.md`; **Governance:** INTEGRATION_PLANS.md, WORKTREE_OWNERSHIP.md | **DONE 2026-09-13:** unified deterministic replay harness over the complete lifecycle (detect → stages → answer → rescue → follow-ups → trust → audio cue) through the REAL V6 save codec; Scenario A exact-trace assertions (stage indices, hint timing, follow-up fire days 6/10, trust 57, cue transitions); Scenario B continuous==interrupted byte-for-byte incl. save-payload fingerprint; Scenario C ignore path differs exactly as authored (trust 48, zero follow-ups); Scenario D trap penalty + genuine follow-up suppression + identity immutable; full-lifecycle fingerprint stable across runs; full suite 11,170/11,170 (dedicated Wave 5 window); content-utilization PASS 0 orphans | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/` (321/321); `dotnet test Ashfall.Core.Tests` (11,170/11,170); `--data-integrity-selftest` PASS; `--content-utilization-selftest` PASS; `dotnet build Ashfall.csproj` 0 errors 0 warnings |
| `CF-P1-DISTRESS-CONTENT-SEAL` | Foreman / integrator (user-authorized) | **Core:** `Assets/Ashfall.Core/CatalogIntegrityValidator.cs` (D1–D3); **Data:** `Assets/StreamingAssets/Data/radio_distress_signals.json` (three invalid follow-up removals); **Tests:** `Ashfall.Core.Tests/Radio/DistressFollowUpTests.cs`, `Ashfall.Core.Tests/Radio/DistressFollowUpPopulationReplayTests.cs`; **Host:** `src/Audio/AudioSelfTest.cs` (distress cue cross-reference); **Docs:** `docs/radio/DISTRESS_SIGNAL_PR3_CLOSEOUT.md`; **Governance:** this row and `WORKTREE_OWNERSHIP.md` claim | **SEALED 2026-09-19:** D1 `distress_followup_expired_requires_consequence`, D2 `distress_followup_trap_only_on_lures`, D3 `distress_followup_max_two`; 18 validator fixture cases + V-19 real-catalog pin; post-remediation population 24 signals / 40 entries / 15 answered / 16 rescue / 7 ambush / 2 expired; V6 replay 46/46; stable two-run fingerprint; audio selftest 645/645 with 7 distress cue IDs; data-integrity 0 errors + 5 pinned warnings; content-utilization CI/deep-chain gates PASS; no scheduler/save/trust/resolver changes | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/DistressFollowUpTests.cs` (39/39); `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/DistressFollowUpPopulationReplayTests.cs` (46/46); `dotnet build Ashfall.csproj --no-restore`; `--data-integrity-selftest`; `--audio-selftest`; `--content-utilization-selftest` |
| `CF-P5-RESTOCK-RECONCILE` | Foreman / integrator (user-authorized) | **Governance only:** `INTEGRATION_PLANS.md`, `docs/governance/DECISION_REGISTER.md`, `docs/plans/wave9_part2/C1_DECISION.md`; `KNOWN_DEBT.md` intentionally untouched | **DONE 2026-09-19:** DEC-05 Option C ratified across the queue ledger, decision register, and source memo; stale deferral and 14/14 evidence corrected to live 6/6 truth; production, data, and tests unchanged; `KNOWN_DEBT.md` has no restock row | `bash scripts/run_test.sh Ashfall.Core.Tests/Economy/Plan147RestockPriorityTests.cs` (6/6); binding/call-site grep; stale-string closure; `python3 scripts/ci/generate-docs-index.py --check` |
| `CF-P6-VEHICLE-ARMOR-GRADES` | Foreman / integrator (user-authorized) | **Data:** `Assets/StreamingAssets/Data/vehicle_armor_grades.json`; **Core:** `Assets/Ashfall.Core/Expeditions/VehicleArmorGradeCatalog.cs`, `VehicleGarageSystem.cs`, `CatalogIntegrityValidator.cs`, `ContentUtilizationScanner.cs`; **Host/UI:** `src/Main.Plans50_53.cs`, `src/Main.ExpansionHub.cs`, `src/Host/HostCli.VehicleGarage.cs`, `src/UI/VehicleGaragePanel.cs`; **Tests:** `Ashfall.Core.Tests/Expeditions/Plan213VehicleArmorGradeTests.cs`; **CI/docs:** `docs/ci/SELFTEST_MANIFEST.json`, `scripts/ci/generate-architecture-map.py`, generated architecture/catalog/docs indexes; **Governance:** this row, `WORKTREE_OWNERSHIP.md`, and `docs/plans/CF_P6_VEHICLE_ARMOR_GRADES_INTEGRATION_PLAN.md` | **DONE 2026-09-19:** corrected the stale D6 premise using the live `VehicleGarageSystem` owner; added one neutral stock row plus four data-driven grades; Core install/reforge, foundry provenance stamp, bounded breakdown mitigation, sacrificial chassis wear, terrain/material gates, depletion, legacy-neutral additive persistence, deterministic soak, truthful garage-panel commands, catalog validator/scanner/architecture registration; existing vehicle garage seam remains the sole authority and no new save section/RNG/combat/radiation path was added | `bash scripts/run_test.sh Ashfall.Core.Tests/Expeditions/Plan213VehicleArmorGradeTests.cs` (21/21); `VehicleGarageSystemTests.cs`; `Plan50VehicleGarageIntegrationTests.cs`; `Plans50_53_SharedIntegrationTests.cs`; `--vehicle-garage-selftest` (27/27); `--data-integrity-selftest`; `--content-utilization-selftest`; `--catalog-boot-preflight`; `--panel-bind-lifecycle-selftest`; architecture/catalog/docs generators `--check`; `dotnet build Ashfall.csproj --no-restore` |
| `CF-P28-ONE-BOOTSTRAP-PATH` | Foreman / integrator (user-authorized) | **Host:** `src/Main.CampaignServices.cs`, `src/Main.Lifecycle.cs`; **Selftests:** `src/Main.UiTests.CompositionRoot.cs`, `src/Main.UiTests.RealCampaignJourney.cs`; **Tests:** `Ashfall.Core.Tests/Tooling/BootstrapPathParityGateTests.cs`; **Plan:** `docs/plans/CF_P28_ONE_BOOTSTRAP_PATH_INTEGRATION_PLAN.md`; **Governance:** census/ledger routed through the existing wave11-part2 integrator claim | **INTEGRATED 2026-09-19 WITH EXTERNAL GATE LIMITATION:** fresh-path manifest bootstrap and reset enrollment shipped; static parity 6/6; `SubsystemManifestTests` 7/7; black-market wiring 3/3; vehicle integration 5/5; build 0/0; 7-day 10/10; real journey PASS; vehicle 27/27; sky defense 17/17; save/load 8/8. The composition-root selftest is blocked before final P28 assertions by the pre-existing `EconomyDetailPanel.tscn` `%Content` binding defect; triad remains 6/7 on unrelated `SetupDifficulty`. |
| `UNBLOCK-01..05-AND-PARTIAL-PLANS` | Integrator (user-authorized) | **Core:** `Assets/Ashfall.Core/Inventory/ItemTypes.cs`, `Assets/Ashfall.Core/Inventory/ItemDefinitions.cs`, `Assets/Ashfall.Core/Inventory/ItemCatalogLoader.cs`, `Assets/Ashfall.Core/Inventory/Inventory.cs`, `Assets/Ashfall.Core/Inventory/EquipLimbGate.cs`, `Assets/Ashfall.Core/Campaign/DayEventVocabulary.cs`, `Assets/Ashfall.Core/Campaign/DailyBriefingReportBuilder.cs`; **Data:** `Assets/StreamingAssets/Data/items.json`; **Host:** `src/Main.Plans126_129.cs`, `src/Main.Lifecycle.cs`; **Tests:** `Ashfall.Core.Tests/Inventory/EquipLimbGateTests.cs`, `Ashfall.Core.Tests/Campaign/DayEventVocabularyTests.cs`; **Governance:** `docs/governance/DECISION_REGISTER.md`, `KNOWN_DEBT.md`, `INTEGRATION_PLANS.md`, `WORKTREE_OWNERSHIP.md` | **DONE 2026-09-20:** Integrated UNBLOCK-01..05 and 3 partial plans: (1) `XP-06` / `DEBT-AMPUTATION-EQUIPMENT-RESTRICTION` Body Integrity & Equipment Gating (`EquipSlot.ProstheticHand`/`ProstheticLeg`, `LimbRequirement`/`LimbProvision`, pure functional `EquipLimbGate` simple vs full grip, `Inventory.Equip` overload, `EquipLimbGateTests` 8/8 PASS); (2) `CF-P3` / `D11` Semantic Kind Authority (`SectionTitleMap`/`SectionTitleFor`, `DailyBriefingReportBuilder` non-heartbeat semantic routing + generic fallback, `DayEventVocabularyTests` 8/8 PASS); (3) `D19c`/`D3`/`D13`/`D21` Truth Repair (`items.json` water_sample_contaminated non-equipable per D3 Option A, `Main.Plans126_129.cs` header truth per D13, `Main.Lifecycle.cs` reset nulling `_silentFoundry`/`_sharedSkillProgression`/`_sharedFactionStance` per D19c, `DECISION_REGISTER.md` DEC-01, DEC-03, DEC-09, DEC-16, DEC-21..DEC-25 signed). | `bash scripts/run_test.sh Ashfall.Core.Tests/Inventory/EquipLimbGateTests.cs` (8/8); `bash scripts/run_test.sh Ashfall.Core.Tests/Campaign/DayEventVocabularyTests.cs` (8/8); `dotnet build Ashfall.csproj --no-restore` |
| `UNBLOCK-PROGRAM-WAVE2-PLANS` | Integrator (user-authorized) | **Core:** `Assets/Ashfall.Core/Economy/FundsLedger.cs`, `Assets/Ashfall.Core/World/WastelandMapCatalogLoader.cs`, `Assets/Ashfall.Core/World/WastelandMapSystem.cs`; **Host:** `src/Host/RadioHostSession.cs`, `src/Host/PanelBindLifecycleSelfTest.cs`, `src/Main.Plans147.cs`; **Tests:** `Ashfall.Core.Tests/Economy/FundsLedgerTests.cs`, `Ashfall.Core.Tests/World/WastelandMapRouteTagTests.cs`; **Governance:** `docs/governance/DECISION_REGISTER.md`, `INTEGRATION_PLANS.md`, `WORKTREE_OWNERSHIP.md` | **DONE 2026-09-20:** Integrated 3 additional unblocking plans: (1) `XP-04` / `UNBLOCK-02` Funds Authority (canonical integer chits `FundsLedger` in Core, TryDebit/TryCredit, 128-entry bounded log, save/restore envelope, `FundsLedgerTests` 8/8 PASS); (2) `UNBLOCK-03` / `D19b` Radio Observation Station Identity Seam (`ActiveStationId` / `SetActiveStation` in `RadioHostSession`, `station_alpha` default preserved, verified by Gate 15 in `--panel-bind-lifecycle-selftest`); (3) `UNBLOCK-04` / `D16` & `D19a` Wasteland Route Tags & Barter Determinism (`tags`, `HasTag`, `IsFlooded`, `IsAmphibious` on `MapRouteDef` and `MapRoute`, `WastelandMapSystem` route tag queries, `Main.Plans147.cs` barter RNG determinism contract, `WastelandMapRouteTagTests` 3/3 PASS). | `bash scripts/run_test.sh Ashfall.Core.Tests/Economy/FundsLedgerTests.cs` (8/8); `bash scripts/run_test.sh Ashfall.Core.Tests/World/WastelandMapRouteTagTests.cs` (3/3); `dotnet build Ashfall.csproj --no-restore`; `--panel-bind-lifecycle-selftest` (17/17 PASS) |

## Next waves (planned, not yet claimed)

- **Wave 8 B2 player-route panels — COMPLETE (2026-09-17):** `WAVE8-B2-PLAYER-ROUTES` — all three settled systems are now player-routed and all mapped rows are ✅ 6/6. (1) `sky_defense_battery`: Expanded `SkyDefenseBatteryPanel` over the campaign-owned `SkyDefenseBatterySystem`; `--sky-defense-selftest` 17/17. (2) `dynamic_quests`: read-only Emergency Dynamic Quests board over the host-driven `DynamicQuestlineSystem`. (3) `vehicle_garage`: the signed Plan 50 decoration seam was completed (mod effects decorate the expedition profile, trip distance feeds component wear, immobilized vehicles are refused dispatch, recovery advances over campaign days) before `VehicleGaragePanel`; `--vehicle-garage-selftest` 19/19. Premise correction + evidence: `docs/plans/wave8_part2/B2_PANEL_WAVE.md`. Claim: `claim-wave8-b2-player-routes-2026-09-17`.

- **C1 Plan 22 one food authority — DONE 2026-09-15:** Opening Continuity Wave 2 (*The Bunker Machine*) (`docs/plans/C1_planintegration[4].md`). 22A one direct consumption authority (subject-aware host seam `Consume(survivorId, itemId, scale)`, authored effects to Needs/Radiation/Contamination, inventory UI wiring, Holdfast terminal delegation, 0 hardcoded hunger arithmetic, 0 hardcoded edible arrays, shelter inventory authority, FeedAllCrew preflight); 22B kitchen & crew table (`ServeMeal` UI wiring, "Serve All" preflight, cellar/refrigeration preservation, meal quality > raw cans, cook duty check, ration conflict grievance hook, OnMealServed/OnPortionsSpoiled day events); 22C medicine treatment decisions (authoritative treatment log in `DoseLedgerSystem`, affliction preflight, dependency hooks, medical timeline UI). Verification: focused tests 7+6+4+6=23 PASS, DayEventParitySourceGateTests 2/2 PASS, KitchenNutritionSystemTests 11/11 PASS, full verify-fast suite 47/47 PASS cleanly.
- **C1 Plan 19 ending continuity & repository truth — DONE 2026-09-15:** Closing Continuity Wave 1 (`docs/plans/C1_planintegration[3].md`). 19A ending projection (`EpilogueContextInputs` → `EpilogueContextFactory.Build`, zero hardcoded booleans, single host projector in `Main.Endgame.cs`, 32/32 branches reachable from campaign state, memorial death ledger authority); 19B cohort & generational legacy (3 scoped links: child rations via data fraction, schooling/apprenticeship capacity + duty roster work eligibility gate, ending fact; memorial circumstance variance; demo literal removed; 3-year deterministic balance simulation); 19C repository truth (14 dangling sidecars removed, UID sidecar gate 47 passed, session continuity journey test passed, anti-all-true regression guard passed, master docs index synchronized). Verification: `Ashfall.Core.Tests/Endgame/` 84/84 PASS, focused 19A/19B/19C suites (22 + 23 + 2 = 47 PASS), compiler warnings 0 across all 3 targets, `verify-fast.sh` ALL 47 GATES PASSED cleanly.
- **C2[2] Plan 17 legibility (executed 2026-09-15, bounded):** 17A-S no-silent-drop repair (Core `DayEventVocabulary` + builder default case + 8 tests), parity matrix (`docs/campaign/EVENT_SEMANTIC_PARITY_MATRIX.md`, gate-enforced), owner-failure briefing visibility, radiation exposure-end lifecycle (Core events + geiger loop wiring in AudioEventBridge, 7 tests), guidance F2 toggle (`ashfall_guidance` — F1 collides with `ashfall_help`, documented). Full classification of executed/stale/deferred phases + evidence: `docs/plans/C2_PLANINTEGRATION_2_CLOSURE_REPORT.md`. Plan doc: `docs/plans/C2_planintegration[2].md`. NOT executed (stale per recon): original 17A mute-owner work (erratum honored), 17B route/visibility (already built), most 17C audio phases (already built). NOT executed (real gaps, need packages): Plan 31 semantic-kind authority, 17C Phase I alert ducking/concurrency, 17C Phase E acquisition sweep, 17B deep test matrix.
- **PR 2 → PR 3 content seal** — **PR 2 DONE 2026-09-15; PR 3 SEALED 2026-09-19:** outcome_hint text remains sealed for all 43 JSON-backed signals. Follow-up payload and authored audio-cue content landed before this package and are now sealed by `CF-P1-DISTRESS-CONTENT-SEAL`; evidence: `docs/radio/DISTRESS_SIGNAL_PR3_CLOSEOUT.md`. The deferred list no longer includes those content classes.
- **Availability consumer — RETIRED (Wave 9 Part 2, Option B Approved):** no dynamic distress-signal selection pool exists in the analog dial radio runtime; `SignalTrustAvailability` is retained as a tested pure math specification pin; runtime consumer integration is formally closed and retired without runtime code alterations (documented in `docs/plans/wave9_part2/C2_DECISION.md` and `docs/radio/SIGNAL_TRUST_CONTRACT.md` §5).

## Previous batch — RESCUE-SIGNAL-RUNTIME (INTEGRATED, presented for acceptance)

**Status:** INTEGRATED — rescue-signal runtime flagship (user-authorized, Tasks 1–4 + four continuation waves) implemented end-to-end 2026-09-13, plus follow-up packages: runtime state/persistence (V4 radio save + mission fingerprint) → authenticity detection (skill-driven, dedicated RNG sub-stream) → ignore consequences (sender_death / faction_standing_loss / faction_ambush, exactly-once) → sender survival (arrival-day resolution, salvage branch) → dispatch-association rules (§5.8) → presentation (RESCUE SIGNALS strip) → two content tranches (7 new scenarios, 12 missions) → debt closure (CLI help contract, CvdDiamond determinism, prose pin) → `FOLLOWUPS-210-213-THINSEAMS` (sanitation power-grid feed, foundry forging buttons, market-rumor band bridge; merchant-restock priority sealed 2026-09-17 by DEC-05 (Option C — deterministic display-order priority, live in ShelterBarterSystem); black-market settlement/action surface subsequently sealed by `WAVE8-PART2-C1-BLACK-MARKET-ACTIONS`) → **`PLANS-122-125-SECOND-TOOL-REVIEW`**: plan §21 cross-tool review EXECUTED (`docs/PLANS_122_125_SECOND_TOOL_REVIEW.md`, verdict PASSED — battery re-run all green with two documented count drifts, architecture DoD audited at source level, all five closeouts updated). Full authority map with premise corrections: `docs/radio/RESCUE_SIGNAL_RUNTIME_AUTHORITY_MAP.md`. Verification: build 0/0; focused suites green (Radio 249/249, Shelter 555/555, Foundry 73/73, Economy 118/118); data-integrity PASS 325/325; content-utilization PASS; panel lifecycle PASS; ui-a11y PASS; triad 6/6; **full suite 11,098/11,098 — fully green**. Presenting to foreman for ACCEPTANCE.

| Package | Owner | Exact paths | Acceptance | Focused verify |
|---|---|---|---|---|
| `RESCUE-SIGNAL-RUNTIME-TASKS-1-4` (all waves) | Builder (this session) | **Core:** `Assets/Ashfall.Core/Radio/DistressRescueMissionManager.cs`, `SignalAuthenticityEvaluator.cs` (new), `RescueDispatchPreflight.cs` (new), `RadioSave.cs`, `Shelter/CvdDiamondSynthesisEngine.cs` (NextBatchId); **Host:** `src/Host/RadioHostSession.cs`, `CvdDiamondHostSession.cs`, `src/Main.Narrative.cs` (one subscription), `src/Main.Expeditions.cs` (bridge rules), `src/Host/HostCli.cs` (help only), `src/UI/RadioPanel.cs` (strip), `src/UI/CvdDiamondPanel.cs` (one line); **Data:** `radio_distress_signals_expansion.json`, `moral_choice_quests_distress.json`, `questline_master.json`; **Tests:** 8 new/extended files under `Ashfall.Core.Tests/Radio/` (71 cases), `HostCliHelpContractTests` (retargeted), `CrossingEndingsWorldHistoryTests` (one pin), `RadioDistressLayerContractTests`, `RadioSaveMigrationTests`, `DistressRescueMissionTests` (count pins); **Governance:** this file, WORKTREE_OWNERSHIP.md, `docs/radio/RESCUE_SIGNAL_RUNTIME_AUTHORITY_MAP.md` | **DONE:** flagship Tasks 1–4 complete per plan §22 DoD; §5.8 association sealed; §24 expansion checklist fully covered; V4 save migration + codec-validated mission fingerprint; three pre-existing debts sealed with documented reasons (test pins retargeted to current truth, determinism violation fixed in source) | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/` (249/249); `godot --headless --path . -- --data-integrity-selftest` (325/325); `dotnet test Ashfall.Core.Tests` (11,089/11,089) |
| `FOLLOWUPS-210-213-THINSEAMS` (economy-batch deferred items, complementary) | Builder (this session) | **Core:** `Assets/Ashfall.Core/Shelter/SanitationSystem.cs` (additive `RoomPowerProvider` + facility-tick feed), `Assets/Ashfall.Core/Economy/EconomyMarketRumorRules.cs` (new), `Assets/Ashfall.Core/Radio/FactionRadioTypes.cs` (additive `MarketRumor=6`); **Host:** `src/Main.Sanitation.cs` (grid feed via `PowerGridSystem.IsRoomPowered`), `src/Foundry/SilentFoundryHostSession.cs` (forging command adapters), `src/UI/SilentFoundryPanel.cs` (FORGING PASS strip: BEGIN / heat·shape·finish·inspect / COMPLETE, textual feedback), `src/Host/RadioHostSession.cs` (`RecordMarketRumor`), `src/Main.Economy.cs` (shock→rumor relay); **Tests:** `Plan210SanitationPowerFeedTests.cs` (3 cases), `Plan212MarketRumorRulesTests.cs` (6 cases) | **DONE 2026-09-13:** three unambiguous deferred items from the PLANS-210-213 ledger sealed — (1) sanitation facilities live-feed grid availability per tick (provider unset → legacy byte-identical); (2) forging pass is player-operable on the foundry panel through the Core-owned deterministic pass; (3) trade rumors from real market state: shock start/expiry events become one restrained band item via Core-projected text (deterministic, no invented prices, restore never replays). **Follow-up truth (2026-09-17):** black-market trade actions are IMPLEMENTED under signed immediate canonical-inventory settlement; see `docs/plans/wave8_part2/C1_HANDOFF.md`. **Sealed (DEC-05, signed 2026-09-17):** merchant restock priority is live — `ShelterBarterSystem.ComputeItemPriorityScore` (pure: authored price_multiplier_bp + optional PriorityScorer seam) consumed by `GetPrioritizedStock` on the arrival restock (`:319`) and by `ShelterBarterPanel` (`:680`); Option C = ordering only, quantities/inclusion unchanged; `Plan147RestockPriorityTests` 6/6 PASS (re-verified 2026-09-19). | `bash scripts/run_test.sh Ashfall.Core.Tests/Shelter/Plan210SanitationPowerFeedTests.cs` (3/3); `Plan212MarketRumorRulesTests.cs` (6/6); C1 settlement 18/18 · panel contracts 3/3 · Economy 184/184 · panel lifecycle 17/17 · a11y 5/5 · black-market snapshot MATCH |

## Previous batch — PLANS-174-177-FLAGSHIP-SURVIVOR-WORLD (INTEGRATED, pending acceptance)
**Premise:** Flagship Plans 174–177 authorized directly by user (foreman) 2026-09-13. Wave A reconnaissance authority map produced from current source evidence BEFORE implementation (FalloutSystem IS the moving-rad authority; AmputationSystem IS the limb/bionic authority; WildlifeEcosystemSystem owns species+taming; RadiationSystem owns dose). Plan 176 landed first as the world-hazard foundation so 174/175/177 could consume it; no duplicate authority was created anywhere.

| Package | Owner | Exact paths | Acceptance | Focused verify |
|---|---|---|---|---|
| `PLAN-176-ANOMALY-HAZARD-CORE+HOST` | Builder (this session) | `Assets/StreamingAssets/Data/anomalies.json`; `Assets/Ashfall.Core/World/AnomalyHazardCatalog.cs`; `Assets/Ashfall.Core/World/AnomalyHazardSystem.cs`; `src/Host/AnomalyHazardSaveStore.cs`; `src/Main.Anomaly.cs`; save registry/stream rows; `Ashfall.Core.Tests/World/Plan176AnomalyHazardTests.cs` | **DONE 2026-09-13:** deterministic moving-hazard authority (spawn cap 4, day-keyed wandered storm fronts, wind-drift bands, 0.01km-quantized positions, capped additive radiation 600, effect-tag union, bounded wildlife modifier ±1.0, Undetected/Signature/Classified detection, one-shot approach warnings, loot-site single resolution); host wire (wind feed, journal, save). 19/19. | `bash scripts/run_test.sh Ashfall.Core.Tests/World/Plan176AnomalyHazardTests.cs` (19/19); data-integrity PASS |
| `PLAN-176-CROSS-SYSTEM-CONSUMERS` | Builder (this session) | `Assets/Ashfall.Core/Radiation/ExposureEnvironment.cs` (additive AnomalyRadRate + provider); `Assets/Ashfall.Core/World/WildlifeEcosystemSystem.cs` (additive sectorHazardModifiers + typed avoidance event); `src/Main.Survivors.cs` (provider bind); `src/Main.Plans162_165.cs` (wildlife modifiers + journal); `Ashfall.Core.Tests/World/Plan176CrossSystemConsumerTests.cs` | **DONE 2026-09-13:** expedition dose via typed ExposureEnvironment handoff (RadiationSystem sole writer; legacy null-provider path byte-identical); detection capability from canonical devices; loot-site host command via scavenging authority; wildlife avoidance migrations through the ONE migration authority. 8/8; adjacent suites green. | `bash scripts/run_test.sh Ashfall.Core.Tests/World/Plan176CrossSystemConsumerTests.cs` (8/8); exposure suites 6/6+1/1 |
| `PLAN-174-COMPANION-CORE+HOST` | Builder (this session) | `Assets/StreamingAssets/Data/companion_animals.json`; `wildlife_ecosystem.json` (+species_ash_hound); `Assets/Ashfall.Core/Ecology/CompanionAnimalSystem.cs`; `src/Host/CompanionSaveStore.cs`; `src/Main.Companion.cs`; `src/Host/ExpeditionHostSession.cs` (additive PackCapacityProvider); `src/Main.Plans162_165.cs` (tame→adopt; guard→raid); `Ashfall.Core.Tests/World/Plan174CompanionAnimalTests.cs`; `Plan174CompanionHostWiringTests.cs` | **DONE 2026-09-13:** persistent companions (identity = wildlife animal_id), canonical food port (preferred→fallback→partial), deterministic bond/training, bounded guard/pack/morale queries routed into DefenseSystem/ExpeditionSystem/NeedsSystem, bounded grief once, veterinary treat command, old-save adoption of existing tamed animals only. 16/16 + 6/6. | `bash scripts/run_test.sh Ashfall.Core.Tests/World/Plan174CompanionAnimalTests.cs` (16/16); `Plan174CompanionHostWiringTests.cs` (6/6) |
| `PLAN-177-BIONICS-CORE+HOST` | Builder (this session) | `Assets/StreamingAssets/Data/bionics.json`; `Assets/Ashfall.Core/Medical/BionicsSystem.cs`; `AmputationSystem.cs` (additive revert + charge flag); `src/Host/BionicsSaveStore.cs`; `src/Main.Bionics.cs`; `Ashfall.Core.Tests/Medical/Plan177BionicsTests.cs`; `Plan177BionicsHostWiringTests.cs` | **DONE 2026-09-13:** surgery through the limb authority (no double charge), integration/rehab (never instant), bounded capability (+200bp cap), maintenance/decay/typed malfunctions, grid-gated charger, typed electrical disruption (passive immune, never health damage), combat-damage command, Plan 176 electrostatic contract live. 14/14 + 4/4. | `bash scripts/run_test.sh Ashfall.Core.Tests/Medical/Plan177BionicsTests.cs` (14/14); `Plan177BionicsHostWiringTests.cs` (4/4) |
| `PLAN-175-ZEALOTRY-CORE+HOST` | Builder (this session) | `Assets/StreamingAssets/Data/wasteland_religions.json`; `Assets/Ashfall.Core/Survivors/ZealotrySystem.cs`; `src/Host/ZealotrySaveStore.cs`; `src/Main.Zealotry.cs`; `Ashfall.Core.Tests/Survivors/ZealotrySystemTests.cs`; `Plan175ZealotryHostWiringTests.cs` | **DONE 2026-09-13:** fictional-only guard enforced in loader; deterministic conversion with real resistance; bounded buff queries (morale authority applies); transactional ritual cadence through canonical inventory; escalation ladder hard-stops at typed AssaultThreat (host routes violence); PsyOps broadcast capped; crisis→bounded morale damage route; friction-belief old-save adoption (deterministic adopt-index RNG, no GetHashCode). 20/20 + 5/5. | `bash scripts/run_test.sh Ashfall.Core.Tests/Survivors/ZealotrySystemTests.cs` (20/20); `Plan175ZealotryHostWiringTests.cs` (5/5) |
| `PLANS-174-177-UNIFIED-REPLAY` | Builder (this session) | `Ashfall.Core.Tests/Plans174To177CampaignIntegrationTests.cs` | **DONE 2026-09-13 (Wave F):** 30-day deterministic campaign over all four systems — companion upkeep → conversion/fervor → moving storm track + route dose + loot resolution → limb loss → bionic surgery → rehab/maintenance → morale consequences; fingerprints cover every authoritative field (invariant culture); `30 continuous == 11 days → save/restore → 19 days` field-by-field + SHA-256 equality (no rerolls); §14 milestones (1 conversion, 1 warning, loot resolve once/duplicate rejected, storm expiry, no instant recovery, bounded bonuses); grief-once test. 4/4. | `bash scripts/run_test.sh Ashfall.Core.Tests/Plans174To177CampaignIntegrationTests.cs` (4/4) |

**Sequencing:** Wave B (176) → consumers → Wave C (174) → Wave D (177) → Wave E (175) → Wave F unified replay. Host/save followed Core per package; presenting UI (KennelUI / BeliefsPanel / hazard map / CyberneticsUI) is the next presentation wave per repo cadence.

## Previous batch — PLANS-210-213-FLAGSHIP-ECONOMY (ACCEPTED)

**Status:** ACCEPTED — all nine waves complete (1 foundation → 9 CI closure); batch closed 2026-09-13
**Final verification:** build 0/0 · flagship scenarios 6/6 (incl. continuous==mid-reload parity) · focused suites: sanitation 501/501 (Shelter dir), economy 112/112, Foundry 73/73, medical 325/325 · full-suite 10,728/10,733 (5 pre-existing failures from earlier batches reported to foreman, outside claimed paths) · data-integrity PASS (321 catalogs) · content-utilization PASS · bridge PASS · panel lifecycle PASS · ui-a11y 5/5 PASS (237 UI files) · PanelRouteGate 20/20 · triad 7/7 · filename registry 4/4 · RNG source gate 2/2 · map 186 --check · save-store matrix 187 --check · catalog registry 605 --check · docs index 2088 --check. Section gates 184→186 (sanitation, black_market). Closeouts: docs/shelter/PLAN_210_SANITATION_CLOSEOUT.md, docs/economy/PLAN_211_BLACK_MARKET_CLOSEOUT.md, docs/economy/PLAN_212_DYNAMIC_ECONOMY_CLOSEOUT.md, docs/crafting/PLAN_213_METALLURGY_RECONCILIATION_CLOSEOUT.md. Known follow-ups: power-grid feed for sanitation facilities; interactive forging buttons; BlackMarketUI trade actions panel polish; pre-existing CLI help/prose/determinism findings (reported).
**Premise:** Flagship Plans 210–213 (waste & sanitation, black market & syndicates, dynamic economy & scarcity, metallurgy reconciliation) authorized by user (foreman) 2026-09-13. Canonical numbering 210–213 adopted because the flagship text's 170–173 collide with the sealed historical corpus (see `docs/foreman/PLAN_170_199_FORENSIC_AUDIT.md` + `docs/foreman/PLANS_210_213_FLAGSHIP_ECONOMY_AUTHORITY_MAP.md` §1). Wave 1 verified: no SanitationSystem existed (greenfield); `MarketSystem` IS the dynamic-economy authority (extend, never fork); `FactionBountySystem` + `LedgerDebtSystem` + `SilentFoundrySystem` partials already own bounties/debt/metallurgy seams. D6 vehicle armor was correctly deferred on 2026-09-13 when no vehicle owner existed; current evidence now shows the Plan 50 `VehicleGarageSystem` owner is live, and CF-P6 extends that owner without creating a parallel authority.

| Package | Owner | Exact paths | Acceptance | Focused verify |
|---|---|---|---|---|
| `PLANS-210-213-FOUNDATION` | Builder (this session) | `docs/foreman/PLANS_210_213_FLAGSHIP_ECONOMY_AUTHORITY_MAP.md`; `docs/INDEX.md` (generator); INTEGRATION_PLANS.md; WORKTREE_OWNERSHIP.md | **Wave 1 DONE 2026-09-13:** authority map written from current source evidence (§2 authorities with file:line refs; §3 day-owner tick ordering; §5 save strategy + legacy defaults; §6 event ids; §7 RNG streams; §8 integrator-only shared paths). Premise correction recorded: user-doc numbers 170–173 collide with sealed historical plans → canonical 210–213. §4 defaults approved by foreman: D1 ids 210–213; D2 room-level waste; D3 scoped UnderworldLedger; D4 real syndicate faction ids; D5 extend MarketSystem in place (v2 additive); D6 vehicle armor deferred; D7 compost fertilizer id. | `python3 scripts/ci/generate-docs-index.py --check`; `dotnet build Ashfall.csproj` |
| `PLANS-212-DYNAMIC-ECONOMY-CORE` | Builder (this session) | `Assets/StreamingAssets/Data/commodity_baselines.json`; `Assets/Ashfall.Core/Economy/CommodityBaselineCatalog.cs`; `Assets/Ashfall.Core/Economy/MarketSystem.cs` (additive v1→v2); `Ashfall.Core.Tests/Economy/Plan212CommodityBaselineCatalogTests.cs`; `Ashfall.Core.Tests/Economy/Plan212DynamicEconomyTests.cs` | **DONE 2026-09-13 (Wave 2 core):** `commodity_baselines.json` — 12 authored rows (all known goods categories; elasticity class low/medium/high; scarcity bounds 200–5000bp enforced, base within own bounds). `CommodityBaselineCatalog.cs` — strict GoodsCatalogLoader-pattern loader (schema envelope, snake_case, nullable DTOs, collected errors). MarketSystem additive v1→v2: `CategoryIndexEntry`/`TradePressureEntry`/`MarketShockState`; `BindCommodityCatalog`; `ApplyShock` (idempotent per category+kind+source, severity bp clamped [500,3000], duration [1,60], `OnShockStarted`/`OnShockExpired`); trade pressure recorded on Buy/Sell/Barter (buy raises index, sell lowers; 0.75/day decay; ±3500bp cap; scales 0.4/0.8/1.6 by elasticity class); daily tick = prune expired shocks → decay pressure → bounded-lerp (α=0.25) indices toward formulaic target → legacy volatility walk unchanged; `ExplainPrice` emits typed Category/Shock factor rows between Demand and clamps (NO rows when catalog unbound or multiplier 1.0 — v1 path byte-identical); `GetCategoryMultiplier` clamps to authored baseline bounds; Capture/Restore v2 (sorted, clamped, first-wins dedupe; v1 save → neutral; newer → throws). `PriceFactorKind` additive Category=3, Shock=4. Focused: catalog 8/8; engine 18/18. Adjacent regression: PriceExplanation 3/3, Plan56 closeout/followup/phase3/phase5 10/10, DeterminismSeedSweep 199/199. Build 0/0. Data-integrity selftest PASS 318 catalogs (317→318, new file walks clean); catalog registry regenerated 602/602; content-utilization PASS; docs index 2084. Merchant stock response + weather-event wiring + rumors deferred to the Wave 2 host/save package. **Phase 2 host/save DONE 2026-09-13:** `EconomyHostSession.Create` binds `commodity_baselines.json` (missing/invalid → legacy v1 path, never hard-fail) and surfaces shock start/expiry in the session event line. Weather→shock mapping extracted to Core as `EconomyWeatherShockRules` (blizzard=1500bp×3d, storm-class=1000bp×2d, calm=none; pure function of `WeatherSeverityCalculator`); host `Main.TickEconomyWeatherBridge` is a thin adapter called from `EconomyMarketDayOwner` before the index update + `market_shocks_active` day event. Wiring tests 6/6 (policy bands, bounded/distinct, catalog-through-market, single economy save section — additive v2, no new section). Final: build 0/0; focused 8/8 + 18/18 + 6/6; data-integrity 318/318; bridge selftest PASS. Deferred (separate content package): trade rumors from real market state (merchant restock priority sealed by DEC-05 / CF-P5). | `bash scripts/run_test.sh Ashfall.Core.Tests/Economy/Plan212CommodityBaselineCatalogTests.cs` (8/8); `bash scripts/run_test.sh Ashfall.Core.Tests/Economy/Plan212DynamicEconomyTests.cs` (18/18); `bash scripts/run_test.sh Ashfall.Core.Tests/Economy/Plan212EconomyHostWiringTests.cs` (6/6); `godot --headless --path . -- --data-integrity-selftest` (318/318); `godot --headless --path . -- --bridge-selftest` PASS |
| `PLANS-210-SANITATION-CORE` | Builder (this session) | `Assets/StreamingAssets/Data/sanitation_facilities.json`; `Assets/Ashfall.Core/Shelter/SanitationFacilityCatalog.cs`; `Assets/Ashfall.Core/Shelter/SanitationSystem.cs`; `Ashfall.Core.Tests/Shelter/Plan210SanitationFacilityCatalogTests.cs`; `Ashfall.Core.Tests/Shelter/Plan210SanitationSystemTests.cs` | **DONE 2026-09-13 (Wave 3 core):** `sanitation_facilities.json` — 9 authored facilities (basic/sealed latrine, wash station, air scrubber, refuse sorting, incinerator, compost unit, chemical locker, shielded waste store) with closed waste-type + room-tag vocabularies (canonical shelter_rooms.json tags verified), ranges enforced; compost output = existing `item_compost_humus` (D7 resolved — live consumer in FarmingPanel/crop_strains, zero new items). `SanitationFacilityCatalog.cs` — strict loader (schema envelope, snake_case, collected errors). `SanitationSystem.cs` — engine-free Core authority: room-level waste state (D2), population-scaled organic accumulation, producer-only chemical/radioactive emission API, facility processing (power/condition-gated; storage facilities contain into own capacity, compost converts to batches), cleaning duty with priority scaling, derived hygiene (never stored; spill-capped), bounded pathogen exposure modifier [1.0,2.0] for the DiseaseSystem seam, deterministic overcapacity-only spills, complaint threshold+cooldown (never-complained bug caught by test), compost queue with typed OnCompostReady, tick-exactly-once guard, versioned Capture/Restore (legacy → clean baseline, never hazardous). Focused: catalog 8/8; engine 20/20. Shelter regression 491/491; build 0/0; data-integrity 319/319 (new file walks clean); catalog registry 603; content-utilization PASS. Host/save DONE 2026-09-13: `sanitation` save section + `sanitation_save.json` registry row; `SanitationSaveStore` (SaveStoreHub.FromCodec + SchemaVersionedEnvelope, newest-batch pattern); `SanitationHostSession` (facility catalog bind, room registration, compost delivery event line); `Main.Sanitation.cs` (SetupSanitation: idempotent room registration mapped from canonical shelter_rooms.json tags — residential→Residential, canteen/nutrition→FoodPrep, heavy_industrial/crafting/power→Industrial, medical/triage/surgery/quarantine→Medical; OnCompostReady→`Inventory.AddById(item_compost_humus)`; SaveSanitation/FlushSanitationIfDirty); SaveOrchestrator setup/save calls added beside economy; `HygieneDayOwner` registered phase 3 (ownerId `hygiene` sorts before `medical_disease`) feeding living-roster population, persisting state, emitting `sanitation_ticked`/`sanitation_spill` day events. Section-count gate 184→185; architecture map regenerated 185 subsystems --check PASS; save-store matrix 186 stores. Wiring tests 2/2; triad gate 7/7; filename registry 4/4; ComprehensiveSave+Sanitation 1142/1142; shelter 493/493; build 0/0; data-integrity PASS. Deferred to Wave 6: power-grid powered/staffed feed for facilities, disease-exposure modifier wiring into TryExpose call sites, SanitationUI panel. | `bash scripts/run_test.sh Ashfall.Core.Tests/Shelter/Plan210SanitationFacilityCatalogTests.cs` (8/8); `bash scripts/run_test.sh Ashfall.Core.Tests/Shelter/Plan210SanitationSystemTests.cs` (20/20); `godot --headless --path . -- --data-integrity-selftest` (319/319) |
| `PLANS-211-BLACK-MARKET-CORE` | Builder (this session) | `Assets/StreamingAssets/Data/black_market_inventory.json`; `Assets/StreamingAssets/Data/faction_lore.json` (2 syndicate lore rows); `Assets/Ashfall.Core/Economy/BlackMarketInventoryCatalog.cs`; `Assets/Ashfall.Core/Economy/BlackMarketSystem.cs`; `Ashfall.Core.Tests/Economy/Plan211BlackMarketTests.cs` | **DONE 2026-09-13 (Wave 4 core):** 3 syndicates (faction_wasteland_outlaws — canonical; faction_ash_market_brokers + faction_cold_ledger — new ids authored in faction_lore.json per D4, data-integrity resolver clean), 7 illicit stock entries (canonical economy_goods item ids; risk premiums ≥ the broker 1.25× band; access tiers 1–3). `BlackMarketInventoryCatalog` strict loader (unknown-syndicate references rejected as unreachable stock). `BlackMarketSystem` engine-free Core: campaign-gated contact discovery (idempotent); daily-persistent stock snapshots (seeded per-day generation, same-day reopen NEVER rerolls — R4 guard tested); pricing = canonical MarketSystem value × risk premium × scarcity response × bounded trust discount (−20% at trust 100), hard floor never below canonical+25% (arbitrage guard R6 tested); atomic Buy/Sell with access-tier + stock preflight; loans (one active per syndicate, credit-limit gated) with idempotent overdue events (fired-key ledger), trust −30/heat +25, bounty escalation DELEGATED to the canonical FactionBountySystem (no second bounty ledger); heat decay daily; versioned Capture/Restore (legacy → undiscovered/zero debt/empty stock; newer → throws). Focused 19/19; economy regression 109/109; Factions 51/51; PatrolBountyHandoff 7/7; build 0/0; data-integrity PASS (320 catalogs); catalog registry 604; content-utilization PASS. Host/save DONE 2026-09-13: `black_market` save section + `black_market_save.json` (owner economy); `BlackMarketSaveStore`; `BlackMarketHostSession` (catalog + canonical market + owned canonical FactionBountySystem binding; bounty persistence sealed additively — FactionBountySystemState rides `BlackMarketState.factionBounties` (§171.18) until a dedicated store exists, capture/restore wired both ways); `Main.BlackMarket.cs` setup/save; SaveOrchestrator calls beside sanitation; `UnderworldMarketDayOwner` phase 4 ownerId `underworld_market` (sorts after `debt_ledger` — map §3 corrected), stock refresh via `CampaignRngManager.Fork(BlackMarketStock, day)` fork-per-day + debt/heat tick, emits `underworld_ticked` day events; 3 underworld RNG streams added (stock/bounty/debt_event, snake_case-gated). Section-count gate 185→186; architecture map regenerated 186 subsystems --check PASS; save-store matrix 187 stores. Wiring tests 3/3; RNG source gate 2/2; triad 7/7; filename registry 4/4; BlackMarket+ComprehensiveSave 1141/1141; build 0/0; data-integrity PASS. Deferred to Wave 7: BlackMarketUI panel, bounty-hunter door-encounter content, patrol contraband-inspection hooks (encounter-authority content).  | `bash scripts/run_test.sh Ashfall.Core.Tests/Economy/Plan211BlackMarketTests.cs` (19/19); `godot --headless --path . -- --data-integrity-selftest` PASS |
| `PLANS-213-METALLURGY-RECONCILIATION` | Builder (this session) | `Assets/StreamingAssets/Data/alloys_and_ores.json`; `Assets/Ashfall.Core/Foundry/MaterialProfileCatalog.cs`; `Assets/Ashfall.Core/Foundry/SilentFoundrySystem.Material.cs` (partial); `Assets/Ashfall.Core/Foundry/SilentFoundryTypes.cs` (additive); `Assets/Ashfall.Core/Foundry/SilentFoundrySystem.Heat.cs` (provenance hook); `Assets/Ashfall.Core/Foundry/SilentFoundrySystem.TreatyLabor.cs` (restore fix); `Ashfall.Core.Tests/Foundry/Plan213MetallurgyReconciliationTests.cs` | **DONE 2026-09-13 (Wave 5 core):** reconciliation per authority map §2.4 — `SilentFoundrySystem` partial extension, NO new metallurgy system/queue/heat-sim/save-store (reflection gate + registry gate enforce it; pre-existing B66 + Plans 130-133 powder authority sanctioned). `alloys_and_ores.json` = 6 material PROFILES only (properties + authored forging sequences; recipes stay solely in metallurgy_recipes.json — asserted). `SilentFoundrySystem.Material.cs`: purity ladder Poor/Standard/High/Exceptional derived from the standard completion path (quality − bounded contamination 12%/slag 10% penalties, never a real percentage); deterministic forging abstraction (BeginForging on latest provenance-bearing batch → closed-command sequence heat/shape/finish/inspect → bounded integer score = sequence fidelity × purity factor 850–1200, clamped [100,1300], ZERO RNG — headless-testable); provenance stamps (purity/materialProfileId/craftQualityPermille) ride `FoundryProductionRecord` additively (old saves = Standard/unknown, NEVER recalculated); `TryGetLatestMaterialQuality` = read-only equipment/vehicle handoff query (D6 — vehicle armor deferred, no mutation). REAL BUG SEALED: `RestoreState` silently dropped the B66 metallurgy fields (activeMetallurgyRecipeId/slag/batches) on restore — now restored additively with legacy-neutral defaults, plus the activeForging session resumes mid-sequence exactly. Focused 11/11; Foundry regression 73/73; build 0/0; data-integrity PASS (321 catalogs); catalog registry 605; content-utilization PASS; architecture map 186 --check. Deferred to Wave 7: forging/minigame UI surface on the foundry panel; `OnSmeltingComplete` host event forwarder. | `bash scripts/run_test.sh Ashfall.Core.Tests/Foundry/Plan213MetallurgyReconciliationTests.cs` (11/11); `godot --headless --path . -- --data-integrity-selftest` PASS |
| `PLANS-WAVE6-CROSS-PLAN-INTEGRATIONS` | Builder (this session) | `Assets/Ashfall.Core/Shelter/SanitationConsequenceRules.cs`; `src/Main.CampaignOwners.cs` (HygieneDayOwner extension — shared root, sole active builder); `Ashfall.Core.Tests/Shelter/Plan210_212CrossPlanIntegrationTests.cs` | **DONE 2026-09-13 (Wave 6):** three cross-plan consequences wired as Core policy + thin host adapter in `HygieneDayOwner`: (1) sanitation→disease — bounded daily cholera sweep through the AUTHORED `foul_water_draw` source (previously unconsumed) feeding `DiseaseSystem.TryExpose` with `GetPathogenExposureModifier` (bounded [1,2]); infection ownership stays with the disease authority — test proves the modifier measurably changes seeded outcomes; (2) sanitation→morale — reversible Hazardous mark via the canonical `MoraleMarkSystem` (set on collapse, CLEARED on recovery to Acceptable — a temporary mess never becomes permanent damage); (3) sanitation→economy — Squalid/Hazardous shelter or active spill applies a bounded 1000bp×2d medical shortage shock via `ApplyShock("sanitation_crisis")` (idempotent refresh). Structurally-present links verified by test: economy→black market (the SAME shock raises both canonical and illicit prices, one pipeline, black market stays strictly above canonical); metallurgy→economy supply response via the existing trade-pressure model (selling production lowers the category index — no direct price mutation at batch completion); compost→fertilizer→farming was already live (Wave 3 delivery). Focused 8/8; shelter 501/501; economy 112/112; medical 325/325; build 0/0; data-integrity PASS; bridge selftest PASS. Deferred to Wave 7/8: ticker/panel presentation; flagship scenarios A–F harness. | `bash scripts/run_test.sh Ashfall.Core.Tests/Shelter/Plan210_212CrossPlanIntegrationTests.cs` (8/8) |
| `PLANS-WAVE7-UI-PRESENTATION` | Builder (this session) | `src/UI/SanitationPanel.cs`; `src/UI/BlackMarketPanel.cs`; `src/Economy/EconomyMarketPanel.cs` (ticker strip); `src/UI/SilentFoundryPanel.cs` (purity/forging cards); `src/Foundry/SilentFoundryHostSession.cs` (OnSmeltingComplete-class forwarder); `Assets/Ashfall.Core/UI/PanelRegistryBootstrap.cs`; `src/Main.PlayerSurfaces.cs`; `src/Main.ExpandedShelterSystems.cs`; `src/Main.GameFlow.cs`; `src/Main.Sanitation.cs`/`src/Main.BlackMarket.cs` (panel ensure/open); `Assets/Ashfall.Core/Foundry/SilentFoundrySystem.Material.cs` (Any-overload) | **DONE 2026-09-13 (Wave 7):** two new Expanded panels registered through the full route battery (registry row + PlayerSurfaces expandedIds + OpenExpandedPanel switch + GameFlow + ensure/open methods): `SanitationPanel` (hygiene band + spill + room waste ledger + compost queue; severity as text/labels, never color-only) and `BlackMarketPanel` (contacts/stock with canonical-derived buy+sell prices/debt ledger with EXPLICIT due dates/heat+trust; hidden stock weights and RNG never shown; NO restock button — reopen must never reroll, R4 honored). Economy ticker: commodity-trend strip on EconomyMarketPanel (per-category ▲rising/▼falling/—stable + numeric multiplier + active shock lines with severity and expiry day — arrows AND words, not color-only; canonical values only, no presentation-side recomputation). Metallurgy UI: purity + forging status cards on the SilentFoundryPanel status rail (purity ladder text, forging session progress); `TryGetLatestMaterialQualityAny` Core overload; OnSmeltingComplete-class semantic payload forwarder (product, amount, tier, purity) + OnForgingCompleted forwarder in the foundry host session. Gates: build 0/0; PanelRouteGateTests 20/20 (switch↔registry parity); PlayerSurfaceCoverageGateTests 8/8; panel-bind-lifecycle 16/16 PASS; ui-accessibility 5/5 PASS (237 UI files); data-integrity PASS; architecture map 186 --check; UI panel catalog regenerated in-sync. Deferred to Wave 8: interactive forging minigame buttons remain Core-command-driven (headless-complete); ticker animation polish. | `godot --headless --path . -- --panel-bind-lifecycle-selftest` PASS; `bash scripts/run_test.sh Ashfall.Core.Tests/UI/PanelRouteGateTests.cs` (20/20) |
| `PLANS-WAVE8-DETERMINISTIC-REPLAY` | Builder (this session) | `Ashfall.Core.Tests/FlagshipEconomyScenarioTests.cs`; `Ashfall.Core.Tests/EconomySystemTests.cs` (v1→v2 contract); `Ashfall.Core.Tests/VersionReportContractTests.cs` (stale count pins 168→180 codecs, 174→186 sections) | **DONE 2026-09-13 (Wave 8):** flagship scenarios A–F as deterministic Core harnesses (6/6 green): A sanitation collapse → hygiene falls → crisis shock raises medical prices → cleaning/facilities recover → index normalizes; B underworld debt spiral → overdue fires EXACTLY ONCE → canonical bounty placed → save/restore → no duplicate events; C winter shock → food price rises (demand pinned to isolate the shock from the legitimate volatility walk) → bounded under the authored ceiling; D metallurgy supply → selling production softens the materials index → black-market premium persists; E toxic spill → deterministic overcapacity trigger → capture/restore exact (spill not recreated) → full brigade resolves; F combined 20-day campaign (sanitation+market+black market+morale marks+loan) — continuous run == mid-reload-at-day-10 replay, field-by-field fingerprint equality (SHA-style composite: room waste, category indices, ledger, debts, fired-event keys, morale mark). Full-suite sweep 10,728/10,733 — fixed 3 stale contracts (MarketSystem v2 stamp; VersionReport counts were already stale before the wave), reported 5 PRE-EXISTING failures outside claimed paths to the foreman (see handoff). | `bash scripts/run_test.sh Ashfall.Core.Tests/FlagshipEconomyScenarioTests.cs` (6/6); `dotnet test Ashfall.Core.Tests` 10,728/10,733 |

**Sequencing:** Wave 2 economy core → Wave 3 sanitation core → Wave 4 black market → Wave 5 metallurgy reconciliation → Wave 6 cross-plan → Wave 7 UI → Wave 8 replay/scenarios → Wave 9 CI closure. Host/save wiring follows Core per package (Plans 122–125 phased pattern).

## Operating rules

- A new batch is created only after this batch is `ACCEPTED` or `BLOCKED`.
- Reference existing plans by path and ID; do not copy their prose here.
- A batch may cite 9-20 plans but must expose no more than three concurrent
  packages with disjoint ownership.
- Every package needs premise evidence, exact paths, acceptance, and focused
  verification before a builder edits.
- Completed batch rows are moved to a short archive table in this file; full
  historical plans remain where they are until separately archived.

## Recent closed batches

| Batch | Closed | Outcome | Evidence |
|---|---|---|---|
| `BATCH-2026-09-13-PLANS-122-125-LATE-TECH-MOBILITY` | 2026-09-13 | Plans 122–125 (SOFC power, sound ranging, CVD diamond, amphibious draisine) implemented end-to-end (Core+catalog+host/save/panel+CLI selftests+75-day harness+balance soaks). Full detail preserved in the 12-phase acceptance cell archived with the closeout docs. One open item: plan §21 second-tool review pending foreman. | `dotnet build Ashfall.csproj` 0/0; batch xUnit 1,261 cases 0 failures; ComprehensiveSave 1106/1106 (184 sections); data-integrity 317/317; `--plans-122-125-selftest` 41/41; 75-day harness 23/23; balance soaks 24/24; panel lifecycle 16/16; a11y PASS; scene lint 30/0; fast tier 46/46 PASS |
| `BATCH-2026-09-12-PLANS-139-141-EXECUTION` | 2026-09-12 | Plans 139 (InSAR), 140 (hydraulic extrusion), 141 (run-flat) implemented end-to-end (Core+data+host/save+panel). Also sealed two partial integrations: Plan 138 panel reachability and architecture-map generator drift (5 missing sections + 3 dead fixtures). Map now 180/180. | `Plan139InSarDeformationTests` 17/17; `Plan140HydraulicExtrusionTests` 14/14; `Plan141RunFlatTireTests` 18/18; host wiring 3/3 each; `generate-architecture-map.sh --check`; panel + a11y selftests PASS; `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-DEBT-170-199-REMAINING-MAPS` | 2026-09-12 | Ten remaining PARTIAL family maps ACCEPTED (no parallel systems). Index: `docs/foreman/PLAN_170_199_REMAINING_FAMILY_MAPS.md`. | `python3 scripts/ci/generate-docs-index.py --check` |
| `BATCH-2026-09-12-DEBT-189-WATER-SOURCE-MAP` | 2026-09-12 | Plan 189 map ACCEPTED: no WaterSourceSystem; piezometer source IDs + WT intake gate + Fluid pipes remain owners. | `docs/water/PLAN_189_WATER_SOURCE_AUTHORITY_MAP.md` ACCEPTED; `python3 scripts/ci/generate-docs-index.py --check` |
| `BATCH-2026-09-12-DEBT-TEST-QUARANTINE-HOLDFAST-NPC` | 2026-09-12 | Reinstated `HoldfastNpcCatalogTests`; rematched stale `HoldfastCatalog.Npcs` to `HoldfastNpcCatalogLoader` owner; dropped Compile Remove. | `HoldfastNpcCatalogTests` 3/3 |
| `BATCH-2026-09-12-DEBT-TEST-QUARANTINE-ARCHIVEINKS` | 2026-09-12 | Reinstated `ArchiveInksCatalogTests`; `InkMaterialDefinition` JsonPropertyName for snake_case JSON; dropped Compile Remove. | `ArchiveInksCatalogTests` 12/12; `ArchiveDeskSystemTests` 10/10 |
| `BATCH-2026-09-12-DEBT-TEST-QUARANTINE-UTILITYAI` | 2026-09-12 | Reinstated `UtilityAiExpandedCatalogTests` (stale 20-vs-6 note); dropped its Compile Remove; other quarantine removes untouched. | `bash scripts/run_test.sh Ashfall.Core.Tests/UtilityAiExpandedCatalogTests.cs` (14/14) |
| `BATCH-2026-09-12-DEBT-184-A11Y-COLORBLIND` | 2026-09-12 | Path β: `colorblind_mode` under UserSettings*; Core `ColorblindColorMapper`; `ToColor` maps Critical/Success per mode; SettingsPanel OptionButton; Theme constants unchanged. | `Ashfall.Core.Tests/Settings/` 30/30; `--settings-selftest` PASS (incl. colorblind asserts); `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-DEBT-184-A11Y-BRIDGE` | 2026-09-12 | Path α: `AccessibilityPresentation` MotionAllowed/scale; dose/radiation refresh on Apply; settings-selftest asserts four-flag effects. | `UserSettingsRecoveryTests` 14/14; `--settings-selftest` PASS; `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-DEBT-184-A11Y-MAP` | 2026-09-12 | Plan 184 expanded a11y authority map ACCEPTED: sole UserSettings*; Plan 80 floors; colorblind IN after Path α; remap/AT/audio-desc/cognitive OUT. Docs-only. | `docs/ui/PLAN_184_EXPANDED_ACCESSIBILITY_AUTHORITY_MAP.md` ACCEPTED; `git diff --check` |
| `BATCH-2026-09-12-DEBT-173-RADIO-PANEL` | 2026-09-12 | Phase 3: equipment/prep-cost StartPrep gates + RadioPanel PROGRAM PRODUCTION strip (start/cancel/LastEvent) + bind sites; map UI cite. | `Plan173RadioProgramProductionTests` 10/10; host wiring 1/1; `ArchitectureTestMapGateTests` 5/5; `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-ARCH-TEST-MAP-GATE` | 2026-09-12 | Restored `bio_fermentation` map coverage; retargeted nuclear/crawler fixture cites; marked surgical_ward test GAP (retired fixture). Doc-only. | `bash scripts/run_test.sh Ashfall.Core.Tests/Tooling/ArchitectureTestMapGateTests.cs` (5/5) |
| `BATCH-2026-09-12-DEBT-173-RADIO-HOST` | 2026-09-12 | Phase 2 host/save: session + `radio_program_production` section + daily tick + catalog load + PsyOps StartCampaign + schedule Resolve delivery + content-utilization claim. No RadioPanel. | `Plan173RadioProgramProductionTests` 7/7; `Plan173RadioProgramProductionHostWiringTests` 1/1; `MainTriadDriftGateTests` 7/7; `CampaignEnvelopeBuilderTests` 10/10; `PersistentFilenameRegistryGateTests` 4/4; `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-DEBT-173-RADIO-PRODUCTION` | 2026-09-12 | Phase 1 Core: `RadioProgramProductionSystem` + `radio_programs.json` consume SlotId / ScheduledBroadcastResult / StartCampaign; Capture/Restore; no UI. | `bash scripts/run_test.sh Ashfall.Core.Tests/Radio/Plan173RadioProgramProductionTests.cs` (7/7) |
| `BATCH-2026-09-12-DEBT-173-RADIO-ADAPTER` | 2026-09-12 | Signed Plan 173 radio adapter map: `RadioProgramSlot.SlotId` bind; `ScheduledBroadcastResult` delivery; `PsyOps.StartCampaign` propaganda input; dedicated production save scope; no second schedule/receiver. | `docs/radio/PLAN_173_RADIO_PROGRAM_ADAPTER_MAP.md` ACCEPTED |
| `BATCH-2026-09-12-DEBT-196-FOOD-SEAM` | 2026-09-12 | Sealed Plan 196 type/temp seam: `AddCohort` food-type gate + host-fed `room_storage_bay` °C decay; Kitchen prepared-meal-only untouched. | `bash scripts/run_test.sh Ashfall.Core.Tests/Shelter/Plan196FoodTypeTempSeamTests.cs` (8/8); `FoodPreservationSystemTests` 8/8; BioFermentation 20/20; `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-DEBT-196-FOOD-BOUNDARY` | 2026-09-12 | Signed Plan 196 ownership map: FoodPreservation sole stored cohorts; Kitchen prepared meals only; food-type + host thermal seams named; §3 defaults approved. | `docs/shelter/PLAN_196_FOOD_SPOILAGE_AUTHORITY_MAP.md` ACCEPTED |
| `BATCH-2026-09-12-DEBT-166-169-RELOAD-REPLAY` | 2026-09-12 | Sealed continuous-versus-mid-reload equality for Research, Espionage (+fired set), Fluid, ProceduralNarrative+QuestRuntime with day-keyed campaign RNG; adjacent WT/greenhouse/caravan spot-checks. | `bash scripts/run_test.sh Ashfall.Core.Tests/Plan166_169ReloadReplayTests.cs` (2/2) |
| `BATCH-2026-09-12-DEBT-167-168-CLOSEOUT` | 2026-09-12 | Sealed DEBT-167 (EspionageConsequenceRouter → Caravan supply disruption / PsyOps jamming / FactionWar defense pressure; fired-set save) and DEBT-168 (default fluid topology; WT→Fluid transfer; Greenhouse/Disease delivery applicator; host daily cadence). | `bash scripts/run_test.sh Ashfall.Core.Tests/Plan167EspionageConsequenceRoutingTests.cs` (4/4); `bash scripts/run_test.sh Ashfall.Core.Tests/Plan168WaterDeliveryTests.cs` (5/5); `dotnet build Ashfall.csproj` 0/0 |
| `BATCH-2026-09-12-PARTIAL-IMPLEMENTATION-REBASE` | 2026-09-12 | Rebased the historical partial portfolio on direct owner/save/UI evidence. Removed already-live Plan 187 from the queue; found Plan 184 has persisted but inactive preferences; separated Plan 173 and 196 into explicit interface-boundary work rather than speculative feature work. | `docs/foreman/PARTIAL_IMPLEMENTATION_REBASE.md`; `python3 scripts/ci/generate-docs-index.py --check`; `git diff --check` |
| `BATCH-2026-09-12-PLAN-170-199-FORENSICS` | 2026-09-12 | Audited 30 historical plans by actual owner/API/data/host/test evidence. Classified live equivalents, partial adjacent systems, unstarted work, and blocked proposals; recorded three promotion candidates without authorizing speculative implementation. | `docs/foreman/PLAN_170_199_FORENSIC_AUDIT.md`; `python3 scripts/ci/generate-docs-index.py --check`; `git diff --check` |
| `BATCH-2026-09-12-INTEGRATION-HARDENING` | 2026-09-12 | Exposed the Core-owned Plan 167 sabotage action through its host boundary, added one typed-intent contract test, and moved 134 ignored .NET cache files (52 MiB) into Twin with verified hashes. Deferred 166–169 work is promotion-gated in `KNOWN_DEBT.md`. | `bash scripts/run_test.sh Ashfall.Core.Tests/Plan167EspionageTests.cs` (7/7); `dotnet build Ashfall.csproj --no-restore --nologo`; 134/134 SHA-256 checks; triad gate (7/7) |
| `BATCH-2026-09-12-FOREMAN` | 2026-09-12 | Installed five governance authorities, compacted and synchronized 13 client rulebooks, preserved 14 pre-rollout files with hashes, and replaced same-file racing with ownership. No production code changed. | `python3 scripts/ci/sync-agent-rulebooks.py --check`; archive `SHA256SUMS`; `bash -n scripts/run_test.sh` |

### Archived batch detail — Plans 138–141 (closed 2026-09-12)

Wave A reconnaissance complete: `docs/plans/PLANS_138_141_WAVE_A_RECONNAISSANCE.md`
(verified live authorities; several plan-text names corrected to real owners).
Implementation follows the proven phased pattern (Plan 173/196 precedent):
Core+catalog+tests → host/save → panel.

| Package | Owner | Exact paths | Acceptance | Focused verify |
|---|---|---|---|---|
| `PLANS-138-138-METROLOGY-PHASE1` | Builder (this session) | `Assets/StreamingAssets/Data/low_background_lead_catalog.json`; `Assets/Ashfall.Core/Radiation/LowBackgroundLeadEngine.cs`; `Ashfall.Core.Tests/Radiation/Plan138LowBackgroundLeadEngineTests.cs` | **DONE 2026-09-12:** 4-profile provenance catalog (very_low/low/ordinary, all residual >0bp — no zero-activity claims); effective-background composition clamped ≥0; mass-weighted cross-contamination downgrade (material conserved); assay detection-limit + below-limit indeterminate band; calibration/skill/duration bounded confidence (0.05..0.97); seeded-RNG deterministic; Capture/Restore round-trip + old-save baseline. Focused 16/16; Radiation suites 117/117; build 0/0. **Phase 2 DONE 2026-09-12:** host session + `low_background_metrology_save.json` save store; `SaveSectionRegistry` row (owner `radiation`); SaveOrchestrator setup+save wiring; forked RNG stream `low_background_metrology` (Fork pattern — cannot shift other streams); environmental background via `EnvironmentBackgroundBpProvider` seam (authored default until the Phase 3 ExposureContext bridge); no day-tick needed (assay is command-driven). Wiring tests 3/3; triad gate 7/7; filename-registry gate 4/4; Plan 173 wiring 1/1; Phase 1 16/16; build 0/0. **Phase 3 DONE 2026-09-12:** `LowBackgroundLeadPanel` (state→confidence→blocker→consequence; honest uncertainty language — below-limit ≠ clean); sample truth wired from water-treatment intake (`incomingContaminationLevel`, never invented by UI); live environmental bridge via `GetLastExposureEnvironment` (shelter-interior EffectiveZoneRadLevel → bp, authored-baseline fallback); panel route `low_background_metrology` in expanded-panel switch + GameFlow; ARCHITECTURE_TEST_MAP Plan 138 row. Arch-map gate 5/5; scene lint 30/0; combined gates 30/30; build 0/0. Plan 138 COMPLETE — next wave: Plan 139 (InSAR). | `bash scripts/run_test.sh Ashfall.Core.Tests/Radiation/Plan138LowBackgroundLeadEngineTests.cs` (16/16); `dotnet build Ashfall.csproj` 0/0 |
| `PLANS-139-INSAR` | Builder (this session) | `Assets/Ashfall.Core/World/InSarDeformationEngine.cs`; `Assets/StreamingAssets/Data/insar_geodesy_catalog.json`; `src/Host/InSarMappingHostSession.cs`; `src/Host/InSarMappingSaveStore.cs`; `src/Main.InSarMapping.cs`; `src/UI/InSarMappingPanel.cs`; `docs/world/PLAN_139_INSAR_INTERFEROMETRY_CLOSEOUT.md` | **DONE 2026-09-12:** repeat-pass required; geometry-incompatible passes refused; stable/slow/accelerating/abrupt/low-confidence classes; weather + terrain decorrelation; skill-bounded confidence; excavation/travel projections; save/load; deterministic. Core 17/17; host wiring 3/3; map `--check` 180/180; panel + a11y selftests PASS; build 0/0. | `bash scripts/run_test.sh Ashfall.Core.Tests/World/Plan139InSarDeformationTests.cs` (17/17) |
| `PLANS-140-EXTRUSION` | Builder (this session) | `Assets/Ashfall.Core/Foundry/HydraulicExtrusionEngine.cs`; `Assets/StreamingAssets/Data/hydraulic_extrusion_catalog.json`; `src/Host/HydraulicExtrusionHostSession.cs`; `src/Host/HydraulicExtrusionSaveStore.cs`; `src/Main.HydraulicExtrusion.cs`; `src/UI/HydraulicExtrusionPanel.cs`; `docs/shelter/PLAN_140_HYDRAULIC_EXTRUSION_CLOSEOUT.md` | **DONE 2026-09-12:** power/water gating; abstract phases; deterministic defect + tool wear; rejected/utility/high_pressure/premium; downstream reliability bounded <100%; save/load. Core 14/14; host wiring 3/3; map `--check` 180/180; panel + a11y selftests PASS; build 0/0. | `bash scripts/run_test.sh Ashfall.Core.Tests/Foundry/Plan140HydraulicExtrusionTests.cs` (14/14) |
| `PLANS-141-RUNFLAT` | Builder (this session) | `Assets/Ashfall.Core/Expeditions/RunFlatTireEngine.cs`; `Assets/StreamingAssets/Data/runflat_tire_catalog.json`; `items.json` (5 install-kit items); `src/Host/RunFlatTireHostSession.cs`; `src/Host/RunFlatTireSaveStore.cs`; `src/Main.RunFlatTire.cs`; `src/UI/RunFlatTirePanel.cs`; `docs/expeditions/PLAN_141_RUNFLAT_TIRE_CLOSEOUT.md` | **DONE 2026-09-12:** compatible-only install; workshop + real inventory kit gate/consume; hazard reduction never immunity; severe can destroy wheel/rim; deterministic heat with load/ambient; fuel penalty; bounded repair; save/load. Core 18/18; host wiring 3/3; map `--check` 180/180; panel + a11y selftests PASS; build 0/0. | `bash scripts/run_test.sh Ashfall.Core.Tests/Expeditions/Plan141RunFlatTireTests.cs` (18/18) |

**Batch outcome:** Plans 138–141 complete. Three partial integrations discovered during the sweep were also sealed: Plan 138's `low_background_metrology` panel was unreachable (missing descriptor/open action); the architecture-map generator graph was missing five registry sections and cited three dead fixtures; both are now fixed with regression gates. A consolidated CLI self-test `--plans-139-141-selftest` (18/18) plus panel/a11y headless gates verify the runtime path; map now 180/180 with the three new systems at ✅ 6/6. No successor batch is open.

**Sequencing:** All four plans implemented in order. The batch is closed; any further plan work requires a new batch in this file.
