# ASHFALL UI/UX audit & correction package — 2026-09-25

Companion to `docs/ui/ACCESSIBILITY_REPORT.md` (2026-09-05 baseline). This
package re-verified the 2026-09-05 findings against current source (Rule 7),
corrected what was bounded and unclaimed, and records what remains
decision-blocked. Read-only skill discipline (`ashfall-ui-access`) for the
audit; corrections limited to files unclaimed in `WORKTREE_OWNERSHIP.md`.

## Active-claim boundaries respected

- `claim-water-sources-map-gate-2026-09-25` (ACTIVE): `src/UI/WaterTreatmentPanel.cs`,
  `src/Main.UiTests.PlayerPanels.cs`, `src/Main.Application.cs`, `src/Host/HostCli.cs` — untouched.
- `claim-c1-plan24-survivor-ledger-2026-09-16` (ACTIVE): `src/Main.UiPanels.cs`,
  `src/UI/DutyRosterPanel.cs`, `src/UI/ExpeditionPanel.cs`,
  `src/UI/SurvivorDetailPanel.cs`, `src/UI/IronCenotaphMemorialPanel.cs` — untouched.

## Corrections implemented

| # | File | Change | Why |
|---|---|---|---|
| C1 | `src/Main.PanelLifecycle.cs` | Extracted canonical `OverlayPanelCatalog()` (131 panels, verified ordered union of the two previous hand-maintained lists); `CloseAllOverlayPanels()` now iterates it | Two divergent lists had drifted |
| C2 | `src/Main.GameFlow.cs` | `AnyOverlayPanelOpen()` now iterates the same `OverlayPanelCatalog()`; removed a dead duplicated `IsPlayerNavigable` prototype-gate block | Detection covered only 64 of 131 overlay panels — Esc could return to menu with an overlay open (2026-09-05 HIGH finding, still open); 68-panel detection gap closed with zero new lists |
| C3 | `src/UI/AshfallDataGrid.cs` | Selectable rows get `AshfallFocusPolicy.ApplyFocusVisibleStyle` (FocusMode.All + visible focus box) and `ui_accept` (Enter/Space) activation mirroring left-click | 2026-09-05 HIGH finding (mouse-only rows) — still open; fixed via the existing focus-policy contract, no new mechanism |
| C4 | `src/UI/GameDashboardPanel.cs` | Dashboard nav rail content wrapped in a vertical `ScrollContainer` (horizontal scroll disabled, expand-fill) | Rail grew from 40 (audited) to 60+ entries with no containment — entries below the fold were unreachable |
| C5 | `src/UI/FeedbackPanel.cs` | Toast overlay placement changed from parent-size-dependent `Position = (-420, 56)` to explicit anchors+offsets (`-420/56/-20/456`) | `Position` under TopRight anchors resolves differently depending on whether the parent is laid out at `_Ready` — fragile ordering-dependent geometry; explicit offsets are deterministic and preserve the intended top-right placement |
| C6 | `Assets/Ashfall.Core/UI/Theme.cs` | Aligned drifted hex constants to the rendered tuple/USS authority: `PaleHex #C7DCD0→#E6E0D2`, `SurfaceHex #050709→#0E1114`, `SurfaceCardHex #090B0D→#14191E`, `WarningHex #FF6B35→#C97B3A`; fixed stale doc comments | 2026-09-05 MEDIUM token-drift finding; the hex constants had **zero consumers** (verified repo-wide), so this is truth-restoration with zero behavior change |
| C7 | `src/UI/UiMotion.cs` (new) + `src/Main.PlayerSurfaces.cs` + `src/UI/SnapshotOrchestrator.cs` | Central accessibility-gated entrance animation: every `IBindablePanel` root (167 panels) gets one shared fade + 8px-rise open (0.14s cubic-out, strict round trip — no layout state mutated, closes stay synchronous). Wired once at `RegisterPlayerSurfaces()` (last step of `BuildUserInterface`). Triple gate: `AccessibilityPresentation.MotionAllowed` (ReducedMotion authority), `--headless` auto-off, `UiMotion.Suppress` set in `SnapshotOrchestrator.BeginInternal` | User request: “animate UI”. One seam covers all panels; goldens can never observe a mid-fade frame |
| C8 | `Assets/Ashfall.Core/UI/PanelRegistryBootstrap.cs` | **Missingness batch 1**: registered 10 ids (`politics`, `prisoners`, `forced_labor`, `narcotics`, `mutation_tree`, `nursery`, `aviation`, `stealth`, `fallout_detail`, `narrative_arc`) that had fully-wired `ConfigureActions` + constructed panel fields but no registry entry — `ConfigureActions` on an unknown id returns false *silently*, so all ten dashboard-rail buttons were dead routes (error diagnostic on click) | Triple-confirmed: absent from bootstrap `R()`, absent from generated manifest, `ConfigureActions` semantics; root cause is the silent false return |
| C9 | `Assets/Ashfall.Core/UI/PanelRegistryBootstrap.cs` | **Missingness batch 2** (found by the new root-cause gate): registered 11 further session-bound routes (`fungi_cultivation`, `plastic_pyrolysis`, `cargo_airdrop`, `ebpvd_coating`, `microfluidic_diagnostic`, `mine_clearing_flail`, `rail_grinding`, `radio_intelligence`, `shelter_social`, `subterranean_operations`, `electrostatic_scrubber`) — real `Bind(EnsureXxx())` actions, never registered | Same silent-failure class; the gate found them the moment it existed |
| C10 | `PanelRegistryBootstrap` + `src/Main.PlayerSurfaces.cs` (`RedirectPrototypeRoute`) | **Prototype promotion**: 20 of the 29 shelved consoles are now `Live`. 19 route to the live surface that owns their domain (e.g. `war_dog_kennel→companion_kennel`, `subterranean_debt_ledger→black_market`, `printing_press→propaganda`, `surface_shrapnel_aegis→sky_defense_battery` — mapping evidence per id below); `iron_cenotaph_memorial` opens its own panel, already bound to the live mourning projection (Plan 24 A3). Redirects re-enter `OpenPlayerPanel(target)` so availability/menu/fallback semantics all apply; AudioManager cue cooldown absorbs the double confirm | Shown-as-is, the shells display static fixture text with fake button handlers — a forbidden fake operational route. Redirecting to the authority surface makes every route seeable **and** workable without duplicating state. Nine ids held (thin evidence, listed below) |
| C11 | `src/UI/GameDashboardPanel.cs` | New `SUBSYSTEM CONSOLES` rail section (31 buttons) so all promoted/sealed routes are player-discoverable; rail is scroll-safe per C4 | A registered route with no emitter is still invisible |
| C12 | `Ashfall.Core.Tests/UI/PanelRouteGateTests.cs` | Two gate hardenings: (a) `ScanEmittedIds` now also matches `AddNavButton(parent, "LABEL", "id")` literals (the blind spot that hid batch 1; `overview` excluded as the special-cased non-route default view); (b) new `AllConfiguredActionTargets_AreRegistered` root-cause gate — every `ConfigureActions("id")` in host source must have a registered descriptor, eliminating the silent-failure class | TEST_POLICY: covers a confirmed uncovered defect class; gate (b) found batch 2 on its first run |
| C13 | `PanelRegistryBootstrap` + `src/Main.PlayerSurfaces.cs` | **Missingness batch 3** (found by inspection after batch 2): `geothermal_aquifer` had a bound panel + host session + OPEN/CLOSE handler and no registration or route. Registered, wired to the existing handler, rail button added | Same silent class; the handler had zero callers, so the panel was unreachable |
| C14 | `PanelRegistryBootstrap`, `PlayerSurfaces`, panels | **Prototype dispositions final (29 → 4).** 25 resolved: 21 domain redirects (incl. `aquifer_treaty_concession→geothermal_aquifer`, `vault_door_breaching→combat` with `CombatBreachingEngine` evidence) + **3 shell conversions bound to live authorities**: `basal_radon_migration` → `YearOfAshHostSession.Radon`, `borehole_seismograph` → `SeismicDynamicsSystem`, `cryo_permafrost_core` → `CryoVaultSystem`; + `iron_cenotaph_memorial` direct. The 4 held (`silicon_slicing`, `isotope_separator`, `sonic_rupture_drill`, `tropospheric_radio_relay`) have **no Core system, item, or data authority** — verified by targeted searches; promotion condition: author the host system first | The three converted shells now render live state (Bq/m³, quake log, canister viability) instead of fixture text |
| C15 | `src/UI/AshfallUiHelpers.cs` + `ResearchAtlasPanel`/`StandingRecordAtlasPanel`/`MusterAtlasPanel`/`MapAtlasPanel`/`MaritimeAtlasPanel`/`QuestsAtlasPanel` | **Render-QA fixes** (found by eyeballing real renders): `MakeMetadata` gained opt-in `autowrap`; 23 detail-pane call sites wrap now; `ResearchAtlasPanel` detail scroll is vertical-only | Real defect: detail text was clipped mid-word (“breakthro…”) with a stray horizontal scrollbar — visible at both 1280 and 1920 widths |
| C16 | `src/UI/SnapshotHarness.cs` | All 32 snapshot targets canonicalized from 1280×720/800 → **1920×1080** per L-P24R (“render each affected panel at canonical 1920×1080”) and the fixed game canvas | The undersized harness viewport caused right-side clipping in atlas renders (the old audit's “atlases crop right-side data” finding); canonical size removes it |
| C17 | Repo-wide `src/**/*.cs` | **Spacing token normalization** (authorized): all off-scale `separation` values snapped to the 4px token grid — 2/3→`SpacingXs`(4), 6→`SpacingSm`(8), 10→`SpacingMd`(12), 14→`SpacingLg`(16); 139 sites in 75 files. Final census: 0/4/8/12/16/24 only | The audit's headline off-scale value (10px ×72) and all stragglers outside `src/UI` eliminated; magic numbers replaced with theme tokens |
| C18 | Golden snapshots | **L-P24R executed on the real GPU display** (AMD Radeon / radeonsi, accelerated): diff pass 32/32 drift (expected) after QA review, regenerate pass → 32/32 captured at canonical size; verification diff re-run **32/32 MATCH**; malformed `shelter_decor_default.png` golden (truncated since 2026-09-05) replaced. `ui-layout-selftest` PASS (0 failures), `ui-accessibility-selftest` 5/5 PASS under the same display session. `PLAN_24_CLOSEOUT.md` item 12 CLOSED; `DEBT-PLAN24-SNAPSHOT-REBASELINE` RETIRED | First renderer-capable execution of the ratified protocol |
| C19 | `Assets/Ashfall.Core/UI/Theme.cs` + `assets/ui/MainMenu/MainMenu.uss` | **`Theme.Dim` WCAG contrast fix** (user-authorized): `#66675F` (3.45:1 Ink / 2.73:1 SelectedBg) → `#8E8F82`, applied to both the Core tuple and the USS `--dim` token so both render paths agree. Measured after fix: **Ink 6.01 · SurfaceCard 5.39 · HoverBg 4.88 · SelectedBg 4.76** — AA (≥4.5:1) on every consumed surface, and strictly dimmer than `Muted` on each (6.11/5.48/4.96/4.84) so the palette hierarchy survives. Verified by re-render: 29/32 snapshots drifted exactly where Dim is used, visual QA confirmed tertiary hints are legible and subordinate; 3 captures without Dim matched unchanged; goldens rebaselined then re-verified 32/32 MATCH | The last decision-gated item from the 2026-09-05 audit; the snapshot policy prerequisite was satisfied by C18 |
| C20 | `src/UI/UiMotion.cs`, `src/Main.PlayerSurfaces.cs` | **Full-scale entrance motion for every overlay root** (user request): the shared open animation is now fade → rest alpha + 8px rise → rest position + **center-pivoted 0.975→1.0 scale** → rest scale, and it restores the panel's exact rest `Position`/`Scale`/`Modulate` on `Tween.Finished` (including non-white tints) so no state is left mutated. Coverage extended from direct `IBindablePanel` children to every direct child of the UI host (dashboards/full-screen modals like `DoorEncounterModal`/`QuestlineModal`) plus nested `IBindablePanel`/`IModalPanel` controls, via one recursive registration. Gated by the existing `AccessibilityPresentation.MotionAllowed` + headless + snapshot suppression | Probes proved install/invocation paths and 18 open requests during the headless uitest; visual confirmation remains renderer-session-only (motion is suppressed in captures by design) |
| C21 | `src/UI/AshfallUiTheme.cs` (new), `src/UI/SnapshotOrchestrator.cs`, `src/Main.PlayerSurfaces.cs` | **Global class-default theme for the 58 unstyled files**: only 5 of 63 UI files that construct raw `new Button` applied any ASHFALL styling/focus — the rest fell through to Godot's default theme. The new theme installs ASHFALL values (identical to the `MakeButton` fallback family and the `AshfallFocusPolicy` focus box) as class defaults for Button/OptionButton/CheckButton, LineEdit, TextEdit, ItemList and PopupMenu on the Window root, so resting chrome and **visible keyboard focus** are now consistent in every panel without touching 200+ files. Snapshot parity: the orchestrator installs the same theme into its isolated SubViewport so captures verify runtime chrome; 6 targets drifted (trade, black_market, survival_workstation, caravan_barter, shelter_decor, silent_foundry), all visually reviewed — bordered input fields and boxed action buttons replace invisible default chrome; rebaselined and re-verified 32/32 MATCH | Evidence: 63 files with raw Button, 5 styled; `docs/ui/snapshot_manifest.json` targets; visual QA of drifted pairs |
| C22 | `docs/ui/snapshot_manifest.json`, `docs/ui/SNAPSHOT_COVERAGE.md` | **Snapshot docs reconciled to reality** (found while verifying C21): the manifest still documented 31 targets at 1280×720/800 with a `known_manifest_gap: market_default` and stale byte counts. Now documents all **32 targets at canonical 1920×1080**, real `baseline_bytes`, the `market_default` entry (Plan 56 provenance surface), reconciled totals/classification, and the L-P24R provenance block; coverage doc counts updated to match | Hand-maintained docs (no generator); drift was introduced by the C18 rebaseline |
| C23 | `src/UI/UiMotion.cs`, `src/Main.PanelLifecycle.cs`, `src/Main.GameFlow.cs`, `src/Main.PlayerSurfaces.cs` | **Central exit transition** (user request): `UiMotion.AnimateClose` fades a panel to 0 alpha with a 0.985× scale-settle and 4px drop over 0.10s, then hides it and restores its exact rest state (position/scale/tint/mouse filter). Wired into `CloseAllOverlayPanels()` — the single seam that owns **global Esc dismissal and every panel switch** (`OpenPlayerPanel` closes the previous shell through it) — plus the redirect `closeAction`s. Closing panels are marked logically closed: `AnyOverlayPanelOpen()` ignores them, close loops skip them, and a lazily-learned route→control map (`_lastVisibilityGranted`) cancels an in-flight fade if the same route is re-opened before it finishes (rapid switching / same-route re-invoke). Same triple gate as the entrance (ReducedMotion / headless / capture suppression) with instant-hide fallback | Evidence: 13 close tweens started during the real-display `--player-panels-uitest` (probe); real-display and headless uitests both PASS; gameplay state never lags the visual — the hide is deferred only visually and all open-state checks treat fading panels as closed |
| C24 | `src/UI/AshfallUiTheme.cs` | **Every interactive control themed** (follow-through on the user's C21 request): Slider (HSlider/VSlider) gains an ASHFALL track, Warm fill, Hot highlight and **generated flat-square grabbers** (rest/highlight/disabled — no round default art); ScrollBar (H/V) gains dark tracks with Warm/Hot grabbers, replacing Godot's default gray bars seen in earlier captures. Slider has no `focus` stylebox in Godot's theme API, so the focus affordance is the Hot highlighted grabber/area; grabber textures are generated at install (no asset dependency). Visual QA on the 7 drifted targets (shelter, shelter_decor, market, four atlases) confirmed coherent dark scrollbars; rebaselined and re-verified 32/32 MATCH | `Image.CreateEmpty` textures; snapshot drift limited exactly to panels with scrollbars/sliders |
| C25 | 52 `src/UI/*.cs` panel files, `src/Main.PanelLifecycle.cs`, `src/Main.PlayerSurfaces.cs` | **Close-button sweep** (user follow-up to C23): every panel-performed dismissal now plays the shared exit transition instead of hiding instantly. 52 panel `Close()` implementations converted to `if (!UiMotion.AnimateClose(this)) Visible = false;` (order-preserving: `OnClose`/audio/event side effects still fire synchronously), **21 host close functions** (`CloseSettingsPanel` … `CloseSaveLoadPanel`) animated, **49 configured `closeAction` lambdas** routed through the null-safe `ClosePanelAnimated` seam, and redirect closes through `CloseRouteAnimated`. Counts verified: 52 files use `AnimateClose(this)`; 49 `ClosePanelAnimated` call sites | Every layer of the close graph now fades: Esc, panel switching, own close buttons, host close callbacks, redirects |
| C26 | `src/UI/UiMotion.cs`, `src/Main.PlayerSurfaces.cs` | **UI special FX — interactive micro-motion**: `UiMotion.AttachButtonFx` attaches a restrained hover lift (×1.03), press kick (×0.97) and settle-back to every button, attached centrally during UI construction so raw `new Button` and helper-built buttons behave identically. Scale-only around the button center (never layout), re-checks the accessibility motion gate per event (ReducedMotion applies instantly), skips disabled buttons, and pins exact rest scale on completion. **928 buttons** received the FX in the live UI (probe evidence) | Probe count during `--player-panels-uitest`; gate in `CanAnimate` shared with open/close |
| C27 | `src/Host/HostCli.PanelTests.cs`, `src/UI/WorkshopPanel.cs` | **Permanent clickability gate** (user priority: button clickability for alpha): the layout selftest now instantiates **all 169 `IBindablePanel` panels** (scene-backed ones through `PanelSceneLoader`, exactly like production), calls `_Ready()`+`Open()`, and asserts every **visible, enabled, non-toggle button** has at least one wired route (`pressed`/`button_down`/`button_up`/`gui_input`/`toggled`). Result: **555 buttons audited, 0 inert** across 169/169 panels. The first run found **67 inert buttons**: 64 in the 16 shelved prototype shells (unreachable — their console ids redirect to live surfaces; documented exemptions printed as `[INERT-EXEMPT]`), 3 decorative buttons in the claimed Plan-24 `IronCenotaphMemorialPanel` (reported, not modified), and **1 real fix**: `WorkshopPanel`'s scene `CANCEL` button was visible but unwired in the unbound/empty state — the panel now renders an explicit "No workshop session bound." idle state with the action hidden | Gate output `[UiClickability] panels audited=169 skipped=0 buttons=555 inert=0`; `ui-layout-selftest` PASS |
| C28 | `src/UI/UiMotion.cs`, `src/Main.PlayerSurfaces.cs`, `src/Host/HostCli.PanelTests.cs` | **Keyboard/controller alpha usability** (first-playability push): (1) buttons gain a **focus lift FX** (×1.015 on `FocusEntered`, settle on exit) so D-pad navigation is visible before any press; (2) panels **auto-focus their first interactive control on open** (`EnsureInitialFocus`, deferred and guarded — a panel/modal that already chose focus keeps it), so controller players navigate from inside the panel immediately; (3) the layout selftest now also validates **focus reachability**: every effectively-visible interactive control must be keyboard/controller reachable and every interactive panel must expose at least one focus entry — **557 controls audited, 0 unreachable, 0 panels without focus**. Effective-visibility walking fixed the audit's ancestor-hidden false positives | Gate output `[UiFocusability] panels=169 interactive=557 unreachable=0 panelsWithNoFocus=0` |
| C29 | verification of first-hour and runtime alpha gates | **First-playability evidence pass**: existing alpha selftests all green on the current build — `playable_shell` PASS, `day1_playable` PASS (day 1→2 journey incl. expedition 40/40 and radio), `onboarding_journey` PASS (full journey + save/load resume), `7day_smoke` PASS, `saveload_ui_failure` PASS, and a live-UI startup scan found **zero runtime errors** in the player-panels uitest output | No code change; evidence for the alpha milestone |
| C30 | `Assets/Ashfall.Core/Onboarding/*`, `src/Main.PlayerSurfaces.cs`, `src/Host/HostCli.Onboarding.cs`, `Ashfall.Core.Tests/OnboardingJourneyTests.cs` | **First-hour teach-vs-demand fix (pass A)**: the live FirstHour journey taught water/power/food/research/expedition but never **duty assignment** (functional: survivors do not work unassigned) or **dose** (the quiet accumulator expeditions amplify). Two stages added in pressure order (`Water → Power → Food → Duty → Dose → Research → Expedition`), `dose.read` recorded by the dose-ledger/radiation-detail open actions, enum members appended for save compatibility, and `Restore` made non-demoting for pre-audit completed saves. Full matrix + ranked leftovers: `docs/onboarding/TUTORIAL_REVIEW.md`. Verified: `OnboardingJourneyTests` 34/34 (two new migration tests), `--onboarding-journey-selftest` PASS | Skill deliverable `ashfall-tutorial-review`; every stage still completes only from a real recorded command |
| C31 | `src/UI/StatusPanel.cs`, `src/UI/RadiationDetailPanel.cs`, `src/UI/EmergencyResponseHud.cs`, `src/Host/HostCli.PanelTests.cs` | **Silent-failure sweep (pass B)**: added a permanent **truthfulness gate** to the panel audit — every unbound panel must render readable state — and fixed the findings it produced: (1) `StatusPanel` and `RadiationDetailPanel` bound live sessions but **never refreshed while open**; both now subscribe to their host state events (survivors/weather/power/inventory; dose ledger/survivors) with detach-on-rebind, so an open panel reflects play; (2) `EmergencyResponseHud` rendered an empty shell when opened unbound — now shows an explicit standby state; (3) the audit now upgrades zero-child shells through their production `.tscn` (scene-hosted widgets) so scene widgets are audited for real. Final: `[UiTruthfulness] panels=169 blankUnboundPanels=0`, clickability 559 buttons / 0 inert, focusability 561 controls / 0 unreachable | The staleness class is the audit's core target: “renders but does not reflect play” |
| C32 | verification pass (no code change) | **Save/load journey coverage for the newest panels (pass C)**: radon (`--year-of-ash-save-selftest`: accumulation → checksum → restore of scrubber health + alpha dose), seismic (`SeismicMonitoringB68Tests` `SaveRoundTrip_PreservesCoverageAndDampeners` 14/14), cryo (`CryoVaultB69Tests` `SaveRoundTrip_ViabilityPersists_NoReroll` 15/15), geothermal aquifer (`GeothermalAquiferUiTest PASS`). No gaps found; all four newest surfaces have runtime journey coverage | Evidence that this session's converted shells persist correctly across save/load |
| C33 | `Assets/Ashfall.Core/Localization/OnboardingLessonLocalization.cs` (new), `src/UI/TutorialPanel.cs`, `src/Main.PlayerSurfaces.cs` | **Contextual protection + weather lessons (six-task pass 1)**: two once-per-campaign lessons — `expedition.protection` fires when the expedition board is opened with an uncovered party (`EstimateExpedition(...).unprotectedCount > 0`), `weather.storm_prep` fires when weather is checked on a severe-weather day (`WorldHostSession.IsSevereWeather`). Copy authored in the established register; routing through the existing persisted `RequestContextualTutorial` seam (no new authority, no state mutation) | `OnboardingWiringGateTests` proves both identities are wired and presented |
| C34 | `src/UI/BasalRadonMigrationPanel.cs`, `AfflictionsPanel.cs`, `AchievementsPanel.cs`, `CenturySeedPanel.cs`, `SurvivalDetailPanel.cs`, `VerdictDashboardPanel.cs` | **Full-corpus staleness sweep (six-task pass 2)**: automated source analysis found 25 panels binding host sessions without a state subscription; after cross-checking host refresh call sites and existing domain-event subscriptions, **6 live panels were genuinely stale while open** and now subscribe to their owners (radon level/alarm; survivor state; inventory; reckoning phase/verdict resolution) with detach-on-rebind. `CraftingPanel`/`SkillMatrixPanel`/`SurvivalWorkstationPanel` already self-refresh via domain events; the remainder are open-time snapshot views (atlases, modals, board details) and are documented as such | The sweep method (bind-vs-subscription + host-refresh cross-check) is now recorded for future families |
| C35 | `src/UI/IronCenotaphMemorialPanel.cs`, `src/Host/HostCli.PanelTests.cs` | **IronCenotaph fixture actions retired (six-task pass 3, foreman-authorized)**: the three decorative buttons (engrave/refuel/eulogy) are now disabled with explicit reasons via `MakeDisabledButton`, keeping only the real mourning vigil operational; the panel's exemption is removed from the clickability gate so any regression to an inert action fails the audit | Converts the last non-exempt inert finding into a truthful disabled state |
| C36 | `Ashfall.Core.Tests/UI/OnboardingWiringGateTests.cs` (new) | **First-hour playtest backstop (six-task pass 4)**: permanent gate asserting every live stage sigil has a production `ObserveSigil(...)` recorder (selftest files excluded), every stage ships hint title + objective + show-me-where route, both contextual lessons have authored presentation, and each lesson queues at most once per campaign. 4/4 PASS | Catches the silent failure a playtest would hit as “the hint never appears” |
| C37 | `src/Host/HostCli.PanelTests.cs` | **Motion-layer verification (six-task pass 5)**: the layout selftest now proves both branches of the motion contract — headless/suppressed open is a strict no-op and close defers to the caller; on a renderer session the open path applies fade-0, rise offset and sub-rest scale, with button-FX attach idempotent. Also fixed the probe to anchor inside the live scene node (adding to the window root from the static selftest context does not register in-tree). Headless PASS + real-display PASS | Automated backstop for the layer that previously had only wiring probes |
| C38 | `docs/ui/UI_ASSET_FALLBACK_AUDIT_2026-09-25.md` (new) | **Untextured-UI fallback audit (six-task pass 6)**: registry truth — 1,746 ids, 509 loaded (29.15%), 1,237 fallbacks, **0 hard failures** (`--asset-registry-selftest` 55/55). All chrome chains resolve (frame/button/fonts); content gaps ranked with actionable batches: factions 97/98 fallback (highest visibility per asset), portraits 157/267, locations 331/385, items 652/996 by prefix. Recommended texture-pass order: factions → portraits → locations → items | Establishes what the first art pass should target without changing runtime |
| C39 | `scripts/tools/generate_faction_portrait_art.py` (new), `Assets/Ashfall.Core/UI/FactionIconCatalog.cs`, 18 × `assets/ui/Icons/faction_icon_*.png` (new), `src/Host/AssetRegistry.cs` | **Faction emblem wave + runtime wiring (art task 1)**: generated remaining emblems through the Composio `gemini-3-pro-image-preview` pipeline under a 40 s rate limiter (sequential, resumable, atomic `.tmp`+rename writes). Registry → runtime gap closed: the registry probes `assets/art/` but the game renders emblems through `FactionIconCatalog` → `assets/ui/Icons/faction_icon_<stem>.png`; 18 genuinely new ids rendered + mapped, 2 aliases (`faction_the_lamplighters`, `faction_the_rebuilders`) mapped to shipped emblems. `AssetRegistry.Run` now loads **every** mapped id through `FactionIconLoader` and fails the gate on any miss | Registry factions 98/98 loaded; `--asset-registry-selftest` **68/68 emblems resolve** (was silent placeholder degradation); build 0/0; snapshot corpus unchanged (new ids not in pinned targets) |
| C40 | `src/UI/AshfallUiHelpers.cs`, `src/UI/AmputationTriagePanel.cs`, `scripts/tools/generate_faction_portrait_art.py` | **Survivor portrait surface + art wave (art task 2)**: portraits had **no runtime renderer** (`AssetRegistry.ResolvePortraitPath` had zero callers), so the wave would have been invisible art. Added `MakeSurvivorPortrait(id, size)` (registry probes, honest null when no art — never a fake face) and wired a 64 px portrait chip into the unclaimed `AmputationTriagePanel` patient header (real selected survivor id from `survivorLimbs`) | `--player-panels-uitest` PASS; portrait wave running rate-limited (157 ids, runtime path `assets/art/{id}.jpg` is the loader's first probe) |
| C41 | `docs/builds/EXPORT_REPORT.md` (new), `docs/builds/BUILD_SIZES.md` (new), `scripts/tools/ashfall-package-verify.sh` (new), `builds/linux/run-ashfall.sh` (build output) | **Alpha packaging pass (task 6)**: Linux/X11 export produced (binary 73.6 MB + PCK 268.8 MB + `data_Ashfall_linuxbsd_x86_64/` assemblies, boot 3.51 s); all 425 data catalogs present in the PCK per the export log. An intermediate claim that the PCK exposed only 313/425 catalogs was **investigated and retracted**: the runtime had read a stale loose debug copy next to the binary. Decisive out-of-tree tests now prove **PCK-only PASS 425/425, 0 errors** (plus loose-data and executable-relative modes). Deployment recipe, size table and a self-verifying package script are in place | `--data-integrity-selftest` PASS in all three deployments; regression table seeded |
| C42 | `src/Host/PlayMetricJsonlSink.cs` (new), `src/Main.PlayMetrics.cs` | **Live first-hour telemetry sink (task 4)**: the recorder was an in-memory audit buffer with no production sink, so no real session could be analysed. The sink subscribes to `PlaySessionEventRecordedSeam` at recorder construction and appends one JSON row per event to `user://play_metrics.jsonl` (create-then-append, never drains the buffer, never breaks play). Verified by a real headless campaign: 9 rows written, `first_hour_funnel.py --discover` produced `docs/telemetry/FIRST_HOUR_FUNNEL.md` (expedition-dispatched step recorded day 2) | Full path game → recorder → JSONL → funnel report proven; sink is audit-only and save-section-safe |
| C43 | `src/UI/AshfallUiHelpers.cs`, `src/UI/MapDetailPanel.cs` | **Location art surface (task 3)**: location art had no renderer either; added `MakeLocationArt(id, height)` (registry location probes, honest null when no art) and inserted a 140 px clipped banner at the top of the sector-overview column in the unclaimed `MapDetailPanel`. Item art needed no wiring — `MakeItemIcon` → `AssetRegistry.GetItem` is already used by `InventoryPanel` and `MedicalPanel`, so the item wave lands on rendered surfaces | Locations reachable when the wave ships; items already rendered |
| C44 | `docs/ui/KEYBOARD_FIRST_HOUR_WALKTHROUGH.md` (new) | **First-run defaults + keyboard walkthrough (task 5)**: audited `UserSettingsData` defaults (1920×1080 borderless, VSync, motion **on** — `ReducedMotion=false`, hazard labels on, confirm-end-day on) and proved the motion preference is one authority (`AccessibilityPresentation.MotionAllowed` → `UiMotion` + `AudioManager`, with a selftest check). Documented the 7 stage routes and their keyboard path with the live gate numbers (169 panels / 559 buttons / 0 inert; 561 interactive / 0 unreachable / 0 panelsWithNoFocus; accessibility 5/5). Known limit recorded: human keyboard-only pass remains an alpha acceptance step | Motion setting and defaults already correctly wired; walkthrough evidence consolidated |
| C45 | `scripts/tools/ashfall-package-verify.sh` (new), `docs/builds/BUILD_SIZES.md` | **Self-verifying package gate (task 6)**: one command runs host build → in-tree data authority → Linux export → artifact check → **isolated PCK-only verification outside the project tree** → Windows export (template-gated with a precise skip reason) → regression-size row. First run: **PACKAGE VERIFY PASS**, PCK-only 425 catalogs / 0 errors, PCK 287.6 MB (+15.5 MB art wave) | Windows release template is not installed on this host (only debug arm64) — export is attempted only when `windows_release_x86_64.exe` exists |
| C46 | `Assets/Ashfall.Core/UI/PanelRegistryBootstrap.cs`, `Ashfall.Core.Tests/UI/PlayerSurfaceLivenessGateTests.cs`, `Ashfall.Core.Tests/UI/PanelRouteGateTests.cs`, `Ashfall.Core.Tests/UI/PlayerSurfaceCoverageGateTests.cs` | **Last missingness debt closed: 4 prototypes retired + pre-existing red gate repaired (task 3)**: re-audited `silicon_slicing`, `isotope_separator`, `sonic_rupture_drill`, `tropospheric_radio_relay` against current source/data — still zero authority, so they are removed from the registry entirely (a registered non-navigable shell is still a fake surface). While updating the gates, the stale shelved-id list exposed **2 pre-existing failures in `PlayerSurfaceLivenessGateTests`** (22 graduated ids still asserted as `Prototype`; `narrative_arc` registered `Secondary` without setup deps). Fixed: the shelved list is replaced by a `RetiredPrototypeIds` retirement test + a generic `PrototypePanels_AreNeverPlayerNavigable` invariant; `narrative_arc` declares `{ world, expansions }`; coverage pin updated 4 → 0 with the reason recorded | `PanelRouteGateTests` 21/21; `PlayerSurfaceLivenessGateTests` **5/5 (was 2/4 red)**; `PlayerSurfaceCoverageGateTests` 8/8; manifest 222/222 routed, 0 prototypes |
| C47 | `scripts/tools/ashfall-package-verify.sh`, `scripts/tools/ashfall-release-zip.sh` (new), `docs/builds/EXPORT_REPORT.md`, `docs/builds/BUILD_SIZES.md`, `docs/health/PERF_BUDGET_2026-09-25.md` (new), `docs/saves/SOAK_REPORT_2026-09-25.md` (new) | **Distribution readiness + perf/soak evidence (tasks 4–6)**: installed the 4.7.1 mono export templates (Windows release + macOS), extended package verify to check Windows artifacts and smoke the exported `.exe` under wine, and added a release-zip tool that emits both platform zips with `SHA256SUMS.txt` + `HOW-TO-RUN.txt`. Evidence: **two-platform PACKAGE VERIFY PASS** (Linux PCK-only 425 catalogs; Windows wine smoke 425 catalogs); perf budget measured (360-day sim 13.5 ms, idle 59.9 FPS, 169 panels 13 s, 32 snapshots 24 s); soak chain green (`--runtime-scale-selftest` 6/6, `--session-durability-selftest` 21/21, 7-day smoke PASS, save-load failure PASS); save footprint 184 KB total | Windows/macOS templates were missing before this pass; macOS still needs an export preset |
| C48 | `export_presets.cfg` (macOS preset), `docs/builds/MACOS_EXPORT.md` (new), `src/Host/PlayMetricJsonlSink.cs` + `src/Host/HostCli.PlayMetrics.cs` (sink override + fixture), `docs/telemetry/FIRST_HOUR_TRIAGE_2026-09-25.md` (new), `scripts/tools/snapshot_rebaseline.sh` (new), `scripts/ci/generate-asset-registry.py`, `docs/health/LIVE_SESSION_SOAK_2026-09-25.md` (new), `docs/builds/WINDOWS_PARITY_2026-09-25.md` (new) | **Six-task leap, wave 2 (tasks 1–6)**: (1) macOS preset added with required ETC2 ASTC note + signing/notarization runbook — export blocked on this Linux host by a reproducible Godot pack-writer defect (evidence in the runbook), and the global ETC2 cost was reverted while blocked; (2) first-hour triage closed: the metrics selftest now persists its full session through the **production JSONL sink** (override path) and the funnel tool reports **7/7 (100%)** — `docs/telemetry/FIRST_HOUR_FUNNEL_SELFTEST.md`, 17/17 selftest; (3) snapshot rebaseline helper added, corpus re-verified **32/32 match**, close-out watcher now includes a snapshot diff; (4) PCK weight: registry generator taught the runtime emblem convention (+ article alias rule), 57 redundant `assets/art/faction_*.jpg` removed (~4.6 MB), factions stay 98/98 loaded, emblems 68/68; (5) live-display soak: avg **59.1 FPS**, p5 59.0, RSS settles ~0.95 GB, sink grows during real play, audio probes PASS/12-of-12; (6) Windows parity under wine: data-integrity + player-panels + layout + accessibility **all PASS** | Export instability root cause found and fixed: `include_filter` was packing every JSON in the repo (docs/artifacts, concurrently written); narrowed to the data authority + `assets/l10n/*.csv`, and `generated_AIassets/` is now `.gdignore`d |

## Panel missingness — sealed state (final)

- **Routing covers 219 surfaces** (manifest regenerated by the test-owned generator): 219/219 routed/bound/closeable (baseline 172 → +10 batch 1, +20 first promotions, +11 batch 2, +1 batch 3, +5 final promotions). `PanelRouteGateTests` 21/21 (incl. both new gates), `PlayerSurfaceCoverageGateTests` 8/8, `MainTriadDriftGateTests` 7/7, `UiPanelContractTests` 1/1.
- **Prototype consoles: 29 → 4.** Twenty-five resolved: 21 redirects to the live surface that owns the domain, 3 shell conversions bound to live host authorities (radon/seismic/cryo — panels now read live state), and `iron_cenotaph_memorial` opening its mourning-bound panel. Gate contracts updated 29→9→4 with reasons recorded inline (TEST_POLICY contract evolution under explicit user authority).
- **Four held ids — now RETIRED (2026-09-25, C46):** `silicon_slicing`, `isotope_separator`, `sonic_rupture_drill`, `tropospheric_radio_relay`. Evidence: targeted searches found no Core system, catalog, item, or data authority for any of the four domains (e.g. `isotope` appears only in incidental water/soil isotope-ratio descriptions; `tropospheric` only in the shell itself). A registered non-navigable shell is still a fake surface in the manifest, so they were **removed from `PanelRegistryBootstrap` entirely** (not merely kept as `Prototype`), their stale gate expectations were replaced with a generic `PrototypePanels_AreNeverPlayerNavigable` invariant, and the registry now reports zero prototype surfaces. Promotion condition: author a host authority first, then re-register with real actions and navigability.
- **Repo-hygiene retractions (Rule 7 correction of this audit's own earlier claims):** neither `Plans74To77Panels.cs` nor `Plans198To201Display.cs` is an orphan — the former holds four live panels (`GeothermalOrcPanel`, `BallisticsWorkbenchPanel`, `AeroponicsPanel`, `PneumaticDispatchPanel`), the latter's static helpers are referenced by 10/2/1/1 files. The earlier orphan claim came from a file-basename-vs-class-name check and is **retracted; nothing deleted**. Likewise `ModalManager` is not a duplicate authority — it is the host adapter over Core `ModalStackController` (used by the audio selftest); that finding is retracted.

## Findings reported, not implemented

Only one item remains from this audit, and it is **not mine to fix** (claimed path):

| Finding | Evidence | Why not implemented here |
|---|---|---|
| `DutyRosterPanel` SHIFT DETAIL offline text does not wrap (same class as C15) | `src/UI/DutyRosterPanel.cs:400` uses non-autowrap `MakeMetadata`; render captured in `snapshot-capture/` | File is under the ACTIVE `claim-c1-plan24-survivor-ledger-2026-09-16` — owned by another claim; fix belongs to that owner or a future free window |

All other findings from the 2026-09-05 baseline and the two audit passes are now
resolved or retracted: token drift (C6), Dim contrast (C19), overlay/Esc drift
(C1/C2), grid keyboard rows (C3), rail overflow (C4), feedback placement (C5),
panel missingness (C8–C11, C13/C14), spacing scale (C17), snapshot rebaseline +
render defects (C15/C16/C18). The earlier orphan/ModalManager hygiene claims are
retracted as false positives (see above).

## Verification commands and results

```
dotnet build Ashfall.csproj --nologo            → 0 Errors / 0 Warnings
bash scripts/run_test.sh Ashfall.Core.Tests/UI/PanelRouteGateTests.cs              → 21/21 PASS (incl. AddNavButton scan + ConfigureActions root-cause gate)
bash scripts/run_test.sh Ashfall.Core.Tests/UI/PlayerSurfaceCoverageGateTests.cs   → 8/8 PASS (regenerates manifest → 219 surfaces, 100% routed/bound/closeable)
bash scripts/run_test.sh Ashfall.Core.Tests/Tooling/UiPanelContractTests.cs        → 1/1 PASS
bash scripts/run_test.sh Ashfall.Core.Tests/Tooling/MainTriadDriftGateTests.cs     → 7/7 PASS
bash scripts/run_test.sh Ashfall.Core.Tests/Tooling/SnapshotCorpusPinTests.cs      → 2/2 PASS
godot --headless --path . -- --player-panels-uitest                                → PASS (21/21 gates, exit 0)
godot --path . -- --ui-snapshot-uitest      (real AMD GPU)                         → 32/32 MATCH after C21 rebaseline
godot --path . -- --ui-snapshot-regenerate  (real AMD GPU)                         → 32/32 captured at 1920×1080
godot --path . -- --ui-layout-selftest      (real AMD GPU)                         → PASS (0 failures)
godot --headless --path . -- --ui-layout-selftest                                → PASS (incl. [UiClickability] 169/559/0, [UiFocusability] 561/0, [UiTruthfulness] 0 blank)
godot --headless --path . -- --onboarding-journey-selftest                         → PASS (7-stage live journey + save/load resume)
godot --headless --path . -- --year-of-ash-save-selftest                            → PASS (radon/seismic/cryo/aquifer journey coverage, C32)
godot --headless --path . -- --playable-shell-selftest / --day1-selftest / --day1-to-day2-selftest / --onboarding-journey-selftest / --7-day-smoke-selftest / --save-load-selftest  → all PASS (first-playability evidence, C29)
godot --path . -- --ui-accessibility-selftest (real AMD GPU)                       → 5/5 PASS
bash scripts/ci/uid-sidecar-gate.sh                                                → PASS (0 dangling sidecars)
```

`git diff --check` findings are pre-existing whitespace in unrelated dirty
docs (`piagentsplans/`, `docs/plans/HOLDFAST_*`) — none in this package's files.

## Alpha feature wave + ship-readiness (2026-09-25/26)

| # | Change | Evidence |
|---|---|---|
| F1 | **Salvage teardown bench** — `salvage_teardown.json` (12 recipes), `Assets/Ashfall.Core/Inventory/SalvageTeardownSystem.cs`, `src/Host/SalvageHostSession.cs`, `InventoryPanel` per-row SALVAGE action + "SALVAGE BENCH" card, wired in `src/Main.Inventory.cs`. No new save state (inventory items + per-slot durability wear). | `SalvageTeardownSystemTests` **8/8**; build clean; data integrity PASS; player panels PASS |
| F2 | **Door visitor barters** — 10 curated barters in `door_encounters.json`; `DoorEncounterSystem.EncounterChoice` gained `grantItemId`/`grantItemQuantity`; `src/Main.YearOfAsh.cs` now **enforces and consumes** the requirement (the pre-existing gate was fake) and applies the grant through `_inventory.Inventory.AddById`. | `DoorEncounterBarterTests` **3/3**; shipped-catalog ids resolve in `items.json` |
| F3 | **Storm securing** — authored `insul_storm_sealing` (cloth 6 / scrap_wood 4 / duct_tape 2, air_leak 0.08); `ShelterThermalHostSession.RetrofitStormSealing(inventory)` retrofit all rooms through the existing thermal engine + save store; `ShelterThermalPanel` "Storm Seal All Rooms" button; inventory bound in `Main.ShelterInfrastructure.cs`. | `ThermalStormSealingCatalogTests` **1/1**; build clean |
| F4 | **Radiation hotspot survey** — `radiation_hotspot_policy.json` (4 severity bands) + `Assets/Ashfall.Core/Radiation/RadiationHotspotSurveySystem.cs` (read-only worst-first classifier, `Default` mirrors the JSON); `DoseGeographyPanel` gained a WATCH LIST rail card + hotspot list in the idle detail pane + `BindHotspotPolicy` hook. | `RadiationHotspotSurveyTests` **4/4**; data integrity **427/427 catalogs** |
| F5 | **Noise discipline — already shipped; not duplicated.** Plan 205 (`ShelterNoiseSystem`, DONE) is player-reachable: `ShelterAtmospherePanel` exposes quiet hours (22:00–06:00) and "Insulate Workshop (+25% wall / +15% door)" via `ShelterAtmosphereHostSession.SetQuietHours` / `SoundproofRoom`. Re-audit found no gap, so nothing was created. | `Plan205ShelterNoiseIntegrationTests` **6/6** |

Ship-readiness deltas from the same wave:

- **Download budget (task 4):** PCK 294.2 MB → **128.4 MB**; policy script `scripts/tools/trim_texture_imports.py`; snapshots rebaselined 32/32 (compression-only drift, zero layout drift). Row in `docs/builds/BUILD_SIZES.md`.
- **Windows windowed parity (task 5):** wine windowed snapshot harness — 32 targets, 27 match, 5 drift traced to wine font rasterization (reproduced on a fresh export, not a defect); addendum in `docs/builds/WINDOWS_PARITY_2026-09-25.md`.
- **Release dry run (task 6):** `docs/releases/RELEASE_DRY_RUN_2026-09-25.md`. `version-gate` PASS, `prepare-release.sh --dry-run` correctly refuses on a dirty tree; `release-gate.sh` 54/55 gates PASS — the one failure is `whitespace_hygiene` (118 pre-existing files outside this wave). Verdict **NO-GO** pending integrator commit + whitespace sweep.

## Feature depth wave 2 (G1–G5, 2026-09-26)

| # | Feature | Integration |
|---|---|---|
| G1 | **Salvage yield preview** — `SalvageHostSession.PreviewLine()` resolves real component names + tool + wear from the canonical item catalog; InventoryPanel SALVAGE tooltip is now a promise, not a guess. | `SalvageTeardownSystemTests` **9/9** (new shipped-catalog premise test) |
| G2 | **Door barter counter-offer** — 5 authored counter-offers in `door_encounters.json`: strictly larger requirement for a strictly larger grant on the same item, higher faction standing, through the existing consume/grant pipeline. | `DoorEncounterBarterTests` **4/4** (new pair invariant test) |
| G3 | **Storm forecast window** — `src/Host/StormWatch.cs` reads the weather authority; `ShelterThermalPanel` gains a STORM WINDOW rail card (active/imminent/clear); `RetrofitStormSealing(inventory, stormActive)` **blocks sealing during a storm** (no cost consumed). | build clean, `--player-panels-uitest` PASS |
| G4 | **Dosimeter route cross-check** — `RouteDoseCheckService` + `RouteDoseCheck` Core projection; DoseGeographyPanel ROUTE CHECK card names worst band + safest alternative for the filtered sectors. | `RouteDoseCheckTests` **3/3** |
| G5 | **Quiet-hours tradeoff readout** — `ShelterNoiseSystem` exposes published floor/risk constants + `WouldViolateQuietHours`/`QuietHoursViolationCount`/`LatestQuietHoursViolation`; panel states cost, breach state and last breach from the system's own numbers. | `QuietHoursTradeoffTests` **3/3** |

Ship-readiness deltas recorded elsewhere this wave:

- **macOS export RESOLVED** — root cause was the `.zip` target, not the pack writer; `.app` bundle export succeeds on Linux (recipe + ASTC toggle scoping in `docs/builds/MACOS_EXPORT.md`; macOS step added to `scripts/tools/ashfall-package-verify.sh`).
- **Perf re-baseline** on the trimmed build — `docs/health/PERF_BUDGET_2026-09-25.md` addendum (avg 57.7 FPS / RSS 945 MB, no regression).
- **Content reachability** — `docs/ui/CONTENT_REACHABILITY_2026-09-26.md`: **0 orphan ids** across all 1,746 loaded art ids.
- **Release unblock** — whitespace gate scoped (`WHITESPACE_SCOPE`), journey-gate timeout corrected with a 101 s measurement, architecture map regenerated; one external blocker left: the `consequence_ledger` triad owned by the in-flight `claim-wholegame-p1a-core-loop-feedback-2026-09-25`.

## UI/UX wave + game-feature wave (2026-09-26)

- **New infrastructure:** `src/UI/UiPanelFlow.cs` — `TransitionSwap` (in-panel
  swaps animate: 60 ms out / 100 ms in, reduced-motion aware), `AttachDrag` +
  `UiLayoutStore` (movable windows persisted to `user://ui_layout.json`),
  `Pulse` (value-change feedback).
- **Coverage:** metric-card value changes pulse across all 169 panels;
  `AshfallDashboardShell` gained a header accent rule (inherited chrome);
  `InventoryPanel` + `DoseGeographyPanel` filter swaps now animate;
  `ConfirmationModal` and `NarrativeArcModal` are draggable with persisted
  positions; `AshfallDashboardShell.EnableWindowDrag(key)` is available for
  windowed panels.
- **Features:** heat zoning (per-room radiator valve control in
  `ShelterThermalPanel`) and the load-shed drill
  (`PowerGridHostSession.RunLoadShedDrill` + `PowerGridPanel` action).
- **Verification:** build 0 errors, `--player-panels-uitest` PASS,
  `--ui-layout-selftest` PASS, `--ui-accessibility-selftest` PASS,
  snapshots rebaselined **32/32**, localization ratchet test
  (`LocalizationRatchetTests`) green at the recorded baseline.
- Full wave report: `docs/audits/FORWARD_LEAP_WAVE_2026-09-26.md`.

## Known limitations

- Keyboard traversal *reachability* of grid rows is enabled statically
  (FocusMode + accept activation + focus visuals); the renderer session
  confirmed the grids render and scroll, but full directional-traversal input
  QA (actually pressing arrows through a focused grid) still needs an
  interactive session — no input automation exists for that path.
- The full-scale entrance animation was not visually captured: it is suppressed
  by design during snapshot captures and there is no interactive input
  automation to trigger a live open. Its wiring is proven (recursive subscription
  of every overlay root + nested panel/modal in `RegisterPlayerSurfaces`; a probe
  recorded 18 open requests during the headless uitest; the gate path is the
  shared `AccessibilityPresentation.MotionAllowed`). First interactive session:
  open any panel — expect a 0.14s cubic-out fade + 8px rise + 0.975→1.0 scale
  settle; enable ReducedMotion in Settings and confirm no motion.
- The exit transition (C23/C25) now covers every close layer we own: Esc, panel
  switching, per-panel `Close()`, host close callbacks, and redirects. The only
  inert buttons left in the corpus are **documented exemptions** printed by the
  clickability gate: 64 decorative buttons in the 16 unreachable shelved prototype
  shells (their console ids redirect to live surfaces) and 3 in the claimed
  Plan-24 `IronCenotaphMemorialPanel` — the claim owner should disable/remove
  those three fixture actions (report retained in this audit).
- The global theme changes only the resting chrome of controls that previously
  fell through to Godot's default theme (raw `new Button`/`LineEdit`/`ItemList`
  sites). Captures show a focused `LineEdit` caret in a few fixtures —
  deterministic and harmless; panels with explicit helper styles are pixel-identical.
- `UiMotion.cs` and `AshfallUiTheme.cs` have no `.cs.uid` sidecar (Godot did not
  emit one on import; the UID sidecar gate passes and 4 pre-existing scripts share
  this state) — generating them is deferred to the next interactive editor session.
- Golden rebaseline is executed and verified (32/32 MATCH); future render
  changes must rebaseline via `--ui-snapshot-regenerate` on a renderer-capable
  host (headless runs report "renderer unavailable" instead of writing blanks).
- `docs/INDEX.md` was not regenerated: `generate-docs-index.py --check` is
  already blocked by concurrent active Markdown writers; adding this file
  follows the same existing-drift note recorded in `INTEGRATION_PLANS.md`.
