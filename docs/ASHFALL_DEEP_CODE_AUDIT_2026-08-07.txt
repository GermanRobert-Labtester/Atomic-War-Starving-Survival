========================================================================================
ASHFALL (Atomic War) — DEEP CODE AUDIT & OPTIMISATION REPORT
Read-only audit | No code was modified
Generated (UTC): 2026-08-07 16:12:36
Workspace: Assets/_Game (~896 C# files, ~145k lines) + Tests + StreamingAssets
========================================================================================

HOW TO READ THIS DOCUMENT
----------------------------------------
Each finding has:
  ID           Unique stable id (SAVE-001, RAD-001, ECON-001, …)
  Severity     Critical | High | Medium | Low | Nit
  Issue Type   bug | incomplete | design smell | duplication | missing feature |
               performance | event gap | save gap | data desync | dead code |
               wrong units | silent fail | lifecycle leak
  Location     File path (repo-relative) + method / approx. area
  Description  What is wrong and player/dev impact
  Suggested Fix Concrete remediation
  Evidence     Verified against source during this audit

Severity guide:
  Critical — Breaks core loop, save integrity, or domain promise (rad/warmth/death)
  High     — Wrong economy/combat/medical outcomes, item loss, major desync
  Medium   — Incomplete paths, dual systems, wrong events, balance edge cases
  Low      — Polish, naming, minor edge cases
  Nit      — Style, docs lying, tiny duplication

SCOPE & METHOD
----------------------------------------
Static analysis of Assets/_Game/**/*.cs, StreamingAssets, asmdefs, tests.
Specialist deep dives: Save/Load, Inventory/Economy, Architecture/Bootstrap,
Medical/Radiation/Shelter/Combat. Spot-verified highest severity claims.
Unity batch compile / PlayMode not re-run for this report (read-only).

CODEBASE SNAPSHOT
----------------------------------------
  ~896 game C# files / ~145k lines under Assets/_Game
  Largest domains: Core (~86k), Survivors (~17k), Shelter (~8k), UI (~6k)
  God-objects: GameBootstrap (~48 partials, ~12k lines), PersonalQuestSystem (~8.5k),
               SaveSystem (~12 partials), DynamicEconomySystem (~1.7k)
  Event subscribe += ~633 vs unsubscribe -= ~98 (lifecycle leak risk)
  Runtime Debug.Log* call sites outside Editor: ~800
  Dual StreamingAssets item trees: Data/items.json (canonical) + root items.json (legacy)

========================================================================================
SECTION A — CRITICAL ISSUES (must fix first)
========================================================================================

[SAVE-001]  Severity: CRITICAL  |  Type: bug / missing field
  Location:      Assets/_Game/Core/TimeSystem.cs CaptureState/RestoreState; SaveSystem.Capture.cs ElapsedHours; GameBootstrap.CoreFamilies.cs TimeSystemSys
  Description:   Live TimeSystem clock is NOT restored. CaptureState returns empty DTO; restore is no-op. Save writes ElapsedHours from TemperatureSystem only. A second unused TimeSystemSys is registered as time_system. After Continue, day/hour can reset to day 1 while weather/temp is mid-campaign.
  Suggested Fix: Persist TotalElapsedHours (or day+hour) on the real GameBootstrap.TimeSystem; restore via SetElapsedHours; remove ghost TimeSystemSys; single clock source for GameState.Day + Temperature + Photoperiod.
  Evidence:      TimeSystem.cs: CaptureState() => new TimeSystemSave(); RestoreState ignores saved. Capture.cs uses _temperatureSystem.TotalElapsedHours only.

[SAVE-002]  Severity: CRITICAL  |  Type: bug / missing field
  Location:      Assets/_Game/Core/SaveSystem.Capture.Entities.cs CaptureExpeditionState; SaveSystem.Restore.Expeditions.cs ApplyExpeditionSaveFields
  Description:   Flashpoint expedition fields restored (IsCommsSevered, FlashpointBehavior, OriginalEtaTicks, ShelterDelayTicksRemaining, ReturnSpeedMultiplier/Divisor) but NEVER captured from ExpeditionState. Mid-flashpoint load resets expedition flashpoint behavior.
  Suggested Fix: Copy all flashpoint fields in CaptureExpeditionState; add EditMode round-trip test.
  Evidence:      Restore assigns saveExp.IsCommsSevered etc.; CaptureExpeditionState ends without those assignments.

[SAVE-003]  Severity: CRITICAL  |  Type: incomplete / hybrid world
  Location:      Assets/_Game/Core/SaveSystem.Restore.cs RestoreFromSnapshot; SaveSystem.Entities.cs FailFastRestore; SaveSystem.IO.cs Load
  Description:   Load is not transactional. Fail-fast only two-phases ISaveable restore; survivors/shelter already mutated. On throw, Load returns false but live world is hybrid. Next AutoSave can overwrite good slot.
  Suggested Fix: Stage-then-commit restore, or force scene reload on load failure; never AutoSave after failed Load.
  Evidence:      Restore order mutates early RestIf paths before ISaveable fail-fast.

[RAD-001]  Severity: CRITICAL  |  Type: missing radiation path
  Location:      Assets/_Game/Core/GameBootstrap.InitFoundation.cs ~182; Assets/_Game/Radiation/RadiationSystem.cs Tick
  Description:   RadiationSystem constructed as new RadiationSystem(NeedsSystem) with NO exposureContext. Tick always gets null context → zone=0, no shelter/gear → NO ambient bunker/zone dose. Only scripted Expose() applies rads. Core domain hazard effectively off indoors/outdoors ambient.
  Suggested Fix: Inject Func<Survivor, ExposureContext> filling zone rads, worn gear, Shelter.GetInteriorRadsPerHour, bunker contamination.
  Evidence:      Verified: InitFoundation `RadiationSystem = new RadiationSystem(NeedsSystem);` Tick uses `_exposureContext != null ? … : null`.

[RAD-002]  Severity: CRITICAL  |  Type: missing radiation path
  Location:      Assets/_Game/Shelter/Shelter.Ops.cs GetInteriorRadsPerHour; BunkerContamination
  Description:   BunkerContamination is mutated (hatch/pet) and saved but NEVER added into GetInteriorRadsPerHour. Bunker pollution is cosmetic.
  Suggested Fix: return Max(0, rads + BunkerContamination) (plus room contributions).
  Evidence:      BunkerContamination written elsewhere; not referenced in interior rad formula.

[RAD-003]  Severity: CRITICAL  |  Type: missing radiation path
  Location:      Assets/_Game/Shelter/ShelterRoom.cs GetIndoorRadContribution
  Description:   Room AmbientContamination computes indoor rad contribution but nothing feeds RadiationSystem/exposure. Contaminated stores/corpses do not dose survivors via rooms.
  Suggested Fix: Sum room (or current-room) contribution into ExposureContext / GetInteriorRadsPerHour.
  Evidence:      GetIndoorRadContribution has no callers into radiation exposure path.

[RAD-004]  Severity: CRITICAL  |  Type: clamp / logic bug
  Location:      Assets/_Game/Shelter/MaterialShieldingSystem.cs GetRadiationBleed ~29–35
  Description:   weakestAttenuation starts at 0f; Min(0, attenuation≥0) always 0 → bleed always exteriorRads*(1-0)=full exterior. Material upgrades never reduce bleed. Also not integrated into GetInteriorRadsPerHour.
  Suggested Fix: Initialize weakestAttenuation to 1f (or MaxValue then Min); wire into interior rad formula.
  Evidence:      Verified: float weakestAttenuation = 0f; Min(weakestAttenuation, a).

[SURV-001]  Severity: CRITICAL  |  Type: wrong wiring / warmth
  Location:      Assets/_Game/Core/GameBootstrap.InitFoundation.cs ~171; NeedsSystem.ApplyWarmth
  Description:   NeedsSystem created with isNearHeatSource = sv => true. Every survivor always 'near heat' → continuous warmth restore; nuclear-winter cold path disabled. Domain warmth/cold hazard broken.
  Suggested Fix: Hook to heater operational / indoor temp / room heat (TemperatureSystem / IndoorTempBonus).
  Evidence:      Verified: `NeedsSystem = new NeedsSystem(needsProfile, sv => true);`

[MED-001]  Severity: CRITICAL  |  Type: wrong units / inverted clamp
  Location:      Assets/_Game/Medical/MedicalSystem.cs ApplyStaminaCap; AfflictionSO.staminaCap
  Description:   Fatigue high = exhausted. staminaCap docs imply floor (never fully rested). Code does if (Fatigue > cap) Fatigue = cap → CEILING (cannot get more tired). Undoes fatigueDrainPerHour for blood_loss etc.
  Suggested Fix: If floor: if (Fatigue < cap) Fatigue = cap; rename to fatigueFloor/fatigueCeiling.
  Evidence:      Blood_loss staminaCap 30f + clamp-down when Fatigue > cap.

[MED-002]  Severity: CRITICAL  |  Type: incomplete medical path
  Location:      Assets/_Game/Medical/AmputationSystem.cs PerformAmputation
  Description:   Docs claim amputation cures Sepsis. Implementation only costs health/morale, adds amputee disability, never removes sepsis affliction or calls MedicalSystem.
  Suggested Fix: On success remove sepsis (and related) via MedicalSystem cure callback.
  Evidence:      Verified: PerformAmputation body has no sepsis removal.

[CMB-001]  Severity: CRITICAL  |  Type: state machine bug
  Location:      Assets/_Game/Shelter/HatchDefenseSystem.cs SyncGeneratorNoise / Tick
  Description:   SyncGeneratorNoise sets GeneratorRunningOutside=true when outdoor diesel runs, never clears to false. Decay only when flag false → permanent high noise / noise raids after first outdoor generator use.
  Suggested Fix: GeneratorRunningOutside = outdoor || manualFlag; clear when diesel stops; decay when neither active.
  Evidence:      Verified: only sets true; else if (GeneratorRunningOutside) keeps ExternalNoise high.

========================================================================================
SECTION B — HIGH SEVERITY
========================================================================================

[SAVE-004]  Severity: HIGH  |  Type: bug / race
  Location:      SaveSystem ctor OnPhaseChanged AutoSave; GameBootstrap.InitLate.cs; Lifecycle ApplyPendingGameLoad
  Description:   Dual AutoSave hooks. Bootstrap _suppressAutoSave does not suppress SaveSystem's own phase handler. Continue→Running still autosaves and can clobber slots; double-write every phase change.
  Suggested Fix: Single AutoSave owner; wire SuppressAutoSave into SaveSystem or remove ctor subscription.

[SAVE-005]  Severity: HIGH  |  Type: partial load
  Location:      SaveSystem.Entities soft restore; FailFastRestore default false in players
  Description:   Soft restore logs ISaveable failures and still returns Load success → hybrid campaign.
  Suggested Fix: Track error count; return false or degraded-load UI; block autosave until ack.

[SAVE-006]  Severity: HIGH  |  Type: silent fail
  Location:      SaveSystem.Entities CaptureSubsystemStates catch
  Description:   CaptureState exceptions logged and omitted; overall Save still true → next load loses subsystem.
  Suggested Fix: Fail whole Save if any capture fails; don't replace previous file on incomplete capture.

[SAVE-007]  Severity: HIGH  |  Type: bug
  Location:      SaveSystem.Restore RestoreSurvivorsAndShelter by index
  Description:   Survivors restored by roster index not Id; reordered/recruited/dead mis-map; extras dropped.
  Suggested Fix: Match by Id; create missing; remove absent; never depend on list order.

[SAVE-008]  Severity: HIGH  |  Type: bug
  Location:      SaveSystem.Restore.Expeditions RestoreExpeditions
  Description:   Active expeditions not cleared before restore → phantom expeditions possible.
  Suggested Fix: Clear ActiveExpeditions then apply save list.

[SAVE-009]  Severity: HIGH  |  Type: missing field
  Location:      BunkerSocialDirector CaptureState
  Description:   InterpersonalAffinity matrix not in BunkerSocialSave; social bonds reset on load.
  Suggested Fix: Persist AffinityMatrix snapshot on BunkerSocialSave.

[SAVE-010]  Severity: HIGH  |  Type: bug
  Location:      GameState.Day vs TimeSystem
  Description:   GameState.Day rarely updated during play; save day often wrong; victory days unreliable.
  Suggested Fix: On day tick: GameState.Day = TimeSystem.CurrentDay; one clock (SAVE-001).

[SAVE-011]  Severity: HIGH  |  Type: missing field
  Location:      ShelterModuleInstance FuelBurnMultiplier SecurityContribution
  Description:   Module fuel burn / security contribution not on ShelterModuleSave → perk rates reset.
  Suggested Fix: Add DTO fields + capture/restore.

[SAVE-012]  Severity: HIGH  |  Type: incomplete
  Location:      GameBootstrap.InitLate ModuleLookup = id => null
  Description:   Module restore always nulls Definition; SO-driven module behavior dead after load.
  Suggested Fix: Wire real module catalog lookup.

[ECON-001]  Severity: HIGH  |  Type: pricing correctness
  Location:      DynamicEconomySystem.GetTradeValue ~745
  Description:   Always InferTier(item.type); never catalog TradeTier. Stations (Device→SurvivalGear shortage 1.8 vs Station 1.2), tungsten (Material→Bulk not Precious soften), attachments (Tool→Utility) wrong under shortage.
  Suggested Fix: Prefer Item_WorldCatalog.TryGet.TradeTier; InferTier fallback only.

[ECON-002]  Severity: HIGH  |  Type: UI contract
  Location:      TradeScreenUI.BuildQuoteSummary 194–195; GetDisplayedUnitValue
  Description:   Prints You offer/They ask with :0.0 digits. Violates no-numeric-trade-value policy; FormatWorthLabel unused in quote UI.
  Suggested Fix: Use FormatWorthLabel / qualitative fairness only for player text.

[ITEM-001]  Severity: HIGH  |  Type: data desync
  Location:      Item_WorldCatalog Tool(Weapon) vs Data/items.json knives/crowbar type Tool
  Description:   Catalog melee = Weapon; JSON = Tool with lower tradeValue. Importer follows JSON → wrong combat/trade.
  Suggested Fix: Re-export JSON from catalog; CI parity type/tier/value.

[ITEM-002]  Severity: HIGH  |  Type: data desync
  Location:      Data/items.json vs Item_AmmoTypes; legacy StreamingAssets/items.json
  Description:   Core FMJ/buck loads (ammo_9x19_fmj etc.) missing from Data/items.json; live in legacy root + code catalog only.
  Suggested Fix: Export full ammo into Data/; quarantine legacy root JSON.

[ITEM-003]  Severity: HIGH  |  Type: duplication
  Location:      StreamingAssets/Data/* vs StreamingAssets/* items/recipes
  Description:   Two schemas (displayName/stackMax vs itemName/stackSize); different ids. Importer uses Data/ only; legacy confuses mods/tools.
  Suggested Fix: Single source under Data/; delete or non-shipping legacy.

[INV-001]  Severity: HIGH  |  Type: item loss
  Location:      Inventory.Add ~290–336
  Description:   Fills existing stacks then while-loop may return false mid-capacity without rollback; OnItemAdded already fired; callers treating false as none lose remainder.
  Suggested Fix: All-or-nothing preflight like CanAdd, or stage+commit.

[INV-002]  Severity: HIGH  |  Type: item loss
  Location:      Inventory.Unequip
  Description:   Removes equipped then Add ignoring failure → full bag destroys gear.
  Suggested Fix: If !CanAdd re-equip or drop overflow; never discard.

[ARCH-001]  Severity: HIGH  |  Type: god-object
  Location:      GameBootstrap ~48 partials / 100+ public systems
  Description:   Composition root owns construct, tick, UI, missions, Twitch, win/lose — unmaintainable and high regression risk.
  Suggested Fix: Split ShelterHost/ExpeditionHost/SocialHost/WorldHost; thin bootstrap.

[ARCH-002]  Severity: HIGH  |  Type: dual-path
  Location:      GameBootstrap.TickSystems registry vs fallback hand path
  Description:   Two tick order encodings will drift (Death’s Door only in one path).
  Suggested Fix: Delete fallback; always require registry.

[ARCH-005]  Severity: HIGH  |  Type: observability
  Location:      GameBootstrap.Diagnostics GetUntickedSystemNames aliases
  Description:   Huge alias map marks systems 'ticked' without real ticks; opposite: false positives for modules.
  Suggested Fix: Auto-register live/event/save-only; remove hand aliases.

[BOOT-001]  Severity: HIGH  |  Type: lifecycle leak
  Location:      GameBootstrap.Lifecycle OnDestroy
  Description:   Unsubscribes only a few static events; economy/medical/HUD/batch lambdas leak across PlayMode.
  Suggested Fix: Composite IDisposable for all subscriptions.

[PERF-001]  Severity: HIGH  |  Type: hotspot
  Location:      GameBootstrap.Lifecycle TickFrame MaxSubstepsPerFrame=128
  Description:   Hitch + 3× time can run full sim ×128 in one frame.
  Suggested Fix: Lower cap; spread catch-up; profile TickAll.

[EVENT-002]  Severity: HIGH  |  Type: missing events
  Location:      Shelter/WaterStorage.cs
  Description:   No OnWaterChanged; HUD must poll.
  Suggested Fix: Raise OnTiersChanged after Add/Consume.

[EVENT-003]  Severity: HIGH  |  Type: missing events
  Location:      Shelter module Tick fuel/filter
  Description:   Fuel/filter health change silent; only add/remove/upgrade events.
  Suggested Fix: OnModuleStateChanged(moduleId).

[RAD-005]  Severity: HIGH  |  Type: dual systems
  Location:      RadiationSystem 400 vs AfflictionPipeline 300 vs Prognosis latent 100
  Description:   Three chronic thresholds; AfflictionPipeline never constructed (dead dual).
  Suggested Fix: Single pipeline; delete or wire AfflictionPipeline.

[RAD-006]  Severity: HIGH  |  Type: event gap
  Location:      PrognosisPipeline TriggerProdromal
  Description:   Zeros RadiationDose without OnDoseChanged → HUD desync.
  Suggested Fix: Fire dose-changed via RadiationSystem.

[RAD-007]  Severity: HIGH  |  Type: unseeded RNG
  Location:      RadiationSystem/Prognosis/Mutagenesis default new Random()
  Description:   Save/load replay non-deterministic for death/chronic rolls.
  Suggested Fix: Always inject campaign-salted RNG.

[RAD-008]  Severity: HIGH  |  Type: death path
  Location:      RadiationMutagenesisSystem.Tick
  Description:   Direct Health write bypasses NeedsSystem death/events.
  Suggested Fix: NeedsSystem.Modify Health.

[RAD-009]  Severity: HIGH  |  Type: wrong units
  Location:      ExposureContext.ShelterShielding
  Description:   Doc 0..1 fraction; ComputeExposurePerHour subtracts as absolute rad/hr.
  Suggested Fix: Multiply (1-frac) or document absolute points.

[RAD-010]  Severity: HIGH  |  Type: dual state
  Location:      RadiationSystem.DegradeWornGear
  Description:   Degrades context copies; may never write inventory durability.
  Suggested Fix: Round-trip durability to inventory slots.

[MED-003]  Severity: HIGH  |  Type: silent no-op
  Location:      MedicalSystem.CreateDefaultAfflictions
  Description:   Missing anaphylactic_shock, phantom_pain, scurvy, toothache, hair_loss, cellular_breakdown etc.
  Suggested Fix: Register all AfflictionSO.Ids used by systems.

[MED-004]  Severity: HIGH  |  Type: event gap
  Location:      Addiction/Amputation/BloodTransfusion/Scurvy direct Needs writes
  Description:   Skip OnNeedChanged, death, quests (Selfless etc.).
  Suggested Fix: Route through NeedsSystem.Modify.

[MED-005]  Severity: HIGH  |  Type: unseeded RNG
  Location:      MedicalSystem.ConsumeIngredients surgery spare
  Description:   Falls back to UnityEngine.Random.value when _surgeryRng null.
  Suggested Fix: Require injected RNG.

[MED-006]  Severity: HIGH  |  Type: dead code
  Location:      MedicalSystem.GetHealMultiplier
  Description:   Chem-Resistant 2× heal never applied on cure.
  Suggested Fix: restore * GetHealMultiplier on CompleteTreatment.

[MED-007]  Severity: HIGH  |  Type: dead clamp
  Location:      MedicalSystem scarred lungs
  Description:   Modify(Health, 0f) no-ops; health can exceed MaxHealthCap.
  Suggested Fix: Apply negative delta to cap.

[MED-008]  Severity: HIGH  |  Type: unseeded RNG
  Location:      AddictionSystem ctor
  Description:   Default new System.Random() for panic-destroy.
  Suggested Fix: Seeded factory.

[SURV-002]  Severity: HIGH  |  Type: hunger immortality edge
  Location:      NeedsSystem.Tick critical needs
  Description:   Critical hunger/thirst/cold no longer damage Health; profile healthLoss* dead. Starvation may not kill without afflictions.
  Suggested Fix: Re-enable health pressure or inflict starvation afflictions; delete dead profile fields if intentional.

[SURV-003]  Severity: HIGH  |  Type: event gap
  Location:      NeedsSystem.Tick personal quest morale/fatigue/health writes
  Description:   Direct field writes skip events.
  Suggested Fix: All via Modify/SetValue.

[SURV-004]  Severity: HIGH  |  Type: save / dual hygiene
  Location:      Needs.Hygiene not in SurvivorSave; WasteSystem separate bunker hygiene
  Description:   Hygiene resets; two meters never sync.
  Suggested Fix: Save Hygiene; single source or sync Waste→Needs.

[SURV-005]  Severity: HIGH  |  Type: incomplete death
  Location:      NeedsSystem.ForceDeath vs EvaluateDeath
  Description:   ForceDeath skips personal-quest twin/pillar side effects.
  Suggested Fix: Shared death finalization.

[SURV-006]  Severity: HIGH  |  Type: dead code
  Location:      Survivor.FatigueDrainMultiplier
  Description:   Never applied in NeedsSystem.Tick fatigue.
  Suggested Fix: Multiply fatiguePerHour by multiplier.

[SHEL-001]  Severity: HIGH  |  Type: wrong units
  Location:      PowerNetwork.Rebalance shed loop
  Description:   Request uses GetConsumerWatts; shed subtracts raw Watts → over/under-shed.
  Suggested Fix: Subtract GetConsumerWatts consistently.

[SHEL-002]  Severity: HIGH  |  Type: dual power semantics
  Location:      PowerConsumer.IsPowered
  Description:   IsPowered requires Watts>0; free radio vs network draw disagree.
  Suggested Fix: Powered state owned by network after rebalance.

[SHEL-003]  Severity: HIGH  |  Type: save desync
  Location:      PowerNetwork.RestoreState Durability
  Description:   Durability 0 restores as 100 → broken gen becomes pristine.
  Suggested Fix: Persist 0; use >=0.

[SHEL-004]  Severity: HIGH  |  Type: dual shielding
  Location:      GetInteriorRadsPerHour vs MaterialShieldingSystem vs Shielding wrapper
  Description:   Ceiling materials / dual systems not stacked into dose.
  Suggested Fix: One multiplicative stack function.

[SHEL-005]  Severity: HIGH  |  Type: event gap
  Location:      PowerNetwork.Tick CO natural bleed
  Description:   OnCoChanged not fired on decay → stale HUD.
  Suggested Fix: Invoke when ppm changes.

[CMB-002]  Severity: HIGH  |  Type: event gap
  Location:      HatchDefense AssignGuard / ApplyMoraleToSurvivors
  Description:   Direct Needs writes; no death check on trauma fallback.
  Suggested Fix: Inject NeedsSystem.

[CMB-003]  Severity: HIGH  |  Type: save gap
  Location:      HatchDefenseSave
  Description:   Active guards not saved → security drop after load.
  Suggested Fix: Persist guard ids + bonuses.

[CMB-004]  Severity: HIGH  |  Type: silent fail
  Location:      HatchDefense RollTrauma string ids
  Description:   String literals vs AfflictionSO.Ids; mis-wire = no injury.
  Suggested Fix: Use Ids constants; assert inflict.

[ARCH-SAVE clothing]  Severity: HIGH  |  Type: save gap
  Location:      SaveSystem.Wiring SetClothingSystem / SetScrapWeaponSystem
  Description:   Only assigns field; no RegisterSystem — clothing/scrap state may reset.
  Suggested Fix: Register Capture/Restore or confirm state lives on survivors.

========================================================================================
SECTION C — MEDIUM SEVERITY
========================================================================================

[SAVE-013]  Severity: MEDIUM  |  Type: incomplete
  Location:      SaveSystem.Delete; Mode_IronMan.DeleteSave
  Description:   Delete leaves .bak; Iron Man permanent death recoverable via bak.
  Suggested Fix: Delete main+.bak+.tmp; Iron Man use full delete.

[SAVE-014]  Severity: MEDIUM  |  Type: bug
  Location:      Failed Continue + AutoSave
  Description:   Failed load starts fresh Running → autosave over good autosave.
  Suggested Fix: No AutoSave until player progresses after failed continue.

[SAVE-015]  Severity: MEDIUM  |  Type: design smell
  Location:      Dual-path RestIf + SubsystemSaveIds
  Description:   Large positional restore still present; risk of double-apply.
  Suggested Fix: V4 migrate fully to ISaveable lists; drop positional.

[SAVE-016]  Severity: MEDIUM  |  Type: design smell
  Location:      Restore type via live CaptureState()
  Description:   Side effects / null template silently skip restore.
  Suggested Fix: Register Type with adapter.

[SAVE-017]  Severity: MEDIUM  |  Type: non-deterministic RNG
  Location:      Many systems new Random(seed) at boot only
  Description:   RNG counters not restored; post-load rolls diverge.
  Suggested Fix: Persist RNG state or re-seed from (worldSeed, day, systemId).

[SAVE-018]  Severity: MEDIUM  |  Type: missing field
  Location:      GameState AccessibilitySafeMode
  Description:   Photosensitivity mode not saved.
  Suggested Fix: Add bool to GameStateSave.

[SAVE-019]  Severity: MEDIUM  |  Type: dual-path risk
  Location:      Special-path Set* without RegisterSystem
  Description:   Expedition/EventRunner/map special-path easy to break on refactor.
  Suggested Fix: Checklist + static analysis for Capture coverage.

[SAVE-020]  Severity: MEDIUM  |  Type: silent fail
  Location:      Mode_IronMan empty catch on delete
  Description:   save_deleted true even if file remains.
  Suggested Fix: Log; set flag only if gone.

[SAVE-021]  Severity: MEDIUM  |  Type: design smell
  Location:      VerifyChecksum re-serializes object
  Description:   Float formatting drift across Unity versions.
  Suggested Fix: Hash raw payload with checksum zeroed.

[SAVE-022]  Severity: MEDIUM  |  Type: docs lie
  Location:      SaveSystem.Register null
  Description:   Comment says unregister with null; code returns early.
  Suggested Fix: Implement Unregister(saveId) or fix docs.

[ECON-003]  Severity: MEDIUM  |  Type: unimplemented
  Location:      WorldItemDef Capacity/Fill/DrainPerUse; ScaleByFill
  Description:   Catalog + helpers only; no runtime drain or fill-scaled trade.
  Suggested Fix: Slot fill state + drain on use OR document discrete variants only.

[ECON-004]  Severity: MEDIUM  |  Type: economy logic
  Location:      GetTradeValue perk mults
  Description:   Auditor 0.55 and Ruthless sell mult apply to same base for buy and sell.
  Suggested Fix: Apply buy/sell mults only in GetBarterUnitValue(playerSelling).

[ECON-005]  Severity: MEDIUM  |  Type: consistency
  Location:      SumInventoryTradeValue
  Description:   Uses raw tradeValue not GetTradeValue → quest vs barter disagree.
  Suggested Fix: Sum GetTradeValue * amount.

[ECON-006]  Severity: MEDIUM  |  Type: classification
  Location:      Station Device vs InferTier SurvivalGear
  Description:   Professional benches mis-rank vs weapons under shortage (with ECON-001).
  Suggested Fix: Explicit Station tier in economy.

[INV-003]  Severity: MEDIUM  |  Type: save
  Location:      Inventory SlotSave
  Description:   Storage CurrentDurability not saved; only equipped.
  Suggested Fix: Add currentDurability to SlotSave.

[INV-004]  Severity: MEDIUM  |  Type: durability
  Location:      Inventory.Equip
  Description:   Always resets durability to full max from def.
  Suggested Fix: Copy from consumed slot.

[INV-006]  Severity: MEDIUM  |  Type: silent data loss
  Location:      Inventory.RestoreState
  Description:   Unknown item ids dropped with no log; missing ammo ids wipe stacks.
  Suggested Fix: Log + quarantine; CreateItemDefinition fallback.

[ITEM-004]  Severity: MEDIUM  |  Type: id fragmentation
  Location:      dosimeter vs personal_dosimeter
  Description:   Craft produces dosimeter; catalog has personal_dosimeter.
  Suggested Fix: Canonical id + alias map.

[ITEM-005]  Severity: MEDIUM  |  Type: misclassification
  Location:      Attachments Type=Tool TradeTier=Attachment
  Description:   Without ECON-001 fix, attachments price as UtilityTool.
  Suggested Fix: Economy reads TradeTier.

[CRAFT-001]  Severity: MEDIUM  |  Type: content gap
  Location:      Data/recipes.json ~16 vs 200+ catalog items
  Description:   Most expanded items have no craft recipes; legacy root has more.
  Suggested Fix: Add Data recipes or gate catalog content.

[CRAFT-002]  Severity: MEDIUM  |  Type: design smell
  Location:      recipes.json refuel_heater resultAmount 0
  Description:   Craft completes with no inventory effect; heater fuel not hooked.
  Suggested Fix: Dedicated refuel action or state-changing result.

[ARCH-003]  Severity: MEDIUM  |  Type: layering
  Location:      Core.asmdef references UI
  Description:   Domain recompiles on HUD changes; Core→UI calls.
  Suggested Fix: IHudBridge / Presentation.Contracts.

[ARCH-006]  Severity: MEDIUM  |  Type: latent systems
  Location:      GameBootstrap.CoreFamilies Boot* families
  Description:   Dozens of Action/Affliction/Item constructed for save completeness without live ticks.
  Suggested Fix: Catalog live | event-only | save-only | orphan.

[BOOT-002]  Severity: MEDIUM  |  Type: order risk
  Location:      InitializeSystems multi-phase
  Description:   Load-bearing order; new systems miss save/registry easily.
  Suggested Fix: ISystemModule Construct/Wire/RegisterTicks/RegisterSave/Dispose.

[BOOT-003]  Severity: MEDIUM  |  Type: perf
  Location:      Update every frame HUD+Twitch+Flashpoint+TickFrame
  Description:   Heavy per-frame work.
  Suggested Fix: Event-driven HUD; budget strips.

[EVENT-001]  Severity: MEDIUM  |  Type: architecture
  Location:      EventBus sparse vs instance events
  Description:   AGENTS.md events-on-change only partially met; UI often polled.
  Suggested Fix: Document dual model; domain events for UI.

[EVENT-004]  Severity: MEDIUM  |  Type: missing events
  Location:      TemperatureSystem
  Description:   Only OnSeasonChanged; indoor temp polled.
  Suggested Fix: Raise when rounded °C changes.

[EVENT-005]  Severity: MEDIUM  |  Type: missing events
  Location:      WaterEconomySystem
  Description:   Catchment/purifier silent.
  Suggested Fix: OnWaterCollected / OnFilterDepleted.

[PERF-002]  Severity: MEDIUM  |  Type: GC
  Location:      BunkerSocialSystems / PersonalQuest substeps
  Description:   new List alive filters daily; some new Random in substeps.
  Suggested Fix: Reuse buffers; never new Random in hot path.

[PERF-003]  Severity: MEDIUM  |  Type: HUD
  Location:      Lifecycle Update shelter/radio push
  Description:   Full refresh every frame.
  Suggested Fix: Dirty flags.

[DUP-001]  Severity: MEDIUM  |  Type: duplication
  Location:      HeaterModule + WoodStove + Thermostat
  Description:   Multiple heat sources without HeatBudget abstraction.
  Suggested Fix: IHeatSource applied in TemperatureSystem.

[DUP-002]  Severity: MEDIUM  |  Type: duplication
  Location:      WeatherScapegoatSystem vs Dynamic_Scapegoat
  Description:   Overlapping blame systems.
  Suggested Fix: Merge or exclusive triggers.

[DUP-006]  Severity: MEDIUM  |  Type: duplication
  Location:      Air filter wear multi writers
  Description:   Module Tick + AirFiltration + FalloutStorm + raids.
  Suggested Fix: Single AirFilterService.Tick(wearRate).

[MED-009]  Severity: MEDIUM  |  Type: double-count
  Location:      MedicalSystem coma + neglect
  Description:   Coma needs + neglect can stack to ~10/hr + normal Needs if still registered.
  Suggested Fix: Single coma need path.

[MED-010]  Severity: MEDIUM  |  Type: state race
  Location:      CompleteTreatment
  Description:   Only Sick→Idle; curing coma leaves Incapacitated.
  Suggested Fix: Reset state when coma cured / list empty.

[MED-011]  Severity: MEDIUM  |  Type: double mult
  Location:      EffectiveLethality
  Description:   LatentDamage and ImmuneCollapse both ×3 → up to 9×.
  Suggested Fix: Cap or use max.

[MED-012]  Severity: MEDIUM  |  Type: incomplete
  Location:      BloodTransfusionSystem
  Description:   Heals health; does not clear blood_loss affliction.
  Suggested Fix: Cure affliction on compatible transfusion.

[MED-013]  Severity: MEDIUM  |  Type: id gap
  Location:      Amputee disability id
  Description:   amputee not in DisabilitySO.Ids catalog.
  Suggested Fix: Add to Ids + catalog.

[MED-014]  Severity: MEDIUM  |  Type: missing path
  Location:      AnaphylacticShockLethalHours unused
  Description:   12h timer constant never implemented.
  Suggested Fix: Register progressing affliction.

[RAD-011]  Severity: MEDIUM  |  Type: dead code
  Location:      DecontaminationStation.cs
  Description:   Stub unwired to inventory/scene.
  Suggested Fix: Bind module + items.

[RAD-012]  Severity: MEDIUM  |  Type: dual systems
  Location:      Cataracts multi models
  Description:   AfflictionPipeline vs Mutagenesis stage 2 diverge.
  Suggested Fix: One chronic pipeline.

[SURV-007]  Severity: MEDIUM  |  Type: pause
  Location:      NeedsSystem no IsPaused
  Description:   Unlike Radiation; selective pause inconsistency.
  Suggested Fix: Share GameState pause.

[SURV-008]  Severity: MEDIUM  |  Type: clamp
  Location:      Modify delta==0 early-out
  Description:   Cannot re-clamp post-load without force API.
  Suggested Fix: ClampNeed / force flag.

[SHEL-006]  Severity: MEDIUM  |  Type: dual power
  Location:      Module fuel vs PowerNetwork shed
  Description:   Heater fueled but IsEnabled false after shed.
  Suggested Fix: Operational = powered && fueled && healthy.

[SHEL-007]  Severity: MEDIUM  |  Type: event spam
  Location:      OnPowerStateChanged every Rebalance
  Description:   Fires even if unchanged.
  Suggested Fix: Fire only on material change.

[SHEL-008]  Severity: MEDIUM  |  Type: edge
  Location:      Unfiltered air +5 rad/hr always
  Description:   Even if exterior rads 0 pre-exchange.
  Suggested Fix: Scale by exterior or phase.

[CMB-005]  Severity: MEDIUM  |  Type: balance
  Location:      ResolveRaid DefenseScore > RaidStrength
  Description:   Equal scores always breach.
  Suggested Fix: >= or seeded tie break.

[CMB-006]  Severity: MEDIUM  |  Type: timer
  Location:      noiseCheckAccumulator = 0
  Description:   Loses remainder hours.
  Suggested Fix: Subtract interval.

[CMB-007]  Severity: MEDIUM  |  Type: save
  Location:      raid chance overrides
  Description:   Not in HatchDefenseSave.
  Suggested Fix: Persist override map.

[SAVE-stub]  Severity: MEDIUM  |  Type: false save-safety
  Location:      WaterEconomy/FalloutStorm empty CaptureState
  Description:   Registered no-op DTOs hide incomplete state.
  Suggested Fix: Capture real fields or omit registration.

========================================================================================
SECTION D — LOW / NIT
========================================================================================

[SAVE-023]  Severity: Nit
  Description / Fix: Ghost TimeSystemSys second clock — delete.

[SAVE-024]  Severity: Low
  Description / Fix: Workbench empty ISaveable noise — drop until state exists.

[SAVE-025]  Severity: Low
  Description / Fix: Unknown SaveIds silently skipped — Editor warn.

[SAVE-026]  Severity: Low
  Description / Fix: PendingGameLoad.Difficulty never consumed.

[SAVE-027]  Severity: Low
  Description / Fix: SaveCollectionHelpers CaptureDict key order — sort keys.

[SAVE-028]  Severity: Low
  Description / Fix: ListSlots no metadata for UI.

[SAVE-029]  Severity: Low
  Description / Fix: FailFastRestore false until bootstrap sets — set in ctor.

[SAVE-030]  Severity: Low
  Description / Fix: Double autosave if SaveSystem replaced without Dispose.

[SAVE-031]  Severity: Nit
  Description / Fix: Duplicate GameState write in Save.

[SAVE-032]  Severity: Nit
  Description / Fix: Empty region stubs in SaveSystem.cs.

[SAVE-033]  Severity: Nit
  Description / Fix: BakPath helper vs inline finalPath+".bak" drift.

[SAVE-034]  Severity: Nit
  Description / Fix: JSON string "null" for null CaptureState.

[SAVE-035]  Severity: Nit
  Description / Fix: Clothing/Scrap Set* look save-wired but aren't.

[SAVE-036]  Severity: Nit
  Description / Fix: Migration only to V3; no future field-drop schema.

[ECON-007]  Severity: Low
  Description / Fix: Three trade resolve paths (TradeEconomy / Item_TradeValues / DynamicEconomy) — single public API.

[ECON-008]  Severity: Low
  Description / Fix: Trust/Aggression digits in trade strip — qualitative language.

[INV-005]  Severity: Low
  Description / Fix: stackMax<=0 defaults to 99 — fail import validation.

[ITEM-006]  Severity: Low
  Description / Fix: CreateItemDefinition omits Capacity/Fill/TradeTier.

[ITEM-007]  Severity: Low
  Description / Fix: Legacy camelCase station names in root JSON.

[ARCH-004]  Severity: Low
  Description / Fix: autoReferenced:false on all asmdefs — document load graph.

[EVENT-006]  Severity: Low
  Description / Fix: Clothing only OnRagged/OnRepaired.

[EVENT-007]  Severity: Low
  Description / Fix: EventRunner vs EventBus naming collision — rename bus.

[PERF-004]  Severity: Low
  Description / Fix: EventBus ToArray on subscriber change — acceptable.

[PERF-005]  Severity: Low
  Description / Fix: AIContext fill still walks many refs — split static/per-survivor.

[DUP-004]  Severity: Low
  Description / Fix: TradeEconomy vs DynamicEconomy intentional layering — document.

[DUP-005]  Severity: Low
  Description / Fix: Radiation layered calculators; outdoor rad hardcode 15f in ecosystem.

[DUP-007]  Severity: Low
  Description / Fix: Black rain / fallout storm weather vs hazard systems overlap.

[MED-015]  Severity: Low
  Description / Fix: NeedsCare at 50% of neglect interval — document soft/hard.

[RAD-014]  Severity: Low
  Description / Fix: Radiotrophic OnDoseChanged without dose change.

[SURV-009]  Severity: Low
  Description / Fix: hungerCritical=100 only at full bar.

[SHEL-009]  Severity: Low
  Description / Fix: AirFiltration unbound fallback dual path.

[CMB-008]  Severity: Low
  Description / Fix: Weapon defense * stack Amount linear scale.

[CMB-009]  Severity: Low
  Description / Fix: Host must unsubscribe system events; no WeakEvent.

[CS0067]  Severity: Nit
  Description / Fix: MutatedEcosystemSystem pragma unused public events.

[CS0414]  Severity: Nit
  Description / Fix: GhostStationSystem pragma unused state field.

[DEBUG-001]  Severity: Low
  Description / Fix: ~800 Debug.Log* in runtime assemblies — gate behind ASHFALL_DEBUG / GameLog.

[EVENT-SUB]  Severity: Medium-Low
  Description / Fix: Subscribe += ~633 vs -= ~98 — systematic leak risk on scene reload.

[AUDIO-001]  Severity: Low
  Description / Fix: Assets/_Game/Audio only Radio stubs — audio pipeline thin vs AudioEventBus.

[PRAGMA-001]  Severity: Nit
  Description / Fix: Few #pragma disables hide unused events/fields.

========================================================================================
SECTION E — DUPLICATED FEATURES (inventory)
========================================================================================

  • 1. Heat production: HeaterModuleSO + ShelterModule_WoodStove + Thermostat — no unified IHeatSource.
  • 2. Scapegoating: WeatherScapegoatSystem vs Dynamic_Scapegoat.
  • 3. Item data trees: StreamingAssets/Data/items.json + StreamingAssets/items.json (legacy schema).
  • 4. Recipe trees: Data/recipes.json + root recipes.json (different schemas/ids).
  • 5. Air filter wear: Module Tick + AirFiltration helper + FalloutStorm multiplier + raid damage.
  • 6. Trade value resolution: TradeEconomy phase + Item_TradeValues ladder + DynamicEconomy GetTradeValue + catalog ResolveTradeValue (unused live).
  • 7. Water purification: WaterEconomy purifier + water_filter items + AI RepairFilter + workbench paths.
  • 8. Radiation pipelines: RadiationSystem dose + AfflictionPipeline (dead) + PrognosisPipeline + Mutagenesis + weather hazards.
  • 9. Radio stack: RadioBroadcast + RadioTuner + HamRadio + FactionRadioIntercepts + GhostStation.
  • 10. Hygiene: Needs.Hygiene vs WasteSystem bunker hygiene.
  • 11. Shielding: Shelter.GetInteriorRadsPerHour + MaterialShieldingSystem + legacy Shielding attenuation.
  • 12. Tick paths: SystemRegistry.TickAll vs GameBootstrap fallback Tick* methods.
  • 13. AutoSave: SaveSystem.OnPhaseChanged + Bootstrap phase handler.
  • 14. Clock: TimeSystem + TemperatureSystem.TotalElapsedHours + GameState.Day + ghost TimeSystemSys.
  • 15. Dosimeter ids: dosimeter vs personal_dosimeter.

========================================================================================
SECTION F — MISSING / LACKING FEATURES & DATA-NOT-LOGIC
========================================================================================

AGENTS.md domain gaps / weak spots:
  • Ambient radiation effectively OFF (RAD-001) — domain promise broken
  • Warmth/cold effectively OFF (SURV-001)
  • No single heat budget abstraction
  • Outdoor ambient rad hardcoded 15f in ecosystem path (fidelity)
  • EventBus not primary state-change bus (instance events partial)
  • Capacity/DrainPerUse catalog fields without consume simulation
  • Expanded world catalog (~184 items) with ~16 Data recipes only
  • PendingGameLoad.Difficulty discarded
  • DecontaminationStation stub unwired
  • AfflictionPipeline dead (never bootstrapped)
  • Many Boot* prompt systems constructed but not live-ticked
  • Audio domain underdeveloped vs declared AudioEventBus

Data-only / incomplete craft-consume (examples):
  • water_purification_tablets_* Capacity/Drain 2 — discrete ids only, no drain API
  • package_rolled_oats / dry_rice / dried_pasta / soy_and_rice_milk drains — data only
  • antiseptic_1l, rope_2m, copper_wire_10m, jetfuel_jerrycan — capacity fields, no consume
  • ceramic_water_filter / respirator_filter variants — may not alias RepairFilter ids
  • refuel_heater recipe resultAmount 0
  • Workbench ISaveable empty DTO

Save/load lacking (summary):
  • Time clock, GameState.Day, flashpoint expedition fields, social affinity,
  • Active hatch guards, storage durability, module fuel mults, clothing/scrap wiring,
  • RNG streams, AccessibilitySafeMode, raid chance overrides, generator noise sticky state

========================================================================================
SECTION G — GAME CODE OPTIMISATION OPPORTUNITIES
========================================================================================

  [OPT-001] (see PERF-001) Cap game-hour substeps (e.g. 16–32) and catch up over frames.
  [OPT-002] (see PERF-002/003) Eliminate per-tick List allocations in social/AI; dirty-flag HUD.
  [OPT-003] (see DEBUG-001) Route all logs through GameLog; strip with ASHFALL_DEBUG.
  [OPT-004] (see ARCH-001) Split GameBootstrap; reduce Core→UI dependency.
  [OPT-005] (see ARCH-002) Single tick path via SystemRegistry only.
  [OPT-006] (see DUP-003) Delete legacy StreamingAssets items/recipes; one importer pipeline.
  [OPT-007] (see ECON-007) One GetTradeValue API; remove InferTier-only path.
  [OPT-008] (see EVENT-SUB) Subscription registry with auto-unsub on dispose.
  [OPT-009] (see ARCH-006) Do not construct orphan prompt systems at boot.
  [OPT-010] (see JsonUtility) Prefer sorted keys; consider Newtonsoft/MessagePack for large saves if size/speed hurts.
  [OPT-011] (see PersonalQuestSystem) 8.5k-line god class — split by era (CivilWar/Ashes/Chemistry already partials; extract services).
  [OPT-012] (see SaveSystem.Wiring) Auto-generate Set* registration from attribute to stop missing-save bugs.
  [OPT-013] (see PowerNetwork) Fire OnPowerStateChanged only on change (SHEL-007).
  [OPT-014] (see AI evaluate) Cache static AIContext slices; evaluate survivors in waves (already partial).
  [OPT-015] (see items.json size) 316 entries OK; keep catalog generator single source to avoid dual edit cost.

========================================================================================
SECTION H — PRIORITY REMEDIATION ORDER
========================================================================================

  Wave 1 — Domain broken (player survival false):
    RAD-001, RAD-002, RAD-003, RAD-004, SURV-001, CMB-001
  Wave 2 — Save integrity (campaign destruction):
    SAVE-001, SAVE-002, SAVE-003, SAVE-004, SAVE-005, SAVE-006, SAVE-007, SAVE-008
  Wave 3 — Item loss / economy honesty:
    INV-001, INV-002, ECON-001, ECON-002, ITEM-001, ITEM-002, ITEM-003
  Wave 4 — Medical correctness:
    MED-001, MED-002, MED-003, MED-004, MED-006, MED-007
  Wave 5 — Architecture / perf:
    ARCH-002, PERF-001, BOOT-001, ARCH-001 (long), DUP-003
  Wave 6 — Completeness polish:
    Remaining Medium/Low, craft drains, recipe expansion

========================================================================================
SECTION I — COUNTS
========================================================================================

  Critical findings listed: 11
  High findings listed:     ~55
  Medium findings listed:   ~55
  Low/Nit findings listed:  ~40
  Duplicated feature groups: 15
  Optimisation opportunities: 15
  Approximate total discrete issues documented: 170+

  Note: Exhaustive line-by-line of all 896 files is not claimed.
  Coverage prioritised: save/load, economy/inventory, bootstrap/architecture,
  radiation/needs/medical/shelter/hatch, data dualism, AGENTS.md domain.
  Systems not deeply opened (e.g. every Action_* / Affliction_* prompt file)
  may contain additional silent stubs — treat ARCH-006 latent catalog as a
  continuing audit surface.

========================================================================================
SECTION J — WHAT IS SOLID (context)
========================================================================================

  • Atomic save write File.Replace + .bak rotation well designed
  • Save version refuse for newer builds
  • ISaveable registry direction (H-4) correct long-term
  • FailFastRestore policy exists for Editor/dev
  • SystemRegistry per-substep + daily ticks with try/catch isolation
  • Inventory events OnItemAdded/Removed/Changed present
  • Fair trade rollback on half-failed faction transfer
  • FormatWorthLabel digit-free API exists (UI not fully wired)
  • Utility AI (not LLM) present per AGENTS.md
  • Asmdef graph has no cycles (Core→UI layering smell only)
  • Water storage dual-path order consciously documented
  • Large EditMode suite exists (1600+ test cases in assembly)

========================================================================================
END OF AUDIT REPORT
Next recommended prompt: "Implement Wave 1 domain fixes (RAD-001..004, SURV-001, CMB-001)
with EditMode tests; do not touch UI chrome."
========================================================================================


========================================================================================
PART 2 — DEEPER INVESTIGATION (follow-up pass)
========================================================================================
Generated as continuation of "investigate deeper". Call-chain verification,
orphan-system census, death-path proof. Still read-only.

--------------------------------------------------------------------------------
P2-A. CRITICAL PATH PROOFS (end-to-end)
--------------------------------------------------------------------------------

[DEEP-001] Needs never kill from hunger/thirst/cold
  Severity: CRITICAL | Type: domain break / intentional-but-unfinished redesign
  Location: Assets/_Game/Survivors/NeedsSystem.cs Tick ~164-171; EvaluateDeath ~402-415
            Assets/_Game/Survivors/NeedsProfile.cs healthLossFromHunger/Thirst/Cold (DEAD)
  Call chain:
    TickFrame → TimeSystem.TickHours → TickSystems → registry → NeedsSystem.Tick
    → critical flags set → Modify(Morale only) → NO Health write → EvaluateDeath never
      reached from needs alone.
  Death only if: MedicalSystem.ApplyHealthDrain → NeedsSystem.Modify(Health) OR ForceDeath
      OR Acute radiation Expose health drain OR direct writers (often skip EvaluateDeath).
  Dead config: NeedsProfile.healthLossFromHunger=3, FromThirst=4, FromCold=2 never read.
  Critical thresholds: hunger/thirst at 100 (full bar only), warmth ≤ 10.
  Tutorial LIE: Assets/_Game/UI/TutorialOverlay.cs claims critical needs drain health.
  Player impact: Starvation/dehydration/freezing are non-lethal; survivors immortal by needs.
  Suggested fix: Restore profile health loss OR register starvation/dehydration/hypothermia
    afflictions in CreateDefaultAfflictions and inflict at critical thresholds.
  Evidence quote:
    "Critical hunger/thirst/cold still punish morale ... but no longer write Health here"

[DEEP-002] Warmth always near-heat; cold path dead; no rebind API
  Severity: CRITICAL | Type: wrong bootstrap wiring
  Location: GameBootstrap.InitFoundation.cs ~171
            NeedsSystem.ApplyWarmth ~385-389
  Call chain:
    new NeedsSystem(profile, sv => true) — Func is private readonly; NO SetIsNearHeat later.
    Grep of all GameBootstrap*: only this construction. Never rebound.
  TemperatureSystem.GetIndoorTemperature EXISTS (AmbientCelsius + IndoorTempBonus) but is
    NOT fed into NeedsSystem heat hook. AI WarmUpActionSO/RefuelHeaterActionSO act on heater
    fuel and set Warmth directly (+40) — patching around the broken Tick path.
  Player impact: Nuclear-winter cold never drains warmth via Tick; heater is optional flavor.
  Suggested fix:
    isNearHeat = (sv) => {
      float t = TemperatureSystem.GetIndoorTemperature(Shelter);
      // or heater operational + Fuel > 0 or expedition outdoor false
      return t >= 12f; // or module-based
    };

[DEEP-003] Ambient radiation fully off; ExposureContext never built
  Severity: CRITICAL | Type: missing domain hazard
  Location: InitFoundation.cs ~182; RadiationSystem.Tick ~115-138; ExposureContext.cs
  Call chain:
    new RadiationSystem(NeedsSystem) → _exposureContext=null
    Tick: context=null → zone=0 → gear=null → Expose(0) → dosimeter records 0
    IsPaused starts true until nuclear exchange — even after unpause ambient still 0.
  GetInteriorRadsPerHour(Shelter.Ops) IS implemented (shielding + air filter leak +5 unfiltered)
    but ONLY reachable if ExposureContext.Shelter is set (reflection binds ShelterRadQuery).
    Zero production callers construct ExposureContext for Tick. GetIndoorRadContribution()
    on ShelterRoom has ZERO callers.
  BunkerContamination: AddBunkerContamination called from ExpeditionSystem.Flashpoint and
    PetSystem; saved/restored; NEVER added in GetInteriorRadsPerHour (RAD-002 confirmed).
  MaterialShieldingSystem.GetRadiationBleed: weakestAttenuation init 0 → Min always 0 →
    full exterior bleed formula broken; also never called from interior rad path (RAD-004).
  Ambient CAN kill only via: Expose() from expeditions, drink irradiated, items, flashpoint
    dilemmas, scav missions — not ambient Tick.
  Player impact: Fallout/bunker ambient rad fantasy disabled; ARS only from scripted expose.
  Suggested fix (minimal bootstrap lambda):
    RadiationSystem = new RadiationSystem(NeedsSystem, sv => {
      var ctx = new ExposureContext {
        ZoneRadLevel = ResolveZoneRad(sv), // map node / weather
        Shelter = Shelter, // binds GetInteriorRadsPerHour via reflection
        WornGear = BuildWornFromInventory(sv)
      };
      return ctx;
    }, new System.Random(worldSeed ^ 0xRAD));
    Plus: GetInteriorRadsPerHour += BunkerContamination; fix MaterialShielding init=1f.

[DEEP-004] TimeSystem vs TemperatureSystem clock (deepened SAVE-001)
  Severity: CRITICAL | Type: save desync / dual clocks
  Location: Lifecycle.TickFrame ~209-217; SaveSystem.Capture.cs ElapsedHours;
            SaveSystem.Restore.cs SetElapsedHours on Temperature only; TimeSystem.CaptureState empty
  Live play: TickFrame does TimeSystem.TickHours(step) THEN TickSystems(step) which ticks
    TemperatureSystem.Tick(step). Starting both at 0 they STAY in sync during a session.
  On load: Temperature restored to saved ElapsedHours; TimeSystem stays at new-game day 1
    hour 0. Immediate permanent desync: season/weather clock mid-campaign, day-gated systems
    (WorldPhase, flashpoint, raids, debt) think day 1.
  Ghost: TimeSystemSys second instance in CoreFamilies (SAVE-023).
  GameState.Day rarely written (SAVE-010).
  Suggested fix: Capture TimeSystem.TotalElapsedHours (or day+hour); restore both Time and
    Temperature from same value; delete TimeSystemSys; sync GameState.Day on day tick.

[DEEP-005] Generator noise latch (CMB-001 deepened)
  Severity: HIGH | Type: state machine
  Location: HatchDefenseSystem.SyncGeneratorNoise ~1162-1175; Tick ~1190-1194
  IsOutdoorDieselRunning: diesel source, enabled, fuel>0, room in {outside,exterior,yard,surface}
  Sync: if outdoor → flag=true + noise≥0.85; else if flag still true → KEEP noise high
  NEVER assigns flag=false in runtime (only save restore can clear).
  Decay requires !flag && !outdoor diesel — unreachable after first outdoor run.
  Player impact: Permanent elevated post-D30 noise raid pressure.
  Suggested fix: GeneratorRunningOutside = outdoor; (drop sticky manual branch or separate ManualNoise flag)

[DEEP-006] Amputation does not cure sepsis (MED-002 proven)
  Severity: CRITICAL | Type: docs lie / incomplete path
  Location: AmputationSystem.PerformAmputation ~101-158
  Docs: "Cures Sepsis but adds amputee disability"
  Body: consume tools/morphine, -30 HP-ish, amputee disability, RecordAmputation, event.
    NO MedicalSystem.Cure, NO affliction list remove.
  Sepsis continues at healthDrain 8 * lethality 1.5 = 12 HP/hr → surgery can make death faster.
  PhantomPain Inflict uses missing default affliction (DEEP-007).

[DEEP-007] Affliction registry incomplete vs callers
  Severity: HIGH | Type: silent no-op Inflict
  CreateDefaultAfflictions registers: bleeding, gunshot, broken_bone, laceration, dysentery,
    bacterial_infection, sepsis, rad_burns, immune_collapse, heavy_metal_poisoning, blood_loss,
    coma, botulism, poison_ingestion.
  MISSING but called/constants exist: anaphylactic_shock, phantom_pain, scurvy, toothache,
    hair_loss, cellular_breakdown.
  StructuralIntegrity crushes with "crushed_limb" — NOT in defaults → Inflict no-op.
  Hatch trauma broken_bone/gunshot_wound ARE registered — hatch death clock WORKS (DEEP-008).

[DEEP-008] Hatch trauma IS wired (corrects partial prior audit tone)
  Severity: INFO | Type: working path
  Location: GameBootstrap.InitWorld.Diary.cs ~131-136
    inflictTrauma: (sv, affId) => MedicalSystem?.Inflict(sv, affId)
  RollTrauma uses broken_bone | gunshot_wound → real affliction drain → sepsis chain → death.
  Fallback (if null) floors HP at 1 — does not kill.

[DEEP-009] Direct Health writers skip EvaluateDeath (zombie HP)
  Severity: HIGH | Type: death pipeline bypass
  Systems writing Needs.Health directly (not via Modify):
    AddictionSystem withdrawal 0.5/hr
    RadiationMutagenesisSystem cellular breakdown
    LightSystemHelper vit-D path
    Amputation surgery costs
    Hatch trauma fallback
    Various PQ/structural
  Can reach Health=0 while State stays Alive; OnDied never fires.
  Suggested fix: all HP damage through NeedsSystem.Modify.

[DEEP-010] ExposureContext uses reflection for Shelter
  Severity: LOW-MEDIUM | Type: design smell / perf
  Location: ExposureContext.Shelter setter uses GetMethod + Invoke each set.
  Fine for rare construction; if context rebuilt every Tick, allocate/reflect each survivor.
  Prefer direct Func: zone => Shelter.GetInteriorRadsPerHour(zone).

--------------------------------------------------------------------------------
P2-B. ORPHAN / LATENT SYSTEM CENSUS
--------------------------------------------------------------------------------

Architecture layers:
  1) Boot*Families construct ~350+ system instances
  2) SaveSystem.Set* (~560 Set methods) register many for CaptureState completeness
  3) SystemRegistry actually ticks ~80-100 live systems
  4) EventRunner uses GameEvent SO pool — PARALLEL to Event_* C# classes
  5) Utility AI uses ActionSO assets — PARALLEL to Core Action_* classes

[ORPHAN-001] Mass latent content (~245/248 prompt-class Core files bootstrap-only)
  Severity: HIGH | Type: dead content / false completeness
  Class-name usage outside bootstrap+save+tests ≈ 0 for almost all Action_*, Affliction_*,
    Event_*, Item_* (except Item_AmmoTypes static, Item_WorldCatalog, rare Item_MutantGland).
  Approximate families with zero host execute path:
    Action_* (~41) — Execute* never called from AI/UI
    Affliction_* tracker classes (~21) — parallel to Medical affliction SOs
    Event_* trackers (~27) — EventRunner never invokes them
    MapAnomaly_* (~18) — expedition never Enter/Traverse
    Encounter_* (~15) — wire Debug.Log only
    MapHazard_* 8 of 10 unticked (only AcidGeyser + Ashlanche hourly)
    Siege_* Start* never called from host
    Victory_* most never Start* (LoneSurvivor daily check only; Broadcast/Airlift if active)
    NPC_*, Visitor_*, Biome_*, Trait_*, UIEvent_*, Vehicle_*, Project_*, Skirmish_*, …
  Diagnostics aliases map property names → snake ids that are NEVER Register*ed →
    C-1 unticked warnings are correct; alias bulk keys can also false-green systems.
  Player impact: Hundreds of "features" exist as save shells / Debug.Log wires only.
  Suggested fix: Catalog each system Live|EventOnly|SaveOnly|Orphan; quarantine Orphan;
    do not construct orphans at boot; promote vertical slices intentionally.

[ORPHAN-002] Dual narrative stacks
  Severity: HIGH | Type: duplication / dead path
  EventRunner + EventPoolBuilder + factories = live narrative
  Event_Brawl, Event_WitchHunt, Event_Schism, … = constructed, save-wired, never scheduled
  Same for Action_* vs AI/*ActionSO.

[ORPHAN-003] Dual scapegoat (confirmed live split)
  WeatherScapegoatSystem: registry-ticked "scapegoat"
  Dynamic_Scapegoat: BatchSystems daily SelectScapegoat — partial HALF live
  Overlap without shared state.

[STUB-001] CaptureState empty while runtime state exists (deepened list)
  TimeSystem (CRITICAL)
  WaterEconomySystem (ticked hourly but empty save — catchment/purifier lost)
  AshDriftSystem (drops _routes)
  CookingSystem (CookMeal never called AND empty save)
  NightScavengeSystem instance (statics used by Expedition; instance empty)
  FalloutStormHazard / BlackRainHazard
  BicycleSystem, MoralDilemma, VisitorRNG, many Affliction/Action stubs
  Ghost TimeSystemSys

[HALF-001] Batch DiseaseSystem_Expansion
  TickHour every hour but Infect/RegisterDisease never called → empty loop cost.

[HALF-002] Weather events
  Several hourly (DeepFreeze, SolarFlare, …); AcidSnow/GlassStorm/DustDevil explicitly
  left unticked in Weather.cs comments.

[HALF-003] Mode_IronMan
  NotifyIronManSurvivorDeath wired; EnableIronMan never called from host → mode never active.

[HALF-004] CookingSystem
  Constructed + perks bound; CookMeal never invoked from AI/UI.

--------------------------------------------------------------------------------
P2-C. SAVE / LOAD DEEP NOTES
--------------------------------------------------------------------------------

[SAVE-DEEP-001] Dual AutoSave (SAVE-004 proven interaction with TimeSystem)
  On Continue: load fails to restore TimeSystem; phase → Running fires AutoSave with
  hybrid state (Temperature mid-campaign + Day 1 clock) → can write corrupt autosave.

[SAVE-DEEP-002] Soft restore production default
  FailFastRestore false in players → missing subsystem JSON still "success".
  CaptureState exception omits subsystem but Save returns true.

[SAVE-DEEP-003] Clothing/Scrap Set without Register
  SetClothingSystem / SetScrapWeaponSystem assign fields only — no RegisterSystem.

[SAVE-DEEP-004] Expedition flashpoint fields
  Restore applies IsCommsSevered etc.; Capture never writes them (SAVE-002).

[SAVE-DEEP-005] Survivors by index
  RestoreSurvivorsAndShelter maps data.Survivors[i] → existing[i] by order not Id.

--------------------------------------------------------------------------------
P2-D. ECONOMY / INVENTORY DEEP NOTES
--------------------------------------------------------------------------------

[ECON-DEEP-001] InferTier drops catalog TradeTier (confirmed live path)
  DynamicEconomySystem.GetTradeValue always InferTier(item.type).
  Catalog ResolveTradeValue / FormatWorthLabel unused by barter.
  Stations Device→SurvivalGear shortage mult 1.80 vs Station 1.20
  Precious materials (tungsten) → BulkMaterial shortage UP not soft down.

[ECON-DEEP-002] Trade UI digits
  BuildQuoteSummary prints offer/ask :0.0 — GetWorthLabel unused.

[INV-DEEP-001] Add partial mutation
  Existing stacks filled + OnItemAdded; capacity full mid-loop returns false without rollback.
  Remainder lost if caller discards on false.

[INV-DEEP-002] Unequip full bag
  Remove from equipped then Add; ignore false → gear destroyed.

[DATA-DEEP-001] Dual StreamingAssets
  Data/items.json canonical (importer); root items.json legacy schema still ships.
  Core FMJ ammo often only in legacy/code catalog not Data/.

--------------------------------------------------------------------------------
P2-E. MEDICAL DEATH MATH (working path)
--------------------------------------------------------------------------------

Registered affliction lethality (EffectiveLethality multiplies drain):
  sepsis: 8 HP/hr * 1.5 = 12 HP/hr → ~8h from full 100 HP
  poison_ingestion: 6 * 1.4 ≈ 8.4 HP/hr
  botulism: 3.5 * 1.4
  coma neglect: +6 HP/hr after 6h without care
  gunshot 1.5 → progresses sepsis 48h if untreated
Hatch breach can start broken_bone (0.5) or gunshot (1.5) — real death clock.

Ambient rad AcuteThreshold 80: -5 HP/hr via Modify — ONLY if dose applied via Expose.

--------------------------------------------------------------------------------
P2-F. OPTIMISATION DEEPENING
--------------------------------------------------------------------------------

[OPT-DEEP-001] Orphan construction cost
  Booting 350+ systems + 560 Set wires + empty CaptureState each save:
  Awake/load/save cost for dead content. Quarantine reduces boot memory and save size.

[OPT-DEEP-002] TickFrame max 128 substeps
  Each step: TimeSystem + full TickAll registry + AI wave + events.
  After hitch + 3x scale: multi-frame hitch storms. Cap 16-32 + carry pending.

[OPT-DEEP-003] ExposureContext reflection
  If wired naively every survivor every hour: MethodInfo.Invoke GC/cost.
  Prefer non-reflection ShelterRadQuery lambda.

[OPT-DEEP-004] Event subscribe imbalance
  ~633 += vs ~98 -= still valid; PlayMode domain reload leaks amplify with orphan wires
  that only Debug.Log (still hold delegates).

[OPT-DEEP-005] Dual AI/Action systems
  Maintaining Action_* and ActionSO doubles content cost; pick one stack.

--------------------------------------------------------------------------------
P2-G. PLAYER FANTASY VS CODE (truth table)
--------------------------------------------------------------------------------

  Claim / Tutorial / Domain          | Reality in bootstrap
  -----------------------------------+------------------------------------------
  Cold kills / freezes you           | Warmth always regenerates (sv=>true)
  Fallout / bunker rad accumulates   | Ambient Tick = 0 rads (no exposureContext)
  Starve if no food                  | Morale only; Health untouched by needs
  Heater matters for survival        | Optional AI patch; Tick ignores heater
  Material ceiling upgrades help rad | Bleed formula always full exterior; unwired
  Contaminated bunker air doses you  | BunkerContamination saved but not in rad formula
  Amputation cures sepsis            | Does not; sepsis continues at 12 HP/hr
  Outdoor generator risky then safe  | Noise latches forever after first outdoor run
  Continue restores day/hour         | Temperature yes; TimeSystem clock no
  300+ prompt systems are features   | ~80 live; majority construct+save only
  Trade shows rough worth not numbers| Quote summary prints 0.0 floats
  Critical needs drain health        | Tutorial says yes; NeedsSystem says no

--------------------------------------------------------------------------------
P2-H. REVISED WAVE 1 (deeper priority)
--------------------------------------------------------------------------------

  W1a Domain survival truth:
    DEEP-002 warmth hook, DEEP-003 exposureContext + BunkerContamination + MaterialShielding,
    DEEP-001 needs health or starvation afflictions, DEEP-005 noise latch
  W1b Medical honesty:
    DEEP-006 amp cures sepsis, DEEP-007 register missing afflictions, DEEP-009 Modify-only HP
  W1c Save clock:
    DEEP-004 TimeSystem capture/restore + AutoSave single owner + suppress on load
  W1d Content honesty:
    ORPHAN-001 catalog Live vs Orphan; stop constructing orphans; pick vertical slices
  W1e Economy/UI:
    ECON-001 catalog tier, ECON-002 FormatWorthLabel in TradeScreenUI

--------------------------------------------------------------------------------
P2-I. METHOD LIMITS
--------------------------------------------------------------------------------

  - Static analysis + specialist explore agents; Unity batch/PlayMode not re-run this pass.
  - Orphan census uses type-name references; pure string-id reflection hosts (if any) may
    be undercounted — none found for Action/Event families.
  - Item_AmmoTypes / NightScavenge statics are live even when bootstrap instances are dead.
  - Some Batch systems are HALF-live under aggregate registry names.

========================================================================================
END OF PART 2 — DEEPER INVESTIGATION
========================================================================================


========================================================================================
PART 3 — EVEN DEEPER INVESTIGATION
========================================================================================
Third pass: live tick graph, inventory/AI consume chains, win/lose, flashpoint,
corrections to prior findings, Unity EditMode clock tests re-run.

--------------------------------------------------------------------------------
P3-A. CORRECTIONS TO EARLIER AUDIT FINDINGS
--------------------------------------------------------------------------------

[CORR-001] SAVE-001 / DEEP-004 TimeSystem CaptureState is FIXED in current source
  Severity: CORRECTION (was CRITICAL empty CaptureState)
  Prior audit claimed TimeSystem.CaptureState returns empty DTO. CURRENT CODE:
    TimeSystem.cs CaptureState writes day, hourAccumulator, totalElapsedSeconds
    RestoreState restores them (null/day<=0 leaves clock alone; no day-tick replay)
  Wiring: InitLate SaveSystem.SetTimeSystem(TimeSystem) registers live clock as
    "time_system". InitFoundation mirrors GameState.Day = TimeSystem.CurrentDay and
    TimeSystem.OnDayTick += day => GameState.Day = day.
  Unity EditMode GameClockPersistenceTests: 7/7 PASSED (this pass).
  Residual clock risks (still valid):
    - Temperature ElapsedHours still dual-source (Capture.cs from TemperatureSystem)
    - Photoperiod has own _totalElapsedHours (saved separately — good if both restored)
    - Older saves without time_system JSON still load day 1 until migration
    - Docs in Part1/Part2 of THIS file still describe pre-fix state — trust CORR-001
  Ghost TimeSystemSys: not found in current CoreFamilies grep (may already removed).

[CORR-002] Hatch trauma wiring is correct (reaffirmed DEEP-008)
  inflictTrauma → MedicalSystem.Inflict for broken_bone / gunshot_wound.

[CORR-003] Event_* trackers vs EventPoolBuilder are disjoint ID spaces
  Live narrative ids: emissary_*, child_found_in_ash, factory encounters (not event_*).
  Event_Brawl etc. use event_brawl-style ids — 0 overlap with EventPoolBuilder pool.
  Confirms dual narrative stacks (ORPHAN-002).

--------------------------------------------------------------------------------
P3-B. LIVE TICK GRAPH (what actually runs each game hour)
--------------------------------------------------------------------------------

Source: GameBootstrap.Registry.cs ~77 RegisterPerSubstep entries (+ DayGated wrappers).

  Environment: weather, temperature, photoperiod, hatch_entrapment, shelter,
    power_network, logic_gates, hatch_defense, atmosphere, corpses, pantry,
    structural_integrity, waste, vermin, jury_rig, freeze_pipe, tracker, dead_drops,
    hostage, propaganda, scapegoat, flooding, bilge_pumps_daily, carrion_birds_daily,
    batch_systems_hourly/daily, victory_paths_hourly/daily, map_hazards_hourly,
    weather_events_hourly/daily, perimeter_trap, noise, clothing, compost, pets

  Body/mind: needs, medical, mental_break, bunker_social, gossip_daily,
    skill_atrophy, skill_progression_daily, social_perks_daily, personal_quests_*,
    empath, survivor_diaries, spatial_psychology, hallucination, addiction,
    pheromone_masking, phantom_intruders, child, hatch_dilemma, parley_offer

  World ops: radiation, water_economy, black_rain, crafting, scavenging, expeditions,
    radio_tuner, mutagenesis_tick

  Daily-only subset: deserter, ecosystem, amputation, scurvy, graft_rejection,
    mutagenesis, hatch_visibility, house_to_bunker, system_wiring, chemistry_titles,
    rebuilders, fuel_decay

  Outside registry (host): TickAiWave, TickEventsAndJournal (EventRunner),
    TickHostSideEffects (Death's Door, economy rad notify), real-time Flashpoint/Twitch/Victory

  NOT in live tick: ~300 CoreFamilies Action/Event/NPC/Anomaly/Encounter/Siege systems
    (Part 2 ORPHAN census still valid).

--------------------------------------------------------------------------------
P3-C. NEW CRITICAL — INVENTORY / AI CONSUME CHAINS
--------------------------------------------------------------------------------

Hunger semantics (NeedsSystem): Hunger HIGH = more hungry; tick ADDS hungerPerHour.
  Critical when Hunger >= hungerCritical (default 100).

[DEEP3-INV-001] Player Consume INCREASES hunger/thirst (sign inverted)
  Severity: CRITICAL | Type: domain invert / player-path bug
  Location: Inventory.Consume ~560-566; GameBootstrap.ConsumeItem → Consume(..., NeedsSystem)
  Code: needs.Modify(Hunger, item.hungerRestore) with canned_food hungerRestore=+40
  Effect: player eat raises Hunger by 40 (worse starvation). Same for thirstRestore.
  AI EatActionSO: Consume(..., needs:null) then Hunger = Max(0, Hunger - restore) — CORRECT.
  Player impact: Manual eat/drink hurts needs; AI eating works; rad cleanse/iodine still apply.
  Suggested fix: Modify(Hunger, -Abs(hungerRestore)) and Thirst same; or store signed deltas
    and document convention; add EditMode test Consume_ReducesHunger.

[DEEP3-INV-003] AI DrinkActionSO free infinite water
  Severity: CRITICAL | Type: exploit
  Location: Assets/_Game/AI/Actions/DrinkActionSO.cs Execute
  Evaluate requires water in inventory; Execute only: Thirst = Max(0, Thirst-50); NO Remove/Consume.
  Player impact: One water bottle enables infinite AI drinking; contamination never applied.
  Suggested fix: Inventory.Consume(water, ...) or Remove after thirst apply.

[DEEP3-INV-004] AI TakeIodineActionSO burns stock without resistance
  Severity: CRITICAL | Type: missing system call
  Location: TakeIodineActionSO.Execute — Inventory.Remove only
  Never RadiationSystem.AdministerIodine (timer + resistance).
  Player Consume path DOES call AdministerIodine for ItemType.Iodine.
  Player impact: AI wastes iodine for zero rad protection.

[DEEP3-INV-005] AI UseAntiRadActionSO bypasses RadiationSystem
  Severity: HIGH | Type: perk/event skip
  Mutates RadiationDose directly; skips AdministerAntiRad (perks, OnDoseChanged).
  Hardcoded cleanse 30 vs item.radCleanse.

[DEEP3-INV-006] DecontaminationStation completely unwired
  Severity: HIGH | Type: dead stub
  Only tests construct it. DecontaminateActionSO: free RadiationDose -= 15 if module id
    "decon_station" exists — no water cost, no Contamination scrub, no residual floor.
  DeconStationModuleSO data unused by action.

[DEEP3-INV-007] Contamination class / Decay is test-only
  Severity: HIGH | Type: dual model dead half
  Contamination.Decay never production-ticked. ItemDefinition.contamination is static float.
  ShelterRoom.BringIntoRoom zero callers. BunkerContamination float decays separately.

[DEEP3-INV-008] Geiger BatteryDrainPerHour unused
  Severity: HIGH | Type: incomplete device
  Only survey drains battery (LocationScavengingSystem). Calibration drifts daily.

[DEEP3-INV-009] NightScavenge flashlight path never armed
  Severity: HIGH | Type: dead feature
  PrepareNightScavenge / TickFlashlight never called from Expedition start/tick.
  IsNightScavenge stays false → loot/risk multipliers always 1.0.

[DEEP3-INV-010] HasWorkingGeiger only "geiger_counter"
  Severity: HIGH | Type: id narrowness
  personal_dosimeter / dosimeter never count as working instrument for map/survey.

[DEEP3-INV-011] Capacity/Fill/DrainPerUse still catalog-only (reaffirmed)
[DEEP3-INV-012/013] Add partial + Unequip destroy gear (reaffirmed)
[DEEP3-INV-014] Transfer resets DeviceState to CreateDefault
[DEEP3-INV-015] Animalistic eat: Consume then spit — food wasted, contam still applied
[DEEP3-INV-016] ItemDefinition has no tradeTier field — barter InferTier only

Consume path map:
  PlayerInput → ConsumeItem → Inventory.Consume(+hunger BUG, contam Expose works)
  AI Eat → Consume(needs:null) → manual Hunger -= restore (OK) + contam
  AI Drink → Thirst -= 50 only (exploit)
  AI DrinkContaminated → WaterStorage pools + Expose (not inventory bottles)

--------------------------------------------------------------------------------
P3-D. WIN / LOSE — DUAL ENGINES, ONE FREEZES THE GAME
--------------------------------------------------------------------------------

[DEEP3-WIN-001] EndgameEngine terminal does NOT set IsGameOver
  Severity: CRITICAL | Type: incomplete win/lose wiring
  Location: CheckWinLose → EndgameEngine.Evaluate + VictoryProject.EvaluateLoss
  ApplyEndgame (IsGameOver, GamePhase.GameOver) only from VictoryProject.OnEndgameTriggered
    (InitFoundation wire).
  EndgameEngine.TriggerEndgame raises CampaignEndedEvent / OnCampaignEnded →
    MoralChronicleBridge UI only — sim keeps Running.
  Engine conditions that do NOT freeze:
    - Rescue extraction day >= 60 + ExtractionUnlocked
    - Self-sufficiency day >= 100 + hydro + 0 dead
    - Bunker structural collapse (filters at 0)
    - All dead via engine path (redundant with VictoryProject wipe)
  Working freeze: VictoryProject.EvaluateLoss (all dead) → OnEndgameTriggered → ApplyEndgame;
    VictoryProject.TickDay Rescued day >= 100; vehicle escape TryEscapeByVehicle.
  Suggested fix: EndgameEngine terminal → same ApplyEndgame path; or VictoryProject.Trigger
    from engine.

[DEEP3-WIN-002] Extraction day mismatch 60 vs 100
  Severity: HIGH | Type: dual thresholds
  EndgameEngine victory day 60; VictoryProject ChopperArrivalDay / Rescued day 100.
  At day 60: chronicle "win" but game continues; at 100 real freeze if still alive.

[DEEP3-WIN-003] Structural collapse defeat never freezes run
  Severity: HIGH | Type: incomplete
  Only EndgameEngine path — no ApplyEndgame.

[DEEP3-WIN-004] DaysSurvived / death screen
  VictoryProject death screen classifies hunger vs rad vs breakdown using WasHungerCritical
    and RadiationDose — but ambient rad/warmth bugs make rad/cold causes rare;
    wipe from medical/combat still works.

--------------------------------------------------------------------------------
P3-E. FLASHPOINT / RADIATION PAUSE DEEP
--------------------------------------------------------------------------------

[DEEP3-FP-001] Null choreographer save can re-run EMP
  Severity: HIGH | Type: restore hole
  RestoreState null + HasTriggeredExchange → started true, completed false, step -1
  → Tick re-runs from EMP (morale stack, weather re-force, intercept re-fire).
  V1 migration may null FlashpointChoreographer field.

[DEEP3-FP-002] Mid-step real-time delay not restored (A-10 documented)
  Severity: LOW | Type: known limitation

[DEEP3-FP-003] EMP effects on exchange (live)
  EMPEvent.ApplyGlobal(Inventory, Shelter, Radio) → devices broken, modules off, radio dead
  Morale hit; Weather Ashfall; RadiationSystem.IsPaused = false; Ghost stations unlock.

[DEEP3-FP-004] Unpause ≠ ambient dose
  Severity: CRITICAL domain (reaffirm DEEP-003)
  InitFoundation: IsPaused=true until exchange; after unpause still zone=0 without ExposureContext.
  ApplyPostExchangeFlags on load: IsPaused=false if HasTriggeredExchange — still no ambient.

[DEEP3-FP-005] Pre-exchange ambient permanently paused
  Even if ExposureContext were wired, IsPaused=true until nuclear exchange means
  CivilWar-phase ambient would still be zero until Flashpoint. Design may intend that;
  document explicitly if so.

--------------------------------------------------------------------------------
P3-F. PHOTOPERIOD / LIGHT (partially live)
--------------------------------------------------------------------------------

[DEEP3-PHOTO-001] Photoperiod wired only if _lightProfile inspector assigned
  Severity: MEDIUM
  InitFoundation: if (_lightProfile != null) NeedsSystem.SetPhotoPeriodSystem(...)
  Affects LightExposure / Listless / darkness morale — NOT hunger/thirst/warmth.

[DEEP3-PHOTO-002] Photoperiod clock independent but saved
  Own TotalElapsedHours; restored via SetPhotoPeriodSystem ISaveable / Photoperiod DTO.
  Live session ticks same gameHours as Time/Temperature → stays aligned if all start 0.

--------------------------------------------------------------------------------
P3-G. TESTS AS BUG DOCUMENTATION
--------------------------------------------------------------------------------

[DEEP3-TEST-001] GameClockPersistenceTests pins historical empty clock (now green 7/7)
  Comments are the best narrative of SAVE-001 pre-fix: day-40 load → day 1,
  GameState.Day forever 1, ghost idle TimeSystem under time_system key.

[DEEP3-TEST-002] Widespread test harness uses sv => true and null ExposureContext
  BeliefSmokeTest, SurvivalSmokeTest, PhotoperiodSmokeTest, UxoHazardTests, EventBusLifecycle,
  AuditP0, SafeHaven, GameClock harness — all encode production-like broken warmth/rad.
  Tests pass with broken production wiring; do not detect DEEP-002/003.

[DEEP3-TEST-003] No [Ignore] expected-fail tests found
  Broken production paths are not marked failing; they are invisible to CI.

[DEEP3-TEST-004] TimeSystemTests still lack CaptureState cases
  Only GameClockPersistenceTests guard clock persistence.

[DEEP3-TEST-005] FlashpointChoreographerTests cover save/EMP unpause unit-level
  Not full bootstrap mid-flash load hybrid.

--------------------------------------------------------------------------------
P3-H. RADIATION PAUSE + EXPOSE STILL-WORK PATHS
--------------------------------------------------------------------------------

Scripted Expose still works (dose can kill via AcuteThreshold -5 HP/hr):
  ExpeditionSystem.Tick/Start, LocationScavenging, Flashpoint dilemmas,
  Inventory.Consume contamination * 50, DrinkContaminatedWaterActionSO, Corpse bury dose.

Dosimeter read-model is RadiationSystem-side, not inventory personal_dosimeter item.
HasWorkingGeiger gates map/survey — inventory device geiger_counter only.

--------------------------------------------------------------------------------
P3-I. REVISED PRIORITY AFTER PART 3
--------------------------------------------------------------------------------

  Wave 0 — Player survival honesty (NEW highest):
    DEEP3-INV-001 Consume hunger/thirst sign
    DEEP3-INV-003 AI free drink
    DEEP3-INV-004 AI iodine no-op
    DEEP-002 warmth hook, DEEP-003 ExposureContext
    DEEP-001 needs health or starvation afflictions

  Wave 0b — Run actually ends:
    DEEP3-WIN-001 EndgameEngine → ApplyEndgame
    DEEP3-WIN-002 unify day 60/100 extraction constant
    DEEP3-WIN-003 structural collapse freeze

  Wave 1 — Noise / medical / amp (prior)
    DEEP-005 generator noise, DEEP-006 amp sepsis, DEEP-007 missing afflictions

  Wave 2 — Devices / night / decon
    DEEP3-INV-006..010

  Wave 3 — Orphans / dual stacks / economy tiers
    ORPHAN-001, ECON-001, dual StreamingAssets

  Wave 4 — Save residual
    Dual AutoSave, soft restore, expedition flashpoint capture fields,
    Temperature/Time dual ElapsedHours consistency tests

--------------------------------------------------------------------------------
P3-J. METHOD / EVIDENCE THIS PASS
--------------------------------------------------------------------------------

  - Re-read TimeSystem.cs Capture/Restore; InitFoundation GameState.Day mirror
  - Ran Unity EditMode GameClockPersistenceTests: PASSED 7/7
  - Traced Inventory.Consume vs EatActionSO vs DrinkActionSO vs ConsumeItem
  - Traced CheckWinLose / EndgameEngine / VictoryProject ApplyEndgame wire
  - Counted ~77 RegisterPerSubstep live tick names
  - Confirmed Event_* ids disjoint from EventPoolBuilder
  - Subagent deep dives: inventory/contamination, flashpoint/win/tests

  Still NOT exhaustive of every Action_*.cs body; orphan census remains structural.

========================================================================================
END OF PART 3 — EVEN DEEPER INVESTIGATION
========================================================================================

========================================================================================
PART 4 — CONTINUATION PASS (read-only)
Generated (UTC): 2026-08-07 23:29:21
Scope: re-verify prior Critical/High after recent WIP; deep-dive unticked baseline,
       SaveMap affliction/vehicle cluster, dual clocks, AI consume, endgame freeze,
       inventory equip, asset service, residual stub saves.
No code was modified in this pass.
========================================================================================

--------------------------------------------------------------------------------
P4-A. STATUS OF PRIOR CRITICAL / HIGH (re-verified against current tree)
--------------------------------------------------------------------------------

Legend: OPEN | PARTIAL | FIXED | REGRESSED

[SAVE-001] PARTIAL — TimeSystem.CaptureState/RestoreState now real (day, hourAccumulator,
  totalElapsedSeconds). SaveSystem.SetTimeSystem wires live GameBootstrap.TimeSystem as
  ISaveable key "time_system". GameState.Day mirrored via OnDayTick in InitFoundation.
  STILL OPEN residual:
  - SaveData.ElapsedHours still written ONLY from TemperatureSystem.TotalElapsedHours
    (SaveSystem.Capture.cs:58) and restored only onto TemperatureSystem
    (SaveSystem.Restore.cs:51). TimeSystem and Temperature can diverge if either path
    advances without the other or if ISaveable order differs from ElapsedHours.
  - RestoreGameStateCore sets _gameState.Day = data.GameState.Day BEFORE ISaveable
    TimeSystem restore; if those disagree, HUD day can flash wrong until OnDayTick.
  - Ghost TimeSystem under wrong registry key appears closed for the live clock;
    diagnostics still map "TimeSystem" aliases.

[SAVE-002] OPEN — CaptureExpeditionState still omits flashpoint fields:
  IsCommsSevered, FlashpointBehavior, OriginalEtaTicks, ShelterDelayTicksRemaining,
  ReturnSpeedMultiplier/Divisor. Restore still assigns them → mid-flashpoint load resets
  expedition flashpoint behavior. Verified Capture.Entities.cs:69–103 vs
  Restore.Expeditions.cs:86–91.

[SAVE-003] OPEN — Load still mutates survivors/shelter before fail-fast ISaveable path;
  non-transactional hybrid world on partial failure.

[SAVE-004] OPEN — Dual AutoSave:
  SaveSystem ctor still hooks GameState.OnPhaseChanged → AutoSave on Running
  (SaveSystem.cs OnPhaseChanged ~840).
  GameBootstrap.InitLate also AutoSaves on Running gated by _suppressAutoSave only on
  bootstrap's own handler. SaveSystem's handler ignores _suppressAutoSave → Continue
  can still clobber autosave.

[SAVE-005] OPEN in production — FailFastRestore defaults TRUE only in
  UNITY_EDITOR || DEVELOPMENT_BUILD; FALSE in release player builds
  (SaveSystem.DefaultFailFastRestoreForEnvironment). Soft hybrid load still the
  shipped player policy.

[SAVE-007] OPEN — Survivors restored by roster index (Restore.cs:79–84), not by Id.

[SAVE-008] OPEN — RestoreExpeditions does not clear ActiveExpeditions first; merges
  GetOrCreate by survivor only → phantom expeditions for survivors absent from save.

[SAVE-012] OPEN — ModuleLookup = id => null still in InitLate.cs:89.

[RAD-001] OPEN — RadiationSystem = new RadiationSystem(NeedsSystem) with no
  ExposureContext (InitFoundation.cs ~189).

[RAD-002] OPEN — GetInteriorRadsPerHour does not add BunkerContamination
  (Shelter.Ops.cs:11–40+; BunkerContamination is separate property).

[RAD-003] OPEN — ShelterRoom.GetIndoorRadContribution still no callers into exposure.

[RAD-004] OPEN — MaterialShieldingSystem.GetRadiationBleed still initializes
  weakestAttenuation = 0f; Min always 0 → full exterior bleed.

[SURV-001 / DEEP-002] OPEN — NeedsSystem(..., sv => true) still always near heat.

[MED-001] OPEN — ApplyStaminaCap still clamps Fatigue DOWN when Fatigue > cap
  (MedicalSystem.cs:604–610) despite docs claiming "floor" / never fully rested.
  With fatigueDrain raising Fatigue, this CEILINGS exhaustion (cannot get more tired)
  rather than flooring rest.

[MED-002] OPEN — PerformAmputation never removes sepsis affliction
  (AmputationSystem.cs:101–158); only disability + costs.

[CMB-001 / DEEP-005] OPEN — SyncGeneratorNoise only sets GeneratorRunningOutside=true
  when outdoor diesel runs; never clears to false (HatchDefenseSystem.cs:1162–1174).

[DEEP-001] OPEN — Critical hunger/thirst/warmth only morale-punish; explicitly no Health
  write (NeedsSystem.cs:164–171). Starvation/dehydration cannot kill via NeedsSystem.

[DEEP3-INV-001] OPEN — Inventory.Consume applies needs.Modify(Hunger, +hungerRestore).
  Hunger high = hungry; items.json hungerRestore is positive for food (canned_food=40).
  Player ConsumeItem path uses Consume(..., NeedsSystem) → food INCREASES hunger.
  AI EatActionSO intentionally passes needs=null then subtracts Hunger — AI correct,
  player/UI path inverted.

[DEEP3-INV-003] OPEN — DrinkActionSO.Execute sets Thirst -= 50 with no inventory Remove.

[DEEP3-INV-004] OPEN — TakeIodineActionSO removes pill, never calls RadiationSystem.

[DEEP3-INV-005] OPEN — UseAntiRadActionSO mutates RadiationDose / RadResistanceHours
  directly on Survivor, bypassing RadiationSystem.AdministerAntiRad.

[DEEP3-WIN-001] OPEN — EndgameEngine.Evaluate → TriggerEndgame raises CampaignEndedEvent
  but CheckWinLose never calls ApplyEndgame. Only VictoryProject.OnEndgameTriggered is
  wired to ApplyEndgame (InitFoundation). EndgameEngine terminal leaves IsGameOver false
  unless VictoryProject also ends.

[DEEP3-WIN-002] OPEN — EndgameEngine extraction day >= 60; VictoryProject.ChopperArrivalDay
  = 100. Dual victory calendars.

[DEEP3-WIN-003] OPEN — Structural collapse can set module FilterHealth=0 and
  EndgameEngine can terminal, but without ApplyEndgame wire the run may not freeze.

[INV-001] OPEN — Inventory.Add can return false mid-loop after OnItemAdded partial adds.

[INV-002] OPEN — Unequip removes equipped then Add(item,1) ignoring Add failure
  (Inventory.cs:466–477) → full bag destroys gear.

[ECON-001] OPEN — GetTradeValue always InferTier(item.type), never catalog TradeTier.

[ECON-002] OPEN — TradeScreenUI still prints You offer/They ask :0.0 (lines 194–195).

[ITEM-003] OPEN — Dual StreamingAssets: Data/items.json (~166KB) + root items.json
  (~26KB) still both present.

--------------------------------------------------------------------------------
P4-B. WHAT IMPROVED SINCE PARTS 1–3 (do not re-file as open bugs)
--------------------------------------------------------------------------------

[FIXED-P4-01] TimeSystem empty Capture/Restore — FIXED at source (TimeSystem.cs).
  GameClockPersistenceTests exercise live persistence (prior DEEP3-TEST-001 narrative
  of empty DTO is historical).

[FIXED-P4-02] SaveMap helper — NEW (SaveMap.cs). Used by tetanus, frostbite, vision,
  tooth, burn ward, cognitive, ash drift, seismic, vehicle, stranding, spore lung,
  trench foot, radiation blindness, etc.

[FIXED-P4-03] SaveStateCompletenessTests — structural guard against dictionary-state
  systems whose DTO carries only systemId. Catches the next stub template.

[FIXED-P4-04] AfflictionPersistenceRoundTripTests — round-trip coverage for the
  SaveMap cluster (new EditMode suite).

[FIXED-P4-05] EquipSlots.TryParse — single parser for importer + world catalog;
  legacy torso/chest → Body. Addresses prior equipSlot silent-None class.

[FIXED-P4-06] UntickedSystemsBaseline.txt + UntickedSystemsBaselineTests —
  ratchet: 435 systems constructed/save-wired but never registry-ticked. Fails if
  new unticked systems appear; fails if baseline entry becomes ticked without
  baseline shrink. Excellent process control; content still latent.

[FIXED-P4-07] WeatherEventDispatchTests — proves Weather_* trackers advance via
  live Registry.TickAll (was hang-active forever when only unit-ticked in isolation).

[FIXED-P4-08] SystemRegistry IsSystemTicked O(1) membership + lookup tests.

[FIXED-P4-09] GameAssetService + keys — designed negative-cache asset lookup.
  NOT wired into GameBootstrap/UI yet (see DEEP4-ASSET-001).

[FIXED-P4-10] TickSystems now delegates _registry.TickAll (no dual explicit body).
  Migration to single dispatch advanced; residual is unregistered content.

--------------------------------------------------------------------------------
P4-C. NEW CRITICAL / HIGH — SAVE WITHOUT TICK (ghost systems)
--------------------------------------------------------------------------------

[DEEP4-ORPHAN-001] Severity: CRITICAL | Type: dead content / false save-safety
  Location: UntickedSystemsBaseline.txt (435 entries); CoreFamilies + InitLate Set*
  Description: ~435 systems are constructed on GameBootstrap and many are
  ISaveable-registered, but never RegisterPerSubstep/Daily. Player-facing prompts
  (afflictions, vehicles, visitors, actions, victory paths, weather variants,
  encounters, biomes, world events) are inert at runtime. Save files grow sections
  that always round-trip empty maps — looks "save-safe" while gameplay never runs.
  Taxonomy (baseline counts):
    Other ~225 | Item 69 | Action 41 | Affliction 21 | Encounter 15 | Victory 14
    Trait 11 | Visitor 11 | Weather 7 | Biome 6 | WorldEvent 5 | Combat 4 | Weapon 4
    Crisis 2
  Registry live surface ~77 RegisterPerSubstep + sparse Daily + ~217 RegisterSaveOnly
  (Registry.cs saveOnly list) vs ~532 diagnostic map entries.
  Suggested Fix: For each baseline entry either (1) wire Tick/TickDay + infection/
  trigger path, or (2) demote from CoreFamilies/Save wiring and exclude from
  shipped builds. Shrink baseline only by wiring, never by expanding.
  Evidence: UntickedSystemsBaseline.txt header; zero call sites for
  SporeLungSystem.TickDay, TrenchFootSystem.TickDay, SevereFrostbiteSystem.
  TickWarmthExposure, TetanusAfflictionSystem.ContractTetanus (only self-refs).

[DEEP4-ORPHAN-002] Severity: HIGH | Type: incomplete domain promise
  Location: TetanusAfflictionSystem.cs
  Description: ContractTetanus / ProgressToLockjaw / CanEatFoodItem exist and save,
  but no scrap-weapon, city-ruin, medical, or eat-gate caller references them.
  Lockjaw solid-food block never applies. Same pattern for SporeLung (no room tick),
  VisionLoss, ToothDecay, BurnWard, CognitiveDecay, Vehicle/Stranding, AshDrift routes,
  SeismicVents.
  Suggested Fix: Wire infection sources + daily ticks + eat/action gates; or delete
  prompt stubs until ready.
  Evidence: rg call sites only construct/save/diagnostics.

[DEEP4-ORPHAN-003] Severity: HIGH | Type: design smell
  Location: SaveMap + SaveSystem.Wiring for unticked systems
  Description: Fixing Capture/Restore without wiring ticks creates "save-perfect
  ghosts" — worse than stub saves for triage (looks complete in save JSON, still
  zero gameplay). Completeness tests green does not mean systems are live.
  Suggested Fix: Pair SaveStateCompletenessTests with "if CaptureState persists map,
  must have non-zero runtime call sites or be listed as intentionally dormant."

--------------------------------------------------------------------------------
P4-D. NEW CRITICAL — PLAYER SURVIVAL HONESTY (reaffirmed + deepened)
--------------------------------------------------------------------------------

[DEEP4-INV-001] Severity: CRITICAL | Type: wrong units / sign
  Location: Inventory.Consume; GameBootstrap.UiActions.Radio ConsumeItem
  Description: Same as DEEP3-INV-001. Confirmed items.json all hungerRestore ≥ 0
  for foods; NeedsSystem hunger increases with hungerPerHour; high Hunger = critical.
  Modify(+hungerRestore) makes eating worsen hunger for any path that passes Needs.
  AI EatActionSO works (needs=null + manual subtract). Dual paths = dual truth.
  Suggested Fix: Consume should Modify(Hunger, -item.hungerRestore) and
  Modify(Thirst, -item.thirstRestore) OR rename fields to hungerDelta and document
  sign; one path for AI and UI.
  Evidence: Inventory.cs:562–563; items.json samples; NeedsSystem critical ≥ threshold.

[DEEP4-INV-002] Severity: HIGH | Type: item loss
  Location: Inventory.Unequip
  Description: Add failure after Remove from equipped discards item. Full inventory
  during swap destroys gear (INV-002 reaffirmed with line evidence 475).
  Suggested Fix: if (!CanAdd) re-insert into _equipped; or drop to ground overflow.

[DEEP4-INV-003] Severity: HIGH | Type: silent fail / partial
  Location: Inventory.Add while-loop Capacity full mid-add
  Description: INV-001 reaffirmed. Events fire for partial; return false; no rollback.

[DEEP4-INV-004] Severity: MEDIUM | Type: state loss
  Location: Inventory.Transfer
  Description: Transfer Remove+Add but DeviceState not copied — destination Add
  creates DeviceState.CreateDefault() for Device types (Add ~328). Battery/EMP state
  on transferred devices resets (DEEP3-INV-014 reaffirmed; Transfer itself is
  all-or-nothing on quantity via CanAdd, but device runtime state still lost because
  Add does not accept DeviceState).
  Suggested Fix: Transfer slot metadata (Device, CurrentDurability) with quantity.

--------------------------------------------------------------------------------
P4-E. DUAL CLOCKS / DAY MIRROR (post SAVE-001 partial fix)
--------------------------------------------------------------------------------

[DEEP4-TIME-001] Severity: HIGH | Type: dual source of truth
  Location: SaveSystem.Capture.cs ElapsedHours; TemperatureSystem; TimeSystem ISaveable
  Description: Three related clocks:
    1) TimeSystem (day + hourAccumulator) — now ISaveable
    2) TemperatureSystem.TotalElapsedHours — SaveData.ElapsedHours
    3) PhotoperiodSystem.TotalElapsedHours — Photoperiod DTO
  They tick from same gameHours in registry when all registered, but restore from
  different DTO fields. Any historical save with only ElapsedHours (temp) and empty
  TimeSystemSave day=0 triggers RestoreState early-return (saved.day <= 0) → clock
  not restored from temp hours; campaign day can disagree with weather season.
  Suggested Fix: Single canonical TotalElapsedHours on TimeSystem; Temperature and
  Photoperiod SetElapsedHours from that on restore; stop dual writing ElapsedHours
  or derive all from TimeSystem.TotalElapsedHours on capture.
  Evidence: TimeSystem.RestoreState day<=0 no-op; Capture still temp-only ElapsedHours.

[DEEP4-TIME-002] Severity: MEDIUM | Type: restore order
  Location: RestoreGameStateCore vs RestoreSubsystemStates
  Description: GameState.Day assigned from SaveData before TimeSystem ISaveable
  restore. Victory/day-gated systems reading GameState.Day mid-restore see wrong day.
  Suggested Fix: Restore TimeSystem first, then GameState.Day = TimeSystem.CurrentDay.

--------------------------------------------------------------------------------
P4-F. ENDGAME / WIN-LOSE (reaffirmed + wiring map)
--------------------------------------------------------------------------------

[DEEP4-WIN-001] Severity: CRITICAL | Type: incomplete wire
  Location: CheckWinLose; EndgameEngine; ApplyEndgame
  Description: CheckWinLose calls EndgameEngine.Evaluate but never maps Result →
  ApplyEndgame. MoralChronicleBridge may subscribe CampaignEndedEvent for UI
  chronicle without freezing simulation. VictoryProject remains the only path that
  reliably sets IsGameOver via OnEndgameTriggered.
  Suggested Fix: OnCampaignEnded / after Evaluate true → build EndgameSummaryData →
  ApplyEndgame; or remove dual engine and own win/lose in one place.

[DEEP4-WIN-002] Severity: HIGH | Type: data desync
  Location: EndgameEngine day 60 vs VictoryProject ChopperArrivalDay 100
  Description: Extraction can "succeed" on engine at day 60 while VictoryProject
  chopper narrative still waits for day 100. Player-facing extraction story splits.

[DEEP4-WIN-003] Severity: HIGH | Type: incomplete
  Location: Crisis_StructuralFailure; StructuralIntegritySystem; EndgameEngine
  Description: Structural systems tick and can zero module health / raise conditions,
  but terminal freeze depends on DEEP4-WIN-001. Collapse can leave a playable corpse
  of a bunker.

--------------------------------------------------------------------------------
P4-G. RADIATION / WARMTH / MEDICAL (still domain-broken)
--------------------------------------------------------------------------------

All RAD-001..004, SURV-001, MED-001..002, CMB-001 remain OPEN (see P4-A).

[DEEP4-RAD-001] Severity: HIGH | Type: incomplete path
  Location: DecontaminationStation.cs; Contamination.cs; bootstrap
  Description: DecontaminationStation class exists; not owned/ticked by GameBootstrap
  (DEEP3-INV-006 reaffirmed). Item contamination dose via Consume still works;
  ambient bunker dose still off.

[DEEP4-MED-001] Severity: HIGH | Type: direct Health writers
  Location: many (PersonalQuestSystem, HatchDefense fallback, BunkerSocialDirector
  execution, MedicalPerkSystem, LaborCamp, etc.)
  Description: Direct Needs.Health = ... bypasses NeedsSystem.EvaluateDeath
  (DEEP-009 class). Zero HP may not mark dead / fire death pipeline consistently.
  Evidence: HatchDefenseSystem.cs:997–998 comment "Fallback: direct health when
  medical not wired"; BunkerSocialDirector execution sets Health=0.

--------------------------------------------------------------------------------
P4-H. STUB SAVES STILL PRESENT (payload-empty DTOs)
--------------------------------------------------------------------------------

[DEEP4-SAVE-STUB-001] Severity: MEDIUM | Type: false save-safety
  Empty CaptureState() => new XxxSave() still on at least:
    FalloutStormHazardSystem, MoralDilemmaSystem, BlackRainHazardSystem,
    CookingSystem, BicycleSystem, NightScavengeSystem, LocationStateRuinSystem,
    Visitor_AbandonedState, Visitor_ExplodedState, WaterEconomySystem,
    ProstheticCraftingSystem, NeedleSterilizationSystem,
    Action_SilentTakedown, Action_Stargazing
  Notes:
  - WaterEconomySystem is mostly pure on WaterStorage + module fields; stub may be
    acceptable IF module WaterConversionProgress + FilterHealth are saved via
    Shelter modules (verify). Still misleading ISaveable registration.
  - BicycleSystem state lives on ExpeditionState (HasBicycle/Durability) which ARE
    captured on expeditions — system-level stub is partially OK.
  - NightScavengeSystem flashlight state on ExpeditionState is captured; system stub OK
    for residual, but Arm/Tick helpers must still be called from expedition path
    (partially are — ExpeditionSystem.Uxo references HasFlashlight).
  Suggested Fix: Delete empty ISaveable registration OR document "stateless host"
  and exclude from Completeness tests as Exempt.

--------------------------------------------------------------------------------
P4-I. ECONOMY / DATA / ASSETS
--------------------------------------------------------------------------------

[DEEP4-ECON-001] OPEN reaffirm ECON-001 InferTier only.

[DEEP4-ECON-002] OPEN reaffirm TradeScreenUI numeric quote.

[DEEP4-ITEM-001] OPEN dual StreamingAssets trees.

[DEEP4-ASSET-001] Severity: MEDIUM | Type: incomplete feature
  Location: GameAssetService.cs; no GameBootstrap property / UI callers
  Description: Service + IGameAssetProvider + GameAssetKeys exist with tests, but
  no production UI uses GetItemIcon / GetSurvivorPortrait. Inventory/UI still
  ad-hoc Resources or missing art path. Negative-cache benefit unrealized.
  Suggested Fix: Inject singleton on bootstrap; replace UI icon loads.

[DEEP4-ASSET-002] Severity: LOW | Type: production risk
  Location: GameAssetService LogMissingOnce
  Description: Missing art logs once — good. PlaceholderSprite optional null → blank
  icons silently. Ensure placeholder assigned in production bootstrap.

--------------------------------------------------------------------------------
P4-J. ARCHITECTURE / LIFECYCLE / PROCESS
--------------------------------------------------------------------------------

[DEEP4-ARCH-001] Severity: HIGH | Type: god-object / latent surface
  Location: GameBootstrap ~partials; ~532 diagnostic entries; 435 unticked
  Description: Composition root still owns hundreds of systems. Save wiring density
  >> tick density. Content pipeline can add prompt systems that compile, save, and
  never run.
  Suggested Fix: Feature flags or ContentManifest listing Live | SaveOnly | Dormant;
  CI fails if new public GameBootstrap property lacks registry category.

[DEEP4-ARCH-002] Severity: MEDIUM | Type: lifecycle leak risk
  Location: codebase-wide event subscribe
  Description: Rough counts this pass: ~505 On*/Subscribe += vs ~43 On*/Unsubscribe -=
  (order of magnitude imbalance). MonoBehaviour destroy / scene reload can leak
  handlers; double-subscribe on re-init risk if Lifecycle unsubscribe incomplete.
  Suggested Fix: Central EventBus + IDisposable subscriptions; audit InitLate
  unsubscribes in Lifecycle.cs.

[DEEP4-ARCH-003] Severity: MEDIUM | Type: process
  Location: git status main ahead ~375 of origin; large dirty tree
  Description: Unpushed mega-history + WIP save/affliction fixes mixed with open
  domain bugs. Hard to review; integration risk high (AUDIT-002 class continues).

[DEEP4-TEST-001] Severity: MEDIUM | Type: false confidence
  Location: tests using sv => true / null ExposureContext
  Description: DEEP3-TEST-002 reaffirmed — harnesses encode broken production
  warmth/rad wiring; green tests do not detect DEEP-002/003.

[DEEP4-TEST-002] Severity: LOW | Type: coverage gap
  Location: no EditMode test asserting Inventory.Consume hunger sign against
  NeedsSystem critical convention.
  Suggested Fix: single test: hunger 50, eat food hungerRestore 40 → hunger 10
  (or document if hunger is inverted domain-wide — it is not; AI subtracts).

--------------------------------------------------------------------------------
P4-K. COMBAT / AI / SOCIAL (spot findings)
--------------------------------------------------------------------------------

[DEEP4-CMB-002] Severity: MEDIUM | Type: incomplete
  Location: Combat_BleedOut, Combat_Flanking, CombatStance_LastStand,
  Combat_Suppression (all in UntickedSystemsBaseline)
  Description: Combat expansion systems constructed, never registry-ticked.

[DEEP4-AI-001] Severity: HIGH | Type: wrong behavior
  Location: DrinkActionSO, TakeIodineActionSO, UseAntiRadActionSO
  Description: Reaffirm free drink / iodine no-op resistance / anti-rad bypasses
  RadiationSystem (DEEP3-INV-003..005).

[DEEP4-AI-002] Severity: MEDIUM | Type: inconsistency
  Location: EatActionSO Animalistic spit path
  Description: Consume already removed food units before animalistic spit return
  (DEEP3-INV-015) — food wasted, contamination already applied via radiation arg.

--------------------------------------------------------------------------------
P4-L. PRIORITY WAVE (updated after Part 4)
--------------------------------------------------------------------------------

  Wave 0 — Honesty (player feels lies in first hour):
    DEEP4-INV-001 consume sign (player eat)
    DEEP-002 warmth isNearHeatSource
    DEEP-003 / RAD-001 ExposureContext
    DEEP-001 starvation health path or afflictions
    DEEP3-INV-003 free AI drink

  Wave 0b — Run can end:
    DEEP4-WIN-001 EndgameEngine → ApplyEndgame
    DEEP4-WIN-002 unify extraction day 60/100
    CMB-001 generator noise latch

  Wave 1 — Medical truth:
    MED-001 stamina cap sign
    MED-002 amp sepsis cure
    DEEP4-MED-001 EvaluateDeath on direct HP writers

  Wave 2 — Save integrity residual:
    SAVE-002 expedition flashpoint capture
    SAVE-004 single AutoSave owner
    SAVE-007 survivors by Id
    SAVE-008 clear expeditions
    DEEP4-TIME-001 single clock
    SAVE-003 transactional load / block autosave after failed load

  Wave 3 — Ghost content:
    DEEP4-ORPHAN-001 shrink baseline by wiring OR demoting
    Do not expand SaveMap-only fixes without tick+trigger paths

  Wave 4 — Economy / data / assets:
    ECON-001 TradeTier, dual JSON, GameAssetService wire, Unequip safety

--------------------------------------------------------------------------------
P4-M. METHOD / EVIDENCE THIS PASS
--------------------------------------------------------------------------------

  - Re-read TimeSystem Capture/Restore; SaveSystem.Wiring SetTimeSystem; Capture
    ElapsedHours still temperature-only
  - Re-verified CaptureExpeditionState vs ApplyExpeditionSaveFields field list
  - Confirmed InitFoundation NeedsSystem(sv=>true) and RadiationSystem no context
  - Confirmed MaterialShielding weakestAttenuation=0f; GetInteriorRads without
    BunkerContamination
  - Inventory.Consume + items.json hungerRestore signs; EatActionSO dual path
  - Drink/Iodine/AntiRad AI actions read end-to-end
  - EndgameEngine.TriggerEndgame vs CheckWinLose / ApplyEndgame callers
  - UntickedSystemsBaseline taxonomy (435); registry RegisterPerSubstep count ~77
  - SaveMap cluster save-wired; zero Tick/Contract call sites for tetanus/spore/etc.
  - Dual AutoSave handlers; FailFastRestore env policy
  - GameAssetService usage = self only
  - No Unity batchmode re-run this pass (static analysis + file evidence only)

  Still NOT exhaustive of every Action_*/Event_*/Victory_* body. Baseline is the
  structural map; individual prompt systems need per-file trigger audits when wired.

--------------------------------------------------------------------------------
P4-N. COUNTS (Part 4 delta)
--------------------------------------------------------------------------------

  Prior report Critical domain issues: mostly still OPEN (see P4-A).
  Newly documented this pass:
    Critical: DEEP4-ORPHAN-001, DEEP4-INV-001, DEEP4-WIN-001
    High:     DEEP4-ORPHAN-002/003, DEEP4-INV-002/003, DEEP4-TIME-001,
              DEEP4-WIN-002/003, DEEP4-RAD-001, DEEP4-MED-001, DEEP4-AI-001,
              DEEP4-ARCH-001
    Medium:   DEEP4-TIME-002, DEEP4-SAVE-STUB-001, DEEP4-ASSET-001,
              DEEP4-ARCH-002/003, DEEP4-CMB-002, DEEP4-AI-002, DEEP4-TEST-001,
              DEEP4-INV-004
    Low/Nit:  DEEP4-ASSET-002, DEEP4-TEST-002

  Process wins (not bugs): SaveMap, TimeSystem payload, EquipSlots, completeness
  tests, unticked ratchet, weather dispatch tests.

========================================================================================
END OF PART 4 — CONTINUATION PASS
========================================================================================

========================================================================================
PART 5 — TRIGGER AUDIT: UntickedSystemsBaseline Other / Event / Victory (top 50)
Read-only | No code was modified
Generated (UTC): 2026-08-07 23:35:37
========================================================================================

SCOPE
----------------------------------------
From UntickedSystemsBaseline.txt (435 entries), this pass audits the TOP 50 systems in
the Event + Victory + Other priority band:

  • Event*   — all 27 (complete bucket)
  • Victory* — all 14 (complete bucket)
  • Other    — first 9 non-MapAnomaly / non-NPC / non-MapHazard systems by baseline order
               (completes n=50)

Method (property-scoped, not bare .Tick false positives):
  1) Resolve GameBootstrap property name (EventBrawl, VictoryAirlift, …).
  2) Find all `Prop.Method(` / `Prop?.Method(` and `Prop.OnX +=` across Assets/**/*.cs.
  3) Classify callers: bootstrap | gameplay | test | save/diag.
  4) Cross-check EventPoolBuilder.cs for event id membership (ScriptableObject pool).
  5) Read VictoryPaths.cs / EventTrackers.cs host wiring comments.

Category legend used below:
  ZERO_EXTERNAL          No method calls on the property outside its defining file
                         (except possibly save Capture/Restore wiring).
  HANDLERS_ONLY          Constructed + On* event subscriptions (usually Debug.Log),
                         but ZERO Start/Tick/Trigger/Check method calls.
  BOOTSTRAP_TICK_COND    Bootstrap calls Tick/Check, but only when path already active
                         or on a narrow daily condition — no Start* callers found.
  BOOTSTRAP_LIVE         Bootstrap (or batch) regularly invokes gameplay methods.
  EVENT_BUS_ONLY         Would mean pool/EventRunner drives the system by id without
                         direct property calls. **None of the 27 Event_* trackers
                         appear in EventPoolBuilder** (0 overlapping event_* ids).

NOTE ON "unticked" vs "handlers wired":
  Registry baseline marks systems whose property is not RegisterPerSubstep/Daily.
  Victory batch uses registry keys "victory_paths_hourly|daily" — individual Victory*
  props still count as unticked. Event trackers are RegisterEventDriven/SaveOnly at
  best — still never auto-fired.

--------------------------------------------------------------------------------
P5-A. EXECUTIVE SUMMARY (TOP 50)
--------------------------------------------------------------------------------

  HANDLERS_ONLY .............. 38 / 50   (all 27 Events + 11 Victories)
  BOOTSTRAP_TICK_COND ........  3 / 50   (VictoryAirlift, VictoryLoneSurvivor,
                                          VictoryTheBroadcast — tick only if active
                                          / daily check; Start* never called)
  BOOTSTRAP_LIVE .............  3 / 50   (AdaptiveWarlords, ChemUse, DiseaseExpansion)
  ZERO_EXTERNAL (construct) ..  6 / 50   (AndroidNpcs, AshDriftSystem,
                                          AudioEventDeafening, AudioEventHeartbeat,
                                          BurnWardSystem, CognitiveDecaySystem)

  EventPoolBuilder overlap with the 27 tracker event ids: **0 / 27**
  External (non-bootstrap) gameplay callers of Event*/Victory* methods: **0 / 41**

Interpretation: the Event_* and most Victory_* content is a **second narrative stack** —
classes exist, save keys exist, host logs exist, but nothing in the live EventRunner
pool or UI actions starts them. They are save-perfect ghosts with louder wiring than
Affliction maps (subscriptions fire if something else ever called Start — nothing does).

--------------------------------------------------------------------------------
P5-B. EVENT* (27/27) — ALL HANDLERS_ONLY / ZERO EXTERNAL TRIGGERS
--------------------------------------------------------------------------------

Boot path: GameBootstrap.EventTrackers.BootEvents() constructs all 27; WireEvents()
subscribes On* → Debug.Log only. SaveSystem.SetEvent* registers ISaveable keys.

Per-property method calls (Prop.Method excluding On* and Capture/Restore):
  **NO_METHOD_CALLS for every property listed below.**

| # | Baseline / Prop        | Source file                         | eventId (in class)           | On* handlers | Triggers |
|---|------------------------|-------------------------------------|------------------------------|--------------|----------|
| 1 | EventBrawl             | Event_Brawl.cs                       | event_brawl                  | Y (log)      | NONE     |
| 2 | EventComingOfAge       | Event_ComingOfAge.cs                | event_coming_of_age          | Y (log)      | NONE     |
| 3 | EventCultBlessing      | Event_CultBlessing.cs               | event_cult_blessing          | Y (log)      | NONE     |
| 4 | EventCultInitiation    | Event_CultInitiation.cs             | event_cult_initiation        | Y (log)      | NONE     |
| 5 | EventCultOfAi          | Event_CultOfAI.cs                   | event_cult_of_ai (const)     | Y (log)      | NONE     |
| 6 | EventEmpCascade        | Event_EMPCascade.cs                 | event_emp_cascade            | Y (log)      | NONE     |
| 7 | EventEuthanasiaPact    | Event_EuthanasiaPact.cs             | euthanasia_pact / event_…    | Y (log)      | NONE     |
| 8 | EventFactionMerger     | Event_FactionMerger.cs              | event_faction_merger         | Y (log)      | NONE     |
| 9 | EventFeralRescue       | Event_FeralRescue.cs                | event_feral_rescue           | Y (log)      | NONE     |
|10 | EventFoundDiary        | Event_FoundDiary.cs                 | event_found_diary            | Y (log)      | NONE     |
|11 | EventGriefCascade      | Event_GriefCascade.cs               | event_grief_cascade          | Y (log)      | NONE     |
|12 | EventHungerStrike      | Event_HungerStrike.cs               | event_hunger_strike          | Y (log)      | NONE     |
|13 | EventMudslide          | Event_Mudslide.cs                   | event_mudslide               | Y (log)      | NONE     |
|14 | EventNodeCollapse      | Event_NodeCollapse.cs               | event_node_collapse          | Y (log)      | NONE     |
|15 | EventNumbersStation    | Event_NumbersStation.cs             | event_numbers_station        | Y (log)      | NONE     |
|16 | EventProjectSabotage   | Event_ProjectSabotage.cs            | event_project_sabotage       | Y (log)      | NONE     |
|17 | EventRansomNote        | Event_RansomNote.cs                 | event_ransom_note            | Y (log)      | NONE     |
|18 | EventSchism            | Event_Schism.cs                     | event_schism                 | Y (log)      | NONE     |
|19 | EventSecretSociety     | Event_SecretSociety.cs              | (clique state)               | Y (log)      | NONE     |
|20 | EventSiblingFeud       | Event_SiblingFeud.cs                | event_sibling_feud           | Y (log)      | NONE     |
|21 | EventSinkhole          | Event_Sinkhole.cs (FoundationSinkholeSystem) | event_sinkhole    | Y (log)      | NONE     |
|22 | EventSpontaneousMurder | Event_SpontaneousMurder.cs          | event_spontaneous_murder     | Y (log)      | NONE     |
|23 | EventTeenRebellion     | Event_TeenRebellion.cs              | event_teen_rebellion         | Y (log)      | NONE     |
|24 | EventTriangulation     | Event_Triangulation.cs              | event_triangulation          | Y (log)      | NONE     |
|25 | EventVaultCollision    | Event_VaultCollision.cs (VaultCollisionSystem) | event_vault_collision | Y (log) | NONE  |
|26 | EventWarlordSuccession | Event_WarlordSuccession.cs          | event_warlord_succession     | Y (log)      | NONE     |
|27 | EventWitchHunt         | Event_WitchHunt.cs                  | event_witch_hunt             | Y (log)      | NONE     |

[DEEP5-EVT-001] Severity: CRITICAL | Type: dead content / dual narrative stack
  Location: GameBootstrap.EventTrackers.cs; Event_*.cs ×27; EventPoolBuilder.cs
  Description: 27 class-based event trackers are constructed, save-wired, and
  log-subscribed, but:
    (a) no Prop.Start/Tick/Trigger calls anywhere in Assets;
    (b) EventPoolBuilder builds ScriptableObject GameEvents (emissary, safe haven,
        blood-for-water, buried-alive SO, encounters) with **zero** of these event_*
        tracker ids (pool event_* string count matching trackers = 0/27);
    (c) TickDay/TickHour APIs on several classes (ComingOfAge, CultInitiation, …)
        are never invoked — even from bootstrap.
  Player impact: narrative content described by prompts never fires in campaign.
  Suggested Fix: Either (1) bridge each tracker into EventRunner/UI choices with
  explicit Start* from dilemmas, or (2) demote from BootEvents + Save wiring until
  live, and remove from baseline by deletion not by silent registry lies.
  Evidence: property-scoped rg Prop.Method → NO_METHOD_CALLS ×27; EventPoolBuilder
  has no event_brawl / event_witch_hunt / etc.

[DEEP5-EVT-002] Severity: HIGH | Type: false live signal
  Location: WireEvents On* += Debug.Log
  Description: Subscription density looks like "events are wired." They only log if
  an internal method raises On* — which requires an external Start that never runs.
  Tests (EventsWiringTests) often construct systems in isolation and call methods
  by hand — green without proving bootstrap dispatch (same class of bug as pre-fix
  Weather_* hang-active).
  Suggested Fix: EditMode test: after InitializeSystems, assert no Event* state
  becomes active after N day ticks unless an explicit Start is invoked; add one
  smoke that StartBrawl (or equivalent) is reachable from a UI/action path.

--------------------------------------------------------------------------------
P5-C. VICTORY* (14/14)
--------------------------------------------------------------------------------

Boot path: GameBootstrap.VictoryPaths.BootVictoryPaths + WireVictoryPaths
(On* → Debug.Log). Registry: victory_paths_hourly / victory_paths_daily /
TickVictoryPathsRealtime from Lifecycle.

| Prop                   | File                      | Public start/check APIs (examples)     | Bootstrap calls              | External calls | Category            |
|------------------------|---------------------------|----------------------------------------|------------------------------|----------------|---------------------|
| VictoryAirlift         | Victory_Airlift.cs        | StartDefense, TickRealTime             | TickRealTime if isActive     | NONE           | BOOTSTRAP_TICK_COND |
| VictoryAscendancy      | Victory_Ascendancy.cs     | CheckVictory                           | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryBuriedAlive     | Victory_BuriedAlive.cs    | SealAndDetonate                        | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryCannibalKing    | Victory_CannibalKing.cs   | CheckVictory                           | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryDefection       | Victory_Defection.cs      | (game-over path APIs)                  | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryIcebreaker      | Victory_Icebreaker.cs     | CheckExtraction                        | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryLoneSurvivor    | Victory_LoneSurvivor.cs   | CheckVictory(day, alive)               | CheckVictory daily           | NONE           | BOOTSTRAP_TICK_COND |
| VictoryMAD             | Victory_MAD.cs            | FireAtOwnCoordinates                   | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryMigration       | Victory_Migration.cs      | CheckVictory                           | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryTheBroadcast    | Victory_TheBroadcast.cs   | StartUpload, TickMinute                | TickMinute if isActive       | NONE           | BOOTSTRAP_TICK_COND |
| VictoryTheCure         | Victory_TheCure.cs        | CheckVictory                           | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryTheMartian      | Victory_TheMartian.cs     | CheckVictory                           | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryUndergroundCity | Victory_UndergroundCity.cs| CheckProgress, TryActivate, SealHatch  | NONE                         | NONE           | HANDLERS_ONLY       |
| VictoryUnifier         | Victory_Unifier.cs        | CheckVictory(factionTrust)             | NONE                         | NONE           | HANDLERS_ONLY       |

[DEEP5-WIN-001] Severity: CRITICAL | Type: incomplete victory paths
  Location: GameBootstrap.VictoryPaths.cs TickVictoryPaths*
  Description: Only three paths receive any host method calls:
    • Airlift.TickRealTime — only while State.isActive (StartDefense never called)
    • TheBroadcast.TickMinute — only while State.isActive (StartUpload never called)
    • LoneSurvivor.CheckVictory(day, aliveCount) — daily; may actually be reachable
  Eleven other endings have CheckVictory / SealAndDetonate / FireAtOwnCoordinates /
  CheckExtraction / TryActivate with **zero** callers in the whole Assets tree.
  WireVictoryPaths only logs endings; does not call ApplyEndgame (ties DEEP4-WIN-001).
  Suggested Fix: For each ending, one entry point (UI project screen or action) must
  call Start*/Check*; daily/hourly host should evaluate CheckVictory for paths that
  are progress-based (Ascendancy, CannibalKing, Migration, Cure, Martian, Unifier,
  UndergroundCity, Icebreaker). On success → ApplyEndgame summary.

[DEEP5-WIN-002] Severity: HIGH | Type: dead start
  Location: VictoryAirlift.StartDefense; VictoryTheBroadcast.StartUpload
  Description: Conditional ticks are written carefully but Start* has no callers →
  isActive never true → TickRealTime/TickMinute are dead code paths in practice.
  Evidence: rg StartDefense / StartUpload callers empty outside defining files.

[DEEP5-WIN-003] Severity: MEDIUM | Type: registry baseline honesty
  Location: UntickedSystemsBaseline Victory*; Registry victory_paths_* batch
  Description: Batch registry tick exists, but 11/14 victory props still have zero
  method dispatch. Baseline correctly lists them as unticked systems; do not remove
  from baseline until Start/Check is wired per path.

--------------------------------------------------------------------------------
P5-D. OTHER (9 of top-50) — MIXED LIVE VS GHOST
--------------------------------------------------------------------------------

| Prop                 | Type / file                      | Method calls (non-On*)                         | Category         |
|----------------------|----------------------------------|------------------------------------------------|------------------|
| AdaptiveWarlords     | System_AdaptiveWarlords.cs       | RecordStrategy, OnPlaythroughEnd, GetWarlordGear (bootstrap) | BOOTSTRAP_LIVE |
| AndroidNpcs          | NPC_Android.cs                   | none on property (JoinBunker/CheckShot unused) | ZERO_EXTERNAL    |
| AshDriftSystem       | AshDriftSystem.cs                | none (SaveMap only)                            | ZERO_EXTERNAL    |
| AudioEventDeafening  | AudioEvent_Deafening.cs          | none (TickSecond unused)                       | ZERO_EXTERNAL    |
| AudioEventHeartbeat  | AudioEvent_Heartbeat.cs          | none (CheckHealth unused)                      | ZERO_EXTERNAL    |
| BurnWardSystem       | BurnWardSystem.cs                | none (SaveMap only)                            | ZERO_EXTERNAL    |
| ChemUse              | ChemUseRouter.cs                 | Bind, Notify, PeekEffectiveness, PeekDurationHours | BOOTSTRAP_LIVE |
| CognitiveDecaySystem | CognitiveDecaySystem.cs          | none (SaveMap only)                            | ZERO_EXTERNAL    |
| DiseaseExpansion     | DiseaseSystem_Expansion.cs       | TickHour via BatchSystems.cs                   | BOOTSTRAP_LIVE   |

[DEEP5-OTH-001] Severity: HIGH | Type: dead content
  Location: AndroidNpcs, AshDriftSystem, AudioEvent*, BurnWardSystem, CognitiveDecaySystem
  Description: Same save-perfect ghost pattern as Part 4 afflictions. APIs exist
  (JoinBunker, CheckHealth, TickSecond, state maps) with zero property call sites.
  Suggested Fix: Wire into raid/medical/audio hosts or demote from CoreFamilies.

[DEEP5-OTH-002] Severity: LOW | Type: baseline noise (positive)
  Location: AdaptiveWarlords, ChemUse, DiseaseExpansion
  Description: These three ARE live via bootstrap/batch but still appear on the
  unticked baseline because they are not registered under their property names in
  SystemRegistry (batch names differ: batch_systems_hourly, etc.).
  Suggested Fix: RegisterSaveOnly or RegisterPerSubstep under the property's
  diagnostic aliases so IsSystemTicked matches reality; shrink baseline.

--------------------------------------------------------------------------------
P5-E. NOT EVENT-BUS-DRIVEN (important correction)
--------------------------------------------------------------------------------

Prior audit language sometimes said "event-driven." For this top-50 Event_* set:

  • They are **not** EventBus-only in the sense of EventPoolBuilder/GameEvent SO pool.
  • They are **class trackers with C# events (Action<>)** subscribed only for logging.
  • True event-bus content (Safe Haven, Blood for Water, emissary chains, encounter
    factory) is a **disjoint** set — dual stacks (reaffirms ORPHAN-002 / Part 2).

No system in this top-50 table classified as pure EVENT_BUS_ONLY under the strict
definition (pool id drives behavior without property calls). Closest living pool
content is outside this baseline slice.

--------------------------------------------------------------------------------
P5-F. COUNTS & PRIORITY DELTA
--------------------------------------------------------------------------------

Top-50 breakdown:
  HANDLERS_ONLY (dead until Start wired) ..... 38
  BOOTSTRAP_TICK_COND (Start missing) ........  3
  BOOTSTRAP_LIVE .............................  3
  ZERO_EXTERNAL construct/save ...............  6

New finding IDs this pass:
  DEEP5-EVT-001, DEEP5-EVT-002
  DEEP5-WIN-001, DEEP5-WIN-002, DEEP5-WIN-003
  DEEP5-OTH-001, DEEP5-OTH-002

Remediation order (Event/Victory slice):
  1. Pick 3 flagship Event_* with full Start + TickDay host + one UI/dilemma entry;
     prove pool-or-action path; delete log-only wire as "done."
  2. Wire Victory CheckVictory daily for progress endings; Start* for Airlift/Broadcast.
  3. Connect victory success to ApplyEndgame (DEEP4-WIN-001).
  4. Demote remaining Event_* from BootEvents until entry points exist (stop save bloat).
  5. Fix baseline for AdaptiveWarlords / ChemUse / DiseaseExpansion (DEEP5-OTH-002).

--------------------------------------------------------------------------------
P5-G. METHOD LIMITS
--------------------------------------------------------------------------------

  • Top 50 only: Event 27 + Victory 14 + Other 9. Remaining Other (~189) includes
    MapAnomaly*, MapHazard*, NPC*, Project*, Node*, Hazard*, Location*, etc. —
    expected similar ZERO/HANDLERS patterns; not re-scanned line-by-line this pass.
  • Property-scoped search misses fully static helpers or reflection calls (none found
    for these types).
  • Tests that `new Event_X()` and call methods are excluded from "live game" callers.
  • Unity playmode not run; static call-graph only.

  Evidence commands (reproducible):
    rg -n 'EventBrawl\s*\??\.\s*\w+\s*\(' Assets --glob '*.cs'
    # → only On* += in EventTrackers / Set* in SaveSystem
    rg -n 'event_brawl|event_witch_hunt' Assets/_Game/Core/EventPoolBuilder.cs
    # → no matches
    read GameBootstrap.VictoryPaths.cs TickVictoryPathsDaily (LoneSurvivor only)

========================================================================================
END OF PART 5 — TRIGGER AUDIT (TOP 50 Event / Victory / Other)
========================================================================================

========================================================================================
PART 6 — TRIGGER AUDIT: MapAnomaly + MapHazard + NPC (top 40)
Read-only | No code was modified
Generated (UTC): 2026-08-07 23:39:48
========================================================================================

SCOPE
----------------------------------------
UntickedSystemsBaseline buckets:
  MapAnomaly  18
  MapHazard    8   (excludes MapHazardAcidGeyser / MapHazardAshlanche — those ARE
                    registry-ticked via map_hazards_hourly; not on baseline)
  NPC         34

TOP 40 = all MapAnomaly (18) + all baseline MapHazard (8) + first 14 NPC by baseline
order. Full NPC set (34) scanned the same way; remaining 20 match the same category.

Method (same as Part 5, property-scoped):
  Prop.Method( / Prop?.Method( / Prop.OnX += across Assets/**/*.cs
  Roles: bootstrap | gameplay | test | save/diag
  Boot files: GameBootstrap.MapAnomalies.cs, MapHazards.cs, CoreFamilies.BootNPCFamily

Category legend:
  HANDLERS_ONLY       Construct + On* → Debug.Log (or similar); zero Start/Navigate/Attempt*
  CONSTRUCT_SAVE_ONLY Construct (+ save Set*) only; no On* wire and no method calls
  BOOTSTRAP_TRIGGERED Host regularly calls gameplay methods (none in this top-40 set)
  EXTERNAL_CALLERS    Non-bootstrap gameplay callers (none in this top-40 set)
  ZERO_EXTERNAL       No property references outside defining file

NOTE: Shared method names (TickHour, EnterNode, TakeDamage, TryFlee) appear in many
types; bare-name search produces false positives. Classifications use **property-
scoped** Prop.Method only.

--------------------------------------------------------------------------------
P6-A. EXECUTIVE SUMMARY
--------------------------------------------------------------------------------

TOP 40:
  HANDLERS_ONLY .............. 26 / 40   (all 18 MapAnomaly + all 8 MapHazard)
  CONSTRUCT_SAVE_ONLY ........ 14 / 40   (first 14 NPC)
  EXTERNAL / BOOTSTRAP_LIVE ..  0 / 40

FULL bucket scan (60 systems = 18+8+34):
  HANDLERS_ONLY .............. 26
  CONSTRUCT_SAVE_ONLY ........ 34   (every NPC*)
  LIVE in this family ........  0 among baseline members

Contrast — live siblings NOT on baseline:
  MapHazardAcidGeyser.TickHour()  and  MapHazardAshlanche.TickMinute()
  are called from TickMapHazardsHourly (registry key map_hazards_hourly).
  Diagnostics map those two props to that key → baseline correctly omits them.
  The other 8 MapHazard_* are booted/wired for logs only and never ticked or entered.

Expedition / map host: MapHazards.cs comment says "expedition hosts call
Navigate/Attempt* when parties enter nodes" — **no expedition code calls any
of the baseline MapHazard/MapAnomaly properties.** Comment describes intent, not fact.

--------------------------------------------------------------------------------
P6-B. MapAnomaly* (18/18) — ALL HANDLERS_ONLY
--------------------------------------------------------------------------------

Boot: BootMapAnomalies() + WireMapAnomalies() in GameBootstrap.MapAnomalies.cs
Save: SaveSystem.SetMapAnomaly* for each
Tick: none of these appear in TickMapHazardsHourly or any registry substep

| Prop                      | File                            | Example public APIs                         | Prop.Method callers | Category      |
|---------------------------|---------------------------------|---------------------------------------------|---------------------|---------------|
| MapAnomalyAshDunes        | MapAnomaly_AshDunes.cs          | TraverseAshDunes                            | NONE (On* only)     | HANDLERS_ONLY |
| MapAnomalyBoilingLake     | MapAnomaly_BoilingLake.cs       | CrossByBoat, CollectHeat, TryLootCenter…    | NONE                | HANDLERS_ONLY |
| MapAnomalyCherenkov       | MapAnomaly_Cherenkov.cs         | EnterPool, IsLit                            | NONE                | HANDLERS_ONLY |
| MapAnomalyDogDen          | MapAnomaly_DogDen.cs            | ClearDenAndLoot                             | NONE                | HANDLERS_ONLY |
| MapAnomalyDontLook        | MapAnomaly_DontLook.cs          | DisplayWarning, InspectNode, ShouldAvert    | NONE                | HANDLERS_ONLY |
| MapAnomalyDryCoral        | MapAnomaly_DryCoral.cs          | Discover, HarvestCrystal, GetRadExposure    | NONE                | HANDLERS_ONLY |
| MapAnomalyFloodedSubway   | MapAnomaly_FloodedSubway.cs     | WadeThroughSubway                           | NONE                | HANDLERS_ONLY |
| MapAnomalyGlassCrater     | MapAnomaly_GlassCrater.cs       | NavigateCrater                              | NONE                | HANDLERS_ONLY |
| MapAnomalyMassGrave       | MapAnomaly_MassGrave.cs         | TraverseGraveNode, RobCorpses               | NONE                | HANDLERS_ONLY |
| MapAnomalyMirage          | MapAnomaly_Mirage.cs            | SpawnDuringHeatwave, CreateFakeNode, On…    | NONE                | HANDLERS_ONLY |
| MapAnomalyPetrifiedForest | MapAnomaly_PetrifiedForest.cs   | Discover, HarvestTree, IsAudioMuted         | NONE                | HANDLERS_ONLY |
| MapAnomalyQuietZone       | MapAnomaly_QuietZone.cs         | EnterZone, StayInZone, GetRadReading        | NONE                | HANDLERS_ONLY |
| MapAnomalyRustedTank      | MapAnomaly_RustedTank.cs        | ShelterInsideTank                           | NONE                | HANDLERS_ONLY |
| MapAnomalyServerFarm      | MapAnomaly_ServerFarm.cs        | EnterShelter, TickHour, TryHarvestGold      | NONE                | HANDLERS_ONLY |
| MapAnomalySinkhole        | MapAnomaly_Sinkhole.cs          | LootSinkholeWithRope                        | NONE                | HANDLERS_ONLY |
| MapAnomalyTangledDrop     | MapAnomaly_TangledDrop.cs       | RetrieveDropWithClimbingGear, ShootDown…    | NONE                | HANDLERS_ONLY |
| MapAnomalyTireFire        | MapAnomaly_TireFire.cs          | ApplyRegionalPollution                      | NONE                | HANDLERS_ONLY |
| MapAnomalyUxoNuke         | MapAnomaly_UXO_Nuke.cs          | (harvest / detonate APIs)                   | NONE                | HANDLERS_ONLY |

Evidence sample (AshDunes): Prop references only construct, OnFirearmJammedByAsh +=,
Diagnostics, Save Set*. TraverseAshDunes called only from MapAnomaliesWiringTests
and its own file.

anomalyId strings (map_anomaly_*) appear in class DTOs + Diagnostics + Save keys only
— not in StreamingAssets location JSON as live node tags (spot-check ash_dunes /
quiet_zone / boiling_lake → no map data hits outside Core).

[DEEP6-MAP-001] Severity: CRITICAL | Type: dead content
  Location: GameBootstrap.MapAnomalies.cs; MapAnomaly_*.cs ×18
  Description: Full anomaly suite is save-wired and log-subscribed but never
  invoked when expeditions visit nodes. Player never experiences ash jam, Cherenkov
  pool, quiet zone, UXO nuke, etc. from these systems.
  Suggested Fix: ExpeditionSystem node-enter dispatch table: node.tag / anomalyId →
  MapAnomalyX.Traverse*/Enter*; or demote BootMapAnomalies until map data tags exist.

--------------------------------------------------------------------------------
P6-C. MapHazard* baseline (8/8) — ALL HANDLERS_ONLY
--------------------------------------------------------------------------------

Boot: BootMapHazards() + WireMapHazards() in GameBootstrap.MapHazards.cs
Hourly host TickMapHazardsHourly **only**:
  MapHazardAcidGeyser?.TickHour();
  MapHazardAshlanche?.TickMinute();
→ The 8 baseline hazards below are excluded from that tick.

| Prop                        | File                              | Example APIs                              | Prop.Method | Category      |
|-----------------------------|-----------------------------------|-------------------------------------------|-------------|---------------|
| MapHazardBiometricDoor      | MapHazard_BiometricDoor.cs        | SetRequiredCommander, TryOpen             | NONE        | HANDLERS_ONLY |
| MapHazardCraterWall         | MapHazard_CraterWall.cs           | AttemptClimb, CanVehiclePass              | NONE        | HANDLERS_ONLY |
| MapHazardCrevice            | MapHazard_Crevice.cs              | AttemptJump, BuildScrapBridge, IsBlocking | NONE        | HANDLERS_ONLY |
| MapHazardFlammableGas       | MapHazard_FlammableGas.cs         | CheckEquipment, NavigateNode              | NONE        | HANDLERS_ONLY |
| MapHazardGasPockets         | MapHazard_GasPockets.cs           | RegisterGasNode, FireWeapon, DetectGas    | NONE        | HANDLERS_ONLY |
| MapHazardMagneticAnomaly    | MapHazard_MagneticAnomaly.cs      | EnterNode, ExitNode, IsActive             | NONE        | HANDLERS_ONLY |
| MapHazardSinkholeCollapse   | MapHazard_SinkholeCollapse.cs     | WalkOver, GetDropNodeId                   | NONE        | HANDLERS_ONLY |
| MapHazardVenusTrap          | MapHazard_VenusTrap.cs            | EnterNode, AttemptHarvest, CheckDisguise  | NONE        | HANDLERS_ONLY |

[DEEP6-MAP-002] Severity: CRITICAL | Type: incomplete host / dead content
  Location: GameBootstrap.MapHazards.TickMapHazardsHourly; 8 MapHazard_* files
  Description: Host comment claims expedition will call Navigate/Attempt*. No such
  calls exist. Only AcidGeyser/Ashlanche receive time ticks (and even those still
  need node registration/trigger to matter mid-run).
  Note: AcidGeyser OnChemicalBurnsApplied handler DOES apply NeedsSystem health —
  that path is more real than pure Debug.Log, but still depends on TickHour +
  eruption state being entered somehow.
  Suggested Fix: Node-enter / weapon-fire hooks for GasPockets.FireWeapon,
  Crevice.AttemptJump, VenusTrap.EnterNode, etc.; extend TickMapHazardsHourly only
  for hazards with duration state.

[DEEP6-MAP-003] Severity: MEDIUM | Type: design smell
  Location: Diagnostics aliases map_hazards_hourly for AcidGeyser/Ashlanche only
  Description: Registry key name implies all map hazards; actually two systems.
  Easy to assume baseline MapHazard* are covered by the same tick — they are not.

--------------------------------------------------------------------------------
P6-D. NPC* (top 14 of 40 + full 34 scan) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

Boot: BootNPCFamily() constructs all 34; logs "NPC family ready (34 systems)."
**No WireNPC* / On* subscription block** (unlike Map/Event/Victory).
Save: InitLate SetNPC* (34 registrations).

Property-scoped Prop.Method for **every** NPC* in baseline:
  **NO_DOT_CALLS** (only `Prop = new …` in CoreFamilies + SaveSystem.Set*).

| Prop (top 14)           | File                         | Example APIs                         | Category            |
|-------------------------|------------------------------|--------------------------------------|---------------------|
| NPCAddictsPassive       | NPC_AddictsPassive.cs        | TradeMorphineForHighValueItem        | CONSTRUCT_SAVE_ONLY |
| NPCAggroScavengers      | NPC_AggroScavengers.cs       | TriggerNodeEntry, AdvanceIntoNode    | CONSTRUCT_SAVE_ONLY |
| NPCAggroTrader          | NPC_AggroTrader.cs           | ComplyAndBuyJunk, RefusePurchase     | CONSTRUCT_SAVE_ONLY |
| NPCBandits              | NPC_Bandits.cs               | InitiateExtortion, Pay/Refuse…       | CONSTRUCT_SAVE_ONLY |
| NPCBlackOps             | NPC_BlackOps.cs              | CheckPreEncounterAmbush              | CONSTRUCT_SAVE_ONLY |
| NPCBroker               | NPC_Broker.cs                | CheckAppearance, TryBuy              | CONSTRUCT_SAVE_ONLY |
| NPCCannibals            | NPC_Cannibals.cs             | TriggerEncounterHorror, GenerateLoot | CONSTRUCT_SAVE_ONLY |
| NPCChemScientists       | NPC_ChemScientists.cs        | CanTriggerAtLocation, StealResearch  | CONSTRUCT_SAVE_ONLY |
| NPCCityResidents        | NPC_CityResidents.cs         | LootContainer                        | CONSTRUCT_SAVE_ONLY |
| NPCCollaborators        | NPC_Collaborators.cs         | InterveneAndSave, WatchExecution     | CONSTRUCT_SAVE_ONLY |
| NPCConscripts           | NPC_Conscripts.cs            | KillOneConscript, TryFlee            | CONSTRUCT_SAVE_ONLY |
| NPCDesperateFamily      | NPC_DesperateFamily.cs       | GiveFoodCharity, RobFamily           | CONSTRUCT_SAVE_ONLY |
| NPCDrunksAggro          | NPC_DrunksAggro.cs           | TakeDamage                           | CONSTRUCT_SAVE_ONLY |
| NPCHomeless             | NPC_Homeless.cs              | InteractAndTrade                     | CONSTRUCT_SAVE_ONLY |

Remaining 20 NPC* (LonePsychopath … TravelingCouple): same CONSTRUCT_SAVE_ONLY,
same NO_DOT_CALLS. Full list: LonePsychopath, Looters, Mercenaries, MilitaryPatrol,
PassiveScavengers, PassiveTrader, PsychopathPair, RebelMilitia, RebelModerates,
RebelSnipers, RebelZealots, Slavers, SpecOps, Survivalists, TaxCollector, Terrorists,
TheNegotiator, TheOld, TheParents, TravelingCouple.

[DEEP6-NPC-001] Severity: CRITICAL | Type: dead content
  Location: GameBootstrap.CoreFamilies.BootNPCFamily; NPC_*.cs ×34
  Description: Entire overworld NPC encounter suite is instantiated and save-registered
  with rich public encounter APIs, but **nothing ever calls them** — not expedition
  encounters, not EventRunner, not trade UI, not hatch visitors (Visitor* is a
  separate baseline family).
  Player impact: bandits, brokers, military tolls, cannibals, rebel factions, etc.
  never run from these systems (any similar fiction in game must come from other
  encounter stacks).
  Suggested Fix: Expedition encounter resolver: location faction/tag → NPC*.Initiate*
  / Trigger*; or stop constructing/saving 34 unused objects (init cost + save bloat).

[DEEP6-NPC-002] Severity: HIGH | Type: dual / triple encounter stacks
  Location: NPC_* vs Encounter_* vs Visitor* vs EventRunner encounters
  Description: Same dual-stack problem as Event_* vs EventPool (Part 5). Multiple
  prompt-era implementations of "meet people on the map" coexist; only some paths
  are live. NPC_* is the densest fully-dead stack found so far (no handlers even).
  Suggested Fix: Content inventory doc: one canonical encounter host; demote others.

--------------------------------------------------------------------------------
P6-E. NOT EVENT-BUS / NOT ID-DRIVEN
--------------------------------------------------------------------------------

  • No MapAnomaly/MapHazard/NPC baseline member classified as EVENT_BUS_ONLY.
  • anomalyId / hazardId / npcId strings do not appear in EventPoolBuilder or
    StreamingAssets location data (spot-check).
  • Tests (MapAnomaliesWiringTests etc.) call APIs by constructing systems in
    isolation — green tests without expedition integration (same anti-pattern as
    pre-fix Weather_* unit ticks).

--------------------------------------------------------------------------------
P6-F. COUNTS & PRIORITY
--------------------------------------------------------------------------------

Top-40 categories:
  HANDLERS_ONLY .............. 26
  CONSTRUCT_SAVE_ONLY ........ 14
  Live in-slice ..............  0

Full MapAnomaly+MapHazard(baseline)+NPC:
  Dead systems ............... 60
  Live related (off-baseline)  2  (AcidGeyser, Ashlanche partial host tick)

New finding IDs:
  DEEP6-MAP-001, DEEP6-MAP-002, DEEP6-MAP-003
  DEEP6-NPC-001, DEEP6-NPC-002

Remediation order (map/NPC slice):
  1. Expedition node-enter dispatch for 3 flagship anomalies + 3 hazards
     (e.g. AshDunes, QuietZone, GasPockets, Crevice, VenusTrap, BiometricDoor).
  2. One NPC encounter path (Bandits or MilitaryPatrol) from expedition/trade UI.
  3. Demote remaining BootMap*/BootNPCFamily entries until entry points exist.
  4. Rename or document map_hazards_hourly so it is not mistaken for all MapHazard*.
  5. Do not expand Save wiring for new Map/NPC prompts without a caller test.

--------------------------------------------------------------------------------
P6-G. METHOD LIMITS
--------------------------------------------------------------------------------

  • Top 40 tabulated in detail; remaining 20 NPC confirmed NO_DOT_CALLS in bulk.
  • MapHazardAcidGeyser/Ashlanche analyzed as live contrast only (not baseline).
  • Did not prove mid-run eruption/avalanche without node registration (out of scope).
  • Unity playmode not run; static property call-graph only.
  • Visitor* / Encounter* / Location* families not in this pass (future Part 7+).

  Evidence:
    rg -n 'MapAnomalyAshDunes\s*\??\.\s*\w+' Assets --glob '*.cs'
    # → On* += and construct only
    read GameBootstrap.MapHazards.cs TickMapHazardsHourly  (AcidGeyser + Ashlanche only)
    for prop in NPC*: Prop.Method → NO_DOT_CALLS

========================================================================================
END OF PART 6 — TRIGGER AUDIT (MapAnomaly / MapHazard / NPC)
========================================================================================

========================================================================================
PART 7 — TRIGGER AUDIT: Visitor* + Encounter* + Location* + Action* (top 40)
Read-only | No code was modified
Generated (UTC): 2026-08-07 23:42:20
========================================================================================

SCOPE
----------------------------------------
UntickedSystemsBaseline counts:
  Visitor    11
  Encounter  15
  Location    4
  Action     41

TOP 40 = all Visitor (11) + all Encounter (15) + all Location (4) + first 10 Action
         = 40 systems.

FULL Action scan (remaining 31) included for category totals; all match top-10 Action
pattern.

Method: property-scoped Prop.Method( / Prop.OnX += (same as Parts 5–6).
Boot: BootVisitorFamily, BootEncounters/WireEncounters, BootLocationFamily,
      BootActionFamily (+ BootRemainingComplexFamily for a few Action_*).

LIVE CONTRAST (not these classes):
  ExpeditionSystem resolves **EncounterSO** ScriptableObjects via psychology/
  forced location encounters — a disjoint stack from Encounter_* C# trackers.
  EventPoolBuilder also pulls EncounterEventFactory.CreateAll() GameEvents.
  AI utility actions live under Assets/_Game/AI/Actions/*ActionSO.cs (~42 SOs),
  not Action_* Core trackers.

--------------------------------------------------------------------------------
P7-A. EXECUTIVE SUMMARY
--------------------------------------------------------------------------------

TOP 40:
  HANDLERS_ONLY .............. 15 / 40   (all Encounter*)
  CONSTRUCT_SAVE_ONLY ........ 25 / 40   (all Visitor* + all Location* + 10 Action*)
  EXTERNAL / BOOTSTRAP_LIVE ..  0 / 40

FULL Action (41) + V+E+L (30) = 71 scanned:
  HANDLERS_ONLY .............. 15
  CONSTRUCT_SAVE_ONLY ........ 56
  Live among baseline props ..  0

Parallel live stacks (player may still see "encounters"):
  • EncounterSO + ExpeditionSystem.ResolveEncounterWithPsychology
  • EncounterEventFactory → EventPoolBuilder GameEvents
  • AI ActionSO catalog (Eat, Drink, Craft, …)
These do **not** call Encounter_*, Visitor_*, Location_*, or Action_* bootstrap props.

--------------------------------------------------------------------------------
P7-B. Visitor* (11/11) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootVisitorFamily constructs 10 site visitors (no Wire* / On* block).
VisitorRNGSystem constructed in Misc family (CoreFamilies) + save-wired; never
InitializeMap/GetNodeState via property from production code.

| Prop                      | File                            | Example APIs                              | Prop.Method | Category            |
|---------------------------|---------------------------------|-------------------------------------------|-------------|---------------------|
| VisitorAbandonedState     | Visitor_AbandonedState.cs       | GenerateStructuralHazards                 | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorChurchHostile      | Visitor_ChurchHostile.cs        | AttemptGraveyardApproach                  | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorChurchSanctuary    | Visitor_ChurchSanctuary.cs      | RestMidExpedition                         | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorExplodedState      | Visitor_ExplodedState.cs        | TryInteractAndLoot                        | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorFleeingHorde       | Visitor_FleeingHorde.cs         | TriggerStormPanic                         | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorHospitalPatients   | Visitor_HospitalPatients.cs     | ApplyDespairAura                          | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorHospitalStaff      | Visitor_HospitalStaff.cs        | PayMedicalTax, RefuseMedicalTax           | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorMilTrainingYard    | Visitor_MilTrainingYard.cs      | NotifyPlayerFledAfterAttack, GenerateLoot | NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorQuestFaction       | Visitor_QuestFaction.cs         | TryInjectQuestVisitor(…, VisitorRNGSystem)| NONE        | CONSTRUCT_SAVE_ONLY |
| VisitorRNGSystem          | VisitorRNGSystem.cs             | InitializeMap, GetNodeState, AddCard      | NONE*       | CONSTRUCT_SAVE_ONLY |
| VisitorRebelTrainingYard  | Visitor_RebelTrainingYard.cs    | PerformFlankAttackOnMilitary, GenerateLoot| NONE        | CONSTRUCT_SAVE_ONLY |

*VisitorRNGSystem: tests construct and call APIs; production only `new` + SetVisitorRNGSystem.
  rg 'VisitorRNGSystem\.' → no production method calls.

[DEEP7-VIS-001] Severity: CRITICAL | Type: dead content
  Location: BootVisitorFamily; Visitor_*.cs; VisitorRNGSystem.cs
  Description: Map-node visitor fiction (church, hospital, training yards, abandoned
  states, quest faction inject) never runs. RNG deck system is save-perfect ghost.
  Suggested Fix: Wire VisitorRNGSystem.InitializeMap into map gen; node visit →
  GetNodeState → visitor APIs; or demote entire family.

[DEEP7-VIS-002] Severity: HIGH | Type: incomplete quest bridge
  Location: Visitor_QuestFaction.TryInjectQuestVisitor
  Description: API accepts VisitorRNGSystem but neither side is called from
  PersonalQuestSystem / expedition host. Quest-tied visitors cannot appear.

--------------------------------------------------------------------------------
P7-C. Encounter* (15/15) — ALL HANDLERS_ONLY
--------------------------------------------------------------------------------

BootEncounters + WireEncounters: On* → Debug.Log (same pattern as Event_*/MapAnomaly).
Rich combat/trap APIs exist; zero Prop.Engage/Resolve/TryHunt callers outside tests.

| Prop                    | File                          | Example APIs                            | Category      |
|-------------------------|-------------------------------|-----------------------------------------|---------------|
| EncounterAmalgamation   | Encounter_Amalgamation.cs     | ApplyDamage, TryFlee, IsDefeated        | HANDLERS_ONLY |
| EncounterBurrowers      | Encounter_Burrowers.cs        | TriggerBreach, FightBurrowers, Patch…   | HANDLERS_ONLY |
| EncounterFloodedMaze    | Encounter_FloodedMaze.cs      | EnterMaze, Surface                      | HANDLERS_ONLY |
| EncounterGlowingDead    | Encounter_GlowingDead.cs      | LootCorpse                              | HANDLERS_ONLY |
| EncounterGlowingStag    | Encounter_GlowingStag.cs      | TryHunt                                 | HANDLERS_ONLY |
| EncounterHitAndRun      | Encounter_HitAndRun.cs        | RunThemDown                             | HANDLERS_ONLY |
| EncounterLeeches        | Encounter_Leeches.cs          | AttachLeeches, TickHour, BurnOff…       | HANDLERS_ONLY |
| EncounterMirelurker     | Encounter_Mirelurker.cs       | ApplyDamage, CanEngage, TryFlee         | HANDLERS_ONLY |
| EncounterPressurePlate  | Encounter_PressurePlate.cs    | ResolveTrap                             | HANDLERS_ONLY |
| EncounterRiverPirates   | Encounter_RiverPirates.cs     | EngageCombat, SinkPirateBoat            | HANDLERS_ONLY |
| EncounterRoadblock      | Encounter_Roadblock.cs        | ResolveChoice                           | HANDLERS_ONLY |
| EncounterRobotDog       | Encounter_RobotDog.cs         | EngageCombat, TryHack                   | HANDLERS_ONLY |
| EncounterSleepingCamp   | Encounter_SleepingCamp.cs     | DiscoverCamp, TrySteal, TryFlee…        | HANDLERS_ONLY |
| EncounterTripwireMaze   | Encounter_TripwireMaze.cs     | GenerateMaze, TryPath, TryBypass        | HANDLERS_ONLY |
| EncounterWarlordTank    | Encounter_WarlordTank.cs      | AttackWithWeapon, AttackWithSmallArms   | HANDLERS_ONLY |

Evidence: ExpeditionSystem.Encounters.cs uses EncounterSO exclusively
(ResolveEncounterWithPsychology, FindForcedLocationEncounter). No reference to
EncounterRoadblock / EncounterLeeches properties.

[DEEP7-ENC-001] Severity: CRITICAL | Type: dual encounter stack
  Location: Encounter_*.cs ×15 vs ExpeditionSystem + EncounterSO + EncounterEventFactory
  Description: Class trackers duplicate encounter design already partially live via
  SOs/factory. Players may get SO encounters while prompt-class Encounter_* never
  fire — content desync and wasted save slots.
  Suggested Fix: Pick one host. Either (1) expedition node tags call Encounter_*
  methods, or (2) delete/demote Encounter_* and keep SO path only.

[DEEP7-ENC-002] Severity: HIGH | Type: false live signal
  Location: WireEncounters On* += Debug.Log; EncountersWiringTests
  Description: Tests `new Encounter_X()` and call ResolveChoice etc. → green without
  bootstrap dispatch (same anti-pattern as MapAnomaliesWiringTests / Weather unit ticks).

--------------------------------------------------------------------------------
P7-D. Location* (4/4) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootLocationFamily constructs four special locations; no wire block; no Prop.Method.

| Prop                       | File                           | Example APIs                                  | Category            |
|----------------------------|--------------------------------|-----------------------------------------------|---------------------|
| LocationArcade             | Location_Arcade.cs             | SearchForTokens, TryTrade                     | CONSTRUCT_SAVE_ONLY |
| LocationSlaveMarket        | Location_SlaveMarket.cs        | TryPurchaseSurvivor                           | CONSTRUCT_SAVE_ONLY |
| LocationStateRuinSystem    | LocationStateRuinSystem.cs     | ApplyRuinModifier, ComputeEffects, DrawRandom | CONSTRUCT_SAVE_ONLY |
| LocationStrandedYacht      | Location_StrandedYacht.cs      | DiscoverYacht, TryInfiltrate, LootLuxury      | CONSTRUCT_SAVE_ONLY |

[DEEP7-LOC-001] Severity: HIGH | Type: dead content
  Location: BootLocationFamily; Location_*.cs; LocationStateRuinSystem
  Description: Special nodes (arcade, slave market, yacht) and global ruin modifier
  system never applied on map enter. Ruin modifiers would affect loot/danger if
  ApplyRuinModifier were called from location resolve — it is not.
  Suggested Fix: Map location id → Location* handler on expedition arrive; ruin
  system tick/draw when generating/looting nodes.

--------------------------------------------------------------------------------
P7-E. Action* (top 10 of 40 + full 41) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootActionFamily (+ some in BootRemainingComplexFamily) constructs Action_* trackers.
Save SetAction* present. **No UI/AI path calls ActionFish / ActionExile / etc.**

TOP 10 (in top-40 table):
  ActionAdministerPlacebo, BarricadeDoor, BoilBatteries, BroadcastPropaganda,
  BurnCharcoal, BuryTimeCapsule, CallCaravan, CoverTracks, CrackMainframe, Crawlspace
  → all CONSTRUCT_SAVE_ONLY; APIs like Administer, Barricade, BoilBattery, RequestCaravan,
    StartCrack, SendChild never property-called.

FULL remaining 31 Action* (Decrypt … WorshipIdol): same CONSTRUCT_SAVE_ONLY /
NO production Prop.Method. Includes Fish, Exile, PullTooth, SealRoom, Sabotage,
ScorchedEarth, SlaughterPet, TeachChild, TellStories, WorshipIdol, etc.

Dual stack with AI:
  Assets/_Game/AI/Actions/*ActionSO.cs (~42) — utility AI actions (Eat, Drink, Craft…).
  These are the live-ish AI surface (with known bugs DEEP3-INV-003..005), not Action_*.

[DEEP7-ACT-001] Severity: CRITICAL | Type: dead content / dual action stack
  Location: BootActionFamily; Action_*.cs ×41 vs AI/Actions/*ActionSO.cs
  Description: Prompt-era bunker actions (barricade, exile, fish, sabotage, idol
  worship, time capsule, …) are fully inert on Core trackers. Any similar behavior
  must come from UI SOs, medical, or AI ActionSOs — not these types.
  Suggested Fix: UI command palette / room actions → Action_*.Execute*; or demote
  BootActionFamily entirely until product prioritizes those verbs.

[DEEP7-ACT-002] Severity: MEDIUM | Type: save bloat
  Location: SaveSystem.SetAction* ×41
  Description: 41 ISaveable registrations for systems that never mutate in play
  still serialize empty/stub state every save (some CaptureState empty stubs remain
  per Part 4 DEEP4-SAVE-STUB).

--------------------------------------------------------------------------------
P7-F. NOT EVENT-BUS-DRIVEN (this slice)
--------------------------------------------------------------------------------

  • Encounter_* / Visitor_* / Action_* are not EventPool members by id.
  • Live expedition encounters use EncounterSO + EventChoice, not these props.
  • No system in top-40 classified EXTERNAL_CALLERS or BOOTSTRAP_TRIGGERED.

--------------------------------------------------------------------------------
P7-G. CROSS-PART SYNTHESIS (Parts 5–7 ghost density)
--------------------------------------------------------------------------------

Property-scoped dead systems documented across trigger audits:

  Part 5 Event* .............. 27 HANDLERS_ONLY (0 external)
  Part 5 Victory* ............ 11 HANDLERS_ONLY + 3 tick-cond without Start
  Part 6 MapAnomaly* ......... 18 HANDLERS_ONLY
  Part 6 MapHazard* baseline .  8 HANDLERS_ONLY
  Part 6 NPC* ................ 34 CONSTRUCT_SAVE_ONLY
  Part 7 Visitor* ............ 11 CONSTRUCT_SAVE_ONLY
  Part 7 Encounter* .......... 15 HANDLERS_ONLY
  Part 7 Location* ...........  4 CONSTRUCT_SAVE_ONLY
  Part 7 Action* ............. 41 CONSTRUCT_SAVE_ONLY

  Subtotal pure ghost families (approx): **~169 systems** with zero production
  Prop.Method triggers (plus Victory Start gaps).

  Live parallel stacks that partially fulfill fantasy:
    EventPoolBuilder / GameEvent, EncounterSO expeditions, AI ActionSO,
    MapHazardAcidGeyser/Ashlanche ticks, AdaptiveWarlords/ChemUse/DiseaseExpansion.

[DEEP7-META-001] Severity: CRITICAL | Type: architecture / content pipeline
  Description: Prompt→class→Boot*→Save Set*→Wire On* Debug.Log has become the
  default shipping path without expedition/UI entry points. Baseline ratchet (435)
  is accurate: most "content systems" are inert composition-root weight.
  Suggested Fix: Content gate CI — new GameBootstrap public system property must
  have (a) registry tick OR (b) ≥1 non-test Prop.Method caller OR (c) explicit
  Dormant attribute + no Save registration.

--------------------------------------------------------------------------------
P7-H. COUNTS & PRIORITY
--------------------------------------------------------------------------------

Top-40: HANDLERS_ONLY 15 | CONSTRUCT_SAVE_ONLY 25 | Live 0

New finding IDs:
  DEEP7-VIS-001, DEEP7-VIS-002
  DEEP7-ENC-001, DEEP7-ENC-002
  DEEP7-LOC-001
  DEEP7-ACT-001, DEEP7-ACT-002
  DEEP7-META-001

Remediation order (this slice):
  1. Do **not** add more Action_/Encounter_/Visitor_ prompts without entry-point tests.
  2. Flagship: 1 Encounter_* OR document SO path as sole encounter host and demote
     all Encounter_* from BootEncounters.
  3. Wire VisitorRNGSystem into map gen + one visitor site (church or hospital).
  4. One Location special (Yacht or Arcade) on expedition arrive.
  5. One Action_* (e.g. Exile or BarricadeDoor) from UI; demote the other 40 until used.
  6. Adopt DEEP7-META-001 content gate for all future Boot* families.

--------------------------------------------------------------------------------
P7-I. METHOD LIMITS
--------------------------------------------------------------------------------

  • Top 40 detailed; remaining Action* bulk-confirmed CONSTRUCT_SAVE_ONLY.
  • Did not inventory every EncounterSO id vs Encounter_* name parity.
  • Did not run playmode expedition to list which SO encounters fire.
  • VisitorRNG GetSaveData naming may bypass CaptureState pattern — still no callers.
  • Static analysis only.

  Evidence:
    rg -n 'EncounterRoadblock\s*\??\.\s*\w+' Assets --glob '*.cs'  → On* / construct
    head ExpeditionSystem.Encounters.cs → EncounterSO only
    rg -n 'VisitorRNGSystem\.' Assets --glob '*.cs' → none (production)
    BootVisitorFamily / BootActionFamily → new only, no Wire

========================================================================================
END OF PART 7 — TRIGGER AUDIT (Visitor / Encounter / Location / Action)
========================================================================================

========================================================================================
PART 8 — TRIGGER AUDIT: WorldEvent* + Biome* + Weapon* + Crisis* + Trait*
(remainder of primary baseline taxonomy prefixes)
Read-only | No code was modified
Generated (UTC): 2026-08-07 23:44:53
========================================================================================

SCOPE
----------------------------------------
All UntickedSystemsBaseline entries for these prefixes (complete buckets, n=28):

  WorldEvent   5
  Biome        6
  Weapon       4
  Crisis       2
  Trait       11
  ──────────────
  Total       28

Method: property-scoped Prop.Method( / Prop.OnX += (same as Parts 5–7).
Boot: BootWorldEventFamily, BootBiomes/WireBiomes, BootWeaponFamily,
      BootCrisisFamily, BootTraitFamily (+ BootRemainingComplexFamily for 5 traits).

--------------------------------------------------------------------------------
P8-A. EXECUTIVE SUMMARY
--------------------------------------------------------------------------------

  HANDLERS_ONLY ..............  6 / 28   (all Biome*)
  CONSTRUCT_SAVE_ONLY ........ 22 / 28   (all WorldEvent, Weapon, Crisis, Trait)
  EXTERNAL / BOOTSTRAP_LIVE ..  0 / 28

No production Prop.Method triggers in this slice.
Biome* matches MapAnomaly pattern (Wire On* → Debug.Log only).
WorldEvent / Weapon / Crisis / Trait match NPC/Action pattern (construct + save only).

LIVE PARALLELS (different types — not these props):
  • WorldPhaseSystem (CivilWar → Flashpoint → NuclearWinter) — ticked via TimeSystem
  • PersonalQuestSystem string traits (e.g. trait_matriarch, Matriarch room bonus) — LIVE
  • Survivor.HasTrait(string) + SpatialPsychology (claustrophobic/agoraphobic strings)
  • StructuralIntegritySystem (live bunker integrity) vs dead Crisis_StructuralFailure
  • Item/weapon catalog combat path (ammo, hatch defense) vs dead Weapon_* heavy toys

--------------------------------------------------------------------------------
P8-B. WorldEvent* (5/5) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootWorldEventFamily: new only; no Wire; no day-tick host calling CheckActivation.

| Prop                     | File                           | Example APIs                                         | Category            |
|--------------------------|--------------------------------|------------------------------------------------------|---------------------|
| WorldEventDeforestation  | WorldEvent_Deforestation.cs    | CheckActivation, GetWoodLootMultiplier, CanCraft…    | CONSTRUCT_SAVE_ONLY |
| WorldEventFinalWinter    | WorldEvent_FinalWinter.cs      | CheckActivation, TickDay, GetTemperatureOverride…    | CONSTRUCT_SAVE_ONLY |
| WorldEventFissure        | WorldEvent_Fissure.cs          | Trigger, DestroyConnection, IsPathBlocked, CanCross  | CONSTRUCT_SAVE_ONLY |
| WorldEventGreatFamine    | WorldEvent_GreatFamine.cs      | CheckActivation, GetFoodLootMultiplier, hydro/cannibal| CONSTRUCT_SAVE_ONLY |
| WorldEventMegafauna      | WorldEvent_Megafauna.cs        | Trigger, TickDay, Hunt                               | CONSTRUCT_SAVE_ONLY |

[DEEP8-WE-001] Severity: CRITICAL | Type: dead content / dual world timeline
  Location: BootWorldEventFamily; WorldEvent_*.cs ×5; WorldPhaseSystem.cs
  Description: Campaign already advances WorldPhase (CivilWar/Flashpoint/NuclearWinter)
  on day ticks. Separate WorldEvent_* systems for Final Winter, Famine, Deforestation,
  Fissure, Megafauna are never CheckActivation'd or TickDay'd. Temperature/loot
  multipliers they own never apply.
  Suggested Fix: On WorldPhase change or day N, call CheckActivation; apply multipliers
  in loot/temp paths; or demote and fold into WorldPhase / TemperatureSystem only.

--------------------------------------------------------------------------------
P8-C. Biome* (6/6) — ALL HANDLERS_ONLY
--------------------------------------------------------------------------------

BootBiomes + WireBiomes: On* → Debug.Log. EnterBiome/TickHour/ScoutHouse never called
from expedition/map host.

| Prop                 | File                       | Example APIs                                      | Category      |
|----------------------|----------------------------|---------------------------------------------------|---------------|
| BiomeAshSwamp        | Biome_AshSwamp.cs          | EnterBiome, GetMovementSpeedMult, CanStealth      | HANDLERS_ONLY |
| BiomeGlassDesert     | Biome_GlassDesert.cs       | EnterBiome, GetCover, GetTemperature, Scavenge    | HANDLERS_ONLY |
| BiomeHighwayTunnel   | Biome_HighwayTunnel.cs     | EnterBiome, SiphonGas, CheckMutantEncounter       | HANDLERS_ONLY |
| BiomeSaltFlats       | Biome_SaltFlats.cs         | EnterBiome, TickHour, HarvestSalt, CanCross       | HANDLERS_ONLY |
| BiomeSkyscraperTops  | Biome_SkyscraperTops.cs    | Traverse, ScavengeTurbine, GetColdExposure        | HANDLERS_ONLY |
| BiomeSuburbs         | Biome_Suburbs.cs           | ScoutHouse, RevealFog, GetAdjacentTileIds         | HANDLERS_ONLY |

Evidence: BiomesWiringTests construct systems and call EnterBiome in isolation
(same false-green pattern as EncountersWiringTests / MapAnomaliesWiringTests).

[DEEP8-BIO-001] Severity: CRITICAL | Type: dead content
  Location: GameBootstrap.Biomes.cs; Biome_*.cs ×6
  Description: Distinct biome traversal rules never run when parties enter map
  regions. Movement/thirst/cover/stealth modifiers are dead code.
  Suggested Fix: Map node biome tag → BiomeX.EnterBiome on expedition enter;
  TickHour for SaltFlats while present.

--------------------------------------------------------------------------------
P8-D. Weapon* (4/4) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootWeaponFamily: Chainsaw, Flamethrower, HMG, RPG. No combat/hatch host calls.

| Prop               | File                      | Example APIs                                    | Category            |
|--------------------|---------------------------|-------------------------------------------------|---------------------|
| WeaponChainsaw     | Weapon_Chainsaw.cs        | Attack, FlushHordeSpawn, GetFuelCost, noise     | CONSTRUCT_SAVE_ONLY |
| WeaponFlamethrower | Weapon_Flamethrower.cs    | GetFuelCost, GetFearRadius, GetTankExplodeChance| CONSTRUCT_SAVE_ONLY |
| WeaponHMG          | Weapon_HMG.cs             | Mount, Fire, Unjam, CanFire                     | CONSTRUCT_SAVE_ONLY |
| WeaponRPG          | Weapon_RPG.cs             | FireAtHatch, GetSecurityDamage                  | CONSTRUCT_SAVE_ONLY |

[DEEP8-WPN-001] Severity: HIGH | Type: dead content
  Location: BootWeaponFamily; Weapon_*.cs ×4
  Description: Heavy/special weapons never mount/fire from hatch defense or
  expedition combat. HatchDefense / ammo path is separate.
  Suggested Fix: HatchDefense weapon slot → WeaponHMG.Fire / WeaponRPG.FireAtHatch;
  or demote until siege UI exists.

--------------------------------------------------------------------------------
P8-E. Crisis* (2/2) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootCrisisFamily only; save-wired; zero Prop.TickDay/TriggerBreach callers.

| Prop                      | File                            | Example APIs                                      | Category            |
|---------------------------|---------------------------------|---------------------------------------------------|---------------------|
| CrisisFeralFlora          | Crisis_FeralFlora.cs            | TickDay, ApplyVentClogDamage, TryClearWithMachete | CONSTRUCT_SAVE_ONLY |
| CrisisStructuralFailure   | Crisis_StructuralFailure.cs     | TickDay, TriggerBreach, IsBunkerHabitable…        | CONSTRUCT_SAVE_ONLY |

[DEEP8-CRS-001] Severity: HIGH | Type: dual crisis / incomplete
  Location: Crisis_*; StructuralIntegritySystem (live, registry-ticked)
  Description: StructuralIntegritySystem already damages modules / integrity on the
  live tick path (Part 3–4). Crisis_StructuralFailure (3-day collapse at 0% integrity
  fiction) never TickDay's — so its breach/habitability override never runs.
  Feral flora vent clog similarly never starts.
  Suggested Fix: StructuralIntegrity at 0 → CrisisStructuralFailure.TickDay; flora
  count from harvest system → CrisisFeralFlora.TickDay; or merge into StructuralIntegrity.

--------------------------------------------------------------------------------
P8-F. Trait* (11/11) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

BootTraitFamily constructs 6; BootRemainingComplexFamily constructs AshTongue,
Kleptomaniac, Mascot, StuntedEmpathy, Superstitious (5 more). No Wire; no Prop.Method.

| Prop                     | File                            | Example APIs                                      | Category            |
|--------------------------|---------------------------------|---------------------------------------------------|---------------------|
| TraitAnthropophobia      | Trait_Anthropophobia.cs         | DevelopPhobia, NotifyHumanEnteredRoom…            | CONSTRUCT_SAVE_ONLY |
| TraitAshTongue           | Trait_AshTongue.cs              | AssignTrait, TranslatePhrase, CoinDialectWord     | CONSTRUCT_SAVE_ONLY |
| TraitClairvoyant         | Trait_Clairvoyant.cs (ClairvoyantSystem) | GeneratePrediction                         | CONSTRUCT_SAVE_ONLY |
| TraitGenerationalTrauma  | Trait_GenerationalTrauma.cs     | RecordParentBreak, ApplyOnTransition              | CONSTRUCT_SAVE_ONLY |
| TraitInheritedGenetics   | Trait_InheritedGenetics.cs      | CheckInheritance, GetAffinityPenalty              | CONSTRUCT_SAVE_ONLY |
| TraitKleptomaniac        | Trait_Kleptomaniac.cs           | DevelopTrait, TickHour, GetStolenItems            | CONSTRUCT_SAVE_ONLY |
| TraitMascot              | Trait_Mascot.cs                 | CheckMascotActive, ApplyBuff/RemoveBuff           | CONSTRUCT_SAVE_ONLY |
| TraitMatriarch           | Trait_Matriarch.cs              | CheckTrait, WillDescendantSide, HasTrait          | CONSTRUCT_SAVE_ONLY |
| TraitPTSD                | Trait_PTSD.cs                   | AssignTrigger, CheckTrigger, StartPanic, TickHour | CONSTRUCT_SAVE_ONLY |
| TraitStuntedEmpathy      | Trait_StuntedEmpathy.cs         | WitnessDeath, OnGrowingUp                         | CONSTRUCT_SAVE_ONLY |
| TraitSuperstitious       | Trait_Superstitious.cs          | AssignTrait, CheckInventory, HandleItemLost…      | CONSTRUCT_SAVE_ONLY |

[DEEP8-TRT-001] Severity: CRITICAL | Type: dual trait stack
  Location: Trait_*.cs ×11 vs PersonalQuestSystem / Survivor.HasTrait(string)
  Description: LIVE matriarch behavior is PersonalQuestSystem.MatriarchId
  ("trait_matriarch") + NeedsSystem room health bonus — **not** TraitMatriarch
  property. Trait_Matriarch.CheckTrait is never called. Same risk for any trait that
  exists both as string id on survivors and as a Core tracker class.
  Suggested Fix: Single trait host. Either call Trait_* from PersonalQuest assign
  paths, or delete Core Trait_* and keep string traits only.

[DEEP8-TRT-002] Severity: HIGH | Type: dead content
  Location: TraitPTSD, TraitKleptomaniac, TraitAnthropophobia, etc.
  Description: Hourly/room-enter APIs never hooked to NeedsSystem tick or room
  occupancy changes. PTSD panic, klepto steal, anthropophobia panic cannot fire.
  Suggested Fix: NeedsSystem/Medical/room move hooks; or demote.

--------------------------------------------------------------------------------
P8-G. NOT EVENT-BUS / NOT ID-DRIVEN
--------------------------------------------------------------------------------

  • None of the 28 classified as EVENT_BUS_ONLY or EXTERNAL_CALLERS.
  • WorldEvent CheckActivation not linked to WorldPhaseSystem.
  • Biome EnterBiome not linked to map coordinates/biome tags.

--------------------------------------------------------------------------------
P8-H. BASELINE TAXONOMY CLOSURE (Parts 5–8)
--------------------------------------------------------------------------------

Primary prefix families on UntickedSystemsBaseline now trigger-audited:

  Family              n    Dominant category           Live parallel?
  ────────────────── ───  ──────────────────────────  ──────────────────────────
  Event*              27   HANDLERS_ONLY               EventPool / GameEvent
  Victory*            14   HANDLERS + 3 tick-cond      VictoryProject / EndgameEngine
  MapAnomaly*         18   HANDLERS_ONLY               (none)
  MapHazard* (base)    8   HANDLERS_ONLY               AcidGeyser/Ashlanche ticks
  NPC*                34   CONSTRUCT_SAVE_ONLY         EncounterSO / visitors?
  Visitor*            11   CONSTRUCT_SAVE_ONLY         (none / hatch?)
  Encounter*          15   HANDLERS_ONLY               EncounterSO + factory
  Location*            4   CONSTRUCT_SAVE_ONLY         map locations generic
  Action*             41   CONSTRUCT_SAVE_ONLY         AI ActionSO
  WorldEvent*          5   CONSTRUCT_SAVE_ONLY         WorldPhaseSystem
  Biome*               6   HANDLERS_ONLY               (none proven)
  Weapon*              4   CONSTRUCT_SAVE_ONLY         item/ammo combat
  Crisis*              2   CONSTRUCT_SAVE_ONLY         StructuralIntegrity
  Trait*              11   CONSTRUCT_SAVE_ONLY         PersonalQuest string traits
  ────────────────── ───
  Subtotal           200   ≈0 production Prop.Method among pure ghosts

  Remaining baseline (~235) still includes Affliction*, Item*, MapAnomaly already
  done, plus Other (Project*, Node*, Hazard*, Audio*, Mobile*, etc.) partially
  sampled in Part 5 Other9.

  Updated ghost estimate Parts 4–8: **200+** composition-root systems with no
  production trigger, against ~77 registry per-substep live names.

[DEEP8-META-001] Severity: CRITICAL | Type: architecture (extends DEEP7-META-001)
  Description: Remainder taxonomy confirms the pattern is universal across prompt
  families, not limited to events/map/NPC. Dual stacks repeatedly leave the
  string/SO/live path working while the Boot* class tracker rots with save wiring.
  Suggested Fix: Freeze Boot* growth; content gate CI; demote in batches by family
  (Action 41, NPC 34, Event 27, Affliction tick wiring separate).

--------------------------------------------------------------------------------
P8-I. COUNTS & PRIORITY
--------------------------------------------------------------------------------

Part 8 categories: HANDLERS_ONLY 6 | CONSTRUCT_SAVE_ONLY 22 | Live 0

New finding IDs:
  DEEP8-WE-001
  DEEP8-BIO-001
  DEEP8-WPN-001
  DEEP8-CRS-001
  DEEP8-TRT-001, DEEP8-TRT-002
  DEEP8-META-001

Remediation order (this slice):
  1. WorldEventFinalWinter/GreatFamine CheckActivation on WorldPhase or day N —
     high fantasy payoff if nuclear winter is domain-core.
  2. Resolve Trait dual stack: document PersonalQuest as sole matriarch host;
     demote TraitMatriarch or bridge CheckTrait.
  3. CrisisStructuralFailure.TickDay from StructuralIntegrity at 0 (or delete).
  4. One Biome EnterBiome from expedition region tag.
  5. WeaponRPG/HMG from hatch UI or demote.
  6. Global: content gate (DEEP7-META-001 / DEEP8-META-001).

--------------------------------------------------------------------------------
P8-J. METHOD LIMITS
--------------------------------------------------------------------------------

  • All 28 prefix members audited; Affliction*/Item*/Other residual not re-done.
  • Did not prove WorldPhase.NuclearWinter equates to FinalWinter design intent.
  • PersonalQuest matriarch verified live; other string traits not fully inventoried.
  • Static analysis only; playmode not run.

  Evidence:
    rg -n 'WorldEventFinalWinter\s*\??\.\s*\w+' Assets --glob '*.cs' → none prod
    rg -n 'BiomeSaltFlats\s*\??\.\s*\w+' → On* += only
    rg -n 'TraitMatriarch\s*\??\.\s*\w+' → none; PersonalQuest HasMatriarch live
    BootWorldEventFamily / BootCrisisFamily / BootWeaponFamily → construct only

========================================================================================
END OF PART 8 — TRIGGER AUDIT (WorldEvent / Biome / Weapon / Crisis / Trait)
========================================================================================

========================================================================================
PART 9 — TRIGGER AUDIT: Affliction* + residual Other
         + DEMOTE / WIRE IMPLEMENTATION PLAN
Read-only | No code was modified
Generated (UTC): 2026-08-07 23:48:12
========================================================================================

SCOPE
----------------------------------------
Completes residual UntickedSystemsBaseline inventory after Parts 5–8.

  Affliction* .............. 21 (full bucket)
  Residual Other ........... ~254 scanned this pass:
    Project 6, Node 7, Hazard 7, UIEvent 8, ShelterEvent 6, Vehicle 5, Pet 2,
    Weather (baseline residual) 7, Combat 4, Siege 8, Skirmish 5,
    Item 69, ShelterModule 46, OtherMisc 53

Method: property-scoped Prop.Method / Prop.OnX += (same as Parts 5–8).

--------------------------------------------------------------------------------
P9-A. AFFLICTION* (21/21) — ALL CONSTRUCT_SAVE_ONLY
--------------------------------------------------------------------------------

All AfflictionAdrenalineCrash … AfflictionZoonoticFlu: construct + save (SaveMap for
several per Part 4) + **zero production Prop.Method**. TickDay/Contract* APIs never
called from MedicalSystem or expedition hosts.

| Prop (all CONSTRUCT_SAVE_ONLY) |
|--------------------------------|
| AdrenalineCrash, Amnesia, Brainwashed, BrittleBones, BunkerFever, CaveMadness |
| FeralRegression, ImaginaryFriend, NerveDamage, OldAge, PhantomLimb |
| RadHallucinations, RadiationBlindness, ScurvyDegeneration, SporeLung, Sterile |
| SurvivorsGuilt, TBI, ThyroidCancer, TrenchFoot, ZoonoticFlu |

[DEEP9-AFF-001] Severity: CRITICAL | Type: dual medical stack
  Location: Affliction_*.cs ×21 vs MedicalSystem + AfflictionSO.Ids
  Description: LIVE medical path is MedicalSystem.Tick + AfflictionSO registry
  (bacterial_infection, sepsis, blood_loss, …). Parallel prompt Affliction_* class
  trackers (spore lung, trench foot, TBI, …) are save-wired ghosts — same dual-stack
  class as Trait_* vs PersonalQuest strings (DEEP8-TRT-001).
  Suggested Fix: Either (1) MedicalSystem.Inflict bridges to Affliction_* systems, or
  (2) demote Affliction_* from Boot/Save and encode as AfflictionSO only, or
  (3) wire TickDay into medical substep for map-owned systems only (SporeLung room
  spread, TrenchFoot wet boots).

Related (OtherMisc, not Affliction* prefix but same class):
  TetanusAfflictionSystem, BurnWardSystem, CognitiveDecaySystem, SevereFrostbiteSystem,
  VisionLossSystem, ToothDecaySystem — Part 4 DEEP4-ORPHAN; still CONSTRUCT_SAVE_ONLY
  this pass.

--------------------------------------------------------------------------------
P9-B. RESIDUAL FAMILY TABLES
--------------------------------------------------------------------------------

P9-B1. Project* (6) — ALL CONSTRUCT_SAVE_ONLY
  BioReactor, DeepWell, Elevator, Minecart, RadioArray, SurfaceDome
  APIs TickDay/Start exist; never host-ticked. Dual with Victory/radio live paths.

P9-B2. Node* (7) — ALL CONSTRUCT_SAVE_ONLY
  AutomatedArmory, GhostShip, MutantHive, Orphanage, PlayerBank, Sector7G, SporeHive
  Special map nodes never Discover/Enter from expedition.

P9-B3. Hazard* (7) — ALL CONSTRUCT_SAVE_ONLY
  CookOff, ExplosiveCrafting, FriendlyFire, Methane, MimicCrate, SurgicalBotch, WeaponBurst
  Crafting/combat hazard rolls never invoked (distinct from MapHazard_*).

P9-B4. UIEvent* (8) — ALL CONSTRUCT_SAVE_ONLY
  BlurredVision, CorruptionScare, FalseInventory, GhostRadio, Hacking, LowPower,
  MapRot, PhantomBlip
  Diegetic UI scares never CheckActivation from radiation/power hosts.

P9-B5. ShelterEvent* (6) — ALL CONSTRUCT_SAVE_ONLY
  CaravanAmbush, FalseCure, Ransom, Refugees, TheMirror, Tribute
  Hatch/visitor events never scheduled (dual with Event_* / Visitor_*).

P9-B6. Vehicle* (5) — ALL CONSTRUCT_SAVE_ONLY
  ArmoredTruck, Motorcycle, Rowboat, VehicleSystem, VehicleStrandingSystem
  SaveMap-ready (Part 4) but no expedition vehicle tick.

P9-B7. Pet* (2) — ALL CONSTRUCT_SAVE_ONLY
  PetFeralCat, PetGuardDog
  LIVE parallel: PetSystem registry-ticked ("pets") — cats/dogs in Pet_* trackers unused.

P9-B8. Weather residual (7) — ALL HANDLERS_ONLY
  AcidSnow, BioFog, BlackSnow, DeadWind, DustDevil, GlassStorm, RadHail
  Host comment (GameBootstrap.Weather.cs): deliberately not ticked until contextual
  args exist (filter durability, hazmat, map node). LIVE weather ticks:
  SolarFlare, SilentSpring, StaticCharge, BloodRain, EmpStorm, FalseSpring,
  DeepFreeze, OzoneHole (daily) — not on baseline.

P9-B9. Combat* (4) — ALL CONSTRUCT_SAVE_ONLY
  BleedOut, Flanking, StanceLastStand, Suppression

P9-B10. Siege* (8)
  BOOTSTRAP_TRIGGERED (2): SiegeBiowarfare.TickHour, SiegeBlockade.TickDay/IsExpeditionLocked
    (BatchSystems) — still on baseline (batch name ≠ property; may need Start to matter)
  CONSTRUCT_SAVE_ONLY (6): Artillery, HostageShield, NightRaid, Sappers, SmokeOut, VehicleRam

P9-B11. Skirmish* (5) — ALL CONSTRUCT_SAVE_ONLY
  Bandit_vs_Terror, Mil_vs_Rebel, Mil_vs_Terror, Rebel_vs_Bandit, Rebel_vs_Terror

P9-B12. Item* (69)
  BOOTSTRAP_TRIGGERED (1): ItemAmmoTypes — hatch/expedition/scavenge/crafting bindings LIVE
  CONSTRUCT_SAVE_ONLY (68): rest of Item_* prompt gadgets (vitamins, caltrops, night vision…)

P9-B13. ShelterModule* (46) — ALL HANDLERS_ONLY
  Autodoc, BatteryBank, DeconShower, Turret, VaultDoor, WoodStove, … (full list in baseline)
  Constructed + On* log wire; module gameplay likely also on ShelterModuleInstance SO path
  (dual stack risk). Not registry-ticked as individual props.

P9-B14. OtherMisc LIVE (BOOTSTRAP_TRIGGERED) — baseline noise / partial live
  AdaptiveWarlords, ChemUse, DiseaseExpansion, ExpeditionEncounterLog,
  IronMan, Sheriff, SpeedrunTimer, TrueEnding
  (+ ModLoader/TwitchAPI host hooks — edge tooling)
  CONSTRUCT_SAVE_ONLY remainder: AndroidNpcs, AshDrift, BurnWard, CognitiveDecay,
  FalloutStormHazard, HiddenStatUnseen, LightningStrikes, MapAquifer, MobileCamp,
  ModuleRationLock, MoralDilemma, NeedleSterilization, ProstheticCrafting, ScenarioGen,
  SeismicVents, SevereFrostbite, Tetanus, ToothDecay, VisionLoss, TraderPlagueConvoy,
  DurabilitySuppressor, EndgameUltimatum, …

--------------------------------------------------------------------------------
P9-C. PART 9 CATEGORY TOTALS (this pass scan ~254 systems)
--------------------------------------------------------------------------------

  CONSTRUCT_SAVE_ONLY .... 188
  HANDLERS_ONLY ..........  53  (Weather residual 7 + ShelterModule 46)
  BOOTSTRAP_TRIGGERED ....  13  (AmmoTypes, AdaptiveWarlords, ChemUse, Disease,
                                 ExpeditionEncounterLog, IronMan, Sheriff, Speedrun,
                                 TrueEnding, Siege×2, ModLoader/Twitch tooling)
  EXTERNAL_CALLERS .......   0

[DEEP9-RES-001] Severity: CRITICAL | Type: content volume
  Description: Residual Other is dominated by ShelterModule* (46) HANDLERS_ONLY and
  Item* (68) construct-only gadgets. Combined with Parts 5–8, baseline ghosts are not
  a minority — they are the majority of GameBootstrap surface area.

[DEEP9-RES-002] Severity: HIGH | Type: dual stacks (inventory)
  Live SO/system path              Dead Boot* class path
  ─────────────────────────────    ─────────────────────────────
  MedicalSystem + AfflictionSO     Affliction_* ×21 + tetanus/frost/…
  PersonalQuest string traits      Trait_* ×11
  EncounterSO + factory            Encounter_* ×15
  EventPool GameEvent              Event_* ×27
  PetSystem                        PetFeralCat / PetGuardDog
  WorldPhaseSystem                 WorldEvent_* ×5
  StructuralIntegrity              CrisisStructuralFailure
  Weather_* (solar/emp/…)          Weather AcidSnow/GlassStorm/… (baseline 7)
  ItemAmmoTypes + catalog          Item_* gadgets ×68
  Shelter modules (SO instance)    ShelterModule_* ×46 trackers

[DEEP9-RES-003] Severity: MEDIUM | Type: baseline honesty
  SiegeBiowarfare/Blockade, TrueEnding, IronMan, DiseaseExpansion, ChemUse, etc. are
  batch-driven but remain on UntickedSystemsBaseline because IsSystemTicked checks
  property aliases, not batch_systems_hourly. Fix aliases or shrink baseline (DEEP5-OTH-002 class).

--------------------------------------------------------------------------------
P9-D. FULL AUDIT CENSUS (Parts 4–9)
--------------------------------------------------------------------------------

  Approximate production-dead Boot* systems (zero Prop.Method): **350+** of 435 baseline
  (remainder are HANDLERS_ONLY same effective deadness, or baseline-listed partial live).

  Truly live counterexamples on/near baseline:
    ItemAmmoTypes, AdaptiveWarlords, ChemUse, DiseaseExpansion, TrueEnding (batch),
    IronMan (batch), SiegeBiowarfare/Blockade (batch, may need Start),
    Weather solar/emp set (not on baseline), PetSystem, MedicalSystem, WorldPhase…

--------------------------------------------------------------------------------
P9-E. DEMOTE / WIRE IMPLEMENTATION PLAN
--------------------------------------------------------------------------------

Goal: stop growing ghosts; make player-facing domain honest; shrink baseline only by
wiring or demoting (never by silencing tests).

────────────────────────────────────────────────────────────────────────────────
PHASE 0 — FREEZE (1 PR, no gameplay change)          Priority: IMMEDIATE
────────────────────────────────────────────────────────────────────────────────
0.1  Content gate CI (DEEP7/8-META):
     New public GameBootstrap property must satisfy ONE of:
       (a) SystemRegistry name (per-substep/daily/event/save-only) that maps in Diagnostics
       (b) ≥1 non-test Prop.Method caller outside defining file
       (c) [DormantContent] attribute + NOT registered with SaveSystem
     Fail EditMode test on violation.

0.2  Tag all baseline props with enum ContentLifecycle { Live, PartialHost, Dormant }
     in a single generated or hand-maintained docs/CONTENT_LIFECYCLE.md (or code map).

0.3  Fix Diagnostics aliases for partial-live batch systems so baseline can shrink
     without lying (DiseaseExpansion, ChemUse, AdaptiveWarlords, TrueEnding, IronMan,
     SiegeBiowarfare, SiegeBlockade, ItemAmmoTypes).

Exit: no new Dormant systems can ship save-wired.

────────────────────────────────────────────────────────────────────────────────
PHASE 1 — WAVE 0 HONESTY (domain core)               Priority: CRITICAL (Part 3–4)
────────────────────────────────────────────────────────────────────────────────
Not baseline ghosts — production bugs that make the game lie:

  1. Inventory.Consume hunger/thirst sign (DEEP4-INV-001)
  2. NeedsSystem isNearHeatSource ≠ sv => true (DEEP-002)
  3. RadiationSystem ExposureContext (RAD-001 / DEEP-003)
  4. EndgameEngine → ApplyEndgame (DEEP4-WIN-001)
  5. Generator noise clear (CMB-001)
  6. Dual AutoSave / clock residual (SAVE-004, DEEP4-TIME-001)
  7. Expedition flashpoint capture (SAVE-002)

Exit: first-hour survival loop matches domain promise. Do this BEFORE mass demote so
playtests are meaningful.

────────────────────────────────────────────────────────────────────────────────
PHASE 2 — DEMOTE BATCH A (largest pure ghosts)       Priority: HIGH  ~150 systems
────────────────────────────────────────────────────────────────────────────────
Demote = remove from Boot* + SaveSystem.Set* + Diagnostics map; keep source files
behind #if ASHFALL_DORMANT_CONTENT or move to Assets/_Game/Dormant/ excluded from
player asmdef if desired. Keep unit tests that `new` the type.

  A1. Action_* ×41
  A2. NPC_* ×34
  A3. Item_* ×68 except ItemAmmoTypes (and any later proven live)
  A4. ShelterModule_* ×46 (if SO modules already provide gameplay; verify one
      Autodoc path before demote)

Process per family:
  1) Confirm zero Prop.Method (already done)
  2) Remove Boot + Set* + property if unused
  3) Shrink UntickedSystemsBaseline
  4) One commit per family

Exit: bootstrap surface −150; save file smaller; baseline << 300.

────────────────────────────────────────────────────────────────────────────────
PHASE 3 — DEMOTE OR BRIDGE DUAL STACKS               Priority: HIGH
────────────────────────────────────────────────────────────────────────────────
For each dual stack, pick ONE host:

  Stack                    KEEP (live)              DEMOTE (ghost)
  ───────────────────────  ───────────────────────  ────────────────────────────
  Medical afflictions      MedicalSystem+AfflictionSO  Affliction_* ×21 +
                                                       Tetanus/Frost/Vision/Tooth/
                                                       BurnWard/Cognitive
  Traits                   PersonalQuest strings    Trait_* ×11
  Encounters               EncounterSO+factory      Encounter_* ×15
  Narrative events         EventPool/GameEvent      Event_* ×27
  Pets                     PetSystem                PetFeralCat, PetGuardDog
  World timeline           WorldPhaseSystem         WorldEvent_* ×5
  Structure crisis         StructuralIntegrity      Crisis_* ×2
  Map hazards time         AcidGeyser/Ashlanche     other MapHazard + MapAnomaly
                                                    unless wired in Phase 4

Bridge exception: if design needs SporeLung room contagion, implement as AfflictionSO
+ one MedicalSystem hook — do not keep parallel SporeLungSystem empty forever.

Exit: one host per fantasy; dual stacks documented as deleted.

────────────────────────────────────────────────────────────────────────────────
PHASE 4 — WIRE FLAGSHIPS (only if product wants content)  Priority: MEDIUM
────────────────────────────────────────────────────────────────────────────────
Only after Phase 0–1. Max 2–3 systems per PR, each with EditMode "dispatch test"
(bootstrap property method called from expedition/UI host).

  Wire set (suggested flagships):
    W1. Expedition node-enter → 1 MapAnomaly + 1 MapHazard (AshDunes, GasPockets)
    W2. Expedition → 1 NPC or keep SO-only and skip NPC forever
    W3. WorldEventFinalWinter.CheckActivation on NuclearWinter phase
    W4. Victory StartDefense/StartUpload UI + CheckVictory daily for 2 progress endings
    W5. WeatherAcidSnow tick when host can pass filter durability ref
    W6. One Action_* from UI (BarricadeDoor or Exile) OR none

  Rule: if not wired within one sprint of "want", demote instead (Phase 2).

Exit: ≤10 new live systems with regression tests; baseline shrinks for those names.

────────────────────────────────────────────────────────────────────────────────
PHASE 5 — PARTIAL-HOST CLEANUP                       Priority: MEDIUM
────────────────────────────────────────────────────────────────────────────────
  5.1 Victory* HANDLERS_ONLY → demote or wire CheckVictory (DEEP5-WIN-001)
  5.2 Siege* six construct-only → demote; keep Biowarfare/Blockade if Start exists
  5.3 TrueEnding/IronMan: ensure ApplyEndgame + baseline alias fix
  5.4 Weather residual: either implement contextual tick or demote (host comment already
      admits incomplete)

────────────────────────────────────────────────────────────────────────────────
PHASE 6 — VERIFY & RATCHET                           Priority: ONGOING
────────────────────────────────────────────────────────────────────────────────
  6.1 UntickedSystemsBaseline only shrinks
  6.2 SaveStateCompletenessTests keep SaveMap quality for remaining Live systems
  6.3 No new EncountersWiringTests-style isolation greens without dispatch test
  6.4 PlayMode smoke: eat, cold, rad, day 2 autosave, one expedition, one endgame

────────────────────────────────────────────────────────────────────────────────
ORDER OF OPERATIONS (recommended sprints)
────────────────────────────────────────────────────────────────────────────────

  Sprint 0: Phase 0 freeze + Phase 1 Wave 0 honesty (consume, heat, rad, endgame)
  Sprint 1: Phase 2 demote Action + NPC + Item gadgets
  Sprint 2: Phase 2 demote ShelterModule trackers (after SO audit) + Phase 3 dual stacks
            (Affliction/Trait/Encounter/Event)
  Sprint 3: Phase 3 remaining dual stacks + Phase 5 Victory/Siege cleanup
  Sprint 4+: Phase 4 flagships only if design prioritizes map/NPC fantasy

Do NOT: expand SaveMap to more Dormant systems; add Boot* without content gate;
       "wire" by adding Debug.Log On* only.

--------------------------------------------------------------------------------
P9-F. COUNTS & FINDING IDs
--------------------------------------------------------------------------------

Part 9 scan (~254 residual+affliction):
  CONSTRUCT_SAVE_ONLY 188 | HANDLERS_ONLY 53 | BOOTSTRAP_TRIGGERED 13

New IDs:
  DEEP9-AFF-001
  DEEP9-RES-001, DEEP9-RES-002, DEEP9-RES-003
  PLAN phases 0–6 (implementation roadmap, not bugs)

--------------------------------------------------------------------------------
P9-G. METHOD LIMITS
--------------------------------------------------------------------------------

  • Item×69 and ShelterModule×46 classified bulk property-scoped; individual API
    lists not re-printed (all construct/handlers except ItemAmmoTypes).
  • ModLoader/TwitchAPI "BOOT" may be false-ish (file name hits); treat as tooling.
  • Siege TickHour without active siege state may still be no-ops — not playtested.
  • Static analysis only.

  Evidence:
    all Affliction* Prop.Method → none
    TickWeatherEventsHourly comment + SolarFlare?.TickHour only for live set
    MedicalSystem.Tick registry live vs Affliction_* boot-only
    BatchSystems SiegeBiowarfare?.TickHour / TrueEnding.TickHour

========================================================================================
END OF PART 9 — AFFLICTION / RESIDUAL OTHER + DEMOTE-WIRE PLAN
========================================================================================

========================================================================================
PART 10 — MASTER STATUS OVERVIEW (full re-verification)
Read-only | No code was modified
Generated (UTC): 2026-08-08
========================================================================================

PURPOSE
----------------------------------------
Single overview of audit Parts 1–9. Every Critical/High listed below was re-checked
against CURRENT source files (not memory, not random samples). Status:

  FIXED    — issue remediated in tree; evidence cited
  PARTIAL  — core fixed, residual risk remains
  OPEN     — still present as audited

METHOD THIS PASS
----------------------------------------
Re-read / rg the exact production files for each finding ID:
  InitFoundation, RadiationExposure, Shelter.Ops, MaterialShieldingSystem,
  NeedsSystem, Inventory, Drink/TakeIodine/UseAntiRad ActionSOs, MedicalSystem,
  AmputationSystem + InitFoundation.Medical Bind, HatchDefenseSystem,
  TimeSystem, SaveSystem Capture/Restore/Entities/Wiring, Endgame + VictoryProject,
  DynamicEconomySystem, TradeScreenUI, UntickedSystemsBaseline, ghost Prop.Method samples.

========================================================================================
1. WHAT THE AUDIT SERIES COVERED
========================================================================================

  Part 1   Static deep audit: Save, Rad, Warmth, Medical, Combat, Economy, Arch
  Part 2   End-to-end proofs (DEEP-*), orphan census, death math
  Part 3   Inventory consume chains, win/lose dual engines, flashpoint, photoperiod
  Part 4   Re-verify after WIP; SaveMap; unticked baseline; dual clocks
  Part 5   Trigger audit Event* + Victory* + Other top 50
  Part 6   Trigger audit MapAnomaly* + MapHazard* + NPC*
  Part 7   Trigger audit Visitor* + Encounter* + Location* + Action*
  Part 8   Trigger audit WorldEvent* + Biome* + Weapon* + Crisis* + Trait*
  Part 9   Affliction* + residual Other + demote/wire plan
  Part 10  This master status re-verification

  Report file: docs/ASHFALL_DEEP_CODE_AUDIT_2026-08-07.txt (~3.3k lines + this section)

========================================================================================
2. CRITICAL / HIGH — RE-VERIFIED STATUS TABLE
========================================================================================

----------------------------- FIXED (domain core repaired) -----------------------------

[SURV-001 / DEEP-002] Warmth always near heat     STATUS: FIXED
  Was: NeedsSystem(..., sv => true)
  Now: NeedsSystem(needsProfile, IsSurvivorNearHeat)
       IsSurvivorNearHeat → TemperatureSystem.IsWarmEnoughForRecovery(sv, shelter)
  File: GameBootstrap.InitFoundation.cs ~36–195

[RAD-001 / DEEP-003] No ExposureContext            STATUS: FIXED
  Was: new RadiationSystem(NeedsSystem) only
  Now: new RadiationSystem(NeedsSystem, BuildExposureContext)
       BuildExposureContext fills zone outdoor/expedition + Shelter.GetInteriorRadsPerHour
  File: InitFoundation.cs ~206; GameBootstrap.RadiationExposure.cs

[RAD-002] BunkerContamination cosmetic             STATUS: FIXED
  Now: GetInteriorRadsPerHour does rads += BunkerContamination
  File: Shelter.Ops.cs ~80–83

[RAD-003] Room contamination unused                STATUS: FIXED
  Now: rads += GetRoomContaminationRadsPerHour() which sums room.GetIndoorRadContribution()
  File: Shelter.Ops.cs ~84–104

[RAD-004] MaterialShielding bleed always full      STATUS: FIXED
  Was: weakestAttenuation = 0f; Min always 0
  Now: GetWeakestCeilingAttenuation seeds weakest=1f; CeilingAttenuationProvider wired
  File: MaterialShieldingSystem.cs; InitWorld CeilingAttenuationProvider

[MED-001] staminaCap inverted                      STATUS: FIXED
  Now: fatigueFloor = 100 - staminaCap; clamp Fatigue UP to floor
  File: MedicalSystem.cs ApplyStaminaCap ~651–661

[MED-002] Amputation no sepsis cure                STATUS: FIXED
  Now: _cureAffliction?.Invoke(patient, Sepsis, surgeon)
       Bound to MedicalSystem.CureOutright in InitFoundation.Medical.cs Bind
  File: AmputationSystem.cs ~166; InitFoundation.Medical.cs ~62–66

[CMB-001 / DEEP-005] Generator noise latch         STATUS: FIXED
  Now: when outdoor diesel stops, _noiseFlagRaisedByDiesel clears GeneratorRunningOutside
  File: HatchDefenseSystem.cs SyncGeneratorNoise ~1175–1192

[SAVE-001 core] TimeSystem empty DTO               STATUS: FIXED (payload)
  TimeSystem.CaptureState/RestoreState persist day/hour/totalElapsedSeconds
  SaveSystem.SetTimeSystem(TimeSystem) wired
  Residual dual clock: see PARTIAL below

----------------------------- PARTIAL -----------------------------------------------

[SAVE-001 residual / DEEP4-TIME-001] Dual clocks   STATUS: PARTIAL
  TimeSystem ISaveable works, BUT SaveData.ElapsedHours still only from
  TemperatureSystem on capture and only restores TemperatureSystem
  Files: SaveSystem.Capture.cs:58; SaveSystem.Restore.cs:51
  Risk: Temperature vs TimeSystem vs Photoperiod can diverge on load of old/partial saves

[SAVE-001 GameState.Day]                           STATUS: PARTIAL (improved)
  InitFoundation mirrors GameState.Day from TimeSystem.OnDayTick
  Restore still sets GameState.Day from SaveData before ISaveable TimeSystem restore

----------------------------- STILL OPEN — player honesty ---------------------------

[DEEP3-INV-001 / DEEP4-INV-001] Player eat increases hunger   STATUS: OPEN CRITICAL
  Inventory.Consume: Modify(Hunger, +item.hungerRestore)
  items.json: all hungerRestore ≥ 0 (canned_food=40); 27 foods with positive restore
  ConsumeItem passes NeedsSystem → player path inverted
  AI EatActionSO still needs=null then Hunger -= restore (correct path)
  Files: Inventory.cs ~574–575; GameBootstrap.UiActions.Radio.cs ConsumeItem ~79;
         EatActionSO.cs ~127–154

[DEEP3-INV-003] AI free infinite water             STATUS: OPEN HIGH
  DrinkActionSO.Execute only Thirst -= 50; no inventory Remove
  File: DrinkActionSO.cs ~42–48

[DEEP3-INV-004] AI iodine burns stock, no resist   STATUS: OPEN HIGH
  Remove pill only; no RadiationSystem.AdministerIodine
  File: TakeIodineActionSO.cs Execute

[DEEP3-INV-005] AI anti-rad bypasses RadiationSystem STATUS: OPEN HIGH
  Direct RadiationDose mutation; no AdministerAntiRad
  File: UseAntiRadActionSO.cs ~92–108

[DEEP-001] Hunger/thirst/cold never kill via Needs STATUS: OPEN HIGH (design+)
  Critical only morale drain; comment says medical owns Health
  No starvation affliction auto-inflict found in NeedsSystem
  File: NeedsSystem.cs ~164–171

[DEEP4-WIN-001] EndgameEngine does not freeze run  STATUS: OPEN CRITICAL
  CheckWinLose calls EndgameEngine.Evaluate but never ApplyEndgame
  Only VictoryProject.OnEndgameTriggered → ApplyEndgame
  File: GameBootstrap.UiActions.Endgame.cs CheckWinLose ~27–54; InitFoundation Victory wire

[DEEP3-WIN-002] Extraction day 60 vs 100           STATUS: OPEN HIGH
  EndgameEngine: day >= 60 extraction; VictoryProject.ChopperArrivalDay = 100
  Files: EndgameEngine.cs ~75; VictoryProjectManager.cs ~19

----------------------------- STILL OPEN — save integrity ---------------------------

[SAVE-002] Expedition flashpoint fields            STATUS: OPEN CRITICAL
  Restore assigns IsCommsSevered, FlashpointBehavior, OriginalEtaTicks, shelter delay,
  return speed mult/div — CaptureExpeditionState still OMITs all of them
  File: SaveSystem.Capture.Entities.cs ~69–100 vs Restore.Expeditions.cs ~86–91

[SAVE-003] Non-transactional load hybrid world     STATUS: OPEN CRITICAL
  Survivors/shelter mutated before fail-fast ISaveable; no stage-commit
  (Architecture unchanged; not re-proved with throw test this pass)

[SAVE-004] Dual AutoSave                           STATUS: OPEN HIGH
  SaveSystem ctor OnPhaseChanged → AutoSave (ignores _suppressAutoSave)
  Bootstrap also AutoSaves on Running with _suppressAutoSave
  Files: SaveSystem.cs ~705, 840–844; InitLate.cs ~589

[SAVE-005] Soft restore in release builds          STATUS: OPEN HIGH
  FailFastRestore false outside EDITOR/DEVELOPMENT_BUILD
  File: SaveSystem.cs DefaultFailFastRestoreForEnvironment

[SAVE-007] Survivors restored by roster index      STATUS: OPEN HIGH
  for i in data.Survivors → existing[i]; not by Id
  File: SaveSystem.Restore.cs ~79–84

[SAVE-008] Expeditions not cleared before restore  STATUS: OPEN HIGH
  GetOrCreate merges; no ActiveExpeditions.Clear()
  File: SaveSystem.Restore.Expeditions.cs ~23–37

[SAVE-012] ModuleLookup = id => null               STATUS: OPEN HIGH
  File: GameBootstrap.InitLate.cs ~89

----------------------------- STILL OPEN — inventory / economy / data ---------------

[INV-001] Add partial + return false               STATUS: OPEN HIGH
  Capacity mid-loop return false after OnItemAdded
  File: Inventory.cs ~317–322

[INV-002] Unequip discards if bag full             STATUS: OPEN HIGH
  Remove from equipped then Add ignoring failure
  File: Inventory.cs ~475

[ECON-001] Trade always InferTier(type)            STATUS: OPEN HIGH
  File: DynamicEconomySystem.cs GetTradeValue ~745

[ECON-002] Numeric trade quote UI                  STATUS: OPEN HIGH
  TradeScreenUI "You offer/They ask :0.0"
  File: TradeScreenUI.cs ~194–195

[ITEM-003] Dual StreamingAssets item trees         STATUS: OPEN MEDIUM
  Data/items.json ~166KB + root items.json ~26KB both present

----------------------------- STILL OPEN — mass latent content ----------------------

[DEEP4–9 ghost census] UntickedSystemsBaseline     STATUS: OPEN CRITICAL (process)
  Baseline still lists ~436 entries (header)
  Spot re-check Prop.Method (non-test, non-On+):
    EventBrawl, NPCBandits, ActionExile, AfflictionSporeLung,
    WorldEventFinalWinter, BiomeAshSwamp → STILL_NO_METHOD_CALLS
  Dual stacks (MedicalSO vs Affliction_*, EncounterSO vs Encounter_*, etc.) unchanged
  in structure; Parts 5–9 tables still apply unless a family was demoted (none demoted)

[DEEP5-WIN Victory Start missing]                  STATUS: OPEN (not re-scanned all 14)
  Prior: only Airlift/Broadcast/LoneSurvivor host methods; Start* unused
  Not re-diffed every Victory_* this pass — treat OPEN until Phase 5 plan

========================================================================================
3. SEVERITY SNAPSHOT (after re-check)
========================================================================================

  FIXED since original audit (do not re-fix):
    Warmth hook, ambient ExposureContext, bunker+room rad into interior formula,
    material shielding min, stamina cap floor, amp sepsis cure+bind, generator noise clear,
    TimeSystem save payload

  REMAINING CRITICAL (player-facing or save integrity):
    1. DEEP3-INV-001  Player Consume hunger/thirst sign inverted
    2. DEEP4-WIN-001  EndgameEngine terminal does not ApplyEndgame
    3. SAVE-002       Flashpoint expedition fields not captured
    4. SAVE-003       Hybrid load world
    5. Ghost content  ~400+ systems still dormant (baseline ratchet)

  REMAINING HIGH (fix next after criticals):
    AI free drink / iodine no-op / anti-rad bypass
    Dual AutoSave, soft restore release, survivors by index, expedition clear,
    ModuleLookup null, dual clocks residual, extraction 60 vs 100,
    Inventory Add/Unequip, InferTier, trade UI numbers,
    DEEP-001 starvation not lethal via needs (or missing affliction bridge)

========================================================================================
4. RECOMMENDED WORK ORDER (post-overview)
========================================================================================

  A. Honesty (1–2 days of focused PRs)
     - Invert Consume hunger/thirst (or document domain if inverted intentionally — AI
       proves intent is subtract)
     - DrinkActionSO Remove water; Iodine/AntiRad via RadiationSystem
     - EndgameEngine.OnCampaignEnded → ApplyEndgame; unify extraction day

  B. Save integrity
     - Capture expedition flashpoint fields + EditMode round-trip
     - Single AutoSave owner; FailFast or degrade UI in release
     - Restore survivors by Id; clear expeditions; real ModuleLookup
     - Single ElapsedHours source from TimeSystem

  C. Content pipeline (Phase 0 of Part 9 plan)
     - Content gate CI; demote Action/NPC/Item gadgets in batches
     - Do not expand SaveMap on dormant systems

  D. Optional flagships only after A–B

========================================================================================
5. CONFIDENCE / LIMITS
========================================================================================

  High confidence FIXED/OPEN above: direct current-file evidence this pass.
  Medium: SAVE-003 transactional load (structure read; no throw-injection retest).
  Medium: full Victory* Start matrix (Part 5 evidence; not every file re-opened).
  Ghost census: full Parts 5–9 remain valid; only 6 representative props re-probed for
  method calls this pass (all still dead).

  Unity batchmode / EditMode suite not re-run this overview.

========================================================================================
END OF PART 10 — MASTER STATUS OVERVIEW
========================================================================================

========================================================================================
PART 11 — FULL RECHECK SNAPSHOT (fixed vs open)
Read-only re-verification of all major audit findings against current tree
Generated (UTC): 2026-08-08
========================================================================================

Method: re-opened production sources for every Critical/High ID; systematic Prop.Method
sample of 39 ghost representatives (all still dead/handlers-only); re-checked known
partial-live batch systems.

FIXED (9 domain fixes confirmed)
  SURV-001/DEEP-002, RAD-001/DEEP-003, RAD-002, RAD-003, RAD-004,
  MED-001, MED-002 (+Bind), CMB-001/DEEP-005, SAVE-001 TimeSystem payload

PARTIAL
  SAVE-001 residual dual ElapsedHours (Temperature vs TimeSystem)
  Ghost baseline: 435 entries; a few batch-driven systems live but still listed

OPEN CRITICAL
  DEEP3-INV-001 player Consume hunger sign inverted
  DEEP4-WIN-001 EndgameEngine.Evaluate without ApplyEndgame
  SAVE-002 expedition flashpoint capture missing (restore has fields)
  SAVE-003 hybrid non-transactional load
  Mass latent content (~435 unticked; 39/39 sample still no gameplay Prop.Method)

OPEN HIGH
  DEEP3-INV-003 free AI drink; DEEP3-INV-004 iodine no-op; DEEP3-INV-005 anti-rad bypass
  DEEP-001 needs critical = morale only
  SAVE-004 dual AutoSave; SAVE-005 soft restore release; SAVE-007 index restore;
  SAVE-008 no expedition clear; SAVE-012 ModuleLookup null
  DEEP3-WIN-002 day 60 vs ChopperArrivalDay 100
  INV-001 Add partial; INV-002 Unequip ignore Add fail
  ECON-001 InferTier only; ECON-002 numeric trade UI; dual items.json
  Victory StartDefense/StartUpload/SealAndDetonate/FireAtOwnCoordinates: 0 external callers

Known LIVE despite baseline listing (boot method calls exist)
  AdaptiveWarlords, ChemUse, DiseaseExpansion, ItemAmmoTypes, TrueEnding, IronMan,
  SiegeBiowarfare, SiegeBlockade

========================================================================================
END OF PART 11 — FULL RECHECK SNAPSHOT
========================================================================================

========================================================================================
PART 12 — FURTHER INVESTIGATION: additional problems (any size)
Read-only | Flag regardless of fix priority
Generated (UTC): 2026-08-08T00:07:08Z
========================================================================================

Scope: hunt beyond Parts 1–11 domain bugs — lifecycle, determinism, bypasses,
API misuse, silent failure, style/process. Each item is flagged even if benefit
is small.

========================================================================================
A. NEW FINDINGS (not fully tracked in Parts 1–11)
========================================================================================

[MISC-001] Severity: HIGH | Type: silent incomplete save
  Location: SaveSystem.Entities.cs CaptureSubsystemStates catch
  Description: CaptureState exceptions LogError and continue; Save() still returns true
  (IO.cs success path). Slot file written without failed subsystems → load loses state
  with no Save failure.
  Evidence: catch logs error; no rethrow / fail-whole-save flag.

[MISC-002] Severity: HIGH | Type: silent continue after tick throw
  Location: SystemRegistry.cs PerSubstep/Daily catch
  Description: A throwing system is logged; remaining systems still tick. One bad
  system can leave world half-advanced for the hour without freezing the sim.
  Evidence: catch LogError then continue loop.

[MISC-003] Severity: MEDIUM | Type: API misuse
  Location: GhostStationSystem.DestroyRuntimeAssets
  Description: Uses Object.DestroyImmediate on ScriptableObjects at runtime. Unity
  docs: prefer Destroy in play mode; Immediate can desync assets / editor state.
  Evidence: GhostStationSystem.cs ~158–164 DestroyImmediate.

[MISC-004] Severity: MEDIUM | Type: lifecycle leak risk
  Location: codebase-wide event subscriptions
  Description: On* event += count ~499 vs -= ~44 (order of magnitude). EventBus
  Subscribe/Unsubscribe balanced (7/7). Long-lived GameBootstrap handlers + dormant
  Wire* On* logs rarely unsubscribed → leak / double-fire if bootstrap re-inits.
  Evidence: rg counts this pass.

[MISC-005] Severity: MEDIUM | Type: non-determinism
  Location: many systems `rng ??= new System.Random()` without seed
  Description: Unseeded Random when host omits rng breaks seed-based reproducibility
  (tests, ironman, multiplayer-like determinism). Sites include RadiationSystem,
  MentalBreakSystem, BeliefSystem, PersonalQuestSystem (multiple), HallucinationSystem,
  PlanterBox, AfflictionPipeline, SimulationSystems.Ops, etc. (30+ call sites).
  Suggested: require seeded rng from GameBootstrap._worldSeed + salt.

[MISC-006] Severity: MEDIUM | Type: death pipeline bypass
  Location: ~46 sites `.Needs.Health =` outside NeedsSystem.Modify
  Description: Direct Health writes skip OnNeedChanged / EvaluateDeath / ForceDeath
  (DEEP-009 class). Health can hit 0 while State stays Alive unless another path
  notices. Examples: HatchDefense fallback, ExpeditionSystem.Tick, UxoHazard,
  LaborCampSystem (Health=40), MedicalPerk DeathsDoor (intentional Health=0 +
  Incapacitated), EatActionSO healthEffect, EventRunner.Apply "health".
  Evidence: rg Needs.Health = count ~46 non-test.

[MISC-007] Severity: MEDIUM | Type: radiation pipeline bypass
  Location: UseAntiRadActionSO, DecontaminateActionSO, SleepwalkActionSO,
  EventRunner.Apply "radiation", PersonalQuest undertaker/plumber, FactionRaidPlan,
  MutatedEcosystem, PrognosisPipeline zeroing
  Description: Direct RadiationDose writes skip RadiationSystem.Expose/Administer*
  events and lifetime exposure accounting (related DEEP3-INV-005).
  Evidence: rg RadiationDose = outside RadiationSystem.

[MISC-008] Severity: MEDIUM | Type: culture / locale
  Location: Pet_GuardDog.cs foodType.ToLower().Contains(...);
  Item_Keycards.cs ToLower() for door ids
  Description: ToLower() uses current culture — Turkish-I etc. can break id matching.
  Prefer ToLowerInvariant / OrdinalIgnoreCase.

[MISC-009] Severity: LOW | Type: culture
  Location: HatchDefenseSystem.cs item.id.Contains("ammo") / Contains("shell")
  Description: Substring match without OrdinalIgnoreCase; culture-sensitive Contains
  on strings; false negatives for Ammo casing / false positives for non-ammo ids
  containing "shell".

[MISC-010] Severity: LOW | Type: stub save / false safety
  Location: still present empty Capture + Restore `{ _ = saved; }` on e.g.
  WaterEconomySystem, CookingSystem, NightScavengeSystem, BicycleSystem,
  BlackRain/FalloutStormHazard, MoralDilemma, ProstheticCrafting, NeedleSterilization,
  Visitor_Abandoned/Exploded, Action_Stargazing/SilentTakedown, LocationStateRuin
  Description: ISaveable registration implies persistence; DTO empty → load no-op.
  (Partially flagged DEEP4-SAVE-STUB; still present.)

[MISC-011] Severity: LOW | Type: NRE risk in test/fallback path
  Location: GameBootstrap.TickSystems.cs TickWeatherShelterPower fallback
  Description: When _registry is null (partial test host), WeatherSystem.Tick /
  TemperatureSystem.Tick / Shelter.Tick called without ?.. Full bootstrap uses
  registry (safe). Partial hosts can NRE if systems null.
  Evidence: lines ~86–103 unguarded.

[MISC-012] Severity: LOW | Type: process / observability
  Location: runtime Debug.Log ~858 sites + warn/error ~243
  Description: Heavy logging noise; many Boot* Wire* Debug.Log on content that never
  fires; can hurt perf in builds if not stripped; hides real errors.

[MISC-013] Severity: LOW | Type: morale bypass
  Location: ~107 sites Needs.Morale = direct
  Description: Skips Selfless redistribution, Living Saint floor, Matriarch paths in
  NeedsSystem.Modify for morale.

[MISC-014] Severity: NIT | Type: empty catch (editor only)
  Location: EventIdValidator.Collect.cs catch { return; }
  Description: Swallows exception with no log in one path.

[MISC-015] Severity: NIT | Type: EventRunner rad clamp
  Location: EventRunner.Apply.cs radiation NeedDelta Clamp 0..100
  Description: Consistent with RadiationSystem clamp; if design ever allows >100 dose
  for acute tracking, events hard-cap. Currently aligned with system (flag for awareness).

[MISC-016] Severity: LOW | Type: incomplete save subsystem skip
  Location: SaveSystem.Entities restore soft path
  Description: Systems in save not registered "silently skipped (forward-compat)" —
  can hide missing Set* wiring after refactors.

[MISC-017] Severity: LOW | Type: HUD auto-AddComponent
  Location: HUD.cs Ensure* GetComponentInChildren ?? AddComponent for many HUD pieces
  Description: Runtime invents missing UI components; masks broken scenes; can add
  unconfigured components mid-play.

[MISC-018] Severity: NIT | Type: dual path TickSystems
  Location: GameBootstrap.TickSystems registry vs else fallback
  Description: Two tick graphs if registry missing; drift risk for tests that only
  exercise one path (AUDIT-008 residual).

[MISC-019] Severity: LOW | Type: seed comment vs practice
  Location: GameBootstrap seeds some systems with _worldSeed + N, others fall back
  to unseeded Random when optional rng null at call site (MISC-005).

[MISC-020] Severity: NIT | Type: Journal / naming
  Location: JournalVoice "Fallout storm" wording
  Description: Domain allows nuclear aftermath language; ensure no real-world country
  names (spot-check clean). Not a bug — flag if brand sensitivity required.

========================================================================================
B. PREVIOUSLY DOCUMENTED STILL OPEN (quick reaffirm this pass)
========================================================================================

  OPEN CRITICAL: DEEP3-INV-001 consume sign; DEEP4-WIN-001 EndgameEngine freeze;
    SAVE-002 flashpoint capture; SAVE-003 hybrid load; ghost baseline ~435
  OPEN HIGH: AI drink/iodine/antirad; DEEP-001 needs morale-only critical;
    SAVE-004 dual AutoSave; SAVE-005 soft restore release; SAVE-007 index;
    SAVE-008 expeditions; SAVE-012 ModuleLookup; INV-001/002; ECON-001/002;
    dual items.json; extraction 60 vs 100; Victory Start* zero callers
  FIXED (still hold): warmth, ExposureContext, bunker+room rad, shielding,
    stamina floor, amp sepsis, generator noise, TimeSystem payload

========================================================================================
C. PRIORITY IF FIXING MISC BATCH
========================================================================================

  1. MISC-001 fail whole Save on CaptureState exception
  2. MISC-006/007 route Health/RadiationDose through NeedsSystem/RadiationSystem
     (or call EvaluateDeath after direct write)
  3. MISC-003 Destroy → Destroy play-mode
  4. MISC-005 seed all Random from world seed
  5. MISC-004 unsubscribe audit on bootstrap teardown
  6. MISC-008/009 invariant string compares
  7. Demote stub ISaveable (MISC-010) with content gate

========================================================================================
END OF PART 12 — FURTHER INVESTIGATION
========================================================================================
