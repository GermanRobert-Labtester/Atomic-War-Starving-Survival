# Phase 11 HUD — Design Asset Handoff

## Figma
- Token reference: `design-tokens.json` (maps to `UIBatch11_A.uss` / `UIBatch11_B.uss`)
- Component map documents 8 widget sizes and states for Code Connect

## Canva (when MCP configured)
Export composition assets to `Canva/` with naming:
`{widget}_{state}_{WxH}.png`

Referenced by USS `background-image` in:
- `phantom-memory-vignette` (radial noise overlay)
- `memorial-wall` (stone texture)
- `keepsake-crack-overlay` (cracked glass)

Until Canva assets land, USS uses solid-color fallbacks.
